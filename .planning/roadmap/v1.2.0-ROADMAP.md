# Roadmap: v1.2.0

**Milestone:** Plugin/Extensibility API  
**Target:** Enable 3rd party libraries to add backend providers via entry points  
**Previous:** v1.1.0 — 12 backends, pytest-adbc-replay integration tests  

---

## Goals

1. **Manual Registration**: `register_backend()` API for in-project backends
2. **Auto-Discovery**: Entry point hooks for 3rd party libs to register automatically
3. **Documentation**: Guide for plugin authors on implementing custom backends

---

## Quality Gates

All phases must pass before completion:
- Unit tests for all new public APIs
- Integration tests using dummy backend plugin
- `uv run basedpyright` passes (strict mode)
- `uv run ruff check` passes
- `uv run ruff format --check` passes

---

## Phases

- [ ] **Phase 1: Registry Infrastructure** — Backend registry + manual registration API
- [ ] **Phase 2: Entry Point Discovery** — Auto-discover backends via `pyproject.toml` entry points
- [ ] **Phase 3: Plugin Author Documentation** — Guide for implementing and publishing custom backends

---

## Phase Details

### Phase 1: Registry Infrastructure

**Goal**: Add a backend registry and manual registration API

**Depends on**: Nothing (first phase)

**Requirements**: 
- REG-01, REG-02, REG-03, REG-04, TEST-INFRA-01

**Success Criteria** (what must be TRUE):
  1. `register_backend(name, config_class, translator, driver_package, ...)` registers a new backend at runtime
  2. Registered backends work identically to built-in backends (pool creation, driver resolution, translation)
  3. `list_backends()` returns all registered backends (built-in + custom)
  4. Attempting to register a duplicate name raises `BackendAlreadyRegisteredError`
  5. Dummy backend fixture created for integration testing

**Plans**: TBD

---

### Phase 2: Entry Point Discovery

**Goal**: Auto-discover 3rd party backends via Python entry points

**Depends on**: Phase 1

**Requirements**:
- EP-01, EP-02, EP-03, TEST-INFRA-02, TEST-INFRA-03

**Success Criteria** (what must be TRUE):
  1. 3rd party packages can declare backends via `[project.entry-points."adbc_poolhouse.backends"]`
  2. Entry points are discovered lazily on first `create_pool()` call (not at import time)
  3. Entry point load failures produce clear error messages identifying the offending package
  4. CI installs dummy backend and runs integration tests
  5. Malformed plugin fixtures verify graceful error handling

**Plans**: TBD

---

### Phase 3: Plugin Author Documentation

**Goal**: Document how to implement and publish custom backends

**Depends on**: Phase 2

**Requirements**:
- DOC-01, DOC-02, DOC-03

**Success Criteria** (what must be TRUE):
  1. "Plugin Development" guide exists with step-by-step instructions
  2. Example plugin package included in docs (minimal working example)
  3. `WarehouseConfig` protocol is documented with all required fields and methods

**Plans**: TBD

---

## Progress

| Phase | Plans Complete | Status | Completed |
|-------|----------------|--------|-----------|
| 1. Registry Infrastructure | 0/2 | Not started | - |
| 2. Entry Point Discovery | 0/2 | Not started | - |
| 3. Plugin Author Documentation | 0/2 | Not started | - |

---

## Risk Register

| Risk | Likelihood | Impact | Mitigation |
|------|------------|--------|------------|
| Entry point load order issues | Low | Medium | Document clear loading contract; fail fast with good errors |
| Breaking existing built-in backends | Low | High | Comprehensive test coverage before merge |
| Plugin authors need more hooks than anticipated | Medium | Low | Start minimal; extend based on feedback |

---

*Roadmap created: 2026-03-10*  
*Status: Draft - awaiting planning kickoff*
