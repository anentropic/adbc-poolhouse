---
phase: 27-dual-backend-test-matrix
plan: 05
type: execute
wave: 3
depends_on: ["27-01", "27-02", "27-03", "27-04"]
files_modified:
  - tests/async/test_meta_guard.py
autonomous: false
requirements: [EDGE-27, EDGE-30]
must_haves:
  truths:
    - "A meta-test asserts scan_async_test_hygiene('tests/async') == [] — no import asyncio, no @pytest.mark.asyncio, every async test @pytest.mark.anyio across the whole async package"
    - "A meta-test asserts scan_for_positive_sleep('tests/async') == [] — no positive-duration sleep literal in the async tests"
    - "Any file the meta-scan flags is fixed in place (only the flagged file), never a blanket rewrite of the ~10 existing EDGE files"
    - "The full async suite is green and the phase concurrency tests are x20 loop-stable (0 hangs)"
  artifacts:
    - path: "tests/async/test_meta_guard.py"
      provides: "EDGE-27/30 real-package meta-tests asserting the new guard callables return []"
      contains: "scan_async_test_hygiene"
      min_lines: 15
  key_links:
    - from: "tests/async/test_meta_guard.py"
      to: "tests._async_harness.guard.scan_async_test_hygiene / scan_for_positive_sleep"
      via: "assert scan(...) == []"
      pattern: "scan_async_test_hygiene\\(.tests/async."
---

<objective>
Close EDGE-27 and EDGE-30 with real-package meta-tests, and run the phase-level
completion gates. The meta-tests assert the two guard callables from Plan 27-01
return `[]` when pointed at the now-complete `tests/async/` directory:
`scan_async_test_hygiene('tests/async') == []` (EDGE-27 — no `import asyncio`, no
`@pytest.mark.asyncio`, every async test `@pytest.mark.anyio`) and
`scan_for_positive_sleep('tests/async') == []` (EDGE-30 — no positive-duration
sleep literal). This plan runs LAST so the scan validates every Wave 2 file too.
If the scan flags any file, fix ONLY that file (D-27-02 — never a blanket rewrite
of the ~10 hardened EDGE files). Finish with the full-suite green, the ×20
loop-stability gate across the phase's concurrency tests, the Linux CI gate, and
the mkdocs docs gate.

Purpose: Enforces the no-`asyncio` / no-positive-`sleep` discipline across the
whole async test package as a standing meta-guard, and certifies phase completion.

Output: `tests/async/test_meta_guard.py` (+ targeted fixes to any flagged file).

CONSTRAINT (locked): NO `src/` changes. EDGE-30 scan scope is `tests/async/` ONLY
(the harness has deliberate positive sleeps under virtual clocks — scanning the
harness root would false-positive). Fix only meta-flagged files; do not rewrite
hardened EDGE tests. If a test cannot pass without a source change, STOP and raise it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@.claude/skills/adbc-poolhouse-docs-author/SKILL.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-dual-backend-test-matrix/27-CONTEXT.md
@.planning/phases/27-dual-backend-test-matrix/27-PATTERNS.md
@.planning/phases/27-dual-backend-test-matrix/27-01-SUMMARY.md

# Analogs:
@tests/test_async_guard.py
@tests/async/test_async_guard.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: EDGE-27/30 real-package meta-tests (assert guard callables == []) + fix any flagged file</name>
  <files>tests/async/test_meta_guard.py</files>
  <read_first>
    - tests/test_async_guard.py:85-186 — `TestRealAsyncPackage` scans the REAL shipped package and asserts `scan_async_package(...) == []`; plain SYNC tests (no `@pytest.mark.anyio` — the guard needs no event loop). Mirror this style for the two new real-package meta-tests.
    - tests/async/test_async_guard.py — the existing thin async-suite-resident sync meta-assert (the EDGE-28 counterpart) showing the in-`tests/async/` sync-meta-test convention to follow.
    - 27-PATTERNS.md "test_meta_guard_*.py" section — the exact meta-test bodies (`assert scan_async_test_hygiene("tests/async") == []`, `assert scan_for_positive_sleep("tests/async") == []`) and the D-27-02 fix-only-flagged-files rule.
  </read_first>
  <action>
    Create `tests/async/test_meta_guard.py` with two plain SYNC meta-tests
    (no `@pytest.mark.anyio` — the guard is pure stdlib, needs no event loop),
    importing `scan_async_test_hygiene` and `scan_for_positive_sleep` from
    `tests._async_harness.guard`:
    - `test_async_test_package_hygiene`: `assert scan_async_test_hygiene("tests/async") == []` (EDGE-27).
    - `test_no_positive_sleep_in_async_tests`: `assert scan_for_positive_sleep("tests/async") == []` (EDGE-30).
    Scope the EDGE-30 scan root to `tests/async/` ONLY (NOT `tests/_async_harness/`,
    which has deliberate positive sleeps under virtual clocks — RESEARCH Pitfall 1).

    Run the meta-tests. If either scan returns findings, fix ONLY the file(s) it
    flags (D-27-02): e.g. swap a stray `@pytest.mark.asyncio` for `@pytest.mark.anyio`,
    remove an `import asyncio`, or replace a positive `sleep(N)` with `sleep(0)` /
    event gating. Do NOT perform a blanket parametrization sweep of the ~10 existing
    EDGE files — a sweep risks reintroducing the lost-wakeup / MockClock landmines
    those tests were hardened against. The expectation (per RESEARCH) is that the
    existing tests already pass clean and the only files in scope are this phase's
    new ones; treat any unexpected flag on a hardened EDGE file as a signal to
    inspect that single file, not to rewrite the suite.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/async/test_meta_guard.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/async/test_meta_guard.py` exists with both meta-tests, plain sync (no `@pytest.mark.anyio`).
    - The EDGE-30 scan root is `tests/async` (not the harness root).
    - `.venv/bin/pytest tests/async/test_meta_guard.py -x -q` exits 0 (both scans return `[]`).
    - Any fix applied touched ONLY a meta-flagged file; no blanket rewrite of hardened EDGE files; no `src/` change.
  </acceptance_criteria>
  <done>Both meta-tests pass — the whole `tests/async/` package is asyncio-free, anyio-parametrized, and free of positive sleep literals.</done>
</task>

<task type="auto">
  <name>Task 2: Full async suite green + docs/mkdocs gate</name>
  <files>tests/async/test_meta_guard.py</files>
  <read_first>
    - CLAUDE.md — the docs quality gate: new public harness symbols (`scan_async_test_hygiene`, `scan_for_positive_sleep`, `snowflake_async_pool`) need Google-style docstrings; `.venv/bin/mkdocs build --strict` must pass.
    - MEMORY uv-sandbox-workaround — prefer `.venv/bin/mkdocs` over `uv run mkdocs` under the sandbox.
  </read_first>
  <action>
    Run the full async + harness suite green and the docs gate. This is a
    verification task (no new code unless a gate fails):
    1. `.venv/bin/pytest tests/async tests/_async_harness tests/test_async_guard.py -q` — all green.
    2. `.venv/bin/mkdocs build --strict` — passes (the new public harness
       callables and the fixture have Google-style docstrings from Plan 27-01; the
       docs autogen must build clean). If `--strict` fails on a docstring rendering
       issue (e.g. rogue RST `:role:` colons — MEMORY Markdown-not-RST rule),
       fix the offending docstring in `guard.py` / `conftest.py` (test-layer, not
       `src/`) and re-run.
    If any docstring needs rewriting for the docs gate, apply a humanizer pass to
    the new prose (CLAUDE.md gate, per the docs-author skill voice).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/async tests/_async_harness tests/test_async_guard.py -q && .venv/bin/mkdocs build --strict</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/async tests/_async_harness tests/test_async_guard.py -q` exits 0.
    - `.venv/bin/mkdocs build --strict` exits 0.
    - The new harness callables and `snowflake_async_pool` fixture have Google-style docstrings (Markdown, not RST).
    - No `src/` modification.
  </acceptance_criteria>
  <done>Full async suite is green and `mkdocs build --strict` passes with the new public symbols documented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full Phase 27 test matrix (read-path, stability, limiter-stress, meta-guards). The project flaky-concurrency rule requires a ×20 loop-stability gate on every new/meta-flagged concurrency test (0 hangs), AND a green Linux CI run — a green local loop does NOT prove cross-platform stability (Phase 24-26 landmine: passes on macOS, hangs on Linux CI).</what-built>
  <how-to-verify>
    1. Run the phase ×20 loop-stability gate on every new concurrency test (0 hangs).
       MEMORY zsh-`!` landmine: use `rc=$?` + grep, NOT `if ! pytest`:
       ```
       for f in test_matrix_readpath test_stability_arrow test_limiter_stress; do
         for i in $(seq 1 20); do
           .venv/bin/pytest tests/async/$f.py -x -q >/tmp/loop27_$f.$i.log 2>&1
           rc=$?
           grep -q "passed" /tmp/loop27_$f.$i.log || { echo "FAIL $f iter $i (rc=$rc)"; break 2; }
         done
       done
       echo "loop gate complete"
       ```
       Confirm all iterations printed "passed" (no break, no hang).
    2. Push the branch and confirm the dual-backend `quality` CI job is GREEN on
       Linux (the real cross-platform gate — VALIDATION.md Manual-Only). Confirm
       the new tests appear in the `quality` job run and are NOT collected by the
       `sync-no-anyio` job (already excluded via `--ignore=tests/async`).
       Also confirm the prior auto-approved `sync-no-anyio` live run (STATE.md
       Session Continuity note) is green on this push.
  </how-to-verify>
  <resume-signal>Type "approved" when the ×20 loop is 0-hang AND the Linux `quality` CI job is green, or describe the failure.</resume-signal>
</task>

</tasks>

<artifacts_produced>
This plan produces:
- **Test file:** `tests/async/test_meta_guard.py` (EDGE-27/30 real-package meta-tests)
- **Phase completion gates:** ×20 loop-stability (0 hangs), Linux CI green, `mkdocs build --strict` green
</artifacts_produced>

<threat_model>
## Trust Boundaries

Test-layer only. No `src/` changes, no network/auth/input surface, no
credentials. The meta-guards are pure-stdlib AST scans (read-only).

| Boundary | Description |
|----------|-------------|
| meta-guard → test source files | Read-only `ast.parse` over `tests/async/`; never imports or runs the inspected modules |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-05 | Tampering | async test hygiene drift | mitigate | The EDGE-27/30 meta-guards are a STANDING source-scan that fails CI if any future async test reintroduces `import asyncio`, `@pytest.mark.asyncio`, or a positive sleep literal — enforcing the no-asyncio / virtual-clock discipline. Pure read-only AST; no network, no credentials, no `src/` surface. No package installs. |

No high-severity threats — block-on-high passes. No new attack surface
(test-layer); cassette/CI are offline/recorded; no credentials introduced.
</threat_model>

<verification>
- `.venv/bin/pytest tests/async/test_meta_guard.py -x -q` passes (both scans `== []`).
- `.venv/bin/pytest tests/async tests/_async_harness tests/test_async_guard.py -q` passes.
- `.venv/bin/mkdocs build --strict` passes.
- ×20 loop-stability gate: 0 hangs across `test_matrix_readpath`, `test_stability_arrow`, `test_limiter_stress`.
- Linux `quality` CI job green; new tests excluded from `sync-no-anyio`.
- No `src/` modification.
</verification>

<success_criteria>
- EDGE-27/30 meta-tests pass — the async package is asyncio-free, anyio-parametrized, and free of positive sleep literals.
- Full async suite green; `mkdocs build --strict` green.
- ×20 loop-stable (0 hangs) and Linux CI green; no `src/` change.
</success_criteria>

<output>
Create `.planning/phases/27-dual-backend-test-matrix/27-05-SUMMARY.md` when done.
</output>
