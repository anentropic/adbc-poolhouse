---
phase: 27-dual-backend-test-matrix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/async/conftest.py
  - tests/_async_harness/guard.py
  - tests/test_async_guard.py
autonomous: true
requirements: [EDGE-27, EDGE-30, TEST-02]
must_haves:
  truths:
    - "A snowflake_async_pool fixture yields a cassette-backed AsyncPool and skips cleanly when the driver or cassette is absent"
    - "scan_async_test_hygiene(root) returns a list of Finding for asyncio imports, @pytest.mark.asyncio decorators, and async test functions missing @pytest.mark.anyio"
    - "scan_for_positive_sleep(root) returns a Finding for every positive-duration sleep literal and allows sleep(0) and non-literal sleep args"
    - "Synthetic-source self-tests prove both new guard callables flag violators and pass clean source"
  artifacts:
    - path: "tests/async/conftest.py"
      provides: "snowflake_async_pool cassette fixture (mirror of duckdb_async_pool)"
      contains: "async def snowflake_async_pool"
    - path: "tests/_async_harness/guard.py"
      provides: "scan_async_test_hygiene and scan_for_positive_sleep AST scanner callables"
      contains: "def scan_async_test_hygiene"
    - path: "tests/test_async_guard.py"
      provides: "synthetic-source self-tests for the two new guard callables"
      contains: "scan_for_positive_sleep"
  key_links:
    - from: "tests/_async_harness/guard.py"
      to: "Finding dataclass"
      via: "new rule ids returned by the two scanners"
      pattern: "Finding\\("
    - from: "tests/async/conftest.py"
      to: "adbc_poolhouse.create_async_pool"
      via: "SnowflakeConfig cassette pool construction"
      pattern: "create_async_pool\\(SnowflakeConfig"
---

<objective>
Add the two shared primitives every Phase 27 test file depends on: the
`snowflake_async_pool` cassette fixture (D-27-04, backing the TEST-02 read-path
matrix) and the two new pure-AST guard callables `scan_async_test_hygiene`
(EDGE-27, D-27-01) and `scan_for_positive_sleep` (EDGE-30). Both guard callables
are exposed exactly like the existing `scan_async_package` so the Wave 3 meta-test
just asserts their results are `== []`. Synthetic-source self-tests prove the
scanners flag violators and pass clean source before they are pointed at the real
package.

Purpose: These are the interface contracts the Wave 2 test files (matrix,
stability, limiter-stress) and the Wave 3 meta-test build against. Defining them
first removes the scavenger hunt — the consuming plans receive the fixture name
and callable signatures from this plan.

Output: A modified `tests/async/conftest.py`, a modified
`tests/_async_harness/guard.py`, and self-tests in `tests/test_async_guard.py`.

CONSTRAINT (locked, non-negotiable): NO changes to `src/adbc_poolhouse/_async/`
or any `src/` file. All work is test-layer. If a test cannot pass without a
source change, STOP and raise it — do not edit the frozen async surface.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@.claude/skills/adbc-poolhouse-docs-author/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-dual-backend-test-matrix/27-CONTEXT.md
@.planning/phases/27-dual-backend-test-matrix/27-RESEARCH.md
@.planning/phases/27-dual-backend-test-matrix/27-PATTERNS.md

# Analogs to read before implementing (read each once, extract the pattern):
@tests/async/conftest.py
@tests/_async_harness/guard.py
@tests/async/test_async_lifecycle.py
@tests/test_async_guard.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the snowflake_async_pool cassette fixture (D-27-04)</name>
  <files>tests/async/conftest.py</files>
  <read_first>
    - tests/async/conftest.py:75-94 — `duckdb_async_pool` is the exact shape to mirror (async-generator fixture, try/yield/finally teardown, `AsyncIterator[AsyncPool]` return annotation, Google-style docstring with a `Yields:` section).
    - tests/async/test_async_lifecycle.py:126-161 — `TestSnowflakeCassetteLeg` is the proven-green cassette body to distill: the `pytest.importorskip("adbc_driver_snowflake.dbapi", ...)`, the cassette-absent `pytest.skip`, the `os.environ.setdefault("SNOWFLAKE_ACCOUNT", "replay-account")`, the `create_async_pool(SnowflakeConfig())` build, and the `close_async_pool` teardown.
    - tests/async/test_async_lifecycle.py:37 — the `_CASSETTE_ROOT = Path(__file__).parent.parent / "cassettes"` constant to mirror in conftest.
    - tests/async/conftest.py:27-43 — current imports; add `import os`, and `SnowflakeConfig` + `close_async_pool` from `adbc_poolhouse`.
  </read_first>
  <action>
    Add a `snowflake_async_pool` async-generator fixture to `tests/async/conftest.py`, mirroring `duckdb_async_pool` (per D-27-04). The fixture: calls `pytest.importorskip("adbc_driver_snowflake.dbapi", reason=...)`; `pytest.skip(...)` when the `snowflake_arrow_round_trip` cassette directory under `_CASSETTE_ROOT` is absent; sets `SNOWFLAKE_ACCOUNT=replay-account` via `os.environ.setdefault` (replay mode only, existing pattern — not a real secret); builds the pool with `create_async_pool(SnowflakeConfig())`; yields it; and closes it via `await close_async_pool(pool)` in a `finally`. Add a `_CASSETTE_ROOT` module constant mirroring `test_async_lifecycle.py:37`, and the new imports (`os`, `SnowflakeConfig`, `close_async_pool`). The fixture is backed by the EXISTING Phase 25 cassette assets — no live Snowflake. Do NOT mount the cassette in the fixture itself (the consuming test carries the `adbc_cassette` marker — see Plan 02). Give the fixture a Google-style docstring with a `Yields:` section (Markdown, not RST — backticks not `:role:`), mirroring `duckdb_async_pool`'s — this is a public harness fixture under the CLAUDE.md docs gate.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import ast,sys; t=ast.parse(open('tests/async/conftest.py').read()); names=[n.name for n in ast.walk(t) if isinstance(n,(ast.FunctionDef,ast.AsyncFunctionDef))]; sys.exit(0 if 'snowflake_async_pool' in names else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/async/conftest.py` defines `async def snowflake_async_pool` decorated `@pytest.fixture`.
    - The fixture calls `pytest.importorskip` for the snowflake driver and `pytest.skip` for the absent cassette.
    - The fixture has a Google-style docstring with a `Yields:` section, Markdown not RST.
    - No `src/` file is modified.
  </acceptance_criteria>
  <done>The cassette fixture exists, collects without error, and the AST check exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Add scan_async_test_hygiene + scan_for_positive_sleep guard callables (EDGE-27, EDGE-30)</name>
  <files>tests/_async_harness/guard.py</files>
  <read_first>
    - tests/_async_harness/guard.py:28-47 — the `Finding` frozen dataclass (path, lineno, rule, message). Add the two new rule ids (`async-test-package-hygiene` family and `positive-sleep-literal`) to the `Finding` docstring rule list.
    - tests/_async_harness/guard.py:49-82 — `_GuardVisitor.visit_Import` / `visit_ImportFrom` asyncio-detection logic to reuse for the hygiene scanner's "no `import asyncio`" rule.
    - tests/_async_harness/guard.py:136-218 — `scan_async_package`: the pure-stdlib `rglob` + tolerant `ast.parse` + absent-root → `[]` + `list[Finding]` return shape both new callables must follow (no anyio/trio/adbc_poolhouse import; read-only; malformed/non-UTF-8 file tolerance).
  </read_first>
  <action>
    Add two new public callables to `tests/_async_harness/guard.py`, each following the `scan_async_package` shape exactly (pure stdlib, `rglob` over `.py` files, tolerant `ast.parse`, absent-root returns `[]`, returns `list[Finding]`, no anyio import).

    `scan_async_test_hygiene(root)` (EDGE-27, D-27-01) returns a `Finding` for: (a) any `import asyncio` / `from asyncio import ...` — reuse the existing import-detection logic; (b) any `@pytest.mark.asyncio` decorator (an `ast.Attribute` chain `pytest.mark.asyncio` on a function); (c) any `async def test_*` function that lacks an `@pytest.mark.anyio` decorator. CRITICAL (RESEARCH Pitfall 2): the "anyio-parametrized" signal is the PRESENCE of `@pytest.mark.anyio`, NOT a literal `anyio_backend` argument — many hardened tests get the axis via the marker plus a plain async fixture, so requiring the literal arg would false-positive. Only inspect `async def` functions whose name starts with `test_` for rule (c).

    `scan_for_positive_sleep(root)` (EDGE-30) returns a `Finding` for any `sleep(...)` call whose first positional arg is a numeric `ast.Constant` strictly `> 0`. Match both `<mod>.sleep(...)` (`ast.Attribute` with `attr == "sleep"`) and bare `sleep(...)` (`ast.Name` with `id == "sleep"`). ALLOW `sleep(0)`, `sleep(0.0)`, and any non-literal arg (e.g. `sleep(deadline)`) — emit no finding for those. Use rule id `positive-sleep-literal` with a message naming the offending value and suggesting `sleep(0)` / event gating.

    Both callables get Google-style docstrings (Args / Returns + an `Example:` admonition block with a fenced ```python``` code block, Markdown not RST, per MEMORY) mirroring `scan_async_package`'s thorough docstring — these are public harness symbols under the CLAUDE.md docs gate.

    Do NOT change `scan_async_package` or `_GuardVisitor`'s existing behavior; add new visitor class(es) / functions alongside.
  </action>
  <verify>
    <automated>.venv/bin/python -c "from tests._async_harness.guard import scan_async_test_hygiene, scan_for_positive_sleep; assert scan_async_test_hygiene('tests/_async_harness/__does_not_exist__')==[]; assert scan_for_positive_sleep('tests/_async_harness/__does_not_exist__')==[]; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "def scan_async_test_hygiene(" tests/_async_harness/guard.py` succeeds.
    - `grep -q "def scan_for_positive_sleep(" tests/_async_harness/guard.py` succeeds.
    - Both callables import cleanly and return `[]` for an absent root.
    - Each new callable has a Google-style docstring with Args/Returns and an `Example:` block (Markdown, no RST `:role:` syntax).
    - `scan_async_package` and `_GuardVisitor` existing behavior is unchanged (`.venv/bin/pytest tests/test_async_guard.py -x` still passes).
  </acceptance_criteria>
  <done>Both callables exist, are importable, return `[]` for an absent root, and the existing guard self-tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Synthetic-source self-tests for the two new guard callables</name>
  <files>tests/test_async_guard.py</files>
  <behavior>
    - `scan_async_test_hygiene` on synthetic source containing `import asyncio` returns a non-empty list (flags the import).
    - `scan_async_test_hygiene` on synthetic source with `@pytest.mark.asyncio` on an async test returns a finding.
    - `scan_async_test_hygiene` on an `async def test_x` lacking `@pytest.mark.anyio` returns a finding; an `async def test_y` WITH `@pytest.mark.anyio` returns none.
    - `scan_for_positive_sleep` on `anyio.sleep(1)` / `time.sleep(0.5)` returns findings; on `anyio.sleep(0)`, `trio.sleep(0.0)`, and `anyio.sleep(deadline)` returns none.
    - Both callables return `[]` for an absent root and for clean synthetic source.
  </behavior>
  <read_first>
    - tests/test_async_guard.py:85-186 — `TestAsyncGuard` writes synthetic source strings into `tmp_path` and asserts on the returned `Finding` list; these are plain SYNC tests (no `@pytest.mark.anyio` — the guard needs no event loop). Mirror this exact style.
  </read_first>
  <action>
    Add synthetic-source self-tests for `scan_async_test_hygiene` and
    `scan_for_positive_sleep`, mirroring the existing `TestAsyncGuard` style:
    write small `.py` source strings into `tmp_path`, run the scanner, assert on
    the returned `Finding` list. Cover the violator cases and the clean/allowed
    cases listed in the behavior block (including `sleep(0)`, `sleep(0.0)`, and
    non-literal `sleep(deadline)` allow-list, and the `@pytest.mark.anyio`-present
    pass case). Keep the tests plain sync (no `@pytest.mark.anyio`) — the guard is
    pure stdlib and needs no event loop. Do NOT run these scanners against the real
    `tests/async/` directory here — that is the Wave 3 meta-test's job; this file
    proves the scanner logic against controlled synthetic input only.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_async_guard.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - New test functions/classes exercise both new callables against synthetic source for violators AND clean/allowed cases.
    - `.venv/bin/pytest tests/test_async_guard.py -x -q` exits 0.
    - The `sleep(0)` / `sleep(0.0)` / non-literal-arg allow-list and the `@pytest.mark.anyio`-present pass case are explicitly asserted.
  </acceptance_criteria>
  <done>Synthetic self-tests pass, proving both scanners flag violators and accept clean/allowed source.</done>
</task>

</tasks>

<artifacts_produced>
This plan produces:
- **Fixture:** `snowflake_async_pool` in `tests/async/conftest.py`
- **Callable:** `scan_async_test_hygiene(root) -> list[Finding]` in `tests/_async_harness/guard.py`
- **Callable:** `scan_for_positive_sleep(root) -> list[Finding]` in `tests/_async_harness/guard.py`
- **Self-tests:** synthetic-source tests in `tests/test_async_guard.py` for both new callables
</artifacts_produced>

<threat_model>
## Trust Boundaries

This is a test-layer plan. No `src/` changes, no new network/auth/input surface,
no credentials introduced. The cassette replay is offline (recorded fixtures, not
a live warehouse). The only honest trust boundary is "test process → recorded
cassette assets on disk (read-only)".

| Boundary | Description |
|----------|-------------|
| test process → cassette assets | Read-only replay of pre-recorded Phase 25 cassette files; no live Snowflake connection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Information disclosure | snowflake_async_pool fixture | accept | No credentials introduced; `SNOWFLAKE_ACCOUNT=replay-account` is a replay-mode dummy, cassette intercepts before any network call. `importorskip`/`skip` guard means the fixture never attempts a live connection. |
| T-27-LW | Tampering | live-warehouse guard | mitigate | The fixture requires the `adbc_cassette` marker (mounted by the consumer) to replay; without the recorded cassette it skips cleanly rather than reaching a real warehouse. No new package installs in this plan (no supply-chain surface). |

No high-severity threats — block-on-high passes. No new attack surface
(test-layer); cassette replay is offline; no credentials introduced.
</threat_model>

<verification>
- `.venv/bin/pytest tests/test_async_guard.py -x -q` passes (self-tests + existing guard tests).
- `.venv/bin/python -c "from tests._async_harness.guard import scan_async_test_hygiene, scan_for_positive_sleep"` imports cleanly.
- `tests/async/conftest.py` collects (`.venv/bin/pytest tests/async --collect-only -q` does not error on the new fixture).
- No `git diff --stat src/` change (frozen-surface constraint).
</verification>

<success_criteria>
- `snowflake_async_pool` fixture exists, mirrors `duckdb_async_pool`, skips cleanly when driver/cassette absent.
- `scan_async_test_hygiene` and `scan_for_positive_sleep` exist as pure-AST `list[Finding]` callables with Google-style docstrings.
- Synthetic self-tests prove both scanners flag violators and accept clean/allowed source.
- Existing guard self-tests still pass; no `src/` modification.
</success_criteria>

<output>
Create `.planning/phases/27-dual-backend-test-matrix/27-01-SUMMARY.md` when done.
</output>
