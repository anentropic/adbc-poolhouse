---
phase: 27-dual-backend-test-matrix
plan: 03
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - tests/async/test_stability_arrow.py
autonomous: true
requirements: [TEST-03]
must_haves:
  truths:
    - "pyarrow.total_allocated_bytes() shows no monotonic growth across N>=100 async cursor lifecycles on the real DuckDB pool"
    - "The pool reset event (the _release_arrow_allocators path) fires exactly once per checkin across the N cycles"
    - "The stability test runs under both asyncio and trio"
  artifacts:
    - path: "tests/async/test_stability_arrow.py"
      provides: "TEST-03 Arrow allocator-stability + reset-event-count proof"
      contains: "total_allocated_bytes"
      min_lines: 30
  key_links:
    - from: "tests/async/test_stability_arrow.py"
      to: "duckdb_async_pool._pool reset event"
      via: "sqlalchemy.event.listen(pool._pool, 'reset', counter)"
      pattern: "event\\.listen"
    - from: "tests/async/test_stability_arrow.py"
      to: "pyarrow allocator counter"
      via: "total_allocated_bytes delta across N cycles"
      pattern: "total_allocated_bytes"
---

<objective>
Prove no Arrow allocator growth across many async cursor lifecycles (TEST-03).
The primary signal is the `pyarrow.total_allocated_bytes()` delta across N>=100
cycles of (open -> execute -> `fetch_arrow_table` -> checkin) on the real DuckDB
pool, asserting the post-drop + `gc.collect()` delta returns to baseline (no
monotonic growth) — NOT process RSS (D-27-07). A belt-and-braces second assertion
counts the pool `reset` event firing once per checkin (the
`_release_arrow_allocators` symmetric-cleanup invariant, ACONN-06 / D-27-08),
proven by a SQLAlchemy event listener — without touching frozen `src/`. Run
×{asyncio, trio} (D-27-09).

Purpose: Closes the highest async-DB-wrapper failure mode (silent Arrow-allocator
leak) with a deterministic, non-flaky metric.

Output: `tests/async/test_stability_arrow.py`.

CONSTRAINT (locked): NO `src/` changes. The reset count is gathered via
`sqlalchemy.event.listen` on the pool, not by patching `_release_arrow_allocators`.
If a test cannot pass without a source change, STOP and raise it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@.claude/skills/adbc-poolhouse-docs-author/SKILL.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-dual-backend-test-matrix/27-CONTEXT.md
@.planning/phases/27-dual-backend-test-matrix/27-PATTERNS.md
@.planning/phases/27-dual-backend-test-matrix/27-RESEARCH.md

# Analogs:
@tests/async/test_edge_resource.py
@tests/async/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Arrow allocator-stability + reset-count test (N>=100, both backends)</name>
  <files>tests/async/test_stability_arrow.py</files>
  <read_first>
    - tests/async/test_edge_resource.py:27-72 — `TestEdge21ArrowLifetime` is the closest analog: same real DuckDB pool, same `execute -> fetch_arrow_table -> checkin -> del tbl` loop, same `checkedout() == 0` assertion, same `del anyio_backend_name` convention (:57) when requested-but-unused. Extend the loop count from `_LIFETIME_LOOPS = 5` (:24) to `_N = 100`.
    - 27-PATTERNS.md "test_stability_*.py" section — the verified allocator-delta snippet (`gc.collect(); baseline = pyarrow.total_allocated_bytes(); ...loop...; gc.collect(); assert delta == 0`) and the `sqlalchemy.event.listen(pool._pool, "reset", counter)` reset-count snippet.
    - 27-RESEARCH.md Pitfall 5 — the documented fallback if exact-zero flakes on a future Arrow build: relax to `delta < single_table_bytes` (still "no monotonic growth").
  </read_first>
  <action>
    Create `tests/async/test_stability_arrow.py` (TEST-03). Define `_N = 100`
    (D-27-07: N>=100). Write an `@pytest.mark.anyio` async test taking
    `duckdb_async_pool` (and `anyio_backend_name`, `del`-ing it if unused, per the
    `test_edge_resource.py:57` convention so it runs ×{asyncio, trio}, D-27-09).

    Before the loop: register a reset listener via
    `sqlalchemy.event.listen(duckdb_async_pool._pool, "reset", _on_reset)` that
    increments a `nonlocal reset_count` (D-27-08 / ACONN-06 — counts the
    `_release_arrow_allocators` reset path WITHOUT touching frozen source). Then
    `gc.collect()` and capture `baseline = pyarrow.total_allocated_bytes()`.

    Loop `_N` times: `async with await duckdb_async_pool.connect() as conn:` ->
    `cur = conn.cursor()` -> `await cur.execute(f"SELECT {i} AS n")` ->
    `tbl = await cur.fetch_arrow_table()` -> `del tbl` inside the loop (drop the
    only ref); the context exit fires the reset event.

    After the loop: `gc.collect()`; compute
    `delta = pyarrow.total_allocated_bytes() - baseline`; assert
    `delta == 0` (no monotonic growth — verified deterministic this session). If
    that proves brittle on a build, the documented fallback is
    `assert delta < single_table_bytes` (RESEARCH Pitfall 5) — try exact-zero
    first. Assert `reset_count == _N` (one reset per checkin, ACONN-06).

    Do NOT use process RSS (D-27-07 rejects it as non-deterministic). Do NOT patch
    `_release_arrow_allocators` or any `src/` symbol.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/async/test_stability_arrow.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/async/test_stability_arrow.py` exists; `_N >= 100`.
    - The test asserts a bounded/zero `total_allocated_bytes()` delta after `del` + `gc.collect()` (no monotonic growth) AND `reset_count == _N`.
    - The test carries `@pytest.mark.anyio` (runs under asyncio and trio); no `import asyncio`, no `@pytest.mark.asyncio`.
    - Reset count is gathered via `sqlalchemy.event.listen` on the pool, not by patching `src/`.
    - `.venv/bin/pytest tests/async/test_stability_arrow.py -x -q` exits 0; `git diff --stat src/` shows no change.
  </acceptance_criteria>
  <done>The stability test proves no Arrow allocator growth over N>=100 cycles and one reset per checkin, under both event loops, with no source change.</done>
</task>

</tasks>

<artifacts_produced>
This plan produces:
- **Test file:** `tests/async/test_stability_arrow.py` (TEST-03 allocator-stability + reset-count)
</artifacts_produced>

<threat_model>
## Trust Boundaries

Test-layer only. No `src/` changes, no network/auth/input surface, no
credentials. Runs entirely against an in-process DuckDB pool.

| Boundary | Description |
|----------|-------------|
| test process → in-proc DuckDB | Local file-backed DuckDB only; no network, no credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03 | (none applicable) | in-proc DuckDB stability loop | accept | Pure in-process test; no network, no credentials, no `src/` surface touched. SQLAlchemy reset listener is read-only observation. No package installs. |

No high-severity threats — block-on-high passes. No new attack surface
(test-layer); no live service; no credentials introduced.
</threat_model>

<verification>
- `.venv/bin/pytest tests/async/test_stability_arrow.py -x -q` passes.
- The new file introduces no hygiene violation (no `import asyncio`, no `@pytest.mark.asyncio`, `@pytest.mark.anyio` present) — confirmed by the Plan 27-05 meta-scan.
- No `src/` modification.
</verification>

<success_criteria>
- No monotonic Arrow allocator growth across N>=100 async cursor cycles.
- Reset event fires once per checkin (ACONN-06), proven via a SQLAlchemy listener.
- Runs under both asyncio and trio; no `src/` change.
</success_criteria>

<output>
Create `.planning/phases/27-dual-backend-test-matrix/27-03-SUMMARY.md` when done.
</output>
