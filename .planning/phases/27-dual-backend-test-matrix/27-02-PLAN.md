---
phase: 27-dual-backend-test-matrix
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - tests/async/test_matrix_readpath.py
autonomous: true
requirements: [TEST-01, TEST-02]
must_haves:
  truths:
    - "The read-path surface (connect -> execute -> fetch_arrow_table -> checkin) runs green parametrized over DuckDB and the Snowflake cassette"
    - "Every read-path test runs under both asyncio and trio via @pytest.mark.anyio"
    - "The Snowflake leg replays the existing cassette offline and skips cleanly when the driver or cassette is absent"
    - "checkedout() == 0 after each read-path round trip on both backends"
  artifacts:
    - path: "tests/async/test_matrix_readpath.py"
      provides: "TEST-01/02 read-path backend-generic matrix"
      contains: "snowflake_async_pool"
      min_lines: 30
  key_links:
    - from: "tests/async/test_matrix_readpath.py"
      to: "snowflake_async_pool / duckdb_async_pool fixtures"
      via: "request.getfixturevalue indirect parametrization"
      pattern: "getfixturevalue"
    - from: "tests/async/test_matrix_readpath.py"
      to: "pytest-adbc-replay cassette"
      via: "pytest.mark.adbc_cassette marker on the snowflake param"
      pattern: "adbc_cassette"
---

<objective>
Prove the async layer is backend-generic and backend-neutral on the read path
(TEST-01/02): the happy-path lifecycle (connect -> execute -> `fetch_arrow_table`
-> checkin) and the `AsyncCursor` fetch surface run green parametrized over
DuckDB (in-proc) AND the Snowflake pytest-adbc-replay cassette, each under both
asyncio and trio. One async layer, two real backends, two event loops.

Purpose: TEST-02 is satisfied by the surface tests passing on both real backends
(D-27-06) — not by forcing gating/cancel tests through the cassette. TEST-01 (the
asyncio/trio axis) comes free from `@pytest.mark.anyio` + the existing
`anyio_backend` fixture.

Output: `tests/async/test_matrix_readpath.py`.

CONSTRAINT (locked): NO `src/` changes. Read-path / cassette only — the
`ReplayCursor` cannot block-gate a worker and lacks `adbc_cancel`, so keep this
file to happy-path SELECT + `fetch_arrow_table` (D-27-05/06). If a test cannot
pass without a source change, STOP and raise it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@.claude/skills/adbc-poolhouse-docs-author/SKILL.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-dual-backend-test-matrix/27-CONTEXT.md
@.planning/phases/27-dual-backend-test-matrix/27-PATTERNS.md
@.planning/phases/27-dual-backend-test-matrix/27-01-SUMMARY.md

# Analogs:
@tests/async/test_async_lifecycle.py
@tests/async/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read-path backend matrix (DuckDB + Snowflake cassette, asyncio + trio)</name>
  <files>tests/async/test_matrix_readpath.py</files>
  <read_first>
    - tests/async/test_async_lifecycle.py:40+ — `TestHappyPath.test_execute_fetch_arrow_table` is the DuckDB read-path round trip to mirror; `:126-161` is the proven cassette leg body.
    - tests/async/conftest.py — `anyio_backend` (asyncio/trio axis), `duckdb_async_pool`, and the NEW `snowflake_async_pool` fixture from Plan 27-01.
    - 27-PATTERNS.md "test_matrix_*.py" section — the exact `@pytest.mark.parametrize("pool_fixture", [...])` + `request.getfixturevalue(pool_fixture)` indirect-request pattern and the `pytest.param("snowflake_async_pool", marks=[pytest.mark.snowflake, pytest.mark.adbc_cassette("snowflake_arrow_round_trip")])` form.
  </read_first>
  <action>
    Create `tests/async/test_matrix_readpath.py` (TEST-01/02). Write read-path
    test(s) that parametrize over the POOL FIXTURE NAME — `"duckdb_async_pool"`
    and `pytest.param("snowflake_async_pool", marks=[pytest.mark.snowflake,
    pytest.mark.adbc_cassette("snowflake_arrow_round_trip")])` — and resolve the
    pool lazily via `request.getfixturevalue(pool_fixture)` so the Snowflake leg
    `importorskip`/`skip`s independently without breaking the DuckDB leg. Each test
    carries `@pytest.mark.anyio` (the asyncio/trio axis — TEST-01 — comes from the
    existing `anyio_backend` fixture; do NOT hand-roll a backend loop). The body
    exercises the read-path surface: `async with await pool.connect() as conn:` ->
    `cur = conn.cursor()` -> `await cur.execute("SELECT 1 AS n, 'hello' AS s")` ->
    `table = await cur.fetch_arrow_table()` -> assert the returned object is a
    `pyarrow.Table` with the expected rows -> after the context exits, assert
    `pool._pool.checkedout() == 0`. Also cover the `AsyncCursor` fetch surface that
    the cassette supports (e.g. `fetchall`/`fetchone` on the same SELECT) per
    D-27-05, keeping strictly to read-path operations.

    CRITICAL (RESEARCH Pitfall 3): the cassette is mounted by the
    `@pytest.mark.adbc_cassette("snowflake_arrow_round_trip")` marker, NOT by the
    fixture — the marker MUST be on the snowflake `pytest.param`, alongside
    `@pytest.mark.snowflake`. Without it the Snowflake leg attempts a real
    connection.

    Use a SELECT shape the existing `snowflake_arrow_round_trip` cassette already
    covers; if a genuinely new query shape is required, STOP and raise it (a
    re-record needs live creds, per D-27-04) rather than inventing one.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/async/test_matrix_readpath.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/async/test_matrix_readpath.py` exists and `.venv/bin/pytest tests/async/test_matrix_readpath.py -x -q` exits 0 (DuckDB legs pass; Snowflake legs pass via cassette or skip cleanly if the driver/cassette is absent).
    - Every test function carries `@pytest.mark.anyio` (no `@pytest.mark.asyncio`, no `import asyncio`).
    - The snowflake param carries both `pytest.mark.snowflake` and `pytest.mark.adbc_cassette("snowflake_arrow_round_trip")`.
    - The body asserts `fetch_arrow_table()` returns a `pyarrow.Table` and `pool._pool.checkedout() == 0` after checkin.
    - `git diff --stat src/` shows no change.
  </acceptance_criteria>
  <done>The read-path matrix file passes on both backends across both event loops (Snowflake via cassette, or clean skip), with checkin proven and no source change.</done>
</task>

</tasks>

<artifacts_produced>
This plan produces:
- **Test file:** `tests/async/test_matrix_readpath.py` (TEST-01/02 read-path matrix)
</artifacts_produced>

<threat_model>
## Trust Boundaries

Test-layer only. No `src/` changes, no new network/auth/input surface, no
credentials. The Snowflake leg replays recorded cassette assets offline.

| Boundary | Description |
|----------|-------------|
| test process → cassette assets | Read-only offline replay of the `snowflake_arrow_round_trip` cassette; no live warehouse |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02 | Information disclosure | snowflake read-path leg | accept | Cassette replay is offline; no credentials introduced; the `adbc_cassette` marker intercepts before any network call. |
| T-27-LW | Tampering | live-warehouse guard | mitigate | Snowflake leg requires the recorded cassette + marker to run; absent either, it skips cleanly rather than reaching a real warehouse. No package installs in this plan. |

No high-severity threats — block-on-high passes. No new attack surface
(test-layer); cassette replay is offline; no credentials introduced.
</threat_model>

<verification>
- `.venv/bin/pytest tests/async/test_matrix_readpath.py -x -q` passes.
- `.venv/bin/python -c "from tests._async_harness.guard import scan_async_test_hygiene; assert scan_async_test_hygiene('tests/async')==[] or all(f.path.endswith('test_matrix_readpath.py')==False for f in [])"` — confirm the new file introduces no hygiene violation (no `import asyncio`, no `@pytest.mark.asyncio`, every async test `@pytest.mark.anyio`).
- No `src/` modification.
</verification>

<success_criteria>
- Read-path surface runs green parametrized over DuckDB and the Snowflake cassette, each under asyncio and trio.
- The Snowflake leg replays offline and skips cleanly when driver/cassette is absent.
- `checkedout() == 0` after each round trip; no `src/` change.
</success_criteria>

<output>
Create `.planning/phases/27-dual-backend-test-matrix/27-02-SUMMARY.md` when done.
</output>
