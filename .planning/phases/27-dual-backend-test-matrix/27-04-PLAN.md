---
phase: 27-dual-backend-test-matrix
plan: 04
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - tests/async/test_limiter_stress.py
autonomous: false
requirements: [TEST-04]
must_haves:
  truths:
    - "Under a 4x(pool_size+max_overflow) stub-gated flood, observed running-max equals pool_size+max_overflow and is never exceeded"
    - "Every queued worker eventually runs (no starvation) and borrowed_tokens drains to 0"
    - "A small real-DuckDB smoke flood completes with checkedout()==0 and no deadlock"
    - "Deadlock detection uses a real-clock time.monotonic() watchdog, never anyio.fail_after; the flood is loop-stable under both asyncio and trio"
  artifacts:
    - path: "tests/async/test_limiter_stress.py"
      provides: "TEST-04 stub-gated limiter flood + real-DuckDB smoke flood"
      contains: "real_clock_watchdog"
      min_lines: 40
  key_links:
    - from: "tests/async/test_limiter_stress.py"
      to: "tests.async._edge_helpers.real_clock_watchdog / await_inside"
      via: "importlib.import_module (async is a reserved word)"
      pattern: "import_module\\(.tests.async._edge_helpers."
    - from: "tests/async/test_limiter_stress.py"
      to: "BlockingStubConnection + AsyncConnection sharing one CapacityLimiter"
      via: "_stub_conn_on(limiter) flood"
      pattern: "CapacityLimiter"
---

<objective>
Prove no deadlock or starvation when concurrency exceeds `pool_size` (TEST-04).
The primary proof is a stub-gated deterministic flood reusing the EDGE-12 pattern
(D-27-10): launch `4 x (pool_size + max_overflow)` workers sharing ONE limiter,
gate them inside `execute`, assert observed running-max ==
`pool_size + max_overflow` (bound never exceeded) and that every queued worker
eventually runs (no starvation, `borrowed_tokens` drains to 0). Add a small
real-DuckDB smoke flood for realism. Deadlock detection uses a real-clock
`time.monotonic()` watchdog thread — NEVER `anyio.fail_after` (it autojumps to its
own deadline under the trio `MockClock`; Phase 24/25 landmine, D-27-11).

Purpose: Confirms the dedicated per-pool `CapacityLimiter(pool_size+max_overflow)`
holds the concurrency bound under saturation without self-deadlock or starvation.

Output: `tests/async/test_limiter_stress.py`.

CONSTRAINT (locked): NO `src/` changes. Stub-gated mechanics stay DuckDB+stub
only (D-27-06 — the cassette `ReplayCursor` cannot block-gate). If a test cannot
pass without a source change, STOP and raise it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@.claude/skills/adbc-poolhouse-docs-author/SKILL.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-dual-backend-test-matrix/27-CONTEXT.md
@.planning/phases/27-dual-backend-test-matrix/27-PATTERNS.md
@.planning/phases/26-packaging-extra-scoping/26-LEARNINGS.md

# Analogs:
@tests/async/test_edge_limiter.py
@tests/async/_edge_helpers.py
@tests/_async_harness/stubs.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stub-gated limiter flood (running-max == bound, no starvation) + real-DuckDB smoke flood</name>
  <files>tests/async/test_limiter_stress.py</files>
  <read_first>
    - tests/async/test_edge_limiter.py:269-318 — `TestEdge12ConcurrencyBound.test_running_max_equals_bound_under_flood` is the VERBATIM pattern to reuse: the `_stub_conn_on(limiter)` helper (:52-56), the flood-of-`bound*4` `tg.start_soon(functools.partial(cur.execute, ...))`, the `await await_inside(lambda: limiter.borrowed_tokens == bound)` saturation poll, the `assert limiter.borrowed_tokens == bound` (never exceeds), the `close()`-to-drain in `finally`, and the `await await_inside(lambda: limiter.borrowed_tokens == 0)` no-starvation assertion.
    - tests/async/test_edge_limiter.py:42-46 — the importlib cross-package helper import (`async` is a reserved word, so `tests.async._edge_helpers` cannot be a dotted import): `importlib.import_module("tests.async._edge_helpers")` then pull `await_inside` and `real_clock_watchdog`.
    - tests/async/test_edge_limiter.py:63-70 — `TestEdge09TokenAccounting` real-pool body to combine with a task-group flood for the smoke test.
    - tests/async/_edge_helpers.py:38-78 — `real_clock_watchdog`: wall-clock side thread that `close()`s stubs on overrun and yields a `[bool]` you assert `is False`. For the real-DuckDB smoke flood (no stubs to break), use `real_clock_watchdog([])`.
    - 26-LEARNINGS.md — the lost-wakeup / sticky-latch / `close()`-not-`release()` rationale.
  </read_first>
  <action>
    Create `tests/async/test_limiter_stress.py` (TEST-04). Import `await_inside`
    and `real_clock_watchdog` via
    `importlib.import_module("tests.async._edge_helpers")` (NOT a dotted import —
    `async` is a reserved word). Copy the `_stub_conn_on(limiter)` helper from
    `test_edge_limiter.py:52-56` (builds an `AsyncConnection` over a fresh
    `BlockingStubConnection` sharing one limiter).

    Stub-gated flood test (`@pytest.mark.anyio`, requests `anyio_backend_name`,
    `del`-ed if unused, runs ×{asyncio, trio}): set `bound = pool_size +
    max_overflow` using the real shipped defaults `5 + 3 = 8` (Open Question 1 —
    prove the shipped bound; the flood of 32 gated stubs is cheap, no real I/O).
    `flood = bound * 4`. Build `flood` stub pairs sharing one
    `anyio.CapacityLimiter(bound)`, take a cursor from each. Wrap in
    `real_clock_watchdog(stub_cursors)`; inside an `anyio.create_task_group()`,
    `tg.start_soon(functools.partial(cur.execute, f"SELECT {i}"))` for every cursor;
    `await await_inside(lambda: limiter.borrowed_tokens == bound)` then assert
    `limiter.borrowed_tokens == bound` (running-max never exceeds the bound); in a
    `finally`, drain by calling `.close()` on every stub cursor (NOT `release()` —
    `close()` latches the sticky `_closed` flag so `_block` short-circuits even for
    a worker that has not yet entered; a plain `release()` strands a late worker —
    the Phase 26 lost-wakeup), then
    `await await_inside(lambda: limiter.borrowed_tokens == 0)` (no starvation —
    every queued worker ran). After the task group, assert the watchdog flag is
    `False` and `limiter.borrowed_tokens == 0`.

    Real-DuckDB smoke flood (`@pytest.mark.anyio`, separate, smaller — D-27-10):
    flood `4 x bound` real `await pool.connect() -> execute -> fetch` tasks against
    `duckdb_async_pool` in a task group, wrapped in `real_clock_watchdog([])`
    (empty list — no stubs to break open); assert all complete and
    `pool._pool.checkedout() == 0` afterwards. Keep it small (the real driver
    returns promptly; the gating proof is the stub test).

    LANDMINES (both load-bearing): (1) drain with `close()`, never `release()`;
    (2) deadlock detection is `real_clock_watchdog`, NEVER `anyio.fail_after`
    (autojumps under the trio `MockClock(autojump_threshold=0)` the instant workers
    block off-loop, tripping every run — D-27-11). No `src/` changes; stub-gated
    mechanics stay DuckDB+stub (no cassette).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/async/test_limiter_stress.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/async/test_limiter_stress.py` exists with a stub-gated flood test AND a real-DuckDB smoke-flood test.
    - The flood uses `bound = 8` (5+3) and `flood = bound * 4`; asserts `borrowed_tokens == bound` at saturation and `== 0` after drain.
    - Drain uses `.close()` on stub cursors (grep shows no `release()` drain); deadlock detection uses `real_clock_watchdog` (grep shows no `anyio.fail_after`).
    - Both tests carry `@pytest.mark.anyio` (run under asyncio and trio); no `import asyncio`, no `@pytest.mark.asyncio`.
    - `.venv/bin/pytest tests/async/test_limiter_stress.py -x -q` exits 0; `git diff --stat src/` shows no change.
  </acceptance_criteria>
  <done>The limiter-stress file proves running-max == bound, no starvation, and a clean real-DuckDB smoke flood, under both event loops, with a real-clock watchdog and no source change.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The TEST-04 limiter-stress flood — the phase's most race-prone test (off-loop worker gating). Per the project flaky-concurrency rule, a single-shot green does not prove cross-platform stability (Phase 24-26 landmine: passes 20/20 on macOS, hangs on Linux CI).</what-built>
  <how-to-verify>
    Run the ×20 loop-stability gate locally (0 hangs). MEMORY landmine: the Bash
    tool runs zsh; `if ! cmd` inside a for-loop silently skips the command and
    fakes a green loop — use `rc=$?` + grep for the pass line:
    ```
    for i in $(seq 1 20); do
      .venv/bin/pytest tests/async/test_limiter_stress.py -x -q >/tmp/loop27_04.$i.log 2>&1
      rc=$?
      grep -q "passed" /tmp/loop27_04.$i.log || { echo "FAIL iter $i (rc=$rc)"; break; }
    done
    ```
    Confirm all 20 iterations printed a "passed" line (no hang, no break). The
    cross-platform Linux CI gate is verified at the phase level in Plan 27-05.
  </how-to-verify>
  <resume-signal>Type "approved" when 20/20 iterations passed with 0 hangs, or describe the failure.</resume-signal>
</task>

</tasks>

<artifacts_produced>
This plan produces:
- **Test file:** `tests/async/test_limiter_stress.py` (TEST-04 stub-gated flood + real-DuckDB smoke flood)
</artifacts_produced>

<threat_model>
## Trust Boundaries

Test-layer only. No `src/` changes, no network/auth/input surface, no
credentials. Stub-gated mechanics + in-proc DuckDB only.

| Boundary | Description |
|----------|-------------|
| test process → in-proc DuckDB / stubs | Local stubs + file-backed DuckDB; no network, no credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-04 | Denial of service (test hang) | off-loop gated flood | mitigate | Deadlock guarded by a real-clock `time.monotonic()` watchdog thread (never `anyio.fail_after`); `close()`-drain avoids the lost-wakeup; ×20 loop gate + Linux CI gate the cross-platform race. No network, no credentials, no `src/` surface. No package installs. |

No high-severity threats — block-on-high passes. No new attack surface
(test-layer); no live service; no credentials introduced.
</threat_model>

<verification>
- `.venv/bin/pytest tests/async/test_limiter_stress.py -x -q` passes.
- ×20 loop-stability gate (checkpoint task) passes locally with 0 hangs.
- The new file introduces no hygiene violation (confirmed by the Plan 27-05 meta-scan).
- No `src/` modification.
</verification>

<success_criteria>
- Running-max == `pool_size + max_overflow` under a 4x flood; never exceeded.
- No starvation (`borrowed_tokens` drains to 0); real-DuckDB smoke flood completes with `checkedout() == 0`.
- Real-clock watchdog (not `fail_after`); ×20 loop-stable; no `src/` change.
</success_criteria>

<output>
Create `.planning/phases/27-dual-backend-test-matrix/27-04-SUMMARY.md` when done.
</output>
