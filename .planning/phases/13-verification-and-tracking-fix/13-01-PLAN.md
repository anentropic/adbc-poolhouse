---
phase: 13-verification-and-tracking-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - justfile
  - .planning/phases/10-sqlite-backend/10-01-SUMMARY.md
  - .planning/phases/10-sqlite-backend/10-02-SUMMARY.md
  - .planning/phases/10-sqlite-backend/10-03-SUMMARY.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - SQLT-01
  - SQLT-02
  - SQLT-03
  - SQLT-04
  - SQLT-05
  - DBC-02

must_haves:
  truths:
    - "justfile install-foundry-drivers uses 'dbc install --pre clickhouse' (alpha driver requires --pre)"
    - "10-01-SUMMARY.md frontmatter contains requirements-completed: [SQLT-01, SQLT-02]"
    - "10-02-SUMMARY.md frontmatter contains requirements-completed: [SQLT-03]"
    - "10-03-SUMMARY.md frontmatter contains requirements-completed: [SQLT-04]"
    - "REQUIREMENTS.md shows [x] for SQLT-01, SQLT-02, SQLT-03, SQLT-04, SQLT-05, DBC-02"
    - "REQUIREMENTS.md traceability table shows SQLT-01–05 as Phase 10 / Complete"
    - "REQUIREMENTS.md traceability table shows DBC-02 as Phase 11 / Complete"
  artifacts:
    - path: "justfile"
      provides: "install-foundry-drivers recipe with --pre flag for ClickHouse"
      contains: "dbc install --pre clickhouse"
    - path: ".planning/phases/10-sqlite-backend/10-01-SUMMARY.md"
      provides: "SQLT-01 and SQLT-02 tracking in frontmatter"
      contains: "requirements-completed"
    - path: ".planning/phases/10-sqlite-backend/10-02-SUMMARY.md"
      provides: "SQLT-03 tracking in frontmatter"
      contains: "requirements-completed"
    - path: ".planning/phases/10-sqlite-backend/10-03-SUMMARY.md"
      provides: "SQLT-04 tracking in frontmatter"
      contains: "requirements-completed"
    - path: ".planning/REQUIREMENTS.md"
      provides: "All six requirement checkboxes marked complete"
      contains: "[x] **SQLT-01**"
  key_links:
    - from: "justfile"
      to: "ClickHouse alpha driver install"
      via: "--pre flag on dbc install"
      pattern: "dbc install --pre clickhouse"
    - from: ".planning/REQUIREMENTS.md"
      to: "SQLT-01–05 checkboxes"
      via: "markdown checkbox update"
      pattern: "\\[x\\] \\*\\*SQLT-0"
    - from: ".planning/REQUIREMENTS.md"
      to: "traceability table"
      via: "Phase 10 / Complete entries"
      pattern: "SQLT-01 \\| Phase 10 \\| Complete"
---

<objective>
Close all mechanical tracking gaps for the v1.0 audit: fix the justfile --pre flag, add requirements-completed frontmatter to three Phase 10 SUMMARY files, and update all six requirement checkboxes and traceability table rows in REQUIREMENTS.md.

Purpose: These are pure tracking repairs — the code for SQLT-01–05 and DBC-02 is already implemented and tested. The 3-source cross-reference (VERIFICATION.md + SUMMARY frontmatter + REQUIREMENTS.md) cannot pass until these three artifacts are consistent.
Output: justfile fixed; 10-01, 10-02, 10-03 SUMMARY files with requirements-completed; REQUIREMENTS.md with SQLT-01–05 and DBC-02 marked [x] Complete.
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
@.claude/skills/adbc-poolhouse-docs-author/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-verification-and-tracking-fix/13-RESEARCH.md
@.planning/v1.0-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix justfile --pre flag and update SUMMARY frontmatter</name>
  <files>justfile, .planning/phases/10-sqlite-backend/10-01-SUMMARY.md, .planning/phases/10-sqlite-backend/10-02-SUMMARY.md, .planning/phases/10-sqlite-backend/10-03-SUMMARY.md</files>
  <action>
Two independent edits in one task (both are small targeted changes):

**justfile fix (closes DBC-02):**

Read `justfile` and change the `dbc install clickhouse` line to `dbc install --pre clickhouse`. Also update the comment block above `install-foundry-drivers` to document why `--pre` is needed:

```just
# Install MySQL and ClickHouse Foundry ADBC drivers into the active virtualenv.
# dbc detects VIRTUAL_ENV automatically — no --level flag required.
# Two separate calls: dbc install with multiple args is not confirmed by official docs.
# ClickHouse requires --pre: only alpha v0.1.0-alpha.1 is currently published.
install-foundry-drivers:
    dbc install mysql
    dbc install --pre clickhouse
```

Only change lines 22-27. Leave all other justfile content unchanged.

**SUMMARY.md frontmatter repairs (closes SQLT-01–04 tracking):**

For each of the three files, read the current content, then rewrite with the `requirements-completed` field inserted after the `completed:` line and before the closing `---`:

- `.planning/phases/10-sqlite-backend/10-01-SUMMARY.md`:
  Add after `completed: 2026-03-01`:
  ```yaml
  requirements-completed:
    - SQLT-01
    - SQLT-02
  ```

- `.planning/phases/10-sqlite-backend/10-02-SUMMARY.md`:
  Add after `completed: 2026-03-01`:
  ```yaml
  requirements-completed:
    - SQLT-03
  ```

- `.planning/phases/10-sqlite-backend/10-03-SUMMARY.md`:
  Add after `completed: 2026-03-01`:
  ```yaml
  requirements-completed:
    - SQLT-04
  ```

The body content of each file (below the frontmatter `---` closer) must remain completely unchanged.

Note: 10-04-SUMMARY.md already has `requirements-completed: [SQLT-05]` — do NOT modify it.

Mapping rationale (from research document):
- 10-01 delivered: SQLiteConfig class (SQLT-01) + translate_sqlite() function (SQLT-02)
- 10-02 delivered: sqlite optional extra in pyproject.toml + uv.lock (SQLT-03)
- 10-03 delivered: Full SQLite test suite — 8 config tests, 3 translator tests, 2 pool factory tests (SQLT-04)
  </action>
  <verify>
    <automated>grep -n "dbc install --pre clickhouse" /Users/paul/Documents/Dev/Personal/adbc-poolhouse/justfile && grep -n "requirements-completed" /Users/paul/Documents/Dev/Personal/adbc-poolhouse/.planning/phases/10-sqlite-backend/10-01-SUMMARY.md && grep -n "requirements-completed" /Users/paul/Documents/Dev/Personal/adbc-poolhouse/.planning/phases/10-sqlite-backend/10-02-SUMMARY.md && grep -n "requirements-completed" /Users/paul/Documents/Dev/Personal/adbc-poolhouse/.planning/phases/10-sqlite-backend/10-03-SUMMARY.md</automated>
  </verify>
  <done>justfile contains "dbc install --pre clickhouse"; all three SUMMARY files contain requirements-completed frontmatter with correct requirement IDs</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md checkboxes and traceability table</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Read `.planning/REQUIREMENTS.md` fully, then write the updated version with two sets of changes:

**1. SQLite section checkboxes (lines ~116-120) — change `[ ]` to `[x]` for all five:**

```markdown
- [x] **SQLT-01**: `SQLiteConfig` — Pydantic `BaseSettings`; `env_prefix="SQLITE_"`; `model_validator` raises `ValueError` for `uri=":memory:"` with `pool_size > 1` (same guard as DuckDB)
- [x] **SQLT-02**: `translate_sqlite()` — pure function mapping `SQLiteConfig` fields to adbc_driver_manager kwargs
- [x] **SQLT-03**: `sqlite` optional extra in pyproject.toml (`adbc-driver-sqlite>=1.0.0`); included in `[all]` meta-extra; uv.lock updated
- [x] **SQLT-04**: Unit tests for `SQLiteConfig` validation; unit tests for `translate_sqlite()` asserting exact kwargs dict; mock-at-`create_adbc_connection` test asserting full pool-factory wiring; integration test with in-memory SQLite database
- [x] **SQLT-05**: `SQLiteConfig` exported from `__init__.py`; SQLite warehouse guide page in docs; API reference entry; `uv run mkdocs build --strict` passes
```

**2. Foundry Tooling section — change DBC-02 from `[ ]` to `[x]`:**

```markdown
- [x] **DBC-02**: `justfile` recipe `install-foundry-drivers` — runs `dbc install mysql clickhouse` with `--level env` to scope drivers to active virtualenv
```

**3. Traceability table — update the 6 affected rows:**

Current state (these 6 rows need updating):
```
| SQLT-01 | Phase 13 | Pending |
| SQLT-02 | Phase 13 | Pending |
| SQLT-03 | Phase 13 | Pending |
| SQLT-04 | Phase 13 | Pending |
| SQLT-05 | Phase 13 | Pending |
| DBC-02  | Phase 13 | Pending |
```

Target state:
```
| SQLT-01 | Phase 10 | Complete |
| SQLT-02 | Phase 10 | Complete |
| SQLT-03 | Phase 10 | Complete |
| SQLT-04 | Phase 10 | Complete |
| SQLT-05 | Phase 10 | Complete |
| DBC-02  | Phase 11 | Complete |
```

The phase column reflects WHERE the code was actually built, not the gap closure phase. Phase 13 is the verification phase — the implementation lives in Phase 10 (SQLT) and Phase 11 (DBC-02).

Also update the footer "Last updated" timestamp line to today's date (2026-03-02) and add a note about Phase 13 gap closure. For example:
```
*Last updated: 2026-03-02 — Phase 13 gap closure: SQLT-01–05 and DBC-02 checkboxes updated; traceability corrected to implementation phases*
```

All other content in REQUIREMENTS.md must remain unchanged.
  </action>
  <verify>
    <automated>grep -c "\[x\] \*\*SQLT-" /Users/paul/Documents/Dev/Personal/adbc-poolhouse/.planning/REQUIREMENTS.md && grep "DBC-02" /Users/paul/Documents/Dev/Personal/adbc-poolhouse/.planning/REQUIREMENTS.md | head -5 && grep "SQLT-01" /Users/paul/Documents/Dev/Personal/adbc-poolhouse/.planning/REQUIREMENTS.md | grep "Phase 10"</automated>
  </verify>
  <done>REQUIREMENTS.md has 5 SQLT checkboxes marked [x]; DBC-02 marked [x]; traceability table shows SQLT-01–05 as "Phase 10 / Complete" and DBC-02 as "Phase 11 / Complete"</done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep "dbc install --pre clickhouse" justfile` returns a match
2. `grep -c "requirements-completed" .planning/phases/10-sqlite-backend/10-01-SUMMARY.md` returns 1
3. `grep -c "requirements-completed" .planning/phases/10-sqlite-backend/10-02-SUMMARY.md` returns 1
4. `grep -c "requirements-completed" .planning/phases/10-sqlite-backend/10-03-SUMMARY.md` returns 1
5. `grep -c "\[x\] \*\*SQLT-" .planning/REQUIREMENTS.md` returns 5
6. `grep "DBC-02" .planning/REQUIREMENTS.md | grep "\[x\]"` returns a match
7. `grep "SQLT-01" .planning/REQUIREMENTS.md | grep "Phase 10"` returns a match
</verification>

<success_criteria>
- justfile line 27: `dbc install --pre clickhouse` (not `dbc install clickhouse`)
- 10-01-SUMMARY.md frontmatter: contains `requirements-completed` with SQLT-01 and SQLT-02
- 10-02-SUMMARY.md frontmatter: contains `requirements-completed` with SQLT-03
- 10-03-SUMMARY.md frontmatter: contains `requirements-completed` with SQLT-04
- REQUIREMENTS.md: SQLT-01 through SQLT-05 all marked [x]
- REQUIREMENTS.md: DBC-02 marked [x]
- REQUIREMENTS.md traceability table: SQLT-01–05 show Phase 10 / Complete; DBC-02 shows Phase 11 / Complete
</success_criteria>

<output>
After completion, create `.planning/phases/13-verification-and-tracking-fix/13-01-SUMMARY.md`
</output>
