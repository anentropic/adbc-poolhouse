---
phase: 25-cancellation
plan: 03
type: execute
wave: 3
depends_on: ["25-01", "25-02"]
files_modified:
  - tests/async/test_edge_cancel_depth.py
  - tests/async/test_edge_backend_parity.py
autonomous: true
requirements: [CANCEL-01, CANCEL-02, CANCEL-03, CANCEL-04, EDGE-01, EDGE-02, EDGE-03, EDGE-04, EDGE-05, EDGE-06, EDGE-07, EDGE-29]
must_haves:
  truths:
    - "Cancel before the offload starts touches the driver not at all (no execute, no adbc_cancel, no invalidate) and propagates"
    - "Cancel during a blocked worker fires adbc_cancel exactly once, invalidates once, leaves checkedout()==0, propagates"
    - "A double-cancel during shielded cleanup stays idempotent (one adbc_cancel, one invalidate, one cancel exception)"
    - "move_on_after on a finished op does nothing (cancelled_caught False, no adbc_cancel, no invalidate)"
    - "fail_after and scope.cancel() both abort identically apart from the surfaced exception type"
    - "The (adbc_cancel_count, invalidate_count, checkedout_after) tuple is equal under asyncio and trio"
    - "Every cancel test is loop-stable (×20, 0 hangs) under both backends, wall-clock-watchdog-wrapped, no stranded worker"
  artifacts:
    - path: "tests/async/test_edge_cancel_depth.py"
      provides: "EDGE-01/02/03/04/05/06/07 stub-driven cancel suite, dual-backend, ×20-loopable"
      min_lines: 120
    - path: "tests/async/test_edge_backend_parity.py"
      provides: "EDGE-29 cross-backend tuple equality (session-scoped dict + cross-leg assert)"
      min_lines: 30
  key_links:
    - from: "tests/async/test_edge_cancel_depth.py"
      to: "BlockingStubCursor.adbc_cancel_call_count / BlockingStubConnection.invalidate_call_count"
      via: "assert the observable cancel signals"
      pattern: "adbc_cancel_call_count|invalidate_call_count"
    - from: "tests/async/test_edge_cancel_depth.py"
      to: "real_clock_watchdog / await_inside"
      via: "wall-clock watchdog + worker-inside gate (no sleeps)"
      pattern: "real_clock_watchdog|await_inside"
    - from: "tests/async/test_edge_backend_parity.py"
      to: "session-scoped tuple dict"
      via: "both backend legs write; one assertion compares"
      pattern: "anyio_backend_name"
---

<objective>
Build the deterministic cancel-depth EDGE suite (EDGE-01 through EDGE-07, covering CANCEL-01/02/03) and the cross-backend parity test (EDGE-29, covering CANCEL-04), driving every cancel through the Phase 23/24 stub harness (`BlockingStubCursor.adbc_cancel`, `await_inside`, `real_clock_watchdog`) with zero sleeps and the dual-backend `anyio_backend` fixture.

Purpose: This is the primary correctness proof for the milestone's highest-risk item. Each EDGE leg asserts the exact observable signals from VALIDATION.md (`adbc_cancel_call_count`, `invalidate_call_count`, `tripped[0]`, surfaced exception identity) and EDGE-29 asserts the `(adbc_cancel_count, invalidate_count, checkedout_after)` tuple is byte-identical under asyncio and trio. Per the MEMORY loop-flaky-concurrency lesson, every test runs in a ×20 loop (0 hangs) under both backends, wall-clock-watchdog-wrapped — a single green run is NOT acceptance.
Output: `test_edge_cancel_depth.py` (NEW) and `test_edge_backend_parity.py` (NEW).
</objective>

<execution_context>
@.claude/skills/adbc-poolhouse-docs-author/SKILL.md
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-cancellation/25-CONTEXT.md
@.planning/phases/25-cancellation/25-RESEARCH.md
@.planning/phases/25-cancellation/25-PATTERNS.md
@.planning/phases/25-cancellation/25-VALIDATION.md
@tests/async/test_edge_aliasing.py
@tests/async/_edge_helpers.py
@tests/async/conftest.py
</context>

<artifacts_this_phase_produces>
This plan produces (of the phase-wide artifact set):
- `tests/async/test_edge_cancel_depth.py` (NEW) — EDGE-01/02/03/04/05/06/07, stub-driven, dual-backend, ×20-loopable.
- `tests/async/test_edge_backend_parity.py` (NEW) — EDGE-29 tuple equality.
Consumes: `cancellable_offload` + `AsyncConnection.invalidate` (25-02), `BlockingStubConnection.invalidate_call_count` (25-01), the existing `make_stub_async_connection`/`await_inside`/`real_clock_watchdog`/`virtual_clock` harness.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: EDGE-01..07 cancel-depth suite (stub-driven, dual-backend, ×20-loopable)</name>
  <read_first>
    - tests/async/test_edge_aliasing.py (lines 18-34 module header + importlib dance for the `async`-reserved dir; lines 59-74 gating choreography) — copy verbatim structure.
    - tests/async/test_edge_limiter.py::TestEdge10CancelWhileQueued (cancel-scope + `await_inside` + `real_clock_watchdog` + release-in-`finally`) — the cancel-trigger analog.
    - tests/async/_edge_helpers.py — `real_clock_watchdog` (line 39) and `await_inside` (line 81) signatures; tests/_async_harness/clock.py `virtual_clock` (line 37).
    - tests/async/conftest.py — `make_stub_async_connection` (line 98), `anyio_backend`/`anyio_backend_name` (line 47), `duckdb_async_pool` (line 76).
    - tests/_async_harness/stubs.py — `BlockingStubCursor.adbc_cancel`/`adbc_cancel_call_count`/`observed_cancel`/`execute_call_count`/re-armable gate/`release`; `BlockingStubConnection.invalidate_call_count` (added in 25-01).
    - .planning/phases/25-cancellation/25-RESEARCH.md Code Examples (EDGE-02 lines 402-423, EDGE-06 lines 425-441), Pitfalls 2/3/4/5, the Observable Signals table.
    - .planning/phases/25-cancellation/25-PATTERNS.md §"test_edge_cancel_depth.py" + §"Gated stub-cancel test choreography".
  </read_first>
  <action>
    Create `tests/async/test_edge_cancel_depth.py`. Copy the module header + importlib helper-import dance from `test_edge_aliasing.py` (lines 18-34). Write one `@pytest.mark.anyio` test per EDGE leg, each requesting `make_stub_async_connection` + `anyio_backend_name`, each wrapping its body in `with real_clock_watchdog(stub_conn.cursors) as tripped:` and asserting `tripped[0] is False` (never `anyio.fail_after` as the watchdog — Pitfall 3). Use `await_inside(lambda: stub_cursor.execute_call_count == 1)` to gate "worker is inside the block" before driving the cancel; no positive-duration sleeps. Legs and their asserted signals (name tests so `-k before/during/escapes/double/checkin/parity/finished` selects them):
      - EDGE-01 `before`: deliver the cancel BEFORE entering `cursor.execute` (cancel the scope before the offload starts) → assert `stub_cursor.execute_call_count == 0`, `adbc_cancel_call_count == 0`, `invalidate_call_count == 0`, cancel propagates.
      - EDGE-02 `during`: gate worker inside, cancel via `anyio.move_on_after(0)` (or a nested task group `cancel_scope.cancel()`) → assert `adbc_cancel_call_count == 1`, `invalidate_call_count == 1`, `observed_cancel is True`, cancel propagates.
      - EDGE-03 `escapes`: assert the exact `get_cancelled_exc_class()` instance escapes (or the caller's `TimeoutError` for `fail_after`) and the test does not hang under trio (`tripped[0] is False`).
      - EDGE-04 `double`: arrange a second cancellation to arrive during the shielded cleanup → assert `adbc_cancel_call_count == 1` (NOT 2), `invalidate_call_count == 1`, exactly one cancel exception surfaces (D-25-07 idempotency).
      - EDGE-05 `checkin`: cancel during `__aexit__`/checkin → assert checkin still completes under shield; on the stub leg assert the cursor+connection cleanup counters; the real-driver `checkedout()==0` is asserted in EDGE-02's DuckDB variant / 25-04.
      - EDGE-06 `parity`: two legs — `with virtual_clock(anyio_backend_name): with pytest.raises(TimeoutError): with anyio.fail_after(5): ...gate... await cur.execute(...)` asserting `adbc_cancel_call_count == 1`; and an explicit `scope.cancel()` leg asserting `adbc_cancel_call_count == 1` with NO surfaced exception. Use `virtual_clock` + `fail_after`/`move_on_after` ONLY as the cancellation trigger under test, never as the safety net.
      - EDGE-07 `finished`: release the worker (happy path) BEFORE the `move_on_after` deadline → assert `move_on_after(...).cancelled_caught is False`, `adbc_cancel_call_count == 0`, `invalidate_call_count == 0`, `observed_cancel is False`.
    Add a DuckDB real-driver leg (name it `-k duckdb`) using `duckdb_async_pool` that cancels a real in-flight op and asserts `pool._pool.checkedout() == 0` end-to-end (CANCEL-01/02). Release every stub cursor in a `finally` so no worker is stranded. Each test must be safe to run repeatedly (re-armable gate); the loop-run command is the wave-merge gate below.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/async/test_edge_cancel_depth.py -q 2>&1 | tail -8; rc=${PIPESTATUS[0]}; [ "$rc" -eq 0 ] && echo DEPTH_PASS</automated>
  </verify>
  <acceptance_criteria>
    - All seven EDGE legs + the DuckDB leg pass under BOTH backends (asyncio + trio): `.venv/bin/pytest tests/async/test_edge_cancel_depth.py -q` prints `DEPTH_PASS`.
    - Signal assertions exactly match the VALIDATION.md table: `adbc_cancel_call_count` == 1 (EDGE-02/06), == 0 (EDGE-01/07), == 1 not 2 (EDGE-04); `invalidate_call_count` == 1 (EDGE-02/04/05), == 0 (EDGE-01/07); `tripped[0] is False` in every leg.
    - No `anyio.fail_after` used as a watchdog (only as a cancellation trigger under `virtual_clock`); no positive-duration `sleep` anywhere (`grep -nE 'sleep\(' tests/async/test_edge_cancel_depth.py` shows only `await_inside`/event-gating, no `time.sleep`/`anyio.sleep(<positive>)`).
    - Loop-stable: `for i in $(seq 1 20); do .venv/bin/pytest tests/async/test_edge_cancel_depth.py -q >/tmp/claude/ed.$i.log 2>&1; rc=$?; [ "$rc" -ne 0 ] && { echo "FAIL run $i"; break; }; done; grep -lc passed /tmp/claude/ed.20.log && echo LOOP20_CLEAN` reports 0 failures/0 hangs (use `rc=$?`, never `if ! pytest` — zsh `!` gotcha).
    - `.venv/bin/ruff check tests/async/test_edge_cancel_depth.py` clean; `.venv/bin/basedpyright tests/async/test_edge_cancel_depth.py` 0 errors.
  </acceptance_criteria>
  <done>EDGE-01..07 + the DuckDB real-driver leg pass under asyncio and trio, signal-exact, watchdog-wrapped, ×20-loop clean with 0 hangs.</done>
</task>

<task type="auto">
  <name>Task 2: EDGE-29 backend-parity tuple equality</name>
  <read_first>
    - tests/async/test_edge_cancel_depth.py (the EDGE-02 `during` body authored in Task 1) — reuse its exact cancel choreography byte-identically so the only variable is the backend.
    - tests/async/test_edge_aliasing.py — dual-backend stub body shape.
    - .planning/phases/25-cancellation/25-RESEARCH.md EDGE-29 example (lines 456-467) + the implementation note (session-scoped dict + cross-leg assert via finalizer or a non-parametrized reader test).
    - .planning/phases/25-cancellation/25-PATTERNS.md §"test_edge_backend_parity.py".
    - tests/async/conftest.py — `anyio_backend`/`anyio_backend_name` to confirm both legs run.
  </read_first>
  <action>
    Create `tests/async/test_edge_backend_parity.py`. Add a session-scoped dict fixture (e.g. `record_cancel_tuple`) that both parametrized backend legs write into, keyed by `anyio_backend_name`. Write one `@pytest.mark.anyio` test that runs the canonical EDGE-02 cancel (the same choreography as Task 1's `during` leg — keep setup byte-identical) and stores `(adbc_cancel_count, invalidate_count, checkedout_after)` into the dict under the current backend name. Then assert cross-leg equality via a session finalizer OR a dedicated non-parametrized test that reads the dict after both legs populated it and asserts `dict["asyncio"] == dict["trio"]`. Wrap the cancel body in `real_clock_watchdog` and gate with `await_inside` (no sleeps). For the stub leg, `checkedout_after` is read from the stub's accounting (or recorded as the invalidate-driven 0); document which signal stands in for `checkedout()` on the stub path. The load-bearing assertion is tuple EQUALITY, not the individual values (those are covered by Task 1).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/async/test_edge_backend_parity.py -q 2>&1 | tail -6; rc=${PIPESTATUS[0]}; [ "$rc" -eq 0 ] && echo PARITY_PASS</automated>
  </verify>
  <acceptance_criteria>
    - Both backend legs populate the session-scoped tuple dict; the cross-leg assertion confirms `dict["asyncio"] == dict["trio"]` (EDGE-29).
    - `.venv/bin/pytest tests/async/test_edge_backend_parity.py -q` prints `PARITY_PASS` (both legs + the equality assertion green).
    - The cancel choreography is byte-identical to Task 1's EDGE-02 `during` leg (only the backend differs).
    - Loop-stable across ×20 with 0 hangs (same loop pattern as Task 1; `rc=$?`, never `if ! pytest`).
    - `.venv/bin/ruff check` + `.venv/bin/basedpyright` clean on the new file.
  </acceptance_criteria>
  <done>EDGE-29 proves the cancel-signal tuple is equal under asyncio and trio; loop-stable; lint/types clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (test-only) | This plan adds tests; it asserts the 25-02 trust boundaries (loop→worker cancel, cancelled-op→pool) are honored. No new boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03-DOS | Denial of Service | a single-shot green run hiding an intermittent deadlock (the MEMORY ~33% lesson) | mitigate | Every cancel test runs in a ×20 loop (0 hangs) under both backends, `real_clock_watchdog`-wrapped; the loop gate is acceptance, not a single run. `rc=$?` + grep the pass line (zsh `!` gotcha) |
| T-25-03-FLK | Denial of Service | `anyio.fail_after` watchdog autojumping under trio MockClock (false green/red) | mitigate | Watchdog is always `real_clock_watchdog` (wall-clock); `fail_after`/`move_on_after` used only as the cancellation trigger under `virtual_clock` (RESEARCH Pitfall 3) |
| T-25-03-SC | Tampering | npm/pip/cargo installs | accept | No installs; no new dependency |

No production-code threat: tests only. Their value is proving the 25-02 DoS/tampering mitigations actually hold under both backends and under load (×20).
</threat_model>

<verification>
- `.venv/bin/pytest tests/async/test_edge_cancel_depth.py tests/async/test_edge_backend_parity.py -q` green under both backends.
- ×20 loop of both files: 0 failures, 0 hangs (verify with `rc=$?` + grep the pass line; never `if ! pytest`).
- No positive-duration sleeps; no `anyio.fail_after` used as a watchdog.
- `.venv/bin/ruff check` + `.venv/bin/basedpyright` clean on both new files.
</verification>

<success_criteria>
- EDGE-01..07 each pass with VALIDATION.md-exact signals under asyncio and trio.
- EDGE-29 tuple equality holds (`asyncio == trio`).
- A DuckDB real-driver leg proves `checkedout()==0` end-to-end (CANCEL-01/02).
- ×20 loop clean (0 hangs) — the MEMORY lesson honored.
</success_criteria>

<output>
Create `.planning/phases/25-cancellation/25-03-SUMMARY.md` when done.
</output>
