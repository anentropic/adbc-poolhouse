---
phase: 25-cancellation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/_async_harness/stubs.py
  - tests/_async_harness/guard.py
  - tests/test_async_guard.py
autonomous: true
requirements: [EDGE-28]
must_haves:
  truths:
    - "BlockingStubConnection exposes invalidate() and a lock-guarded invalidate_call_count"
    - "The AST guard flags asyncio.CancelledError attribute access with a banned-asyncio-cancelled-error finding"
    - "The real _async/ package still scans clean (== []) after the new rule is added"
  artifacts:
    - path: "tests/_async_harness/stubs.py"
      provides: "BlockingStubConnection.invalidate + invalidate_call_count (D-04 LOCKED contract extension)"
      contains: "def invalidate"
    - path: "tests/_async_harness/guard.py"
      provides: "visit_Attribute rule banned-asyncio-cancelled-error"
      contains: "banned-asyncio-cancelled-error"
    - path: "tests/test_async_guard.py"
      provides: "self-test that the new rule fires on synthetic asyncio.CancelledError"
      contains: "banned-asyncio-cancelled-error"
  key_links:
    - from: "tests/_async_harness/guard.py"
      to: "Finding"
      via: "visit_Attribute appends a Finding"
      pattern: "banned-asyncio-cancelled-error"
    - from: "tests/test_async_guard.py"
      to: "scan_async_package"
      via: "synthetic-source scan asserts the rule"
      pattern: "scan_async_package"
---

<objective>
Land the two Wave-0 harness prerequisites that every cancel test in later waves depends on (VALIDATION.md Wave 0 gaps): (1) a `BlockingStubConnection.invalidate()` method plus a lock-guarded `invalidate_call_count` so stub-backed EDGE-02/04/05/29 can read the invalidate signal, and (2) the `banned-asyncio-cancelled-error` AST guard rule (EDGE-28, D-25-06) plus its synthetic self-test.

Purpose: These are PREREQUISITES. The cancel-logic plan (25-02) drives `AsyncConnection.invalidate()` → `self._fairy.invalidate()`, which the stub must expose; the EDGE tests assert `invalidate_call_count`. The guard rule is the EDGE-28 deliverable. Sequencing them first prevents churn and a half-built harness.
Output: An extended stub contract, a new AST rule, and a passing self-test — all in the test tree, zero production-code changes.
</objective>

<execution_context>
@.claude/skills/adbc-poolhouse-docs-author/SKILL.md
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-cancellation/25-CONTEXT.md
@.planning/phases/25-cancellation/25-RESEARCH.md
@.planning/phases/25-cancellation/25-PATTERNS.md
@.planning/phases/25-cancellation/25-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This plan produces (of the phase-wide artifact set):
- `BlockingStubConnection.invalidate()` + `invalidate_call_count` (NEW, lock-guarded; D-04 LOCKED-contract style) in `tests/_async_harness/stubs.py`.
- The `banned-asyncio-cancelled-error` guard rule (NEW `visit_Attribute` on `_GuardVisitor`) in `tests/_async_harness/guard.py`.
- A new self-test `test_bans_asyncio_cancelled_error` in `tests/test_async_guard.py` (top-level, sync — NOT `tests/async/`).
Downstream consumers (25-02..05): the stub `invalidate_call_count` signal and the guard rule.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add invalidate() + invalidate_call_count to BlockingStubConnection</name>
  <read_first>
    - tests/_async_harness/stubs.py — the file being modified; the `BlockingStubConnection` class, its `__init__` (zeroed counters), `close` (lines ~337-340: lock-guarded counter), and `adbc_cancel` (the D-04 LOCKED counter+flag contract). The class docstring `Attributes:` block is the LOCKED public contract.
    - .planning/phases/25-cancellation/25-PATTERNS.md §"tests/_async_harness/stubs.py" — the exact mirror instructions and the `close` analog.
    - .planning/phases/25-cancellation/25-RESEARCH.md Open Q1 / Assumption A4 — why this is the single harness gap.
  </read_first>
  <behavior>
    - Test 1: a fresh `BlockingStubConnection` has `invalidate_call_count == 0`.
    - Test 2: after one `conn.invalidate()` call, `invalidate_call_count == 1`.
    - Test 3: after two `conn.invalidate()` calls, `invalidate_call_count == 2` (counter increments, never resets).
  </behavior>
  <action>
    In `tests/_async_harness/stubs.py`, add `invalidate_call_count: int = 0` to `BlockingStubConnection.__init__` alongside the existing `close_call_count`/`adbc_cancel_call_count` initializers. Add an `invalidate(self) -> None` method that increments `self.invalidate_call_count` under `self._lock` (mirror the exact shape of the existing `close` method — one lock, one increment). Add `invalidate_call_count` to the class docstring `Attributes:` block (it is part of the D-04 LOCKED contract that EDGE assertions read by name). Per D-25-03, `AsyncConnection.invalidate()` calls `self._fairy.invalidate()`, so the stub method name must be exactly `invalidate`. Counter is the asserted signal: `invalidate_call_count == 1` after a cancelled scope, `== 0` on the no-cancel paths. Do NOT add any anyio import — `stubs.py` stays strictly pure-`threading` (D-03). Add a `>>>`-free `Example:` line or extend the existing one only if it stays under the docs-author skill's style; this is a test fake, so a one-line docstring is sufficient.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/_async_harness -q 2>&1 | tail -5; rc=${PIPESTATUS[0]}; [ "$rc" -eq 0 ] && echo HARNESS_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `BlockingStubConnection().invalidate_call_count == 0` on a fresh instance.
    - After one `invalidate()`, `invalidate_call_count == 1`; the increment is under `self._lock`.
    - `invalidate_call_count` appears in the class docstring `Attributes:` block.
    - `stubs.py` imports no anyio/trio (D-03 preserved) — verify with `grep -nE 'import (anyio|trio)' tests/_async_harness/stubs.py` returning nothing.
    - `.venv/bin/pytest tests/_async_harness -q` prints `HARNESS_PASS`.
  </acceptance_criteria>
  <done>BlockingStubConnection has a lock-guarded invalidate()/invalidate_call_count documented in the LOCKED Attributes contract; harness self-tests pass; no anyio leaked into stubs.py.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the banned-asyncio-cancelled-error AST rule + self-test</name>
  <read_first>
    - tests/_async_harness/guard.py — the file being modified; `_GuardVisitor` with `visit_Import` (lines ~53-65) and `visit_ImportFrom` (lines ~67-79) as the `Finding`-append analog; the module docstring's "Two rules are enforced" enumeration; the `Finding.rule` docstring.
    - .planning/phases/25-cancellation/25-PATTERNS.md §"tests/_async_harness/guard.py" and §"tests/test_async_guard.py" — the verbatim `visit_Attribute` body and the self-test mirror. NOTE THE CORRECTION: the self-test lives at top-level `tests/test_async_guard.py`, NOT `tests/async/`.
    - tests/test_async_guard.py — `TestAsyncGuard.test_bans_asyncio_import` (lines ~88-94) as the self-test analog; `TestRealAsyncPackage.test_scan_real_async_package_is_clean` (line ~155) which already covers the real-package clean scan for free.
    - .planning/phases/25-cancellation/25-RESEARCH.md Pattern 4 + D-25-06 (EDGE-28 intent).
  </read_first>
  <behavior>
    - Test 1: synthetic source `try:\n    pass\nexcept asyncio.CancelledError:\n    pass\n` scanned via `scan_async_package(tmp_path)` yields exactly one finding whose `rule == "banned-asyncio-cancelled-error"`.
    - Test 2 (regression-by-existing-test): the real `src/adbc_poolhouse/_async/` package still scans to `[]` (covered by the existing `test_scan_real_async_package_is_clean`).
  </behavior>
  <action>
    In `tests/_async_harness/guard.py`, add a `visit_Attribute(self, node: ast.Attribute) -> None` method to `_GuardVisitor` that appends a `Finding(self.path, node.lineno, "banned-asyncio-cancelled-error", "...use anyio.get_cancelled_exc_class()")` when `node.attr == "CancelledError"` AND `isinstance(node.value, ast.Name)` AND `node.value.id == "asyncio"`, then calls `self.generic_visit(node)` (mirror the exact append shape of `visit_Import`). Extend the module docstring's enforced-rules enumeration ("Two rules" → three) and the `Finding.rule` docstring to list `banned-asyncio-cancelled-error`. In `tests/test_async_guard.py`, add `test_bans_asyncio_cancelled_error(self, tmp_path)` inside `TestAsyncGuard` mirroring `test_bans_asyncio_import`: write the synthetic except-block source into `tmp_path`, call `scan_async_package(tmp_path)`, assert exactly one `banned-asyncio-cancelled-error` finding. Do NOT add a new real-package scan test — `TestRealAsyncPackage.test_scan_real_async_package_is_clean` already covers it. Note `import asyncio` is already banned, so this rule covers the `asyncio.CancelledError` attribute-chain form for completeness/intent.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_async_guard.py -q 2>&1 | tail -5; rc=${PIPESTATUS[0]}; [ "$rc" -eq 0 ] && echo GUARD_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `scan_async_package` on synthetic `except asyncio.CancelledError:` source returns exactly one finding with `rule == "banned-asyncio-cancelled-error"`.
    - The real `_async/` package still scans clean: `.venv/bin/pytest tests/test_async_guard.py::TestRealAsyncPackage -q` passes.
    - The module docstring and `Finding.rule` docstring both list the new rule id.
    - `.venv/bin/pytest tests/test_async_guard.py -q` prints `GUARD_PASS`.
  </acceptance_criteria>
  <done>The guard flags `asyncio.CancelledError` attribute access; the synthetic self-test passes; the real `_async/` scan stays empty.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-harness-only change; no untrusted input crosses any boundary. The guard parses source with `ast.parse` (read-only, never executes the inspected source). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01-G | Tampering | AST guard rule completeness | mitigate | The rule is unit-asserted (synthetic positive) AND the real-package clean scan is asserted — a silently-broken guard fails the self-test |
| T-25-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; no new third-party dependency (RESEARCH Package Legitimacy Audit: none added) |

No meaningful runtime threat: this plan touches only the test harness (stub fake + static AST linter) and adds no production code path, no I/O, no secrets.
</threat_model>

<verification>
- `.venv/bin/pytest tests/_async_harness tests/test_async_guard.py -q` is green.
- `grep -nE 'import (anyio|trio)' tests/_async_harness/stubs.py` returns nothing (D-03 preserved).
- `.venv/bin/ruff check tests/_async_harness tests/test_async_guard.py` clean.
- `.venv/bin/basedpyright tests/_async_harness/stubs.py tests/_async_harness/guard.py` 0 errors.
</verification>

<success_criteria>
- `BlockingStubConnection.invalidate()` + `invalidate_call_count` exist, lock-guarded, documented in the LOCKED contract.
- `banned-asyncio-cancelled-error` rule fires on synthetic source and the real `_async/` scan stays `[]`.
- All harness + guard self-tests pass; ruff + basedpyright clean.
</success_criteria>

<output>
Create `.planning/phases/25-cancellation/25-01-SUMMARY.md` when done.
</output>
