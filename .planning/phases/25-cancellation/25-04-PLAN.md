---
phase: 25-cancellation
plan: 04
type: execute
wave: 3
depends_on: ["25-01", "25-02"]
files_modified:
  - tests/async/test_edge_exceptiongroup.py
  - tests/async/test_edge_limiter.py
autonomous: true
requirements: [EDGE-19]
must_haves:
  truths:
    - "A genuine ADBC error survives the cancellable_offload task group as a bare AdbcError (not an ExceptionGroup), preserving exact type"
    - "After a non-cancel ADBC error the connection returns via the reset path (checkedout()==0), NOT invalidated"
    - "A limiter token returns to borrowed_tokens==0 exactly once on the cancel path (the D-24-02 cancel-mid-block leg), x50 loop"
  artifacts:
    - path: "tests/async/test_edge_exceptiongroup.py"
      provides: "EDGE-19 — bare-AdbcError unwrap + checkedout()==0 on a real DuckDB pool"
      min_lines: 25
    - path: "tests/async/test_edge_limiter.py"
      provides: "EDGE-09 cancel-mid-block leg (D-24-02) — borrowed_tokens==0 after a cancelled offload, x50"
      contains: "test_token_returns_after_cancel"
  key_links:
    - from: "tests/async/test_edge_exceptiongroup.py"
      to: "duckdb_async_pool"
      via: "pytest.raises(AdbcError) on a bad query; assert _pool.checkedout()==0"
      pattern: "pytest.raises|checkedout"
    - from: "tests/async/test_edge_limiter.py"
      to: "AsyncPool._limiter.borrowed_tokens"
      via: "assert borrowed_tokens==0 after a cancelled cancellable_offload"
      pattern: "borrowed_tokens"
---

<objective>
Prove the two cancellation-adjacent contracts on the limiter / exception-fidelity seam: (1) EDGE-19 — a genuine `AdbcError` raised by the worker survives the `cancellable_offload` task group as a BARE `AdbcError` (the single-member `ExceptionGroup` is unwrapped, preserving Phase 24's EDGE-17 `pytest.raises(AdbcError)` contract) and the connection returns via the reset path (`checkedout()==0`, NOT invalidated — the non-cancel path); and (2) the EDGE-09 cancel-mid-block leg owed from Phase 24 (D-24-02) — a limiter token returns to `borrowed_tokens == 0` exactly once on the cancel path, proven across a x50 loop.

Purpose: EDGE-19 is the guardrail that the 25-02 single-member-EG unwrap did not regress the bare-error contract, AND that the invalidate path fires ONLY on cancel (Pitfall 6) — a non-cancel error must NOT invalidate. The EDGE-09 cancel leg closes the transient-token accounting on the one path Phase 24 deferred: a cancelled offload still releases its token exactly once.
Output: `test_edge_exceptiongroup.py` (NEW), `test_edge_limiter.py` (MOD — add the cancel-token leg, update the module docstring's "no cancel leg" note).
</objective>

<execution_context>
@.claude/skills/adbc-poolhouse-docs-author/SKILL.md
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-cancellation/25-CONTEXT.md
@.planning/phases/25-cancellation/25-RESEARCH.md
@.planning/phases/25-cancellation/25-PATTERNS.md
@.planning/phases/25-cancellation/25-VALIDATION.md
@tests/async/test_edge_limiter.py
@tests/async/_edge_helpers.py
@tests/async/conftest.py
</context>

<artifacts_this_phase_produces>
This plan produces (of the phase-wide artifact set):
- `tests/async/test_edge_exceptiongroup.py` (NEW) — EDGE-19 bare-`AdbcError` unwrap + `checkedout()==0`.
- `tests/async/test_edge_limiter.py` (MOD) — `test_token_returns_after_cancel` (EDGE-09 cancel-mid-block leg, x50), plus the module-docstring update removing the "deliberately NO cancel leg" note.
Consumes: `cancellable_offload` + invalidate-on-cancel (25-02), `BlockingStubConnection.invalidate` (25-01), the existing `duckdb_async_pool`/`make_stub_async_connection`/`await_inside`/`real_clock_watchdog` harness.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: EDGE-19 — bare AdbcError unwrap + checkedout()==0 on a real DuckDB pool</name>
  <read_first>
    - tests/async/test_edge_limiter.py::test_token_returns_after_error (lines 69-78) — the real-DuckDB `pytest.raises(AdbcError)` + pool-state assertion analog; the `from adbc_driver_manager import Error as AdbcError` import (line ~30).
    - tests/async/conftest.py — `duckdb_async_pool` fixture (line 76) and how `_pool`/`checkedout()` are reached.
    - .planning/phases/25-cancellation/25-RESEARCH.md EDGE-19 example (lines 443-454), Pitfall 1 (the EG unwrap), Pitfall 6 (do NOT invalidate a non-cancel error), Assumption A2 (the cursor method sees a bare `AdbcError`, not a group).
    - .planning/phases/25-cancellation/25-PATTERNS.md §"test_edge_exceptiongroup.py".
  </read_first>
  <action>
    Create `tests/async/test_edge_exceptiongroup.py`. Import `from adbc_driver_manager import Error as AdbcError`. Write a `@pytest.mark.anyio` test using `duckdb_async_pool` that runs `SELECT * FROM does_not_exist` inside `async with await duckdb_async_pool.connect() as conn:` and asserts `pytest.raises(AdbcError)` — explicitly NOT `ExceptionGroup`/`BaseExceptionGroup` (this proves the 25-02 single-member-EG unwrap preserved the bare type after the `cancellable_offload` rewire). Capture the raised exception (`excinfo.value`) and assert `not isinstance(excinfo.value, BaseExceptionGroup)` to make the unwrap explicit. After the `with` block, assert `duckdb_async_pool._pool.checkedout() == 0` — the errored connection returned via the reset path and was NOT invalidated (the non-cancel branch; Pitfall 6 / EDGE-18 preserved). Run the body inside a small loop (reuse the existing `_ACCOUNTING_LOOPS` constant if available, else a fixed small count) so repeated errored round-trips never leak. Optionally assert the off-loop worker frame appears in the traceback to lock probe-E fidelity.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/async/test_edge_exceptiongroup.py -q 2>&1 | tail -6; rc=${PIPESTATUS[0]}; [ "$rc" -eq 0 ] && echo EG_PASS</automated>
  </verify>
  <acceptance_criteria>
    - A bad-query round-trip raises a bare `AdbcError` (NOT `ExceptionGroup`): the test asserts both `pytest.raises(AdbcError)` and `not isinstance(excinfo.value, BaseExceptionGroup)`.
    - `duckdb_async_pool._pool.checkedout() == 0` after the errored block (connection returned, NOT invalidated — proves the invalidate path fires only on cancel, Pitfall 6 / EDGE-18 preserved).
    - Passes under both backends; `.venv/bin/pytest tests/async/test_edge_exceptiongroup.py -q` prints `EG_PASS`.
    - `.venv/bin/ruff check` + `.venv/bin/basedpyright` clean on the new file.
  </acceptance_criteria>
  <done>EDGE-19 proves a genuine ADBC error escapes the task group as a bare `AdbcError` and the connection returns (not invalidated), under both backends.</done>
</task>

<task type="auto">
  <name>Task 2: EDGE-09 cancel-mid-block leg — borrowed_tokens==0 after a cancelled offload (x50)</name>
  <read_first>
    - tests/async/test_edge_limiter.py — `TestEdge09TokenAccounting` (lines 55-78), `test_token_returns_after_success` (lines 59-66, the `_ACCOUNTING_LOOPS` accounting analog), `test_token_returns_after_error` (69-78), and the module docstring (lines 12-14) that explicitly notes the cancel-mid-block leg's ABSENCE (deferred to Phase 25, D-24-02); the `_stub_conn_on` helper (line ~48); `TestEdge10` cancel-scope choreography.
    - tests/async/_edge_helpers.py — `await_inside`/`real_clock_watchdog`; tests/_async_harness/stubs.py — the re-armable gate + `release`.
    - .planning/phases/25-cancellation/25-RESEARCH.md EDGE-09 row (phase_requirements table, line 64) + the transient-token model note (CONTEXT D-24-01); .planning/phases/24-core-async-wrapper/24-CONTEXT.md D-24-02 (the owed leg).
    - .planning/phases/25-cancellation/25-PATTERNS.md §"test_edge_limiter.py (MOD)".
  </read_first>
  <action>
    In `tests/async/test_edge_limiter.py`, extend `TestEdge09TokenAccounting` with `test_token_returns_after_cancel` (`@pytest.mark.anyio`, x50 loop matching `_ACCOUNTING_LOOPS`). Each iteration: gate a stub-backed worker inside `execute` via `_stub_conn_on` + `await_inside`, cancel it (via `anyio.move_on_after(0)` or a nested task-group `cancel_scope.cancel()`), then assert the pool limiter's `borrowed_tokens == 0` after the cancelled `cancellable_offload` — proving the transient token releases exactly once even on the cancel path (`adbc_cancel` unblocks the worker, the offload returns, the token releases; `abandon_on_cancel=False` guarantees the join). Wrap each iteration's body in `real_clock_watchdog` (NOT `anyio.fail_after`) and release every stub cursor in a `finally`. Update the module docstring (lines 12-14) to remove the "deliberately NO cancel-mid-block test" note now that the leg exists. Read the limiter identically to the success/error legs (keep the only difference the cancel) so the accounting comparison is apples-to-apples. Name the test so `-k cancel_token` selects it (e.g. `test_token_returns_after_cancel`).
  </action>
  <verify>
    <automated>.venv/bin/pytest "tests/async/test_edge_limiter.py::TestEdge09TokenAccounting" -q 2>&1 | tail -6; rc=${PIPESTATUS[0]}; [ "$rc" -eq 0 ] && echo TOKEN_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `test_token_returns_after_cancel` asserts `borrowed_tokens == 0` after a cancelled `cancellable_offload`, across a x50 loop, under both backends.
    - The full `tests/async/test_edge_limiter.py` (success + error + new cancel leg) stays green: `.venv/bin/pytest tests/async/test_edge_limiter.py::TestEdge09TokenAccounting -q` prints `TOKEN_PASS`.
    - The module docstring no longer claims the cancel-mid-block leg is absent.
    - Each iteration is `real_clock_watchdog`-wrapped (no `anyio.fail_after` as watchdog); no positive-duration sleeps.
    - Loop-stable: `for i in $(seq 1 20); do .venv/bin/pytest "tests/async/test_edge_limiter.py::TestEdge09TokenAccounting" -q >/tmp/claude/tok.$i.log 2>&1; rc=$?; [ "$rc" -ne 0 ] && { echo "FAIL run $i"; break; }; done` reports 0 failures/0 hangs (`rc=$?`, never `if ! pytest`).
    - `.venv/bin/ruff check` + `.venv/bin/basedpyright` clean on the modified file.
  </acceptance_criteria>
  <done>The EDGE-09 cancel-mid-block leg (D-24-02) proves `borrowed_tokens==0` after a cancelled offload across x50, under both backends; the docstring note is removed; loop-stable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (test-only) | Asserts the 25-02 boundaries hold on the non-cancel error path (no over-invalidation) and the cancel path (token released exactly once). No new boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04-DOS | Denial of Service | a leaked limiter token on the cancel path exhausting the per-pool limiter (CapacityLimiter starvation) | mitigate | `test_token_returns_after_cancel` asserts `borrowed_tokens==0` after a cancelled offload across x50 — proving the transient token releases exactly once on the cancel path (D-24-01/02) |
| T-25-04-TMP | Tampering / Information | over-invalidating a healthy-but-errored connection (false poison) — or under-invalidating a real poison | mitigate | EDGE-19 asserts a non-cancel `AdbcError` returns the connection (`checkedout()==0`, NOT invalidated), pinning the invalidate-only-on-cancel contract (Pitfall 6 / EDGE-18) |
| T-25-04-SC | Tampering | npm/pip/cargo installs | accept | No installs; no new dependency |

No production-code threat: tests only; they pin the resource-cleanup correctness the 25-02 mitigations claim.
</threat_model>

<verification>
- `.venv/bin/pytest tests/async/test_edge_exceptiongroup.py tests/async/test_edge_limiter.py -q` green under both backends.
- x20 loop of the limiter cancel leg: 0 failures, 0 hangs (`rc=$?` + grep the pass line; never `if ! pytest`).
- No positive-duration sleeps; no `anyio.fail_after` used as a watchdog.
- `.venv/bin/ruff check` + `.venv/bin/basedpyright` clean on both files.
</verification>

<success_criteria>
- EDGE-19: a genuine ADBC error escapes as a bare `AdbcError`; the connection returns (`checkedout()==0`), not invalidated.
- EDGE-09 cancel leg (D-24-02): `borrowed_tokens==0` after a cancelled offload across x50, both backends, loop-stable.
- The limiter module docstring no longer claims the cancel leg is absent.
</success_criteria>

<output>
Create `.planning/phases/25-cancellation/25-04-SUMMARY.md` when done.
</output>
