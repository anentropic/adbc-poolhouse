---
phase: 25-cancellation
plan: 05
type: execute
wave: 4
depends_on: ["25-01", "25-02", "25-03", "25-04"]
files_modified:
  - docs/src/guides/async.md
  - tests/async/test_async_guard.py
autonomous: true
requirements: [EDGE-28, CANCEL-04]
must_haves:
  truths:
    - "The async guide documents cooperative cancellation/timeout: fail_after/move_on_after around execute/fetch_arrow_table, adbc_cancel abort, invalidate-on-cancel, identical asyncio/trio behaviour"
    - "AsyncConnection.invalidate and the cancel-behaviour docstrings render in the API reference with mkdocs --strict passing"
    - "The real _async/ package scans clean including the new banned-asyncio-cancelled-error rule (EDGE-28)"
    - "The whole async cancel suite is x20-loop stable (0 hangs) under asyncio and trio"
  artifacts:
    - path: "docs/src/guides/async.md"
      provides: "cancellation/timeout section replacing the 'lands in a later release' placeholder"
      contains: "adbc_cancel"
    - path: "tests/async/test_async_guard.py"
      provides: "EDGE-28 meta-assert: scan_async_package(real _async/) == [] incl. the new rule"
      contains: "banned-asyncio-cancelled-error"
  key_links:
    - from: "docs/src/guides/async.md"
      to: "AsyncConnection.invalidate"
      via: "API-reference cross-link in the See also block"
      pattern: "invalidate"
    - from: "tests/async/test_async_guard.py"
      to: "scan_async_package"
      via: "assert the real _async/ package is clean incl. the new rule"
      pattern: "scan_async_package"
---

<objective>
Close the phase: (1) the documentation gate (CLAUDE.md, phases >= 7) — replace the "full cancellation lands in a later release" placeholder in the async guide with a real cooperative-cancellation/timeout section, cross-link the new `AsyncConnection.invalidate`, and make `mkdocs build --strict` pass; and (2) the EDGE-28 / CANCEL-04 meta-assert that the live `_async/` package scans clean including the new `banned-asyncio-cancelled-error` rule, plus the phase-wide x20 loop-stability gate proving no-leak under both backends.

Purpose: This is the completion gate. The phase is not done until the cancellation behaviour is reflected in the consumer-facing guide with strict docs build, the trio-neutrality static guard is asserted against the real package (EDGE-28), and the full cancel suite is proven loop-stable (the MEMORY ~33%-deadlock lesson). Note: `tests/test_async_guard.py` (top-level, sync) already covers the real-package clean scan via `TestRealAsyncPackage`; this plan adds an explicit async-side meta-assert in `tests/async/test_async_guard.py` per the VALIDATION.md per-task map and runs the phase gate.
Output: `docs/src/guides/async.md` (MOD), `tests/async/test_async_guard.py` (NEW thin meta-assert wrapper for the async suite).
</objective>

<execution_context>
@.claude/skills/adbc-poolhouse-docs-author/SKILL.md
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-cancellation/25-CONTEXT.md
@.planning/phases/25-cancellation/25-RESEARCH.md
@.planning/phases/25-cancellation/25-PATTERNS.md
@.planning/phases/25-cancellation/25-VALIDATION.md
@docs/src/guides/async.md
@src/adbc_poolhouse/_async/_connection.py
@src/adbc_poolhouse/_async/_cursor.py
</context>

<artifacts_this_phase_produces>
This plan produces (of the phase-wide artifact set):
- `docs/src/guides/async.md` cancellation/timeout section (replaces the placeholder) + `AsyncConnection.invalidate` cross-link.
- `tests/async/test_async_guard.py` (NEW) — EDGE-28 async-side meta-assert that `scan_async_package("src/adbc_poolhouse/_async/") == []` incl. the new rule.
Runs the phase gate: full suite + x20 loop + mkdocs --strict + scan clean.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the async guide cancellation/timeout section + cross-link invalidate</name>
  <read_first>
    - docs/src/guides/async.md — the "Cleanup is shielded" section (lines 151-161) containing the placeholder to REPLACE ("Full cancellation handling ... lands in a later release"); the "See also" block (lines 163-169) for the cross-link style; the overall register/voice of the guide.
    - .claude/skills/adbc-poolhouse-docs-author/SKILL.md — Google-style, Markdown not RST, `Example:` singular for admonitions, humanizer pass.
    - src/adbc_poolhouse/_async/_connection.py (the new `invalidate` docstring) and _cursor.py (the rewired cancel-behaviour docstrings) authored in 25-02 — keep the guide consistent with them.
    - User MEMORY: Markdown (not RST) in docstrings/docs; prefer `.venv/bin/mkdocs` over `uv run` under sandbox.
    - .planning/phases/25-cancellation/25-PATTERNS.md §"docs/src/guides/async.md".
  </read_first>
  <action>
    In `docs/src/guides/async.md`, replace the placeholder paragraph (lines ~159-161, "Full cancellation handling ... lands in a later release") with a cooperative-cancellation/timeout section. Document: wrapping `await cursor.execute(...)` / `await cursor.fetch_arrow_table(...)` in `anyio.fail_after(...)` or `anyio.move_on_after(...)`; that on cancel/timeout the in-flight C call is aborted cooperatively via the driver's thread-safe `adbc_cancel`, the worker is joined, and the connection is invalidated (dropped from the pool, never returned poisoned) so `pool.checkedout()` stays correct; that a cancel delivered before the call starts touches the driver not at all; and that behaviour is identical under asyncio and trio (only the surfaced exception type differs — `TimeoutError` for `fail_after`, nothing for an explicit `scope.cancel()`). Use a singular `Example:` admonition with a ` ```python ` fenced block showing a `fail_after` around `execute`. Cross-link `AsyncConnection.invalidate` in the "See also" block using the existing inline-code/autorefs style (per the Phase 24 note: method-level mkdocstrings refs that fail autorefs --strict must use inline code, not a full autoref — match what the existing guide does). Markdown not RST (no `:func:`/`:class:` roles). Apply a humanizer pass to the new prose (no AI-tell cadence, no filler).
  </action>
  <verify>
    <automated>.venv/bin/mkdocs build --strict 2>&1 | tail -8; rc=${PIPESTATUS[0]}; [ "$rc" -eq 0 ] && echo DOCS_STRICT_PASS</automated>
  </verify>
  <acceptance_criteria>
    - The "lands in a later release" placeholder is gone; the section documents `fail_after`/`move_on_after`, cooperative `adbc_cancel`, invalidate-on-cancel (`checkedout()` correct), and asyncio/trio parity.
    - `AsyncConnection.invalidate` is cross-linked in "See also" using a style that passes `--strict`.
    - `.venv/bin/mkdocs build --strict` passes (prints `DOCS_STRICT_PASS`) — no broken autorefs, no warnings-as-errors.
    - No RST roles in the new prose: `grep -nE ':(func|class|meth|mod|attr):' docs/src/guides/async.md` returns nothing in the new section.
    - The new docstrings from 25-02 (`AsyncConnection.invalidate`, rewired cursor methods) render without mkdocstrings warnings under `--strict`.
  </acceptance_criteria>
  <done>The async guide documents cooperative cancellation/timeout with a working Example and a strict-passing invalidate cross-link; `mkdocs build --strict` is green; Markdown not RST; humanized.</done>
</task>

<task type="auto">
  <name>Task 2: EDGE-28 async-side meta-assert + phase-wide x20 loop gate</name>
  <read_first>
    - tests/test_async_guard.py — `TestRealAsyncPackage.test_scan_real_async_package_is_clean` (line ~155, the real-package clean-scan analog) and `scan_async_package` usage.
    - tests/_async_harness/guard.py — `scan_async_package` signature + the new `banned-asyncio-cancelled-error` rule (added in 25-01).
    - .planning/phases/25-cancellation/25-VALIDATION.md — the per-task map row for EDGE-28 (`tests/async/test_async_guard.py -q`), the Sampling Rate / Phase gate section, the x20 loop command.
    - .planning/phases/25-cancellation/25-RESEARCH.md Pattern 4 (EDGE-28 meta-test asserts `scan_async_package("src/adbc_poolhouse/_async/") == []`).
  </read_first>
  <action>
    Create `tests/async/test_async_guard.py` (the async-suite meta-assert the VALIDATION.md per-task map references — distinct from the top-level `tests/test_async_guard.py` self-tests). Add a single test that imports `scan_async_package` from `tests._async_harness.guard` (via the same `importlib` dance the other `tests/async/` files use if needed for the reserved-keyword dir) and asserts `scan_async_package("src/adbc_poolhouse/_async/") == []` — proving the live `_async/` package contains no `import asyncio`, no `asyncio.CancelledError`, and no un-limitered `to_thread.run_sync` after the 25-02 rewire (EDGE-28 / CANCEL-04 trio-neutrality). This is a static scan, not an anyio test, but keeping it in `tests/async/` matches the VALIDATION.md command; no `@pytest.mark.anyio` is required. Then run the phase-wide loop gate (x20, both backends) over the full cancel suite and record the result in the SUMMARY: `for i in $(seq 1 20); do .venv/bin/pytest tests/async -q >/tmp/claude/phase25.$i.log 2>&1; rc=$?; [ "$rc" -ne 0 ] && { echo "FAIL run $i"; break; }; done` — 0 failures, 0 hangs required (use `rc=$?`, never `if ! pytest` — zsh `!` gotcha; grep the `passed` line in each log to confirm the run actually executed and did not silently skip).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/async/test_async_guard.py -q 2>&1 | tail -5; rc=${PIPESTATUS[0]}; [ "$rc" -eq 0 ] && echo GUARD_META_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `scan_async_package("src/adbc_poolhouse/_async/") == []` (incl. the `banned-asyncio-cancelled-error` rule): `.venv/bin/pytest tests/async/test_async_guard.py -q` prints `GUARD_META_PASS`.
    - Phase x20 loop over `tests/async` reports 0 failures and 0 hangs under both backends; each run's log contains a `passed` line (proves the loop executed, not silently skipped). Record the count in the SUMMARY.
    - Full suite green: `.venv/bin/pytest -q` passes (sync + harness + async, both backends).
    - `.venv/bin/basedpyright src/adbc_poolhouse/_async/` 0 errors; `.venv/bin/ruff check`/`format --check` clean.
  </acceptance_criteria>
  <done>EDGE-28 meta-assert confirms the live `_async/` package scans clean incl. the new rule; the phase-wide x20 loop is clean (0 hangs) under both backends; full suite + types + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (docs + static guard) | No runtime boundary. The EDGE-28 scan is a read-only `ast.parse` static check (never executes the inspected source). Docs are prose. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-05-TMP | Tampering | a stray `asyncio.CancelledError`/`import asyncio`/un-limitered offload slipping into `_async/` (trio-neutrality regression) | mitigate | EDGE-28 meta-assert: `scan_async_package(real _async/) == []` incl. the new rule — fails the gate if any banned form appears |
| T-25-05-DOS | Denial of Service | an intermittent cancel deadlock surviving to release (single-shot green run) | mitigate | The phase gate is a x20 loop over `tests/async` under both backends, 0 hangs required; per-run `passed`-line grep confirms the loop actually ran (zsh `!` / silently-skipped guard) |
| T-25-05-SC | Tampering | npm/pip/cargo installs | accept | No installs; no new dependency. `mkdocs` is an existing docs-group dep |

No production-code threat: docs + a static meta-assert + the loop gate. Their value is enforcing the trio-neutrality and no-leak guarantees the earlier plans implemented.
</threat_model>

<verification>
- `.venv/bin/mkdocs build --strict` passes (docs gate, phases >= 7).
- `.venv/bin/pytest tests/async/test_async_guard.py -q` green (EDGE-28 scan clean).
- Full suite `.venv/bin/pytest -q` green under both backends; x20 loop over `tests/async` 0 hangs (`rc=$?`, grep `passed`, never `if ! pytest`).
- `.venv/bin/basedpyright src/adbc_poolhouse/_async/` 0 errors; ruff check/format --check clean.
- No RST roles in new docs prose.
</verification>

<success_criteria>
- The async guide documents cooperative cancellation/timeout; `mkdocs build --strict` passes; invalidate is cross-linked.
- EDGE-28: the live `_async/` package scans clean incl. the new rule.
- The phase-wide x20 loop is clean (0 hangs) under asyncio and trio; full suite + types + lint green.
</success_criteria>

<output>
Create `.planning/phases/25-cancellation/25-05-SUMMARY.md` when done.
</output>
