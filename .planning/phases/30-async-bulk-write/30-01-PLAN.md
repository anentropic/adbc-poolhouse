---
phase: 30-async-bulk-write
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/_async_harness/stubs.py
  - tests/async/test_ingest_roundtrip.py
  - tests/async/test_ingest_modes.py
  - tests/async/test_ingest_cancel.py
  - tests/async/test_ingest_signature.py
autonomous: true
requirements: [INGEST-01, INGEST-02, INGEST-03, INGEST-04]

must_haves:
  truths:
    - "The BlockingStubCursor exposes a blockable adbc_ingest with an ingest_call_count (deterministic in-flight cancel harness) — INGEST-04"
    - "Four RED test files exist and FAIL (no AsyncCursor.adbc_ingest yet), pinning INGEST-01 round-trip, INGEST-02 modes, INGEST-03 data pass-through, and INGEST-04 cancel/invalidate parity"
    - "The cancel test is marked with the project concurrency_marks (x-loop repeat) and runs under both asyncio and trio — INGEST-04, T-30-01"
  artifacts:
    - path: "tests/_async_harness/stubs.py"
      provides: "BlockingStubCursor.adbc_ingest sticky-release gate + ingest_call_count counter"
      contains: "def adbc_ingest"
    - path: "tests/async/test_ingest_roundtrip.py"
      provides: "INGEST-01 create/append round-trip + INGEST-03 test_data_passthrough (DuckDB)"
    - path: "tests/async/test_ingest_modes.py"
      provides: "INGEST-02 all four Literal modes reach the driver (replace=drop, create_append)"
    - path: "tests/async/test_ingest_cancel.py"
      provides: "INGEST-04 cancel → adbc_cancel once + invalidate + checkedout()==0, asyncio+trio, looped"
    - path: "tests/async/test_ingest_signature.py"
      provides: "Asserts _SyncCursor Protocol carries adbc_ingest + public method keyword-only under strict basedpyright"
  key_links:
    - from: "tests/async/test_ingest_cancel.py"
      to: "tests/_async_harness/stubs.py BlockingStubCursor.adbc_ingest"
      via: "await_inside gate on ingest_call_count then cancel scope"
      pattern: "ingest_call_count"
    - from: "tests/async/test_ingest_cancel.py"
      to: "tests/async/_edge_helpers.py"
      via: "importlib.import_module concurrency_marks + real_clock_watchdog + await_inside"
      pattern: "real_clock_watchdog"
---

<objective>
Wave 0 (RED) test scaffolding for Phase 30 async bulk write. Land the failing tests
and the harness stub extension BEFORE the production method exists, following the
Phase 29 RED-first TDD rhythm.

Purpose: Pin every INGEST-01..04 behavior as an executable test so that Plan 30-02's
implementation is a GREEN pass with no interpretation. The cancel test is the
load-bearing one — it must be deterministic (blocking stub, not a slow real ingest)
and looped (a single green run is NOT acceptance — Phase 23 missed a ~33% deadlock
single-shot).

Output:
- `BlockingStubCursor.adbc_ingest` (sticky-release blocking gate + `ingest_call_count`)
- Four RED test files: round-trip (INGEST-01/03), modes (INGEST-02), cancel
  (INGEST-04), signature/typecheck (INGEST-01 Protocol + keyword-only).

These tests MUST FAIL at the end of this plan (there is no `AsyncCursor.adbc_ingest`
yet) — that failure is the acceptance signal.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@.claude/skills/adbc-poolhouse-docs-author/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-async-bulk-write/30-CONTEXT.md
@.planning/phases/30-async-bulk-write/30-RESEARCH.md
@.planning/phases/30-async-bulk-write/30-PATTERNS.md
@.planning/phases/30-async-bulk-write/30-VALIDATION.md
</context>

<artifacts_this_phase_produces>
Phase 30 produces exactly four net-new symbols/files-of-record. This plan (Wave 0)
produces the test-side subset:

| Symbol / Artifact | File | Owner Plan |
|-------------------|------|------------|
| `BlockingStubCursor.adbc_ingest` (stub + `ingest_call_count`) | `tests/_async_harness/stubs.py` | 30-01 (this plan) |
| `tests/async/test_ingest_roundtrip.py` | new test file | 30-01 (this plan) |
| `tests/async/test_ingest_modes.py` | new test file | 30-01 (this plan) |
| `tests/async/test_ingest_cancel.py` | new test file | 30-01 (this plan) |
| `tests/async/test_ingest_signature.py` | new test file | 30-01 (this plan) |
| `AsyncCursor.adbc_ingest` method | `src/adbc_poolhouse/_async/_cursor.py` | 30-02 |
| `_SyncCursor.adbc_ingest` Protocol member | `src/adbc_poolhouse/_async/_cursor.py` | 30-02 |
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extend BlockingStubCursor with a blockable adbc_ingest</name>
  <files>tests/_async_harness/stubs.py</files>
  <read_first>
    - tests/_async_harness/stubs.py — modify target. Read `BlockingStubCursor.execute`
      (`stubs.py:287`), `fetch_arrow_table` (`stubs.py:301`), `adbc_cancel`
      (`stubs.py:314`), `_block` (`stubs.py:237`), the counter inits (`stubs.py:158-161`),
      and the class-attributes docstring block (`stubs.py:109-112`).
    - .planning/phases/30-async-bulk-write/30-RESEARCH.md §"Pattern 2" (lines 208-232)
      and §"Recommended Project Structure" — the exact stub shape.
    - .planning/phases/30-async-bulk-write/30-PATTERNS.md §"BlockingStubCursor.adbc_ingest"
      (lines 169-228) — record-under-lock → `self._block()` → return idiom.
  </read_first>
  <action>
    Add a `def adbc_ingest(self, table_name, data, mode="create", *, catalog_name=None,
    db_schema_name=None, temporary=False) -> int` method to `BlockingStubCursor`,
    mirroring `fetch_arrow_table` (`stubs.py:301`): `del` all six params, increment a
    new `self.ingest_call_count` under `self._lock`, call the existing sticky-release
    `self._block()` gate, then `return 3`. Keep `mode` positional-or-keyword here to
    match the driver/Protocol (only the public `AsyncCursor` method tightens it to
    keyword-only). Add `self.ingest_call_count: int = 0` in `__init__` beside the
    existing `execute_call_count`/`fetch_call_count`/`adbc_cancel_call_count`/
    `close_call_count` inits, and document `ingest_call_count` in the class-attributes
    docstring block. Do NOT add any cancel wiring — the existing `adbc_cancel`/`close`/
    `release` sticky-`_cancelled` latch already unblocks `_block()`. Google-style
    docstring (Markdown, not RST). This delivers the deterministic in-flight cancel
    harness for INGEST-04 (Claude's Discretion resolved: blocking stub, not slow DuckDB).
  </action>
  <verify>
    <automated>.venv/bin/python -c "from tests._async_harness.stubs import BlockingStubCursor; c=BlockingStubCursor(); assert hasattr(c,'adbc_ingest'); assert c.ingest_call_count==0; print('stub ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `BlockingStubCursor` has an `adbc_ingest` method and an `ingest_call_count`
      attribute initialized to 0 (import-and-assert command above exits 0).
    - `.venv/bin/basedpyright tests/_async_harness/stubs.py` reports 0 errors.
    - The method calls `self._block()` (grep: `_block` appears inside `adbc_ingest`) and
      increments `ingest_call_count` under `self._lock`.
    - No new cancel/latch machinery added (the method body does not touch `_cancelled`).
  </acceptance_criteria>
  <done>BlockingStubCursor exposes a blockable adbc_ingest with an ingest_call_count counter, basedpyright-clean, ready to drive the INGEST-04 cancel test.</done>
</task>

<task type="auto">
  <name>Task 2: RED round-trip + modes + signature tests (INGEST-01, INGEST-02, INGEST-03)</name>
  <files>tests/async/test_ingest_roundtrip.py, tests/async/test_ingest_modes.py, tests/async/test_ingest_signature.py</files>
  <read_first>
    - tests/async/test_reader_stream.py — DuckDB happy-path analog: `duckdb_async_pool`
      fixture → `await pool.connect()` → `cur = conn.cursor()` → `await cur.execute(...)`.
    - tests/async/conftest.py — `duckdb_async_pool` fixture and the `anyio_backend`
      parametrization (both backends).
    - .planning/phases/30-async-bulk-write/30-RESEARCH.md §"DuckDB round-trip"
      (lines 329-339) and §"Test Map" (lines 421-429) — exact round-trip body + per-req
      commands.
    - .planning/phases/30-async-bulk-write/30-PATTERNS.md §"test_ingest_roundtrip.py +
      test_ingest_modes.py" (lines 285-311) and §"test_ingest_signature.py" (315-321).
  </read_first>
  <action>
    Write three test files against the `duckdb_async_pool` fixture, each parametrized
    over both anyio backends (asyncio + trio) via the existing `anyio_backend` conftest
    param — mark with `@pytest.mark.anyio`. (1) `test_ingest_roundtrip.py`: a create
    round-trip (`await cursor.adbc_ingest("people", tbl, mode="create")` returns int 3;
    `SELECT count(*)` returns 3) then an append (`mode="append"` returns 3; count now 6)
    per RESEARCH lines 332-339; plus a `test_data_passthrough` case that ingests a
    `pyarrow.Table` AND a `pyarrow.RecordBatch` unchanged and round-trips them (INGEST-03,
    zero conversion). (2) `test_ingest_modes.py`: exercise all four `Literal` modes
    reaching the driver — `create`, `append`, `create_append`, and `replace` (assert
    `replace` DROPS then recreates: ingest 3 rows, then `replace` with a 2-row table,
    assert count is 2 not 5). (3) `test_ingest_signature.py`: a runtime introspection
    test asserting `AsyncCursor.adbc_ingest` exists with keyword-only `mode`/
    `catalog_name`/`db_schema_name`/`temporary` (use `inspect.signature` — assert those
    four params have `KEYWORD_ONLY` kind and `table_name`/`data` do not), plus a
    module-level comment that basedpyright-strict over `_cursor.py` is the authoritative
    Protocol-coverage gate (run in Plan 30-02). These MUST FAIL now (no
    `AsyncCursor.adbc_ingest`) — that is the RED signal. Do NOT stub or xfail them.
    No fenced-code implementations copied verbatim beyond the analog structure; write
    real assertions. Use no positive-duration sleeps.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/async/test_ingest_roundtrip.py tests/async/test_ingest_modes.py tests/async/test_ingest_signature.py -q 2>&1 | grep -Eq 'error|failed|no attribute|AttributeError' && echo RED-as-expected</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist and are collected by pytest (no collection errors from
      syntax) — the run reaches assertion/attribute failures, printing `RED-as-expected`.
    - Every test function is decorated `@pytest.mark.anyio` (grep: each `def test_`
      preceded by the mark) so both asyncio and trio run.
    - `test_ingest_roundtrip.py` contains a `test_data_passthrough` function (INGEST-03).
    - `test_ingest_modes.py` asserts `replace` yields the post-replace row count, not the
      cumulative count (drop-then-create semantics).
    - `test_ingest_signature.py` uses `inspect.signature` and asserts KEYWORD_ONLY kind
      for `mode`/`catalog_name`/`db_schema_name`/`temporary`.
    - No `time.sleep`/`anyio.sleep(<positive>)` in any of the three files (source-scan).
  </acceptance_criteria>
  <done>Three RED integration/typecheck test files exist, collected cleanly, failing only because AsyncCursor.adbc_ingest does not yet exist — pinning INGEST-01/02/03.</done>
</task>

<task type="auto">
  <name>Task 3: RED cancel/invalidate parity test (INGEST-04, T-30-01)</name>
  <files>tests/async/test_ingest_cancel.py</files>
  <read_first>
    - tests/async/test_reader_cancel.py — the exact template: `TestStream05CancelPull`
      (lines 53-127) stub cancel + timeout legs, `TestStream05CancelDrainsPool::
      test_invalidate_drains_pool_duckdb` (lines 130-159) checkedout()==0 leg, and the
      module preamble (lines 24-47): `pytestmark = _helpers.concurrency_marks`,
      `importlib.import_module("tests.async._edge_helpers")`, `real_clock_watchdog`,
      `await_inside`, `virtual_clock`.
    - tests/_async_harness/stubs.py — the `adbc_ingest`/`ingest_call_count` added in
      Task 1, plus `adbc_cancel_call_count` (`stubs.py:160`) and `release` sticky machinery.
    - .planning/phases/30-async-bulk-write/30-RESEARCH.md §"Pattern 2" (208-232) and
      §"Cancel-path assertion shape" (342-350).
    - .planning/phases/30-async-bulk-write/30-PATTERNS.md §"test_ingest_cancel.py"
      (lines 231-281) — the mandatory harness discipline list.
  </read_first>
  <action>
    Clone `test_reader_cancel.py`, swapping the blocked call from a reader pull to
    `adbc_ingest`. Copy the module preamble verbatim: `pytestmark =
    _helpers.concurrency_marks` (x-loop repeat + timeout), `importlib.import_module`
    helper load (`await_inside`, `real_clock_watchdog`, `concurrency_marks`,
    `virtual_clock`), both-backends via `anyio_backend_name`. Three legs: (a) STUB CANCEL
    leg — start a task group, `start_soon` a helper that calls
    `await cur.adbc_ingest("t", <data>, mode="create")` on a stub-backed async connection,
    `await_inside(lambda: stub.cursors[-1].ingest_call_count >= 1)` then
    `tg.cancel_scope.cancel()`, `finally` release all stub cursors; assert the
    `real_clock_watchdog` did NOT trip, `adbc_cancel_call_count == 1`, and
    `invalidate_call_count == 1`. (b) TIMEOUT leg — clone
    `test_timeout_during_pull_aborts_and_invalidates` with `virtual_clock` +
    `anyio.fail_after`, same assertions. (c) DUCKDB DRAIN leg — clone
    `test_invalidate_drains_pool_duckdb`: drive `conn.invalidate()` directly (do NOT race
    a live `adbc_cancel` against an in-flight DuckDB ingest — it can wedge the worker) and
    assert `pool._pool.checkedout()` goes 1 → 0. Assert ONLY connection recovery — NEVER
    table state after a cancelled ingest (D-30-10 / Pitfall 5: partial-write
    non-atomicity is scoped OUT). Use `real_clock_watchdog`, NOT `anyio.fail_after`, for
    the hang backstop on the stub leg (fail_after autojumps under the trio MockClock). No
    positive-duration sleeps. This test MUST FAIL now (no `AsyncCursor.adbc_ingest`) —
    the RED signal. Closes threat T-30-01 (poisoned-connection-returned-to-pool /
    pool-starvation) once GREEN.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/async/test_ingest_cancel.py -q 2>&1 | grep -Eq 'error|failed|no attribute|AttributeError' && echo RED-as-expected</automated>
  </verify>
  <acceptance_criteria>
    - File exists, collected cleanly (no import/syntax error), fails only on the missing
      `AsyncCursor.adbc_ingest` — prints `RED-as-expected`.
    - `pytestmark = _helpers.concurrency_marks` present at module top (grep).
    - Uses `real_clock_watchdog` for the stub-leg hang backstop and `await_inside`
      event-gating (grep: both present; no `anyio.fail_after` on the stub leg).
    - Helpers loaded via `importlib.import_module("tests.async._edge_helpers")` (no dotted
      `tests.async._edge_helpers` import — `async` is a keyword).
    - Asserts `adbc_cancel_call_count == 1` and `invalidate_call_count == 1` on the
      stub/timeout legs, and `checkedout()` 1 → 0 on the DuckDB drain leg.
    - No assertion about post-cancel table row state anywhere (grep: no `count(*)` in the
      cancel legs).
    - No positive-duration sleeps (source-scan).
  </acceptance_criteria>
  <done>The RED cancel/invalidate parity test exists — looped, both-backends, deterministic via the blocking stub — asserting adbc_cancel-once + invalidate + checkedout()==0, and failing only because the production method is absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test task → CapacityLimiter/connection C-access | The cancel test drives the shared offload/limiter machinery; a leaked or poisoned connection here is what INGEST-04 guards against |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Denial of Service | poisoned connection returned to pool after a cancelled mid-write (pool starvation) | mitigate | This plan lands the RED test (`test_ingest_cancel.py`) that pins `adbc_cancel`-once + `invalidate` + `checkedout()==0` under asyncio AND trio; the mitigation itself (`on_abort=self._owner.invalidate`) is reused verbatim from the shipped Phase 25/29 contract and wired in Plan 30-02. No new control invented — the reused machinery is the mitigation; this plan proves it. |
| T-30-02 | Tampering | import-lint guard (PKG-03) over `_async/` | accept (verified) | No production code changes in this plan; `functools` import lands in 30-02. Guard re-run is a 30-02 acceptance gate. RESEARCH verified `scan_async_package` returns `[]` for a `functools.partial` file. |
</threat_model>

<verification>
- `.venv/bin/basedpyright tests/_async_harness/stubs.py` — 0 errors.
- The four RED test files are collected without import/syntax errors and FAIL only on the
  missing `AsyncCursor.adbc_ingest` (RED-as-expected signal in each verify command).
- `test_ingest_cancel.py` carries `concurrency_marks` and uses `real_clock_watchdog` +
  `await_inside` (no `fail_after` on the stub leg).
</verification>

<success_criteria>
- `BlockingStubCursor.adbc_ingest` + `ingest_call_count` exist and are basedpyright-clean.
- Four RED test files exist (`test_ingest_roundtrip.py`, `test_ingest_modes.py`,
  `test_ingest_cancel.py`, `test_ingest_signature.py`), collect cleanly, and fail solely
  because `AsyncCursor.adbc_ingest` is not yet defined.
- The cancel test is looped (concurrency_marks) and runs under asyncio + trio; it asserts
  connection recovery only, never post-cancel table state (D-30-10).
</success_criteria>

<output>
Create `.planning/phases/30-async-bulk-write/30-01-SUMMARY.md` when done.
</output>
