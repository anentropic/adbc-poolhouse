---
phase: 31-dataframe-convenience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/_async_harness/stubs.py
  - tests/async/test_df_roundtrip.py
  - tests/async/test_df_lifetime.py
  - tests/async/test_df_missing_dep.py
  - tests/async/test_df_busy.py
  - tests/async/test_df_cancel.py
  - tests/async/test_df_signature.py
autonomous: true
requirements: [DF-01, DF-02, DF-03, DF-04, PKG-02]

must_haves:
  truths:
    - "BlockingStubCursor exposes blockable fetch_df/fetch_polars with df_call_count/polars_call_count and optional fetch_df_raises/fetch_polars_raises worker-raise injection — the deterministic DF-03 + busy + cancel harness (D-31-01, D-31-03)"
    - "Seven RED test files exist and FAIL (no AsyncCursor.fetch_df/fetch_polars yet), pinning DF-01/DF-02 round-trip, DF-03 native ModuleNotFoundError propagation unchanged, DF-04 self-owning frame valid after checkin, busy-guard parity, cancel/invalidate parity, and signature/Protocol coverage"
    - "The DF-03 test asserts the exact native ModuleNotFoundError (.name == pandas/polars) reaches the caller and is NOT a PoolhouseError — no find_spec, no wrapping (D-31-05)"
    - "The cancel and busy tests carry the project concurrency_marks (x-loop repeat) and run under both asyncio and trio (D-31-09)"
    - "Positive round-trip/lifetime tests are pytest.importorskip-guarded so import adbc_poolhouse stays unaffected when pandas/polars are absent (PKG-02, D-31-08)"
  artifacts:
    - path: "tests/_async_harness/stubs.py"
      provides: "BlockingStubCursor.fetch_df/fetch_polars sticky-release gate + df_call_count/polars_call_count + fetch_df_raises/fetch_polars_raises injection"
      contains: "def fetch_df"
    - path: "tests/async/test_df_roundtrip.py"
      provides: "DF-01/DF-02 positive round-trip on DuckDB, importorskip-guarded, dual-backend"
    - path: "tests/async/test_df_lifetime.py"
      provides: "DF-04 frame valid-after-checkin, importorskip-guarded, dual-backend"
    - path: "tests/async/test_df_missing_dep.py"
      provides: "DF-03 stub-raising native ModuleNotFoundError propagates unchanged, not PoolhouseError, dual-backend"
    - path: "tests/async/test_df_busy.py"
      provides: "Busy-guard parity: second in-flight op raises ConnectionBusyError, concurrency_marks, dual-backend, looped"
    - path: "tests/async/test_df_cancel.py"
      provides: "Cancel/invalidate parity: adbc_cancel once + invalidate + checkedout()==0, concurrency_marks, real_clock_watchdog, dual-backend, looped"
    - path: "tests/async/test_df_signature.py"
      provides: "Signature + _SyncCursor Protocol coverage + import adbc_poolhouse-without-pandas surface (PKG-02)"
  key_links:
    - from: "tests/async/test_df_missing_dep.py"
      to: "tests/_async_harness/stubs.py BlockingStubCursor.fetch_df/fetch_polars"
      via: "fetch_df_raises=ModuleNotFoundError injection raised inside the released worker"
      pattern: "fetch_df_raises"
    - from: "tests/async/test_df_cancel.py"
      to: "tests/async/_edge_helpers.py"
      via: "importlib.import_module concurrency_marks + real_clock_watchdog + await_inside"
      pattern: "real_clock_watchdog"
    - from: "tests/async/test_df_cancel.py"
      to: "tests/_async_harness/stubs.py BlockingStubCursor.fetch_df"
      via: "await_inside gate on df_call_count then cancel scope"
      pattern: "df_call_count"
---

<objective>
Wave 0 (RED) test scaffolding for Phase 31 DataFrame convenience. Land the failing
tests, the harness stub extension, AND the pandas/polars dev-group deps BEFORE the two
production methods exist, following the Phase 29/30 RED-first TDD rhythm.

Purpose: Pin every DF-01..04 + PKG-02 behavior as an executable test so Plan 31-02's
implementation is a GREEN pass with no interpretation. This is the simplest phase of
v1.5.0 — the risk is over-engineering — so the tests exist precisely to LOCK the
"do-nothing-extra" contract: DF-03 asserts the *native* `ModuleNotFoundError` reaches
the caller unwrapped, which any spurious `find_spec`/`try-except` would break.

Output:
- `pandas`/`polars` added to `[dependency-groups].dev` in `pyproject.toml`, then `uv sync`
  (so the positive round-trip tests can pass and basedpyright resolves the annotations)
- `BlockingStubCursor.fetch_df`/`fetch_polars` (sticky-release gate + `df_call_count`/
  `polars_call_count` + `fetch_df_raises`/`fetch_polars_raises` worker-raise injection)
- Seven RED test files: round-trip (DF-01/02), lifetime (DF-04), missing-dep (DF-03),
  busy-guard parity, cancel/invalidate parity, signature/Protocol + import-surface (PKG-02)

The seven test files MUST FAIL at the end of this plan (there is no `AsyncCursor.fetch_df`/
`fetch_polars` yet) — that failure is the acceptance signal. The stub-extension task and
the `pyproject.toml` dev-dep task are the GREEN prerequisites they run against.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@.claude/skills/adbc-poolhouse-docs-author/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-dataframe-convenience/31-CONTEXT.md
@.planning/phases/31-dataframe-convenience/31-RESEARCH.md
@.planning/phases/31-dataframe-convenience/31-PATTERNS.md
@.planning/phases/31-dataframe-convenience/31-VALIDATION.md
</context>

<artifacts_this_phase_produces>
Phase 31 produces the following net-new symbols/files-of-record. This plan (Wave 0)
produces the test-side + packaging subset; Plan 31-02 lands the two production methods.

| Symbol / Artifact | File | Owner Plan |
|-------------------|------|------------|
| `pandas`/`polars` in `[dependency-groups].dev` | `pyproject.toml` | 31-01 (this plan) |
| `BlockingStubCursor.fetch_df` (+ `df_call_count`, `fetch_df_raises`) | `tests/_async_harness/stubs.py` | 31-01 (this plan) |
| `BlockingStubCursor.fetch_polars` (+ `polars_call_count`, `fetch_polars_raises`) | `tests/_async_harness/stubs.py` | 31-01 (this plan) |
| `tests/async/test_df_roundtrip.py` | new test file | 31-01 (this plan) |
| `tests/async/test_df_lifetime.py` | new test file | 31-01 (this plan) |
| `tests/async/test_df_missing_dep.py` | new test file | 31-01 (this plan) |
| `tests/async/test_df_busy.py` | new test file | 31-01 (this plan) |
| `tests/async/test_df_cancel.py` | new test file | 31-01 (this plan) |
| `tests/async/test_df_signature.py` | new test file | 31-01 (this plan) |
| `AsyncCursor.fetch_df` method | `src/adbc_poolhouse/_async/_cursor.py` | 31-02 |
| `AsyncCursor.fetch_polars` method | `src/adbc_poolhouse/_async/_cursor.py` | 31-02 |
| `_SyncCursor.fetch_df` / `fetch_polars` Protocol members (`-> object`) | `src/adbc_poolhouse/_async/_cursor.py` | 31-02 |
| `import pandas` / `import polars` under `TYPE_CHECKING` | `src/adbc_poolhouse/_async/_cursor.py` | 31-02 |
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add pandas/polars to the dev group + extend BlockingStubCursor with blockable fetch_df/fetch_polars</name>
  <files>pyproject.toml, tests/_async_harness/stubs.py</files>
  <read_first>
    - pyproject.toml — read `[dependency-groups].dev` (the block that already contains
      `pyarrow>=23.0.1`); this is the ONLY block to edit. Do NOT read/touch
      `[project.dependencies]` or `[project.optional-dependencies]`.
    - tests/_async_harness/stubs.py — modify target. Read `fetch_arrow_table`
      (`stubs.py:304-315`, the zero-arg record-under-lock → `self._block()` → `return None`
      shape), `adbc_ingest` (`stubs.py:317-361`, the counter + `_block` idiom + docstring),
      `_block` (`stubs.py:240`), the counter inits (`stubs.py:160-164`), the `__init__`
      signature (around `stubs.py:138-159`), and the class-attributes docstring block
      (`stubs.py:109-114`).
    - .planning/phases/31-dataframe-convenience/31-PATTERNS.md §"BlockingStubCursor
      extension" (lines 153-204) — the exact stub shape incl. the `fetch_df_raises`
      injection idiom (`self._block()` FIRST so the error crosses the real to_thread
      boundary, THEN raise).
    - .planning/phases/31-dataframe-convenience/31-RESEARCH.md §"Open Questions" #1
      (lines 338-341) — recommended ctor-kwarg injection (default None).
  </read_first>
  <action>
    (A) In `pyproject.toml`, add `"pandas>=2.0"` and `"polars>=1.0"` to
    `[dependency-groups].dev` beside the existing `pyarrow>=23.0.1` entry (D-31-08,
    PKG-02). Leave version `1.4.0` untouched. Do NOT add any `[pandas]`/`[polars]`/
    `[dataframe]` extra and do NOT touch `[project.dependencies]` or
    `[project.optional-dependencies]`. Then run `uv sync` (or `.venv/bin/uv sync` under
    the sandbox — see MEMORY uv-sandbox-workaround) so `.venv` resolves pandas/polars for
    basedpyright and the positive tests.
    (B) In `tests/_async_harness/stubs.py`, add two methods to `BlockingStubCursor`
    mirroring `fetch_arrow_table` (`stubs.py:304`): `def fetch_df(self) -> object` and
    `def fetch_polars(self) -> object`. Each increments a new counter under `self._lock`
    (`self.df_call_count` / `self.polars_call_count`), calls the existing sticky-release
    `self._block()` gate, then — if the matching injected exception is set — raises it,
    else `return None`. Add ctor kwargs `fetch_df_raises: BaseException | None = None` and
    `fetch_polars_raises: BaseException | None = None` to `__init__` (default None),
    storing them as `self._fetch_df_raises` / `self._fetch_polars_raises`. The raise must
    come AFTER `self._block()` so the exception crosses the real `to_thread.run_sync`
    boundary (proving the `_offload.py` EDGE-17 re-raise, not a synchronous raise). Do NOT
    wrap the raise. Add `self.df_call_count: int = 0` and `self.polars_call_count: int = 0`
    beside the existing counter inits (`stubs.py:160-164`), and document both counters +
    both new ctor kwargs in the class-attributes docstring block (`stubs.py:109-114`, the
    D-04 LOCKED-name public-counter contract). Add NO cancel wiring — the existing
    `adbc_cancel`/`close`/`release` sticky-`_cancelled` latch already unblocks `_block()`.
    Google-style docstrings, Markdown not RST (MEMORY docstring rules).
  </action>
  <verify>
    <automated>.venv/bin/python -c "from tests._async_harness.stubs import BlockingStubCursor; c=BlockingStubCursor(); assert hasattr(c,'fetch_df') and hasattr(c,'fetch_polars'); assert c.df_call_count==0 and c.polars_call_count==0; r=BlockingStubCursor(fetch_df_raises=ModuleNotFoundError('x')); print('stub ok')" && .venv/bin/basedpyright tests/_async_harness/stubs.py 2>&1 | grep -Eq '0 errors|no errors' && echo TYPES-OK</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` `[dependency-groups].dev` contains `pandas>=2.0` and `polars>=1.0`
      (grep: both present under that block).
    - No `[pandas]`/`[polars]`/`[dataframe]` extra exists anywhere in `pyproject.toml`
      (grep: `grep -v '^#' pyproject.toml | grep -Ec '\[(pandas|polars|dataframe)\]'` == 0),
      and `[project.dependencies]`/`[project.optional-dependencies]` are byte-unchanged.
    - `.venv/bin/python -c "import pandas, polars"` exits 0 after `uv sync`.
    - `BlockingStubCursor` has `fetch_df` + `fetch_polars` methods, `df_call_count` +
      `polars_call_count` attributes initialized to 0, and accepts
      `fetch_df_raises`/`fetch_polars_raises` ctor kwargs (import-and-assert command exits 0).
    - Each new stub method calls `self._block()` BEFORE the injected raise (grep: `_block`
      appears in both `fetch_df`/`fetch_polars` bodies, above the `raise`).
    - `.venv/bin/basedpyright tests/_async_harness/stubs.py` reports 0 errors (TYPES-OK).
    - No new cancel/latch machinery added (the two method bodies do not touch `_cancelled`).
  </acceptance_criteria>
  <done>pandas/polars are in the dev group and importable; BlockingStubCursor exposes blockable fetch_df/fetch_polars with counters and worker-raise injection, basedpyright-clean — the deterministic DF-03/busy/cancel harness is ready.</done>
</task>

<task type="auto">
  <name>Task 2: RED positive tests — round-trip (DF-01/02), lifetime (DF-04), signature + import-surface (DF-01/02 Protocol, PKG-02)</name>
  <files>tests/async/test_df_roundtrip.py, tests/async/test_df_lifetime.py, tests/async/test_df_signature.py</files>
  <read_first>
    - tests/async/test_ingest_roundtrip.py — the DuckDB happy-path analog (whole file):
      RED-first header, `duckdb_async_pool` fixture → `async with await pool.connect() as
      conn:` → `cur = conn.cursor()` → `await cur.execute(...)` → fetch.
    - tests/async/test_ingest_signature.py — the introspection + header-note analog (whole
      file); `hasattr(AsyncCursor, "adbc_ingest")` + `inspect.signature` pattern and the
      "basedpyright over `_cursor.py` is the authoritative Protocol gate" module note.
    - tests/async/conftest.py — `duckdb_async_pool` (`:88`) and `anyio_backend` dual-backend
      parametrization (asyncio + trio).
    - .planning/phases/31-dataframe-convenience/31-RESEARCH.md §"Code Examples" DF-01
      (lines 269-283) and DF-04 (lines 285-296) — exact round-trip + valid-after-checkin
      bodies, both `importorskip`-guarded.
    - .planning/phases/31-dataframe-convenience/31-PATTERNS.md §"Positive round-trip /
      lifetime tests" (lines 207-246) and §"Signature + import-surface test" (249-269).
  </read_first>
  <action>
    Write three test files against the `duckdb_async_pool` fixture, each `@pytest.mark.anyio`
    (dual-backend via `anyio_backend`). (1) `test_df_roundtrip.py`: a `fetch_df` case —
    `pandas = pytest.importorskip("pandas")`, execute `SELECT 1 AS a, 2 AS b`, `df = await
    cur.fetch_df()`, assert `isinstance(df, pandas.DataFrame)` and `df.to_dict("list") ==
    {"a": [1], "b": [2]}`; and a `fetch_polars` case — `polars = pytest.importorskip(
    "polars")`, execute the same, assert `isinstance(df, polars.DataFrame)` and the values
    round-trip (`df.to_dict(as_series=False) == {"a": [1], "b": [2]}` or equivalent). Keep
    them as two separately `-k`-selectable functions (`fetch_df` / `fetch_polars` in the
    names) per the VALIDATION test map. (2) `test_df_lifetime.py`: DF-04 — for each flavour
    (importorskip-guarded), read the frame AFTER `async with ... as conn:` EXITS (connection
    checked in), asserting `df["a"].tolist() == [1]` (pandas) / `df["a"].to_list() == [1]`
    (polars) — proves the frame is self-owning (no dangling C stream, no segfault). (3)
    `test_df_signature.py`: runtime introspection — `assert hasattr(AsyncCursor, "fetch_df")`
    and `"fetch_polars"`, and via `inspect.signature` that each takes only `self` (no extra
    params — simpler than ingest, no keyword-only checks); PLUS a PKG-02 `test_import_surface`
    (name contains `import_surface`) that does `importlib.import_module("adbc_poolhouse")`
    and asserts it succeeds (env-free — the current `.venv` proves the runtime surface is
    untouched even before uv sync; after uv sync it still must not runtime-import pandas/
    polars); include the module-note that `.venv/bin/basedpyright` over `_cursor.py` is the
    authoritative `_SyncCursor` Protocol-coverage gate (run in Plan 31-02). All test functions
    MUST FAIL now (no `AsyncCursor.fetch_df`/`fetch_polars`) — that is the RED signal; the
    `test_import_surface` case may already PASS (it only imports the package) and that is
    acceptable. Do NOT stub or xfail. Use no positive-duration sleeps.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/async/test_df_roundtrip.py tests/async/test_df_lifetime.py tests/async/test_df_signature.py -q 2>&1 | grep -Eq 'error|failed|no attribute|AttributeError' && echo RED-as-expected</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist and are collected by pytest without syntax/collection errors —
      the run reaches assertion/attribute failures, printing `RED-as-expected`.
    - Every DuckDB test function is decorated `@pytest.mark.anyio` (grep: each `def test_`
      in roundtrip/lifetime preceded by the mark) so both asyncio and trio run.
    - `test_df_roundtrip.py` and `test_df_lifetime.py` each call `pytest.importorskip("pandas")`
      and `pytest.importorskip("polars")` in their respective flavour cases (grep both).
    - `test_df_lifetime.py` reads the frame OUTSIDE (after) the `async with ... as conn:`
      block (grep: the `.tolist()`/`.to_list()` assertion is dedented below the `async with`).
    - `test_df_signature.py` contains a function whose name includes `import_surface` that
      calls `importlib.import_module("adbc_poolhouse")` (grep).
    - `test_df_signature.py` uses `inspect.signature` and asserts `fetch_df`/`fetch_polars`
      take only `self` (grep: `inspect.signature`).
    - No `time.sleep`/`anyio.sleep(<positive>)` in any of the three files (source-scan).
  </acceptance_criteria>
  <done>Three RED positive/typecheck test files exist, collected cleanly, importorskip-guarded, failing only because AsyncCursor.fetch_df/fetch_polars do not yet exist — pinning DF-01/DF-02 round-trip, DF-04 self-owning frame, the signature/Protocol contract, and the PKG-02 import surface.</done>
</task>

<task type="auto">
  <name>Task 3: RED missing-dep (DF-03) + busy + cancel/invalidate parity tests</name>
  <files>tests/async/test_df_missing_dep.py, tests/async/test_df_busy.py, tests/async/test_df_cancel.py</files>
  <read_first>
    - tests/async/test_ingest_cancel.py — the concurrency template: module preamble (the
      `pytestmark = _helpers.concurrency_marks` line, `importlib.import_module(
      "tests.async._edge_helpers")`, `real_clock_watchdog`, `await_inside`, `virtual_clock`),
      the STUB CANCEL leg (`await_inside(lambda: stub.cursors[-1].<counter> >= 1)` → cancel),
      the TIMEOUT leg (`virtual_clock` + `anyio.fail_after`), and the DUCKDB DRAIN leg
      (`conn.invalidate()` → `checkedout()` 1→0). Copy the preamble VERBATIM.
    - tests/async/test_reader_busy.py — the lighter busy-guard preamble + the "second
      in-flight op raises `ConnectionBusyError`" body; note `ConnectionBusyError` is imported
      from `adbc_poolhouse`.
    - tests/_async_harness/stubs.py — the `fetch_df`/`fetch_polars`, `df_call_count`/
      `polars_call_count`, and `fetch_df_raises`/`fetch_polars_raises` added in Task 1, plus
      `adbc_cancel_call_count` (`stubs.py:163`) and `release` sticky machinery; and
      `BlockingStubConnection.invalidate_call_count` (`stubs.py:789`).
    - tests/async/conftest.py — `make_stub_async_connection` (`:156`, zero-arg factory
      returning `(async_conn, stub_conn)`; the `BlockingStubCursor` is `stub_conn.cursors[-1]`)
      and `duckdb_async_pool` (`:88`).
    - .planning/phases/31-dataframe-convenience/31-RESEARCH.md §"Code Examples" DF-03
      (lines 298-310) and §"Validation Architecture / DF-03 mechanism decision" (394-403).
    - .planning/phases/31-dataframe-convenience/31-PATTERNS.md §"Missing-dep / busy / cancel
      tests" (lines 273-327) — the mandatory harness discipline + the note that the factory
      is zero-arg (set `fetch_df_raises` on `stub_conn.cursors[-1]` after building, OR forward
      via the stub ctor — see below).
  </read_first>
  <action>
    Write three stub-backed test files. (1) `test_df_missing_dep.py` (DF-03): for each
    flavour, build a stub-backed connection via `make_stub_async_connection()`, then inject
    the raise so the worker raises the NATIVE error — since the factory is zero-arg, set it
    on the cursor after `conn.cursor()` (`stub_conn.cursors[-1]._fetch_df_raises =
    ModuleNotFoundError("No module named 'pandas'")`), or pre-seed by constructing through
    the stub ctor if the test builds its own connection; either satisfies the harness. Then
    `with pytest.raises(ModuleNotFoundError) as ei: await cur.fetch_df()` and assert
    `ei.value.name == "pandas"` AND `not isinstance(ei.value, PoolhouseError)` (import
    `PoolhouseError` from `adbc_poolhouse`). Mirror for `fetch_polars` / `"polars"`. Keep the
    two flavours `-k pandas` / `-k polars` selectable. `@pytest.mark.anyio` (dual-backend).
    This proves the native error crosses the real chokepoint UNWRAPPED (D-31-05) — any
    `find_spec`/`try-except` in the production method would make this FAIL. (2) `test_df_busy.py`:
    copy the `test_reader_busy.py` preamble; gate `fetch_df` (then a second file/case for
    `fetch_polars`) inside the stub via `await_inside(lambda: stub.cursors[-1].df_call_count
    >= 1)`, and while it is in flight assert a second offloaded op on the SAME connection
    raises `ConnectionBusyError` (parity with `fetch_arrow_table`). Carry `pytestmark =
    _helpers.concurrency_marks` (looped) + dual-backend. (3) `test_df_cancel.py`: clone
    `test_ingest_cancel.py`, swapping the blocked call to `await cur.fetch_df(...)` /
    `fetch_polars`. Copy the preamble VERBATIM (`concurrency_marks`, `importlib.import_module`
    helper load, `real_clock_watchdog`, `await_inside`, `virtual_clock`, dual-backend). Three
    legs: (a) STUB CANCEL — `start_soon` the fetch on a stub-backed connection,
    `await_inside(lambda: stub.cursors[-1].df_call_count >= 1)` then `tg.cancel_scope.cancel()`,
    `finally` release all stub cursors; assert `real_clock_watchdog` did NOT trip,
    `adbc_cancel_call_count == 1`, `invalidate_call_count == 1`. (b) TIMEOUT — `virtual_clock`
    + `anyio.fail_after`, same assertions. (c) DUCKDB DRAIN — drive `conn.invalidate()`
    directly (do NOT race a live `adbc_cancel` against an in-flight DuckDB fetch_df — it can
    wedge the worker, MEMORY loop-flaky) and assert `pool._pool.checkedout()` goes 1 → 0. Use
    `real_clock_watchdog`, NOT `anyio.fail_after`, for the stub-leg hang backstop (fail_after
    autojumps under the trio MockClock — MEMORY platform-dependent lost-wakeup / RESEARCH
    Pitfall 3). Load helpers via `importlib.import_module("tests.async._edge_helpers")`
    (`async` is a keyword). All test functions MUST FAIL now (no `AsyncCursor.fetch_df`/
    `fetch_polars`) — the RED signal. These close threats T-31-01/02/03 once GREEN. No
    positive-duration sleeps.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/async/test_df_missing_dep.py tests/async/test_df_busy.py tests/async/test_df_cancel.py -q 2>&1 | grep -Eq 'error|failed|no attribute|AttributeError' && echo RED-as-expected</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist, collected cleanly (no import/syntax error), and fail only on the
      missing `AsyncCursor.fetch_df`/`fetch_polars` — prints `RED-as-expected`.
    - `test_df_missing_dep.py` asserts BOTH `ei.value.name == "pandas"`/`"polars"` AND
      `not isinstance(ei.value, PoolhouseError)` (grep: both `.name` and `PoolhouseError`),
      with the injected error crossing the worker via `_block` (the raise is worker-side).
    - `test_df_missing_dep.py` has `-k pandas` and `-k polars` selectable cases (grep: both
      substrings appear in function names).
    - `test_df_busy.py` and `test_df_cancel.py` carry `pytestmark = _helpers.concurrency_marks`
      (grep each).
    - `test_df_cancel.py` uses `real_clock_watchdog` for the stub-leg hang backstop and
      `await_inside` gating on `df_call_count`/`polars_call_count` (grep: both present; NO
      `anyio.fail_after` on the stub leg).
    - Helpers loaded via `importlib.import_module("tests.async._edge_helpers")` in both
      concurrency files (grep: no dotted `tests.async._edge_helpers` import statement).
    - `test_df_cancel.py` asserts `adbc_cancel_call_count == 1` + `invalidate_call_count == 1`
      on the stub/timeout legs and `checkedout()` 1 → 0 on the DuckDB drain leg; NO assertion
      about post-cancel frame/table state (grep: no `count(*)` in the cancel legs).
    - `test_df_busy.py` asserts a second in-flight op raises `ConnectionBusyError`
      (grep: `ConnectionBusyError`).
    - No positive-duration sleeps in any of the three files (source-scan).
  </acceptance_criteria>
  <done>Three RED stub-backed test files exist — DF-03 native-error propagation (dual-backend), busy-guard parity (looped), cancel/invalidate parity (looped, real_clock_watchdog) — all failing only because the production methods are absent, pinning the "no wrapping, connection-recovery-only" contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test task → CapacityLimiter / connection C-access | The cancel/busy tests drive the shared offload/limiter machinery; a leaked or poisoned connection here is what DF-04 / cancel-parity guard against. No NEW boundary beyond what `fetch_arrow_table` already crosses (RESEARCH §Security Domain). |
| worker thread → caller (exception path) | The DF-03 test drives the single `to_thread.run_sync` chokepoint: a native `ModuleNotFoundError` must cross UNCHANGED (no wrapping, no swallow). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Denial of Service | poisoned connection returned to pool after a cancelled materialization (pool starvation) | mitigate | This plan lands the RED `test_df_cancel.py` pinning `adbc_cancel`-once + `invalidate` + `checkedout()==0` under asyncio AND trio. The mitigation itself (`on_abort=self._owner.invalidate`, D-31-09) is reused verbatim from the shipped `fetch_arrow_table`/Phase 25 contract and wired in Plan 31-02 — no new control invented; this plan proves parity. |
| T-31-02 | Denial of Service | use-after-free / segfault reading a frame after checkin | mitigate | This plan lands the RED `test_df_lifetime.py` asserting the frame is readable AFTER connection checkin. The control (frame-owned buffers via `read_pandas` / `polars.from_arrow(read_all())`, DF-04/EDGE-21) is inherited from Approach A (D-31-01/D-31-04) — no machinery added; the test proves parity with `fetch_arrow_table`. |
| T-31-03 | Tampering | concurrent C-access on one connection | mitigate | This plan lands the RED `test_df_busy.py` asserting a second in-flight op raises `ConnectionBusyError`. The control (`_offloading()` `_in_use` guard) is reused unchanged and wired via the offload bracket in Plan 31-02. |
| T-31-SC | Tampering | dev-group `pandas`/`polars` installs | accept (verified) | Both are canonical top-download PyPI packages (RESEARCH §Package Legitimacy Audit: OK/OK, github.com/pandas-dev/pandas + github.com/pola-rs/polars); dev-group ONLY, never shipped to consumers, never a runtime import (D-31-08/PKG-02). No `[SLOP]`/`[SUS]` packages; no legitimacy checkpoint required. |
</threat_model>

<verification>
- `.venv/bin/basedpyright tests/_async_harness/stubs.py` — 0 errors; `.venv/bin/python -c
  "import pandas, polars"` exits 0 after `uv sync`.
- `[dependency-groups].dev` gains pandas/polars; NO extra added; `[project.dependencies]`/
  `[project.optional-dependencies]` unchanged.
- The seven RED test files are collected without import/syntax errors and FAIL only on the
  missing `AsyncCursor.fetch_df`/`fetch_polars` (RED-as-expected signal per verify command),
  except `test_import_surface` which may pass (it only imports the package).
- `test_df_cancel.py` + `test_df_busy.py` carry `concurrency_marks`; `test_df_cancel.py`
  uses `real_clock_watchdog` + `await_inside` (no `fail_after` on the stub leg).
- `test_df_missing_dep.py` asserts the native `ModuleNotFoundError` (`.name`) is NOT a
  `PoolhouseError` (DF-03, no wrapping).
</verification>

<success_criteria>
- pandas/polars in `[dependency-groups].dev`, importable after `uv sync`; extras/runtime
  surface untouched (PKG-02, D-31-08).
- `BlockingStubCursor.fetch_df`/`fetch_polars` + `df_call_count`/`polars_call_count` +
  `fetch_df_raises`/`fetch_polars_raises` exist and are basedpyright-clean.
- Seven RED test files exist (`test_df_roundtrip.py`, `test_df_lifetime.py`,
  `test_df_missing_dep.py`, `test_df_busy.py`, `test_df_cancel.py`, `test_df_signature.py`
  — six files; the signature file carries both the introspection and the PKG-02
  import-surface cases), collect cleanly, and fail solely because `AsyncCursor.fetch_df`/
  `fetch_polars` are not yet defined.
- The cancel/busy tests are looped (concurrency_marks) under asyncio + trio; the cancel test
  asserts connection recovery only, never post-cancel frame/table state.
- DF-03 test asserts the native `ModuleNotFoundError` reaches the caller unwrapped.
</success_criteria>

<output>
Create `.planning/phases/31-dataframe-convenience/31-01-SUMMARY.md` when done.
</output>
