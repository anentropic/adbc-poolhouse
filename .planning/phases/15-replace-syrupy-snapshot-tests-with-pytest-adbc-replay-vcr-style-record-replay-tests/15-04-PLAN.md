---
phase: 15-replace-syrupy-snapshot-tests-with-pytest-adbc-replay-vcr-style-record-replay-tests
plan: 04
type: execute
wave: 1
depends_on: [15-03]
files_modified:
  - tests/conftest.py
  - tests/integration/test_snowflake.py
  - tests/integration/test_databricks.py
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "Unit tests (test_configs, test_translators) pass when warehouse env vars are present in os.environ"
    - "Integration test helpers load credentials from per-warehouse dotenv files, not the shared .env"
  artifacts:
    - path: "tests/conftest.py"
      provides: "Autouse fixture clearing all warehouse env prefixes before each test"
      contains: "_clear_warehouse_env_vars"
    - path: "tests/integration/test_snowflake.py"
      provides: "Snowflake integration test loading .env.snowflake instead of .env"
      contains: ".env.snowflake"
    - path: "tests/integration/test_databricks.py"
      provides: "Databricks integration test loading .env.databricks instead of .env"
      contains: ".env.databricks"
  key_links:
    - from: "tests/conftest.py"
      to: "tests/test_configs.py"
      via: "autouse fixture runs before every test function"
      pattern: "autouse=True"
    - from: "tests/conftest.py"
      to: "tests/test_translators.py"
      via: "autouse fixture runs before every test function"
      pattern: "autouse=True"
---

<objective>
Fix env var contamination that causes 4 unit test failures when warehouse credentials
are loaded from .env, and restore per-warehouse dotenv file loading in integration tests.

Purpose: Unit tests that construct SnowflakeConfig/DatabricksConfig with explicit kwargs
currently fail because pydantic-settings reads SNOWFLAKE_*/DATABRICKS_* env vars from
os.environ at __init__ time. Integration tests load `.env` which bleeds into unit tests.

Output: An autouse conftest fixture that clears all warehouse env prefixes before every
test, and integration test helpers that load per-warehouse dotenv files (.env.snowflake,
.env.databricks) instead of the shared .env.
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-replace-syrupy-snapshot-tests-with-pytest-adbc-replay-vcr-style-record-replay-tests/15-03-SUMMARY.md

<interfaces>
<!-- All warehouse config env_prefix values from the codebase -->
BIGQUERY_
CLICKHOUSE_
DATABRICKS_
DUCKDB_
FLIGHTSQL_
MSSQL_
MYSQL_
POSTGRESQL_
REDSHIFT_
SNOWFLAKE_
SQLITE_
TERADATA_
TRINO_
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add autouse fixture to clear warehouse env vars</name>
  <files>tests/conftest.py</files>
  <action>
Replace the placeholder content in tests/conftest.py with an autouse fixture that
clears all warehouse-prefixed environment variables before each test function.

The fixture must:
1. Use `monkeypatch` (request it as a parameter via `@pytest.fixture(autouse=True)`)
2. Iterate over `os.environ` and call `monkeypatch.delenv(key, raising=False)` for
   every key matching any of these prefixes:
   BIGQUERY_, CLICKHOUSE_, DATABRICKS_, DUCKDB_, FLIGHTSQL_, MSSQL_, MYSQL_,
   POSTGRESQL_, REDSHIFT_, SNOWFLAKE_, SQLITE_, TERADATA_, TRINO_
3. Name the fixture `_clear_warehouse_env_vars`
4. Add a module docstring explaining WHY: pydantic-settings BaseSettings reads env vars
   at __init__ time, so unit tests that construct config objects with explicit kwargs
   will pick up stray env vars if not cleared

The fixture must use `list(os.environ)` to snapshot keys before iterating (avoid
RuntimeError from dict mutation during iteration).

Do NOT use monkeypatch.setenv or environment-modifying calls that persist across tests;
monkeypatch automatically restores on teardown.
  </action>
  <verify>
    <automated>cd /Users/paul/Documents/Dev/Personal/adbc-poolhouse && uv run pytest tests/test_configs.py tests/test_translators.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>All unit tests in test_configs.py and test_translators.py pass regardless of whether warehouse env vars exist in os.environ</done>
</task>

<task type="auto">
  <name>Task 2: Fix integration test dotenv loading to use per-warehouse files</name>
  <files>tests/integration/test_snowflake.py, tests/integration/test_databricks.py</files>
  <action>
In tests/integration/test_snowflake.py, change the `_snowflake_kwargs()` helper:
- Change `load_dotenv(Path(__file__).parent.parent.parent / ".env", override=False)`
  to `load_dotenv(Path(__file__).parent.parent.parent / ".env.snowflake", override=False)`
- This matches the .gitignore pattern (.env.snowflake) and isolates Snowflake
  credentials from the shared .env

In tests/integration/test_databricks.py, change the `_databricks_connect_kwargs()` helper:
- Change `load_dotenv(Path(__file__).parent.parent.parent / ".env", override=False)`
  to `load_dotenv(Path(__file__).parent.parent.parent / ".env.databricks", override=False)`
- This matches the .gitignore pattern (.env.databricks) and isolates Databricks
  credentials from the shared .env

Also update the recording instructions in the docstrings:
- Snowflake tests: "set credentials in .env.snowflake" (not ".env")
- Databricks tests: "set credentials in .env.databricks" (not ".env")

Do NOT change the conftest.py pool fixtures — those already load from .env which is
correct for the session-scoped pool fixtures used during ad-hoc recording.

Actually, ALSO update conftest.py pool fixtures to use per-warehouse dotenv files:
- snowflake_pool: load_dotenv(.env.snowflake)
- databricks_pool: load_dotenv(.env.databricks)

This ensures complete isolation: no single .env file needs to contain credentials for
multiple warehouses simultaneously.
  </action>
  <verify>
    <automated>cd /Users/paul/Documents/Dev/Personal/adbc-poolhouse && grep -n "\.env\." tests/integration/test_snowflake.py tests/integration/test_databricks.py tests/integration/conftest.py</automated>
  </verify>
  <done>All integration test files reference per-warehouse dotenv files (.env.snowflake, .env.databricks) instead of .env; recording instructions in docstrings match the file paths</done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/test_configs.py tests/test_translators.py -x -q` passes (unit tests immune to env vars)
2. `grep -r 'load_dotenv.*\.env"' tests/integration/` returns NO matches (no raw .env loading)
3. `grep -r '\.env\.snowflake\|\.env\.databricks' tests/integration/` returns matches in all 3 integration files
</verification>

<success_criteria>
- 0 unit test failures from env var contamination (test_configs + test_translators all pass)
- Integration test helpers load from .env.snowflake and .env.databricks respectively
- Autouse fixture in tests/conftest.py clears all 13 warehouse env prefixes
</success_criteria>

<output>
After completion, create `.planning/phases/15-replace-syrupy-snapshot-tests-with-pytest-adbc-replay-vcr-style-record-replay-tests/15-04-SUMMARY.md`
</output>
