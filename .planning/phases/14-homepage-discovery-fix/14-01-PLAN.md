---
phase: 14-homepage-discovery-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/src/index.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - CH-05
  - MYSQL-05

must_haves:
  truths:
    - "docs/src/index.md ADBC drivers install table contains a ClickHouse row pointing to guides/clickhouse.md"
    - "docs/src/index.md ADBC drivers install table contains a MySQL row pointing to guides/mysql.md"
    - "docs/src/index.md 'First pool' config class list includes ClickHouseConfig and MySQLConfig in the Foundry group"
    - "uv run mkdocs build --strict exits 0 after changes"
    - "REQUIREMENTS.md shows [x] Complete for MYSQL-05 and CH-05"
  artifacts:
    - path: "docs/src/index.md"
      provides: "Homepage with full driver table (PyPI + Foundry groups) and complete config class list"
      contains: "ClickHouseConfig"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Requirement tracking with MYSQL-05 and CH-05 marked complete"
      contains: "[x] **MYSQL-05**"
  key_links:
    - from: "docs/src/index.md"
      to: "docs/src/guides/clickhouse.md"
      via: "Markdown link [Foundry installation](guides/clickhouse.md)"
      pattern: "guides/clickhouse\\.md"
    - from: "docs/src/index.md"
      to: "docs/src/guides/mysql.md"
      via: "Markdown link [Foundry installation](guides/mysql.md)"
      pattern: "guides/mysql\\.md"
---

<objective>
Add MySQL and ClickHouse to the two places they are missing on docs/src/index.md: the ADBC drivers install table and the "First pool" config class list. Mark MYSQL-05 and CH-05 complete in REQUIREMENTS.md. Confirm uv run mkdocs build --strict passes.

Purpose: New users landing on the homepage can discover MySQL and ClickHouse as supported backends, completing the discoverability requirements CH-05 and MYSQL-05.
Output: Updated docs/src/index.md with both backends visible; updated REQUIREMENTS.md with both requirements marked complete; confirmed strict mkdocs build.
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
@.claude/skills/adbc-poolhouse-docs-author/SKILL.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@docs/src/index.md
@docs/src/guides/clickhouse.md
@docs/src/guides/mysql.md
@.planning/phases/14-homepage-discovery-fix/14-CONTEXT.md
@.planning/phases/14-homepage-discovery-fix/14-RESEARCH.md

<interfaces>
<!-- Current docs/src/index.md state verified by direct read: -->

ADBC drivers table currently has these rows (10 total, MySQL and ClickHouse absent):
- DuckDB | `pip install adbc-poolhouse[duckdb]`
- Snowflake | `pip install adbc-poolhouse[snowflake]`
- BigQuery | `pip install adbc-poolhouse[bigquery]`
- PostgreSQL | `pip install adbc-poolhouse[postgresql]`
- Apache Arrow Flight SQL | `pip install adbc-poolhouse[flightsql]`
- SQLite | `pip install adbc-poolhouse[sqlite]`
- Databricks | Foundry-distributed — see [Foundry installation](guides/databricks.md)
- Redshift | Foundry-distributed — see [Foundry installation](guides/redshift.md)
- Trino | Foundry-distributed — see [Foundry installation](guides/trino.md)
- MSSQL / Azure SQL / Fabric | Foundry-distributed — see [Foundry installation](guides/mssql.md)

Config class list currently (flat inline, 10 classes, MySQL and ClickHouse absent):
`DuckDBConfig`, `SQLiteConfig`, `SnowflakeConfig`, `BigQueryConfig`, `PostgreSQLConfig`,
`FlightSQLConfig`, `DatabricksConfig`, `RedshiftConfig`, `TrinoConfig`,
`MSSQLConfig`

REQUIREMENTS.md lines that need changing:
- `- [ ] **MYSQL-05**: ...` → `- [x] **MYSQL-05**: ...`
- `- [ ] **CH-05**: ...` → `- [x] **CH-05**: ...`
- Traceability table: MYSQL-05 row "Pending" → "Complete"; CH-05 row "Pending" → "Complete"
- Last updated footer line → update date to 2026-03-02 and add note

Locked decisions from CONTEXT.md:
- PyPI group order: Apache Arrow Flight SQL, BigQuery, DuckDB, PostgreSQL, Snowflake, SQLite
- Foundry group order: ClickHouse, Databricks, MSSQL / Azure SQL / Fabric, MySQL, Redshift, Trino
- Foundry cell format: `Foundry-distributed — see [Foundry installation](guides/X.md)`
- ClickHouse alpha --pre flag stays in clickhouse.md guide only (NOT on homepage)
- Config class list mirrors table grouping exactly
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite docs/src/index.md ADBC drivers table and config class list</name>
  <files>docs/src/index.md</files>
  <action>
Edit docs/src/index.md in two places.

**Place 1 — Replace the ADBC drivers install table:**

Replace the current flat 10-row table with a two-group table. CONTEXT.md locks the exact grouping and ordering. Use sub-headers within the table to separate the two groups (improves scannability per Claude's Discretion).

New table structure:

```markdown
| Warehouse | Install command |
|---|---|
| **PyPI drivers** | |
| Apache Arrow Flight SQL | `pip install adbc-poolhouse[flightsql]` |
| BigQuery | `pip install adbc-poolhouse[bigquery]` |
| DuckDB | `pip install adbc-poolhouse[duckdb]` |
| PostgreSQL | `pip install adbc-poolhouse[postgresql]` |
| Snowflake | `pip install adbc-poolhouse[snowflake]` |
| SQLite | `pip install adbc-poolhouse[sqlite]` |
| **Foundry-distributed drivers** | |
| ClickHouse | Foundry-distributed — see [Foundry installation](guides/clickhouse.md) |
| Databricks | Foundry-distributed — see [Foundry installation](guides/databricks.md) |
| MSSQL / Azure SQL / Fabric | Foundry-distributed — see [Foundry installation](guides/mssql.md) |
| MySQL | Foundry-distributed — see [Foundry installation](guides/mysql.md) |
| Redshift | Foundry-distributed — see [Foundry installation](guides/redshift.md) |
| Trino | Foundry-distributed — see [Foundry installation](guides/trino.md) |
```

Notes:
- Do NOT mention `dbc install --pre clickhouse` anywhere on the homepage table — that stays in clickhouse.md only.
- The bold sub-header rows use empty second column cells.

**Place 2 — Replace the config class list prose:**

Find the paragraph that begins "All supported warehouses have a typed config class:" and replace the inline class list with a two-group structure that mirrors the table:

```markdown
All supported warehouses have a typed config class.

PyPI-installed: `BigQueryConfig`, `DuckDBConfig`, `FlightSQLConfig`, `PostgreSQLConfig`, `SnowflakeConfig`, `SQLiteConfig`.

Foundry-distributed: `ClickHouseConfig`, `DatabricksConfig`, `MSSQLConfig`, `MySQLConfig`, `RedshiftConfig`, `TrinoConfig`.
```

Preserve the "The example below uses DuckDB..." sentence and everything after it unchanged.

Apply the docs-author humanizer pass: review both changed sections for promotional language, AI vocabulary, and vague attributions. The table cells and class names are data — no humanizer pass needed there. Only check any prose changes around the two-group structure.
  </action>
  <verify>
    <automated>grep -n "ClickHouseConfig\|MySQLConfig\|guides/clickhouse\.md\|guides/mysql\.md" /Users/paul/Documents/Dev/Personal/adbc-poolhouse/docs/src/index.md</automated>
  </verify>
  <done>
    - index.md ADBC drivers table has 12 rows (6 PyPI + 6 Foundry) plus 2 bold sub-header rows
    - ClickHouse row present with link to guides/clickhouse.md
    - MySQL row present with link to guides/mysql.md
    - Config class list has ClickHouseConfig in Foundry group, MySQLConfig in Foundry group
    - No mention of --pre flag on homepage
  </done>
</task>

<task type="auto">
  <name>Task 2: Mark MYSQL-05 and CH-05 complete in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Make three targeted edits to .planning/REQUIREMENTS.md:

**Edit 1 — MYSQL-05 checkbox:**
Change `- [ ] **MYSQL-05**:` to `- [x] **MYSQL-05**:` (line in the MySQL section)

**Edit 2 — CH-05 checkbox:**
Change `- [ ] **CH-05**:` to `- [x] **CH-05**:` (line in the ClickHouse section)

**Edit 3 — Traceability table:**
Find the two rows in the traceability table:
- `| MYSQL-05 | Phase 14 | Pending |` → `| MYSQL-05 | Phase 14 | Complete |`
- `| CH-05 | Phase 14 | Pending |` → `| CH-05 | Phase 14 | Complete |`

**Edit 4 — Last updated footer:**
Update the last line to:
`*Last updated: 2026-03-02 — Phase 14 gap closure: MYSQL-05 and CH-05 marked Complete (index.md now lists MySQL and ClickHouse)*`
  </action>
  <verify>
    <automated>grep -n "\[x\] \*\*MYSQL-05\*\*\|\[x\] \*\*CH-05\*\*\|MYSQL-05 | Phase 14 | Complete\|CH-05 | Phase 14 | Complete" /Users/paul/Documents/Dev/Personal/adbc-poolhouse/.planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    - MYSQL-05 checkbox changed from [ ] to [x]
    - CH-05 checkbox changed from [ ] to [x]
    - Both traceability table rows show "Complete"
    - Last updated line updated to 2026-03-02 with Phase 14 note
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify mkdocs build --strict passes and commit</name>
  <files></files>
  <action>
Run the strict mkdocs build to confirm no broken links or malformed markdown:

```bash
cd /Users/paul/Documents/Dev/Personal/adbc-poolhouse && uv run mkdocs build --strict
```

If build fails, read the error output carefully and fix the specific issue in docs/src/index.md (most likely a broken link path — verify `guides/clickhouse.md` and `guides/mysql.md` resolve correctly relative to docs/src/).

When build passes, commit all changes:

```bash
git add docs/src/index.md .planning/REQUIREMENTS.md
git commit -m "feat(14-01): add MySQL and ClickHouse to homepage table and config class list

- Restructure ADBC drivers table into PyPI and Foundry groups
- Add ClickHouse row linking to guides/clickhouse.md
- Add MySQL row linking to guides/mysql.md
- Update config class list to two-group structure (12 classes total)
- Mark MYSQL-05 and CH-05 complete in REQUIREMENTS.md"
```
  </action>
  <verify>
    <automated>cd /Users/paul/Documents/Dev/Personal/adbc-poolhouse && uv run mkdocs build --strict 2>&1 | tail -5</automated>
  </verify>
  <done>
    - uv run mkdocs build --strict exits 0 with no errors
    - All changes committed to git
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `grep -c "ClickHouseConfig\|MySQLConfig" docs/src/index.md` returns ≥ 2
2. `grep "guides/clickhouse.md\|guides/mysql.md" docs/src/index.md` shows both links
3. `grep "\[x\] \*\*MYSQL-05\*\*" .planning/REQUIREMENTS.md` returns a match
4. `grep "\[x\] \*\*CH-05\*\*" .planning/REQUIREMENTS.md` returns a match
5. `uv run mkdocs build --strict` exits 0
</verification>

<success_criteria>
- docs/src/index.md ADBC drivers install table includes rows for MySQL and ClickHouse with correct Foundry-distributed cell format and links to the respective guide pages
- docs/src/index.md "First pool" config class list includes `MySQLConfig` and `ClickHouseConfig` in the Foundry group
- uv run mkdocs build --strict passes with zero errors after changes
- REQUIREMENTS.md shows `[x]` Complete for MYSQL-05 and CH-05; traceability table updated to Complete for both
</success_criteria>

<output>
After completion, create `.planning/phases/14-homepage-discovery-fix/14-01-SUMMARY.md` using the summary template.

Required frontmatter fields:
- phase: 14-homepage-discovery-fix
- plan: "14-01"
- status: complete
- requirements-completed: [CH-05, MYSQL-05]
</output>
