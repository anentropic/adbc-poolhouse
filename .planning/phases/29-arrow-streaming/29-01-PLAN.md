---
phase: 29-arrow-streaming
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/_async_harness/stubs.py
  - tests/async/test_reader_stream.py
  - tests/async/test_reader_close.py
  - tests/async/test_reader_lifetime.py
  - tests/async/test_reader_cancel.py
  - tests/async/test_reader_busy.py
  - tests/async/test_reader_resource.py
  - tests/async/conftest.py
autonomous: true
requirements: [STREAM-01, STREAM-02, STREAM-03, STREAM-04, STREAM-05, STREAM-06, EDGE-33, EDGE-20, EDGE-22, EDGE-23]
must_haves:
  truths:
    - "A stub reader satisfying _SyncReader (schema property, blocking read_next_batch, StopIteration at end, close) is available in the harness"
    - "BlockingStubCursor.fetch_record_batch() returns the stub reader"
    - "Six failing test files exist covering STREAM-01..06 + EDGE-20/22/23/33, each parametrized asyncio x trio"
    - "The Snowflake cassette smoke result (can it replay a streaming fetch_record_batch?) is recorded as a decision"
  artifacts:
    - path: "tests/_async_harness/stubs.py"
      provides: "BlockingStubReader class + BlockingStubCursor.fetch_record_batch()"
      contains: "class BlockingStubReader"
    - path: "tests/async/test_reader_stream.py"
      provides: "STREAM-01/02 failing tests"
    - path: "tests/async/test_reader_lifetime.py"
      provides: "STREAM-04/EDGE-33 read-after-checkin tests (DuckDB + Snowflake cassette)"
    - path: "tests/async/test_reader_busy.py"
      provides: "STREAM-06 two-tier guard tests"
    - path: "tests/async/test_reader_resource.py"
      provides: "EDGE-22/23 ResourceWarning discipline tests"
  key_links:
    - from: "tests/_async_harness/stubs.py::BlockingStubReader"
      to: "_SyncReader structural surface"
      via: "schema property + read_next_batch + close"
      pattern: "read_next_batch"
    - from: "tests/async/test_reader_lifetime.py"
      to: "pyarrow.lib.ArrowInvalid"
      via: "read-after-checkin assertion"
      pattern: "ArrowInvalid"
---

<objective>
Build the Wave-0 test scaffolding for Phase 29 Arrow streaming: extend the sync-only
`BlockingStubCursor` harness with a stub `RecordBatchReader` satisfying the (about-to-exist)
`_SyncReader` structural surface, add a `fetch_record_batch()` on the stub cursor, and author
the six failing reader test files that pin STREAM-01..06 + EDGE-20/22/23/33. Run the Snowflake
cassette smoke to resolve research assumption A1 and record the decision.

Purpose: Nyquist Dimension 8 — every production task in later waves must map to an automated
test that already exists (RED) so GREEN is observable. This plan creates that RED surface.
Output: One extended harness file, six new test files, one conftest fixture helper if needed,
and a recorded A1 decision. Production code does NOT exist yet, so these tests are expected to
FAIL (import errors / assertion failures) — that is correct for Wave 0.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@.claude/skills/adbc-poolhouse-docs-author/SKILL.md
</execution_context>

<context>
@.planning/phases/29-arrow-streaming/29-CONTEXT.md
@.planning/phases/29-arrow-streaming/29-RESEARCH.md
@.planning/phases/29-arrow-streaming/29-PATTERNS.md
@.planning/phases/29-arrow-streaming/29-VALIDATION.md
@tests/_async_harness/stubs.py
@tests/async/conftest.py
@tests/async/test_edge_resource.py
</context>

<artifacts_this_phase_produces>
This plan produces the TEST-SIDE artifacts. The production symbols they target (created in
plans 02/03) are: `AsyncRecordBatchReader`, `_SyncReader` Protocol, `AsyncCursor.fetch_record_batch`,
`AsyncConnection._reader_open`, the `_EXHAUSTED` sentinel + module-level `_pull` helper, the
`from_reader` kw on `_enter_offload`/`_offloading`, the reader's `_detached` flag, and the
`_SyncCursor.fetch_record_batch` Protocol line.

Artifacts THIS plan creates:
- `BlockingStubReader` (harness stub) + `BlockingStubCursor.fetch_record_batch()`
- `tests/async/test_reader_stream.py`, `test_reader_close.py`, `test_reader_lifetime.py`,
  `test_reader_cancel.py`, `test_reader_busy.py`, `test_reader_resource.py`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extend the harness with a BlockingStubReader + stub fetch_record_batch</name>
  <read_first>
    - tests/_async_harness/stubs.py (the file being modified; read the module docstring lines 1-49 on the sticky-release / lost-wakeup discipline, `BlockingStubCursor.__init__`, `_block`, `adbc_cancel`, `close`, `release`, and the public-attribute HARD CONTRACT table)
    - .planning/phases/29-arrow-streaming/29-PATTERNS.md (section "tests/_async_harness/stubs.py (EXTEND — add a stub reader)")
    - tests/async/conftest.py (the `make_stub_async_connection` helper + `entered`/`on_enter`/`register_on_enter` gating wiring)
  </read_first>
  <action>
    Add a `BlockingStubReader` class to `tests/_async_harness/stubs.py`, mirroring `BlockingStubCursor`'s
    sticky-release design (`_closed`/`_cancelled` latch under the lock, checked at `_block` entry BEFORE the
    re-arm `clear()`; happy-path `release()` stays transient). Keep it anyio-free — `threading` only (D-03).
    The stub reader must structurally satisfy the forthcoming `_SyncReader` Protocol: a `schema`
    `@property` (no-I/O passthrough returning a caller-supplied stub schema object), a `read_next_batch()`
    that blocks-then-returns-the-next-configured-batch via the same `_block()` gate idiom and raises bare
    `StopIteration` when its configured batch sequence is exhausted (so the worker-side `_pull` catch is
    exercised), and a `close()` mirroring `BlockingStubCursor.close` (terminal `_closed` latch + event set).
    Give it LOCKED public contract attributes in the `BlockingStubCursor` style: `read_call_count`,
    `close_call_count`, `entered`, `on_enter`, plus `register_on_enter`. The reader has NO `adbc_cancel` of
    its own (Pitfall 4) — cancel is fired via the owning cursor; do NOT add `adbc_cancel` to the reader.
    Then add `fetch_record_batch(self)` to `BlockingStubCursor` returning a fresh `BlockingStubReader`
    (accepting an injected batch sequence + schema so tests configure exhaustion). Wire the reader's
    `entered`/`on_enter` into the same gating bridge the cursor uses so cancel/aliasing tests stay
    deterministic. Document every new public attribute inline as a HARD CONTRACT (D-04, locked names).
  </action>
  <verify>
    <automated>.venv/bin/python -c "from tests._async_harness.stubs import BlockingStubReader, BlockingStubCursor; c=BlockingStubCursor(); r=c.fetch_record_batch(); assert hasattr(r,'schema') and hasattr(r,'read_next_batch') and hasattr(r,'close'); assert not hasattr(r,'adbc_cancel')"</automated>
  </verify>
  <acceptance_criteria>
    - `from tests._async_harness.stubs import BlockingStubReader` imports without error
    - `BlockingStubCursor().fetch_record_batch()` returns a `BlockingStubReader`
    - The stub reader exposes `schema` (property), `read_next_batch`, `close` and does NOT expose `adbc_cancel`
    - `read_next_batch()` raises bare `StopIteration` (args==()) once its configured batch sequence is drained
    - Module stays anyio-free (source scan: no `import anyio` added)
  </acceptance_criteria>
  <done>The harness provides a `_SyncReader`-shaped blocking stub reader and a stub `fetch_record_batch`, reusing the sticky-release latch idiom.</done>
</task>

<task type="auto">
  <name>Task 2: Author the six failing reader test files (STREAM-01..06 + EDGE-20/22/23/33)</name>
  <read_first>
    - tests/async/test_edge_resource.py (whole file — the `_LIFETIME_LOOPS` + `concurrency_marks` pattern and the fetch-inside-scope / assert-outside-scope structure to copy)
    - tests/async/conftest.py (`anyio_backend` dual-param, `duckdb_async_pool`, `snowflake_async_pool` cassette fixture, `make_stub_async_connection`)
    - tests/async/_edge_helpers.py (the `concurrency_marks` loop+timeout markers required on hang-prone tests — MEMORY "loop flaky concurrency tests")
    - .planning/phases/29-arrow-streaming/29-VALIDATION.md (the Per-Task Verification Map: exact observable per requirement)
    - .planning/phases/29-arrow-streaming/29-RESEARCH.md (§Validation Architecture "Key observables to assert")
  </read_first>
  <action>
    Create six test files under `tests/async/`, every reader test parametrized `asyncio x trio` via the
    existing `anyio_backend` fixture; hang-prone tests (cancel, busy, lifetime loops) carry the
    `concurrency_marks` loop+timeout markers. Write them against the NOT-YET-EXISTING production surface
    (`await cursor.fetch_record_batch()` → `AsyncRecordBatchReader`) so they FAIL now (RED).
    - `test_reader_stream.py` (STREAM-01/02): assert `fetch_record_batch()` returns an `AsyncRecordBatchReader`;
      `async for batch in reader:` yields `pyarrow.RecordBatch` chunks; each pull runs off the loop thread
      (thread-id ≠ loop) and is limiter-bound (stub counts limiter acquires / DuckDB integration).
    - `test_reader_close.py` (STREAM-03 + EDGE-20): assert `async with await cursor.fetch_record_batch() as reader:`
      closes offloaded-and-shielded; add `test_shielded_cleanup_chains` making the stub reader's `close` raise
      and asserting the body error chains via `__context__` while `_reader_open` is still cleared / connection
      still released.
    - `test_reader_lifetime.py` (STREAM-04/EDGE-33): DuckDB (`duckdb_async_pool`) AND Snowflake cassette
      (`snowflake_async_pool`, importorskip-clean) legs; assert drain-then-checkin yields the correct rows,
      and a read AFTER checkin (or after close) raises native `pyarrow.lib.ArrowInvalid` (message contains
      "stream that has already been closed") — NOT a poolhouse error type, never a segfault. Copy the
      `_LIFETIME_LOOPS` structure from `test_edge_resource.py`.
    - `test_reader_cancel.py` (STREAM-05): using `make_stub_async_connection` + a blocking pull, cancel/timeout
      the pull and assert `stub_cursor.adbc_cancel_call_count == 1`, `stub_conn.invalidate_call_count == 1`,
      and `pool.checkedout() == 0`, under asyncio and trio (looped).
    - `test_reader_busy.py` (STREAM-06): while a reader is live (`_reader_open` True), a foreign op
      (`execute` / a second `fetch_record_batch` / `commit`) raises `ConnectionBusyError`; the reader's OWN
      pulls do NOT (reentrancy exemption).
    - `test_reader_resource.py` (EDGE-22/23): an unclosed reader's `__del__` emits `ResourceWarning`
      (`pytest.warns(ResourceWarning)`) and NO "coroutine was never awaited" `RuntimeWarning`
      (`recwarn` assert); the happy path (closed via `async with`) emits neither.
    Do NOT assert on any bespoke async error type — the read-after-checkin error is the driver's native
    `ArrowInvalid` (RESEARCH non-goal). No positive-duration sleeps in timeout tests (source-scannable).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/async/test_reader_stream.py tests/async/test_reader_close.py tests/async/test_reader_lifetime.py tests/async/test_reader_cancel.py tests/async/test_reader_busy.py tests/async/test_reader_resource.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - All six files are collected by pytest with zero collection errors (tests parametrize asyncio x trio)
    - Running them now FAILS (RED) — production symbols absent; a full run exits non-zero
    - `test_reader_lifetime.py` references `pyarrow.lib.ArrowInvalid` and both `duckdb_async_pool` + `snowflake_async_pool`
    - `test_reader_cancel.py` and `test_reader_busy.py` carry `concurrency_marks`
    - Source scan: no positive-duration `sleep(` in the timeout/cancel tests; no bespoke `ReaderClosedError` referenced
  </acceptance_criteria>
  <done>Six RED test files pin every Phase-29 requirement to an automated assertion, parametrized over both backends, ready to turn GREEN as plans 02/03 land.</done>
</task>

<task type="auto">
  <name>Task 3: Snowflake cassette streaming smoke (resolve A1) and record the decision</name>
  <read_first>
    - tests/async/conftest.py (`snowflake_async_pool` cassette fixture — how it importorskips + which recorded interaction it replays)
    - .planning/phases/29-arrow-streaming/29-RESEARCH.md (§Open Questions #1 / Assumption A1)
    - .planning/phases/29-arrow-streaming/29-VALIDATION.md (Wave 0 Requirements: the cassette smoke)
  </read_first>
  <action>
    Add a small smoke test (in `test_reader_lifetime.py` or a dedicated `test_reader_cassette_smoke.py`,
    Claude's discretion) that opens the `snowflake_async_pool` and attempts `await cursor.fetch_record_batch()`.
    Determine whether the checked-in Snowflake cassette can replay a streaming `fetch_record_batch` (vs. only
    `fetch_arrow_table`). If it CAN, keep the full Snowflake EDGE-33 leg (drain-then-checkin rows + read-after-checkin
    `ArrowInvalid`). If it CANNOT (cassette lacks the streaming interaction), scope the Snowflake leg to the
    read-after-checkin `ArrowInvalid` path only (which does not require live streaming rows), mark the
    rows-drain Snowflake assertion `pytest.skip`/xfail with a reason, and record the decision. Either way,
    write the resolved A1 decision into the plan SUMMARY (and, if the cassette needs re-recording, flag it as a
    manual-only follow-up per 29-VALIDATION §Manual-Only Verifications — do NOT block DuckDB coverage on it).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/async -k "cassette_smoke or (reader_lifetime and snowflake)" -q; rc=$?; test $rc -eq 0 -o $rc -eq 5</automated>
  </verify>
  <acceptance_criteria>
    - The smoke either passes or skips cleanly (never hangs, never segfaults) when the Snowflake driver/cassette is absent
    - The SUMMARY records a definitive A1 outcome: "cassette replays streaming reader" OR "scoped Snowflake leg to ArrowInvalid path; re-record flagged manual-only"
    - DuckDB EDGE-33 coverage is NOT gated on the Snowflake result
  </acceptance_criteria>
  <done>Assumption A1 is resolved by an actual cassette probe and the decision is recorded; the Snowflake test leg is scoped accordingly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| loop thread → worker thread | blocking `read_next_batch`/`close` cross `to_thread`; `StopIteration` must not leak |
| reader C-stream → checked-in connection | reading a C Arrow stream after checkin is a use-after-free risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Denial (use-after-free/segfault) | read-after-checkin on a live reader | mitigate | `test_reader_lifetime.py` asserts native `ArrowInvalid` (clean exception) on both DuckDB + Snowflake cassette — proves no segfault (STREAM-04/EDGE-33) |
| T-29-04 | Denial (resource leak) | unclosed reader | mitigate | `test_reader_resource.py` asserts `__del__` `ResourceWarning` + checkin backstop (EDGE-22/23) |
| T-29-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit: none); all deps pre-existing |
</threat_model>

<verification>
- All six reader test files collect (`--collect-only`) with zero errors.
- `.venv/bin/python -c "from tests._async_harness.stubs import BlockingStubReader"` succeeds.
- The stub reader satisfies the `_SyncReader` shape (schema/read_next_batch/close; no adbc_cancel).
- A1 resolved and recorded in SUMMARY.
- Tests are RED (production absent) — expected for Wave 0.
</verification>

<success_criteria>
- Harness extended with a `_SyncReader`-shaped blocking stub reader + stub `fetch_record_batch`.
- Six RED test files pin STREAM-01..06 + EDGE-20/22/23/33, each parametrized asyncio x trio, hang-prone ones looped.
- Snowflake cassette A1 assumption resolved and the Snowflake test leg scoped accordingly.
</success_criteria>

<output>
Create `.planning/phases/29-arrow-streaming/29-01-SUMMARY.md` when done, recording the A1 decision.
</output>
