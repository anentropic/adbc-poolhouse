---
phase: 29-arrow-streaming
plan: 04
type: execute
wave: 3
depends_on: [29-03]
files_modified:
  - docs/src/guides/async.md
  - docs/scripts/gen_ref_pages.py
autonomous: false
requirements: [STREAM-01, STREAM-02, STREAM-03, STREAM-04, EDGE-33, PKG-01]
must_haves:
  truths:
    - "The async guide documents Arrow streaming: fetch_record_batch -> async for -> close, with the reader-lifetime contract and honest concurrency framing"
    - "The API reference renders AsyncRecordBatchReader and AsyncCursor.fetch_record_batch (docstrings authored in plan 03; reference generation includes the new symbol)"
    - "mkdocs build --strict passes"
    - "A humanizer pass is applied to all new/rewritten prose"
  artifacts:
    - path: "docs/src/guides/async.md"
      provides: "Arrow streaming how-to section (canonical async with usage, lifetime contract)"
      contains: "fetch_record_batch"
  key_links:
    - from: "docs/src/guides/async.md"
      to: "AsyncRecordBatchReader"
      via: "documented canonical usage async with await cursor.fetch_record_batch() as reader"
      pattern: "async with await cursor.fetch_record_batch"
---

<objective>
Close the Phase 29 documentation gate (CLAUDE.md, phase >= 7): document Arrow streaming in the
async guide — the `fetch_record_batch` → `async for` → `close` flow, the reader-lifetime contract
(read-after-checkin surfaces a native driver error), the canonical `async with` usage (D-29-18),
and honest concurrency framing (per-batch offload; GIL re-acquired during materialization). Ensure
the API reference renders `AsyncRecordBatchReader` and the new `AsyncCursor.fetch_record_batch`,
confirm `mkdocs build --strict` passes, and apply a humanizer pass.

Purpose: DOCS coverage for the streaming surface is a per-phase completion requirement, not deferred
to Phase 33. (Phase 33 is the consolidation point; each earlier phase still documents its own surface.)
Output: an updated async guide section, verified reference rendering, a clean strict build.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@.claude/skills/adbc-poolhouse-docs-author/SKILL.md
</execution_context>

<context>
@.planning/phases/29-arrow-streaming/29-CONTEXT.md
@.planning/phases/29-arrow-streaming/29-PATTERNS.md
@docs/src/guides/async.md
@docs/scripts/gen_ref_pages.py
@src/adbc_poolhouse/_async/_reader.py
@src/adbc_poolhouse/_async/_cursor.py
</context>

<artifacts_this_phase_produces>
This plan produces the consumer-facing docs for the streaming surface. The docstrings themselves
(`AsyncRecordBatchReader`, `fetch_record_batch`, `schema`) are authored in plan 03; this plan adds
the guide prose and confirms the API-reference generation picks up the new symbol (`_reader.py`),
then runs the strict build + humanizer pass. No new production symbols are created here.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Document Arrow streaming in the async guide + confirm reference rendering</name>
  <read_first>
    - docs/src/guides/async.md (the guide being extended — match its existing voice, section structure, and tabbed-content style)
    - .claude/skills/adbc-poolhouse-docs-author/SKILL.md (doc-type classification: this is a how-to guide; voice, humanizer patterns to eliminate)
    - docs/scripts/gen_ref_pages.py (how reference pages are auto-generated from source — confirm `_reader.py` public symbols are included; NEVER hand-write `docs/src/reference/`)
    - src/adbc_poolhouse/_async/_reader.py + src/adbc_poolhouse/_async/_cursor.py (the shipped API + docstrings to reference accurately)
    - $HOME/.claude/skills/humanizer/SKILL.md (for the Step 3 humanizer pass)
  </read_first>
  <action>
    Add an Arrow-streaming section to `docs/src/guides/async.md`. Show the canonical usage (D-29-18):
    `async with await cursor.fetch_record_batch() as reader:` then `async for batch in reader:`, in the
    guide's existing illustrative-snippet style (reader supplies their own setup; use tabbed content if a
    driver-specific variant helps, per the docs-author skill). Document the reader-lifetime contract plainly:
    the reader is bound to its checked-out connection, the canonical `async with` guarantees `close` fires
    under errors/cancellation/early `break`, and reading after checkin (or after close) surfaces the driver's
    native closed-stream error (`pyarrow.lib.ArrowInvalid`) — poolhouse adds no bespoke error type. Add the
    honest concurrency framing: each `read_next_batch` pull is individually offloaded through the pool limiter,
    and the GIL is re-acquired during Arrow materialization (no false "fully parallel" claims). Confirm
    `gen_ref_pages.py` includes `_async/_reader.py` so `AsyncRecordBatchReader` and `fetch_record_batch` render
    in the API reference; if the generation script filters modules by an explicit list, add `_reader.py` (this
    is the only reason `docs/scripts/gen_ref_pages.py` appears in `files_modified` — do NOT hand-write
    reference pages). Use Markdown, second person, no promotional/AI vocabulary. Then apply the full humanizer
    checklist to the new prose (max one em dash per paragraph; no "powerful"/"seamlessly"/"leverage"/"ensure
    that"; no rule-of-three padding; no superficial -ing openers).
  </action>
  <verify>
    <automated>DISABLE_MKDOCS_2_WARNING=true .venv/bin/mkdocs build --strict</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "async with await cursor.fetch_record_batch" docs/src/guides/async.md` succeeds
    - The guide states the read-after-checkin behavior surfaces a native driver error (no invented error type)
    - The guide includes the per-batch-offload + GIL-reacquired concurrency framing
    - `.venv/bin/mkdocs build --strict` exits 0 (exit code, not grep); the built API reference contains `AsyncRecordBatchReader`
    - Humanizer scan: no banned promotional/AI vocabulary in the new section; <= 1 em dash per paragraph
  </acceptance_criteria>
  <done>The async guide documents the streaming surface with the lifetime contract and honest concurrency framing; the API reference renders the new symbols; the strict build passes; a humanizer pass is applied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>An Arrow-streaming how-to section in the async guide (canonical `async with` usage, reader-lifetime contract, per-batch-offload / GIL-reacquired concurrency framing), plus API-reference rendering of `AsyncRecordBatchReader` and `AsyncCursor.fetch_record_batch`. `mkdocs build --strict` passes and a humanizer pass has been applied.</what-built>
  <how-to-verify>
    1. Run `DISABLE_MKDOCS_2_WARNING=true .venv/bin/mkdocs serve` and open the local docs.
    2. Read the async guide's new Arrow-streaming section: is the `async with` canonical usage clear, is the reader-lifetime / read-after-checkin behavior stated honestly (native driver error, no bespoke type), and is the concurrency framing truthful (per-batch offload, GIL re-acquired — no over-claiming parallelism)?
    3. Open the API reference for `AsyncRecordBatchReader` and `AsyncCursor.fetch_record_batch`: do the Google-style docstrings render with Args/Returns/Raises + the Example block, with no rogue RST colons?
    4. Confirm the prose reads like the rest of the guide (direct, practical, no promotional/AI filler).
  </how-to-verify>
  <resume-signal>Type "approved" or describe what to revise (voice, accuracy of the lifetime/concurrency framing, or reference rendering).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → user expectation | inaccurate concurrency/lifetime framing could lead a user into a use-after-free footgun |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-07 | Information disclosure (misleading docs) | streaming guide concurrency/lifetime framing | mitigate | Document the reader-lifetime contract + `async with` canonical usage explicitly; honest per-batch-offload / GIL framing; human-verify checkpoint gates accuracy |
| T-29-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `docs/src/guides/async.md` documents `fetch_record_batch` → `async for` → `close`, the lifetime contract, and honest concurrency framing.
- API reference renders `AsyncRecordBatchReader` + `fetch_record_batch`.
- `.venv/bin/mkdocs build --strict` exits 0.
- Humanizer pass applied; human-verify checkpoint approved.
</verification>

<success_criteria>
- The streaming surface is documented in the async guide with an accurate lifetime + concurrency framing.
- The API reference includes the new symbols; the strict docs build passes.
- Humanizer pass applied and the docs are human-approved.
</success_criteria>

<output>
Create `.planning/phases/29-arrow-streaming/29-04-SUMMARY.md` when done.
</output>
