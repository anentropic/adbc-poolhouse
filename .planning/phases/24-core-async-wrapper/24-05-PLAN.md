---
phase: 24-core-async-wrapper
plan: 05
type: execute
wave: 4
depends_on:
  - 24-03
  - 24-04
files_modified:
  - docs/src/guides/async.md
  - mkdocs.yml
  - docs/src/index.md
  - src/adbc_poolhouse/_async/_factory.py
  - src/adbc_poolhouse/_async/_pool.py
  - src/adbc_poolhouse/_async/_connection.py
  - src/adbc_poolhouse/_async/_cursor.py
autonomous: true
requirements:
  - CORE-04
  - APOOL-01
  - ACONN-03
  - ACUR-04
  - EDGE-15
user_setup: []

must_haves:
  truths:
    - "An async usage guide shows create_async_pool→connect→execute→fetch_arrow_table→checkin, honest about I/O-bound vs materialization-bound concurrency (per the Phase 22 SPIKE findings)"
    - "The async guide documents the forbidden connection-aliasing antipattern (ConnectionBusyError) with the canonical wrong/right example from CONTEXT D-24-03"
    - "Every new public async symbol (create_async_pool, managed_async_pool, close_async_pool, AsyncPool, AsyncConnection, AsyncCursor, ConnectionBusyError) has a Google-style docstring; the entry points have an Example block"
    - "uv run mkdocs build --strict (or .venv/bin/mkdocs build --strict) passes and a humanizer pass is applied to all new prose"
  artifacts:
    - path: "docs/src/guides/async.md"
      provides: "Async usage guide + forbidden-aliasing antipattern + I/O-vs-materialization honesty"
      contains: "ConnectionBusyError"
    - path: "mkdocs.yml"
      provides: "nav entry for the async guide"
      contains: "async"
  key_links:
    - from: "docs/src/guides/async.md"
      to: "Phase 22 SPIKE-03 go/no-go findings"
      via: "honest I/O-bound (execute parallelizes) vs materialization-bound (fetch partially serializes) framing"
      pattern: "fetch_arrow_table"
    - from: "docs/src/guides/async.md"
      to: "ConnectionBusyError (D-24-03)"
      via: "the wrong (aliased) / right (one-conn-per-task) canonical example"
      pattern: "ConnectionBusyError"
---

<objective>
Close the docs gate for Phase 24 (project rule, phase >= 7): an honest async usage guide,
the forbidden connection-aliasing antipattern documented with `ConnectionBusyError`, complete
Google-style docstrings with `Example:` blocks on every new public async symbol, and a green
`mkdocs build --strict`. The API reference is auto-generated (`gen_ref_pages.py`) — do NOT
hand-write `docs/src/reference/`; just ensure the docstrings are complete so the generated
reference is correct.

Note: ROADMAP scopes the full Documentation milestone to Phase 28, but the CLAUDE.md docs
gate applies to EVERY phase >= 7. This plan delivers the per-phase docs completion
requirement (guide + docstrings + strict build); Phase 28 consolidates and expands.

Purpose: satisfy the phase completion requirement that documentation is not optional, and
ship the consumer-facing aliasing warning that D-24-03's reject-don't-lock decision demands.
Output: `docs/src/guides/async.md`, mkdocs nav + index updates, and finalized docstrings
across the four `_async/` modules.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@.claude/skills/adbc-poolhouse-docs-author/SKILL.md
@$HOME/.claude/skills/humanizer/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-core-async-wrapper/24-CONTEXT.md
@.planning/phases/24-core-async-wrapper/24-RESEARCH.md
@.planning/phases/24-core-async-wrapper/24-03-SUMMARY.md
@.planning/phases/24-core-async-wrapper/24-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize Google-style docstrings on all public async symbols</name>
  <files>src/adbc_poolhouse/_async/_factory.py, src/adbc_poolhouse/_async/_pool.py, src/adbc_poolhouse/_async/_connection.py, src/adbc_poolhouse/_async/_cursor.py</files>
  <read_first>
    - .claude/skills/adbc-poolhouse-docs-author/SKILL.md (Step 2 docstring format, quality checklist).
    - MEMORY docstring style: Google-style, Markdown not RST (`` `create_pool` `` not
      `:func:`); `Example:` (singular) = admonition with ```python fences.
    - PATTERNS "Google-style / Markdown docstrings" § (lines 342-346) — analog
      `_pool_factory.py` `create_pool` docstring (lines 163-229: Args/Returns/Raises/Example)
      and `_exceptions.py` (lines 5-27).
    - The Plan 02/03 SUMMARYs and the four `_async/` modules as written.
  </read_first>
  <action>
    Audit every PUBLIC symbol in `_async/` and ensure complete Google-style Markdown
    docstrings (Args/Returns/Raises). The entry points — `create_async_pool`,
    `managed_async_pool`, `close_async_pool` — and the classes `AsyncPool`, `AsyncConnection`,
    `AsyncCursor` MUST each have an `Example:` block (singular, admonition, ```python fences).
    `ConnectionBusyError` (in `_exceptions.py`, already documented in Plan 02) — verify its
    docstring states the serialized-not-concurrent rule. Document on `AsyncConnection.cursor`
    that it is synchronous (no await — ACONN-03) and on the cursor properties that they are
    sync passthroughs (ACUR-07). Document that `fetch_arrow_table` returns a materialized
    `pyarrow.Table` safe after checkin (ACUR-04). Use Markdown link syntax, never RST roles.
    Do NOT hand-write `docs/src/reference/` — it is auto-generated from these docstrings.
  </action>
  <verify>
    <automated>.venv/bin/ruff check src/adbc_poolhouse/_async/ && .venv/bin/mkdocs build --strict</automated>
  </verify>
  <acceptance_criteria>
    - Source: each of `create_async_pool`, `managed_async_pool`, `close_async_pool`,
      `AsyncPool`, `AsyncConnection`, `AsyncCursor` has an `Example:` block —
      `grep -rc "Example:" src/adbc_poolhouse/_async/` >= 6.
    - Source: no RST role syntax (`:func:`/`:class:`/`:meth:`) in `_async/` —
      `grep -rcE ':(func|class|meth|attr|mod):' src/adbc_poolhouse/_async/` == 0.
    - Lint: `.venv/bin/ruff check src/adbc_poolhouse/_async/` clean (incl. D docstring rules).
    - Docs: `.venv/bin/mkdocs build --strict` passes (the auto-generated reference renders the docstrings).
  </acceptance_criteria>
  <done>All public async symbols have complete Google-style Markdown docstrings; entry points
  and classes have Example blocks; no RST; ruff + mkdocs --strict clean.</done>
</task>

<task type="auto">
  <name>Task 2: Async usage guide (I/O-vs-materialization honesty + forbidden-aliasing antipattern) + nav/index</name>
  <files>docs/src/guides/async.md, mkdocs.yml, docs/src/index.md</files>
  <read_first>
    - .claude/skills/adbc-poolhouse-docs-author/SKILL.md (how-to guide rules, voice, See-also,
      humanizer pass) and @$HOME/.claude/skills/humanizer/SKILL.md.
    - 24-CONTEXT.md "User-Facing Documentation Requirement" (lines 123-156) — the CANONICAL
      wrong/right aliasing example and the "why a hard error" rationale to adapt.
    - .planning/phases/22-feasibility-spike/ SPIKE-03 go/no-go (the honest concurrency claims:
      execute parallelizes ~2.77x@N=4; fetch_arrow_table partially serializes ~1.67x@N=4 —
      see STATE.md Accumulated Context lines 66) for the I/O-vs-materialization framing.
    - mkdocs.yml (the nav structure + existing guides/ entries) and docs/src/index.md (the
      configuration/entry-point listing pattern) and an existing guide (e.g.
      docs/src/guides/quack.md) for the See-also + tabbed-content house style.
  </read_first>
  <action>
    Write `docs/src/guides/async.md`: a how-to guide (second person, direct/practical voice)
    showing the canonical async flow `create_async_pool` → `await pool.connect()` →
    `conn.cursor()` (sync) → `await cur.execute(...)` → `await cur.fetch_arrow_table()` →
    checkin via `async with`. Be HONEST about concurrency per the Phase 22 SPIKE findings:
    `execute` (and other I/O-bound C calls) parallelize because ADBC releases the GIL, but
    `fetch_arrow_table` materialization partially serializes on the GIL — state this plainly,
    do not oversell (DOCS-01 framing, even though DOCS-01 formally lands in Phase 28). Include
    a "Do not share one async connection across concurrent tasks" section with the canonical
    ❌ (aliased — raises `ConnectionBusyError`) / ✅ (one connection per task) example from
    CONTEXT D-24-03, and the "why a hard error, not silent queuing" rationale. Note the
    `[async]` extra install (`pip install adbc-poolhouse[async]`) is declared in Phase 26 —
    reference it without over-claiming if not yet shipped. Add a "See also" section.

    Add the guide to `mkdocs.yml` nav (under the guides section). Add the async entry points to
    `docs/src/index.md` where the sync entry points / configuration are listed. Apply the
    humanizer pass to ALL new prose (eliminate promotional/AI vocabulary, max one em dash per
    paragraph, no rule-of-three padding).
  </action>
  <verify>
    <automated>.venv/bin/mkdocs build --strict</automated>
  </verify>
  <acceptance_criteria>
    - Source: `docs/src/guides/async.md` exists and contains both `ConnectionBusyError` and a
      `create_async_pool` → `fetch_arrow_table` flow (grep both).
    - Source: the guide states the I/O-bound-vs-materialization honesty (grep for both
      `execute` parallelism and `fetch_arrow_table` serialization framing).
    - Source: `mkdocs.yml` references the async guide (`grep -c "async" mkdocs.yml` >= 1 nav entry).
    - Docs: `.venv/bin/mkdocs build --strict` passes.
    - Humanizer: no banned terms (`powerful|seamlessly|robust|comprehensive|leverage|delve|streamline`)
      in async.md (`grep -ciE` == 0); max one em dash per paragraph (manual check).
  </acceptance_criteria>
  <done>The async guide ships with honest concurrency framing + the forbidden-aliasing
  antipattern; nav + index updated; mkdocs --strict passes; humanizer pass applied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs ↔ user mental model | the aliasing antipattern doc is the boundary preventing users from accidentally sharing a connection across tasks (the EOP threat from Plan 03) |

Documentation only — no shipped runtime surface of its own.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05-EOP | Elevation of Privilege | users aliasing one connection across tasks (the runtime threat T-24-03-EOP) reinforced by clear docs | mitigate | the guide documents the forbidden antipattern + ConnectionBusyError with a copy-paste-safe correct pattern (Task 2) |
| T-24-05-INFO | Information Disclosure | overselling concurrency (claiming materialization parallelizes) misleads users into wrong capacity planning | mitigate | honest I/O-vs-materialization framing per the Phase 22 SPIKE go/no-go (Task 2, DOCS-01) |
| T-24-05-SC | Tampering | npm/pip/cargo installs | N/A | no packages installed; docs build uses existing mkdocs dev deps |
</threat_model>

<verification>
- `.venv/bin/mkdocs build --strict` passes.
- `.venv/bin/ruff check src/adbc_poolhouse/_async/` clean.
- `grep -rc "Example:" src/adbc_poolhouse/_async/` >= 6; no RST roles.
- async.md contains the aliasing antipattern + honest concurrency framing.
- Humanizer pass applied (no banned terms in new prose).
</verification>

<success_criteria>
- Async usage guide is honest about I/O-bound vs materialization-bound concurrency.
- Forbidden-aliasing antipattern documented with ConnectionBusyError (D-24-03).
- All public async symbols documented; entry points + classes have Example blocks.
- mkdocs build --strict passes; humanizer pass applied.
</success_criteria>

<output>
Create `.planning/phases/24-core-async-wrapper/24-05-SUMMARY.md` when done.
</output>
