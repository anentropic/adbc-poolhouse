---
phase: 24-core-async-wrapper
plan: 04
type: execute
wave: 3
depends_on:
  - 24-01
  - 24-03
files_modified:
  - tests/async/conftest.py
  - tests/async/test_async_lifecycle.py
  - tests/async/test_edge_limiter.py
  - tests/async/test_edge_aliasing.py
  - tests/async/test_edge_exceptions.py
  - tests/async/test_edge_resource.py
  - tests/async/test_edge_loophygiene.py
  - tests/test_async_guard.py
autonomous: true
requirements:
  - CORE-01
  - CORE-02
  - CORE-03
  - CORE-04
  - APOOL-01
  - APOOL-02
  - APOOL-03
  - ACONN-01
  - ACONN-02
  - ACONN-03
  - ACONN-04
  - ACONN-05
  - ACONN-06
  - ACUR-01
  - ACUR-02
  - ACUR-03
  - ACUR-04
  - ACUR-05
  - ACUR-06
  - ACUR-07
  - EDGE-09
  - EDGE-10
  - EDGE-11
  - EDGE-12
  - EDGE-15
  - EDGE-17
  - EDGE-18
  - EDGE-21
  - EDGE-25
  - EDGE-26
user_setup: []

must_haves:
  truths:
    - "The happy path create_async_pool→connect→cursor→execute→fetch_arrow_table→checkin passes against the real DuckDB driver under BOTH asyncio and trio"
    - "EDGE-09 success+error legs: pool._limiter.borrowed_tokens==0 after a normal return AND after an AdbcError, across a x50 loop"
    - "EDGE-10: a token queued-waiting on a saturated limiter that is cancelled leaks no token; concurrency fully recovers"
    - "EDGE-12: observed running-max in execute == pool_size+max_overflow under a 4x flood; EDGE-15: a 2nd concurrent caller raises ConnectionBusyError and max_concurrent_in_execute==1"
    - "EDGE-25: scan_async_package('src/adbc_poolhouse/_async') == [] AND no backend-specific names appear in _async/; worker thread-id != loop thread-id"
    - "Every async test is @pytest.mark.anyio (both backends); the Snowflake cassette leg drives the async layer (D-24-04)"
  artifacts:
    - path: "tests/async/conftest.py"
      provides: "anyio_backend fixture (asyncio+trio) scoped to tests/async only + DuckDB real-pool fixture + stub-backed AsyncConnection/AsyncPool fixtures"
      contains: "anyio_backend"
    - path: "tests/async/test_async_lifecycle.py"
      provides: "happy-path lifecycle + sync-surface (cursor()/properties no-await) coverage"
      contains: "fetch_arrow_table"
    - path: "tests/async/test_edge_limiter.py"
      provides: "EDGE-09/10/11/12"
      contains: "borrowed_tokens"
    - path: "tests/async/test_edge_aliasing.py"
      provides: "EDGE-15 ConnectionBusyError"
      contains: "ConnectionBusyError"
    - path: "tests/test_async_guard.py"
      provides: "EDGE-25 real-package scan + D-24-04 no-backend-names rule"
      contains: "src/adbc_poolhouse/_async"
  key_links:
    - from: "tests/async/conftest.py:anyio_backend"
      to: "tests/_async_harness/conftest.py:anyio_backend"
      via: "verbatim mirror — fixture lives in tests/async only, NOT the root conftest (Pitfall 6)"
      pattern: "anyio_backend"
    - from: "tests/async/test_edge_*.py"
      to: "tests/_async_harness/test_harness.py:_await_inside"
      via: "reuse the bounded-sleep(0) inside-execute poll + always-release-in-finally"
      pattern: "_await_inside"
---

<objective>
Prove the surface. Write the happy-path lifecycle test (real DuckDB driver, both backends)
and the structural EDGE suite assigned to Phase 24 — EDGE-09 (success+error legs only;
cancel-mid-block is Phase 25 per D-24-02), EDGE-10/11/12 (limiter), EDGE-15 (aliasing →
ConnectionBusyError), EDGE-17/18 (exceptions/leak), EDGE-21 (Arrow lifetime), EDGE-25/26
(loop hygiene) — plus the D-24-04 structural-genericity guard. Every test is
`@pytest.mark.anyio` (asyncio + trio), and the concurrency tests are loop-stable.

Purpose: this is the verification backbone for the whole phase — the must-haves of Plans 02
and 03 are only proven here. The harness Wave-0 redesign (Plan 01) is what makes the
execute-then-fetch and gated-concurrency tests possible without deadlock.
Output: `tests/async/conftest.py` + 6 test files + the extended `tests/test_async_guard.py`.

CRITICAL (MEMORY + RESEARCH Pitfall 2): run the concurrency/EDGE tests in a x20 loop, never
single-shot — a ~33% deadlock slipped past a single green in Phase 23. Wrap concurrency
bodies in `anyio.fail_after(<watchdog>)`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@.claude/skills/adbc-poolhouse-docs-author/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-core-async-wrapper/24-CONTEXT.md
@.planning/phases/24-core-async-wrapper/24-RESEARCH.md
@.planning/phases/24-core-async-wrapper/24-PATTERNS.md
@.planning/phases/24-core-async-wrapper/24-01-SUMMARY.md
@.planning/phases/24-core-async-wrapper/24-03-SUMMARY.md
@.planning/research/ASYNC-EDGE-CASES.md
@tests/_async_harness/conftest.py
@tests/_async_harness/test_harness.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: conftest fixtures + happy-path lifecycle + sync-surface tests (real DuckDB, both backends)</name>
  <files>tests/async/conftest.py, tests/async/test_async_lifecycle.py</files>
  <read_first>
    - tests/_async_harness/conftest.py lines 31-76 — the `anyio_backend` fixture (asyncio +
      trio MockClock); PATTERNS says copy this VERBATIM into tests/async/conftest.py.
    - tests/test_pool_factory.py lines 30, 56-69 — the real-driver create→connect→cursor→
      execute→close flow + `pool._adbc_source.close()` teardown discipline (PATTERNS
      test_async_lifecycle analog).
    - RESEARCH "Recommended Project Structure" (lines 238-268, esp. the conftest-placement
      load-bearing note) and Pitfall 6 (lines 469-476 — never hoist anyio_backend to root
      conftest).
    - The Plan 02/03 SUMMARYs for the final AsyncPool/AsyncConnection/AsyncCursor signatures.
  </read_first>
  <action>
    Create `tests/async/conftest.py`: copy the `anyio_backend` fixture verbatim from
    tests/_async_harness/conftest.py (asyncio + trio; function-scoped). Placement is
    load-bearing — it MUST live in `tests/async/` only (never the root conftest, or the sync
    suite is dragged under the anyio plugin and PKG-04 breaks — Pitfall 6). Add fixtures:
    (a) a real-driver DuckDB `AsyncPool` fixture using `create_async_pool(DuckDBConfig(...))`
    with proper teardown (`await close_async_pool(pool)`); (b) a stub-backed `AsyncPool`/
    `AsyncConnection` fixture that wraps a `BlockingStubConnection`/`BlockingStubCursor` from
    the Phase 23 harness so the limiter/aliasing/loop-hygiene EDGE tests can gate workers
    deterministically. Add a `tests/async/__init__.py` if the suite layout needs it.

    Create `tests/async/test_async_lifecycle.py` (all tests `@pytest.mark.anyio`, taking
    `anyio_backend_name`): (1) happy path — `create_async_pool(DuckDBConfig)` →
    `async with await pool.connect() as conn` → `cur = conn.cursor()` →
    `await cur.execute("SELECT 42 AS answer")` → `tbl = await cur.fetch_arrow_table()` →
    assert `tbl` is a `pyarrow.Table` with the expected value → checkin via `__aexit__`
    (covers APOOL-01, ACONN-01/02, ACUR-01/04). (2) sync-surface — assert `conn.cursor()`
    returns without await (the call is not a coroutine) and `cur.description`/`rowcount`/
    `arraysize` are read without `await` (ACONN-03/ACUR-07). (3) lifecycle — `await
    conn.commit()`/`rollback()` (ACONN-04), `await conn.close()` (ACONN-05),
    `async with managed_async_pool(DuckDBConfig(...)) as pool:` auto-close (APOOL-02/03), and
    assert `pool._pool.checkedout() == 0` after each scope. Add a Snowflake
    `pytest-adbc-replay` cassette leg driving the async layer (D-24-04 backend-generic proof)
    — mark/skip cleanly if the cassette is unavailable, but wire it. Docstrings per docs gate.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/async/test_async_lifecycle.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "anyio_backend" tests/async/conftest.py` >= 1 and the fixture is NOT in
      any root/ancestor conftest of the sync suite (`grep -rL anyio_backend tests/conftest.py`
      or confirm tests/conftest.py has no anyio_backend).
    - Behavior: `.venv/bin/pytest tests/async/test_async_lifecycle.py -q` passes under both the
      asyncio and trio legs (parametrized by anyio_backend).
    - Behavior: the happy-path test asserts `isinstance(tbl, pyarrow.Table)` and the answer
      value 42; checkedout()==0 after each scope.
    - Sync suite untouched: `.venv/bin/pytest tests/test_pool_factory.py -q` still green.
  </acceptance_criteria>
  <done>conftest fixtures live in tests/async only; the happy path + sync-surface + lifecycle
  + managed_async_pool tests pass under asyncio and trio against real DuckDB; the Snowflake
  cassette leg is wired; checkedout()==0 after every scope.</done>
</task>

<task type="auto">
  <name>Task 2: limiter + aliasing EDGE tests (EDGE-09/10/11/12/15), loop-stable with watchdog</name>
  <files>tests/async/test_edge_limiter.py, tests/async/test_edge_aliasing.py</files>
  <read_first>
    - tests/_async_harness/test_harness.py — `_await_inside` poll (lines 60-76),
      `test_max_concurrent` (143-175, the gated-two-workers + high-water-mark template),
      `test_block_then_release` (82-102, always-release-in-finally), `test_block_then_adbc_cancel`
      (105-117, gated-cancel shape for EDGE-10). PATTERNS calls these the exact templates.
    - RESEARCH Code Examples "EDGE-09 success+error token accounting" (lines 525-548) and
      "EDGE-15 aliasing rejection" (lines 550-575); Pattern 2 transient token (299-308);
      EDGE-11 safe-by-construction (line 87); EDGE-12 (line 88, stub max_concurrent counter).
    - .planning/research/ASYNC-EDGE-CASES.md — the EDGE-09/10/11/12/15 designs.
    - 24-CONTEXT.md D-24-02 (EDGE-09 cancel-mid-block leg is Phase 25 — do NOT write it here)
      and D-24-01 (transient token, no hold-and-wait).
    - The Plan 01 SUMMARY (re-armable gate + entered-after-block) for the stub gating API.
  </read_first>
  <action>
    Create `tests/async/test_edge_limiter.py` (all `@pytest.mark.anyio`):
    - EDGE-09 success+error legs ONLY (D-24-02 — NO cancel-mid-block leg): in a x50 loop, run
      the real-DuckDB happy path then assert `pool._limiter.borrowed_tokens == 0`; then in a
      x50 loop run a deliberately bad query (`SELECT * FROM does_not_exist`) expecting an
      AdbcError and assert `borrowed_tokens == 0` and `available_tokens == total_tokens` after.
    - EDGE-10: saturate the limiter with `pool_size+max_overflow` gated stub workers, start one
      MORE offload (which must queue waiting to acquire a token), cancel that queued task
      (cancel-while-queued — no worker starts, no driver touched), release the held workers,
      and assert the limiter fully recovers (`borrowed_tokens == 0`, a fresh offload succeeds).
    - EDGE-11: hold a connection and await a second offload on it; assert no self-deadlock
      under `anyio.fail_after(<watchdog>)` (transient token makes this safe by construction).
    - EDGE-12: under a 4x flood of concurrent gated stub `execute`s, assert the observed
      running-max == `pool_size + max_overflow` (read the stub's `max_concurrent_in_execute`
      and/or the limiter's `borrowed_tokens` high-water). Use `_await_inside` to gate workers
      and ALWAYS release in `finally`.

    Create `tests/async/test_edge_aliasing.py` (EDGE-15, `@pytest.mark.anyio`): with the
    stub-backed AsyncConnection, start one task that enters `execute` and blocks (gate via
    `_await_inside`), then start a SECOND task that calls `execute` on the SAME cursor/conn
    concurrently; assert the second raises `ConnectionBusyError`, no concurrency-violation
    occurs, `conn._stub`'s `max_concurrent_in_execute == 1`, and `checkedout()` stays correct.
    Release the first worker in `finally`.

    Wrap every concurrency body in `anyio.fail_after(<watchdog seconds>)` (real clock). Docstrings.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/async/test_edge_limiter.py tests/async/test_edge_aliasing.py -q</automated>
  </verify>
  <acceptance_criteria>
    - EDGE-09: `grep -c "borrowed_tokens == 0" tests/async/test_edge_limiter.py` >= 2 (success
      AND error legs); each in a `range(50)` loop. No cancel-mid-block test present (D-24-02).
    - EDGE-12: a test asserts running-max `== pool_size + max_overflow` under a 4x flood.
    - EDGE-15: `tests/async/test_edge_aliasing.py` asserts a raised `ConnectionBusyError` and
      `max_concurrent_in_execute == 1`.
    - Behavior (single): `.venv/bin/pytest tests/async/test_edge_limiter.py tests/async/test_edge_aliasing.py -q` passes under both backends.
    - Behavior (loop-stable, MANDATORY): `for i in $(seq 1 20); do .venv/bin/pytest tests/async/test_edge_limiter.py tests/async/test_edge_aliasing.py -q || { echo "HANG/FAIL run $i"; break; }; done` → 20 runs, 0 hangs/failures.
    - Every concurrency body wrapped in `fail_after` (`grep -c fail_after` >= the number of concurrency tests).
  </acceptance_criteria>
  <done>EDGE-09 (success+error), 10, 11, 12, 15 pass under asyncio and trio, single-shot AND
  across 20 loop runs with 0 hangs; the cancel-mid-block leg is correctly absent (Phase 25).</done>
</task>

<task type="auto">
  <name>Task 3: exception/resource/loop-hygiene EDGE tests + guard extension (EDGE-17/18/21/25/26, D-24-04)</name>
  <files>tests/async/test_edge_exceptions.py, tests/async/test_edge_resource.py, tests/async/test_edge_loophygiene.py, tests/test_async_guard.py</files>
  <read_first>
    - tests/test_async_guard.py lines 20-84 — `scan_async_package` self-tests against
      synthetic source (the analog to extend); tests/_async_harness/guard.py
      `_GuardVisitor` rules (lines 53-94) and `scan_async_package` (115+), and the documented
      alias limitation (lines 148-158).
    - RESEARCH EDGE-17/18 (lines 90-91), EDGE-21 (line 92, Pitfall 7 lines 478-485),
      EDGE-25/26 (lines 93-94), Open Q2 (lines 612-617 — the no-backend-names rule for D-24-04).
    - .planning/research/ASYNC-EDGE-CASES.md — EDGE-17/18/21/25/26 designs.
    - tests/test_pool_factory.py (real-driver checkedout() assertions for EDGE-18/21).
    - The list of 13 backend config class names (from src/adbc_poolhouse/__init__.py __all__)
      for the no-backend-names denylist.
  </read_first>
  <action>
    Create `tests/async/test_edge_exceptions.py` (`@pytest.mark.anyio`): EDGE-17 — a worker
    that raises an ADBC `Error` subclass (use the stub to raise, or a real bad DuckDB query)
    propagates to the awaiting task with the EXACT type and a traceback that includes the
    worker frame (assert `type(exc)` and `traceback` content). EDGE-18 — force an exception in
    `__aenter__`/post-checkout and assert `pool._pool.checkedout() == 0` with no cumulative
    leak over N (e.g. 20) iterations.

    Create `tests/async/test_edge_resource.py` (`@pytest.mark.anyio`, real DuckDB): EDGE-21 —
    `await cur.fetch_arrow_table()`, exit the connection scope (checkin), THEN read the table
    and assert it is a `pyarrow.Table` (not a reader) and the values are intact (no
    use-after-checkin / segfault). Loop a few times to catch allocator issues.

    Create `tests/async/test_edge_loophygiene.py` (`@pytest.mark.anyio`): EDGE-25 (behavioral
    leg) — capture the worker thread-id via the stub's `execute_thread_ids` and assert it
    differs from the loop thread-id (`threading.get_ident()` on the loop). EDGE-26 — while a
    stub `execute` is blocked off-loop, a concurrent coroutine advances across several
    `anyio.sleep(0)` checkpoints (assert a counter incremented while the offload blocked).
    Wrap in `fail_after`; release in `finally`.

    Extend `tests/test_async_guard.py`: add a test asserting
    `scan_async_package("src/adbc_poolhouse/_async") == []` (EDGE-25/CORE-03 static leg — the
    guard tolerates an absent root, so this is meaningful now that `_async/` exists). Add the
    D-24-04 `no-backend-specific-names` check (Open Q2): either a new guard rule on
    `_GuardVisitor` or a grep-based test asserting none of the 13 backend config class names
    (DuckDBConfig, SnowflakeConfig, BigQueryConfig, ClickHouseConfig, DatabricksConfig,
    FlightSQLConfig, MSSQLConfig, MySQLConfig, PostgreSQLConfig, QuackConfig, RedshiftConfig,
    SQLiteConfig, TrinoConfig) appear anywhere in `src/adbc_poolhouse/_async/`. Document the
    chosen mechanism. Docstrings per docs gate.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/async/test_edge_exceptions.py tests/async/test_edge_resource.py tests/async/test_edge_loophygiene.py tests/test_async_guard.py -q</automated>
  </verify>
  <acceptance_criteria>
    - EDGE-17: a test asserts the propagated exception's exact type AND that the worker frame
      is in the traceback.
    - EDGE-18: a test loops >=20 iterations forcing an `__aenter__` failure and asserts
      `checkedout() == 0` every iteration (no cumulative leak).
    - EDGE-21: a test reads the `pyarrow.Table` AFTER checkin and asserts `isinstance(tbl, pyarrow.Table)` and value integrity.
    - EDGE-25: `tests/test_async_guard.py` asserts `scan_async_package("src/adbc_poolhouse/_async") == []`
      (grep the path string) AND the no-backend-names check returns no hits for any of the 13 names.
    - EDGE-26: a test asserts a concurrent coroutine's counter advanced while the offload was blocked.
    - Behavior: the combined pytest command exits 0 under both backends; loop-stable x20 for the loophygiene/concurrency tests.
  </acceptance_criteria>
  <done>EDGE-17/18/21/25/26 pass under asyncio and trio; the guard scans the real _async/
  package and enforces no-asyncio + no-bare-to_thread + no-backend-names (D-24-04);
  loop-stable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test ↔ worker thread | EDGE tests gate and assert across the offload boundary via the harness stubs |
| static analysis ↔ shipped `_async/` source | the guard meta-test is the boundary enforcing no-asyncio / no-bare-to_thread / no-backend-names |

Test code only — no shipped runtime surface; the threats it MITIGATES belong to Plans 02/03.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04-DOS | Denial of Service | a flaky concurrency test masks a real deadlock (the Phase 23 ~33% miss) | mitigate | x20 loop-run acceptance gate + `fail_after` watchdog on every concurrency body (Tasks 2-3) |
| T-24-04-TAMPER | Tampering | per-backend code or `import asyncio` or a bare `to_thread` sneaks into `_async/` | mitigate | guard meta-test scans the REAL `_async/` package and enforces all three rules incl. D-24-04 no-backend-names (Task 3) |
| T-24-04-INFO | Information Disclosure | the limiter test reading `pool._limiter` is the verification of the resource-exhaustion mitigation (T-24-02-DOS) | mitigate | EDGE-12 proves the bound; EDGE-09 proves no token leak (Task 2) |
| T-24-04-COLL | Tampering | hoisting `anyio_backend` to the root conftest drags the sync suite under the anyio plugin (breaks PKG-04) | mitigate | fixture confined to tests/async/conftest.py; acceptance asserts root conftest has none (Task 1) |
| T-24-04-SC | Tampering | npm/pip/cargo installs | N/A | no new packages |
</threat_model>

<verification>
- `.venv/bin/pytest tests/async tests/test_async_guard.py -q` green under both backends.
- x20 loop run of the concurrency/EDGE files → 0 hangs.
- `scan_async_package('src/adbc_poolhouse/_async') == []` and no-backend-names check clean.
- Full suite green: `.venv/bin/pytest -q`.
- `.venv/bin/ruff check tests/async tests/test_async_guard.py` clean.
</verification>

<success_criteria>
- Happy path (real DuckDB) + Snowflake cassette leg pass under asyncio and trio.
- EDGE-09 (success+error), 10, 11, 12, 15, 17, 18, 21, 25, 26 all green and loop-stable.
- Guard enforces no-asyncio + no-bare-to_thread + no-backend-names against the real _async/.
- EDGE-09 cancel-mid-block leg correctly ABSENT (deferred to Phase 25).
</success_criteria>

<output>
Create `.planning/phases/24-core-async-wrapper/24-04-SUMMARY.md` when done.
</output>
