---
phase: 28-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/src/guides/async.md
  - docs/src/index.md
autonomous: true
requirements: [DOCS-01]
must_haves:
  truths:
    - "The async guide leads with an explicit experimental + incomplete caveat before any usage content"
    - "The caveat names every not-yet-present async feature so 'incomplete' is concrete"
    - "The index Async section flags experimental status and links the guide for the full caveat"
    - "The guide's concurrency claims match the measured numbers in 22-GO-NO-GO.md (no over-claim)"
  artifacts:
    - path: "docs/src/guides/async.md"
      provides: "Experimental admonition + audited honest-concurrency prose"
      contains: '!!! warning "Experimental"'
    - path: "docs/src/index.md"
      provides: "Experimental flag line in the Async section"
      contains: "experimental"
  key_links:
    - from: "docs/src/guides/async.md"
      to: ".planning/phases/22-feasibility-spike/22-GO-NO-GO.md"
      via: "concurrency numbers sourced from the go/no-go (execute ~2.77x, fetch_arrow_table ~1.67x)"
      pattern: "2\\.77|1\\.67"
---

<objective>
Add the D-28-01 experimental/incomplete caveat to the two consumer-facing prose surfaces (the async guide and the index Async section) and reconcile the guide's honest-concurrency claims against the Phase 22 go/no-go (D-28-03), tightening any over-claim. This is DOCS-01: an honest async usage guide distinguishing I/O-bound wins from materialization-bound limits.

Purpose: The deferred-feature list is long and the API may still shift; an honest experimental banner sets correct expectations and is the reason D-28-02 keeps the surface from hardening into the public API.

Output: An updated `docs/src/guides/async.md` (experimental admonition at the top + audited concurrency prose) and an updated `docs/src/index.md` (one experimental-flag line in the Async section).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@.claude/skills/adbc-poolhouse-docs-author/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-documentation/28-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the experimental + incomplete admonition to the async guide</name>
  <read_first>
    docs/src/guides/async.md (the file being edited),
    .planning/phases/28-documentation/28-CONTEXT.md (D-28-01 placement + the named deferred-feature list),
    .planning/REQUIREMENTS.md ("Future Requirements (deferred)" block — the canonical not-yet-present feature list)
  </read_first>
  <files>docs/src/guides/async.md</files>
  <action>
    Insert a `!!! warning "Experimental"` admonition at the very top of `docs/src/guides/async.md` — after the `# Async pool` H1 and the opening paragraph, but BEFORE the `## Install` section (per D-28-01 placement). The admonition body MUST state two things: (1) the async API is experimental and may change between minor releases; (2) name the not-yet-present features so "incomplete" is concrete — Arrow streaming (`fetch_record_batch` / `async for batch`), async bulk write (`adbc_ingest`), DataFrame convenience (`fetch_df` / `fetch_polars`), async ADBC metadata, and async prepared statements. Source that feature list verbatim-in-spirit from the REQUIREMENTS.md "Future Requirements (deferred)" block — do not invent features. Use the project voice (direct, second person) per the docs-author skill. Do NOT add per-symbol caveats to docstrings; the guide carries the status (D-28-01).
  </action>
  <verify>
    <automated>grep -q '!!! warning "Experimental"' docs/src/guides/async.md && grep -q 'fetch_record_batch' docs/src/guides/async.md && grep -q 'adbc_ingest' docs/src/guides/async.md && grep -q 'fetch_df' docs/src/guides/async.md && grep -qi 'prepared statement' docs/src/guides/async.md && echo OK</automated>
  </verify>
  <done>The admonition renders above "Install", names all five deferred feature areas, and states the API may change.</done>
</task>

<task type="auto">
  <name>Task 2: Audit the guide's concurrency claims against 22-GO-NO-GO.md and add the index experimental flag</name>
  <read_first>
    docs/src/guides/async.md ("What actually runs in parallel" section),
    .planning/phases/22-feasibility-spike/22-GO-NO-GO.md (canonical measured numbers + the "must not claim" + "inference gap" sections),
    docs/src/index.md (the existing `## Async` section)
  </read_first>
  <files>docs/src/guides/async.md, docs/src/index.md</files>
  <action>
    Two edits, no source-code changes.

    (a) In `docs/src/guides/async.md` "What actually runs in parallel" section: reconcile every concurrency number and claim against 22-GO-NO-GO.md (D-28-03). Confirm the quoted figures match the go/no-go — execute ~2.77x at N=4 (~0.69 efficiency) and fetch_arrow_table ~1.67x at N=4 (~0.42 efficiency). Verify the guide does NOT over-claim: it must keep parallelism (real CPU/C-side work) distinct from I/O concurrency (inferred, not directly measured — in-proc DuckDB has no network wait, per the go/no-go "inference gap" section), and must not promise linear speedup for large `fetch_arrow_table` fan-out. Tighten any wording that overstates the measured result; if the existing prose is already faithful, make only the minimal correction needed and note "verified against go/no-go, no over-claim found" in the SUMMARY. Do not restate numbers from memory — derive them from the go/no-go.

    (b) In `docs/src/index.md` `## Async` section: add ONE line flagging the async API as experimental and linking the async guide for the full caveat. Do not duplicate the extra/entry-point listing already present there (DOCS-03 confirms it exists) — only add the experimental flag line.
  </action>
  <verify>
    <automated>grep -Eq '2\.77|1\.67' docs/src/guides/async.md && grep -qi 'inferenc\|in-process DuckDB\|network' docs/src/guides/async.md && grep -qi 'experimental' docs/src/index.md && echo OK</automated>
  </verify>
  <done>The guide's numbers match the go/no-go, the parallelism-vs-I/O-concurrency distinction is intact with the inference caveat, and index.md has an experimental flag line linking the guide.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Documentation-only change: no runtime code paths, no inputs, no auth surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01-I | Information Disclosure | example code in async.md | mitigate | example snippets use DuckDB with no credentials and never print secrets — confirm no credential literals are introduced |
| T-28-01-T | Tampering | doc prose accuracy | accept | concurrency claims are sourced from 22-GO-NO-GO.md (D-28-03), removing the over-claim risk; no executable surface |
</threat_model>

<verification>
- `.venv/bin/mkdocs build --strict` still parses both edited files (full strict gate runs in Plan 28-04; this plan only needs the admonition syntax to be valid).
- The experimental admonition appears before "Install" in async.md.
- index.md Async section flags experimental and links the guide.
</verification>

<success_criteria>
DOCS-01 satisfied: the async guide leads with an explicit experimental + incomplete caveat naming the deferred features, and its I/O-bound-vs-materialization-bound concurrency claims are reconciled against the Phase 22 go/no-go with no over-claim. The index Async section flags experimental status.
</success_criteria>

<artifacts_this_phase_produces>
- `docs/src/guides/async.md`: new `!!! warning "Experimental"` admonition (before Install); audited "What actually runs in parallel" prose.
- `docs/src/index.md`: new experimental-flag line in the `## Async` section.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/28-documentation/28-01-SUMMARY.md` when done
</output>
