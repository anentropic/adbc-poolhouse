---
phase: 28-documentation
plan: 01
subsystem: documentation
tags: [mkdocs, async, docs, experimental-caveat]

# Dependency graph
requires:
  - phase: 22-feasibility-spike
    provides: "22-GO-NO-GO.md measured concurrency numbers (execute 2.77x, fetch_arrow_table 1.67x) and the inference-gap disclaimer"
  - phase: 24-async-core
    provides: "the existing async.md guide (first-query walkthrough, What actually runs in parallel section)"
provides:
  - "Experimental + incomplete caveat on the two consumer-facing async prose surfaces (guide + index)"
  - "Named deferred-feature list (streaming, adbc_ingest, dataframe, metadata, prepared statements) so 'incomplete' is concrete"
  - "Audit confirmation that async.md concurrency claims match the Phase 22 go/no-go with no over-claim"
affects: [28-documentation remaining plans (reference rendering, configuration gap, changelog, strict gate)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Async status carried by the guide + index prose (D-28-01), NOT per-symbol docstring caveats"

key-files:
  created: []
  modified:
    - "docs/src/guides/async.md"
    - "docs/src/index.md"

key-decisions:
  - "D-28-01 placement: experimental admonition sits after the H1/opening paragraph and before ## Install"
  - "D-28-03 audit: async.md concurrency prose already faithful to 22-GO-NO-GO.md; no over-claim, no edit needed in the parallelism section"

patterns-established:
  - "Deferred-feature list sourced verbatim-in-spirit from REQUIREMENTS.md Future Requirements, not invented"

requirements-completed: [DOCS-01]

# Metrics
duration: ~12min
completed: 2026-06-29
---

# Phase 28 Plan 01: Async Experimental Caveat + Concurrency Audit Summary

**Experimental + incomplete admonition added to the async guide (naming all five deferred async feature areas) and a matching experimental flag line to the index, with the guide's concurrency claims audited against the Phase 22 go/no-go and confirmed free of over-claim.**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-06-29T10:17:50Z
- **Completed:** 2026-06-29
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Added a `!!! warning "Experimental"` admonition to `docs/src/guides/async.md`, placed before the Install section, stating the API may change between minor releases and naming the five not-yet-present feature areas (Arrow streaming / `fetch_record_batch`, async bulk write / `adbc_ingest`, DataFrame convenience / `fetch_df` / `fetch_polars`, async ADBC metadata, async prepared statements).
- Added one experimental + incomplete flag line to the `## Async` section of `docs/src/index.md`, linking the guide for the full caveat.
- Audited the guide's "What actually runs in parallel" section against `22-GO-NO-GO.md` (D-28-03): the quoted figures (execute ~2.77x / ~69% efficiency at N=4, `fetch_arrow_table` ~1.67x / ~42% at N=4), the parallelism-vs-I/O-concurrency distinction, the in-process-DuckDB inference caveat, and the "do not expect linear speedup for large fetch fan-out" warning all match the go/no-go. No over-claim found, so no edit to the parallelism prose was required.
- `.venv/bin/mkdocs build --strict` passes (exit 0).

## Task Commits

Each task was committed atomically:

1. **Task 1: Add experimental + incomplete admonition to the async guide** - `b81cb33` (docs)
2. **Task 2: Audit concurrency claims + add index experimental flag** - `c9e64aa` (docs)

**Plan metadata:** committed separately with SUMMARY.md, STATE.md, ROADMAP.md.

## Files Created/Modified
- `docs/src/guides/async.md` - New "Experimental" warning admonition before Install, naming the five deferred async feature areas; concurrency prose audited (unchanged — already faithful).
- `docs/src/index.md` - New experimental + incomplete flag line in the Async section, linking the guide.

## Decisions Made
- **D-28-03 audit outcome:** Verified against the go/no-go, no over-claim found. The existing "What actually runs in parallel" prose already quoted the measured numbers (2.77x, 1.67x, ~69%, ~42%) directly, kept CPU/C-side parallelism distinct from inferred I/O concurrency, named the in-process DuckDB inference gap, and warned against linear-speedup expectations for large `fetch_arrow_table` fan-out. No tightening was needed; only the admonition (Task 1) and the index flag line (Task 2) were added.

## Deviations from Plan

None - plan executed exactly as written. (Task 2a was an audit task; the audit found the prose faithful, which the plan explicitly anticipated with "if the existing prose is already faithful, make only the minimal correction needed and note 'verified against go/no-go, no over-claim found' in the SUMMARY.")

## Issues Encountered
None. The `reference/` relative-link INFO messages from `mkdocs build --strict` are pre-existing (the reference pages are auto-generated by `gen_ref_pages.py`) and also appear on unmodified files; they are not errors and do not fail the strict build.

## Threat Surface
- T-28-01-I (information disclosure via example code): satisfied — no credential literals introduced; the new admonition and flag line contain prose only.
- No new trust boundaries, runtime code paths, inputs, or auth surface (documentation-only change).

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- DOCS-01 satisfied. The two consumer-facing async surfaces now lead with the experimental + incomplete caveat, and the concurrency claims are reconciled against the Phase 22 go/no-go.
- Remaining Phase 28 work (per 28-CONTEXT.md): reference-page rendering for `AsyncPool`/`AsyncConnection`/`AsyncCursor` (D-28-02), the `configuration.md` async gap (DOCS-03), the v1.4.0 changelog entry, and the full strict + humanizer docs gate (DOCS-04).

## Self-Check: PASSED

- `docs/src/guides/async.md` — FOUND
- `docs/src/index.md` — FOUND
- `.planning/phases/28-documentation/28-01-SUMMARY.md` — FOUND
- Commit `b81cb33` (Task 1) — FOUND
- Commit `c9e64aa` (Task 2) — FOUND

---
*Phase: 28-documentation*
*Completed: 2026-06-29*
