---
phase: 28-documentation
plan: 04
type: execute
wave: 2
depends_on: ["28-01", "28-02", "28-03"]
files_modified:
  - docs/src/guides/async.md
  - docs/src/index.md
  - docs/src/guides/configuration.md
  - docs/src/changelog.md
  - docs/src/reference/adbc_poolhouse.md
autonomous: true
requirements: [DOCS-04]
must_haves:
  truths:
    - "uv run mkdocs build --strict passes with zero warnings"
    - "A humanizer pass has been applied to all new or substantially rewritten prose from plans 28-01/02/03"
    - "No promotional/AI-vocabulary patterns remain in the new async prose"
  artifacts:
    - path: "site/index.html"
      provides: "Proof the strict build produced a site"
  key_links:
    - from: ".venv/bin/mkdocs build --strict"
      to: "docs/src/**"
      via: "strict build gate over all phase-28 prose"
      pattern: "mkdocs"
---

<objective>
Close the DOCS-04 docs quality gate for Phase 28: run `mkdocs build --strict` to zero warnings across all the prose written in plans 28-01/02/03, and apply a humanizer pass to every new or substantially rewritten passage. This is the final wave — it runs only after all async prose exists.

Purpose: A strict build with no warnings is the project's completion bar for docs (CLAUDE.md Documentation Quality Gate, phases ≥ 7), and the humanizer pass keeps the docs in the project voice (no promotional/AI vocabulary).

Output: Humanized prose across the five touched files and a green `mkdocs build --strict`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@.claude/skills/adbc-poolhouse-docs-author/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-documentation/28-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Humanizer pass over all new/rewritten phase-28 prose</name>
  <read_first>
    .claude/skills/adbc-poolhouse-docs-author/SKILL.md (Step 3 humanizer pass + the technical-docs pattern list),
    @/Users/paul/.claude/skills/humanizer/SKILL.md (the full humanizer checklist referenced by the docs-author skill),
    docs/src/guides/async.md (new admonition + audited concurrency prose from 28-01),
    docs/src/index.md (experimental flag line from 28-01),
    docs/src/guides/configuration.md (new async subsection from 28-03),
    docs/src/changelog.md (v1.4.0 entry from 28-03),
    docs/src/reference/adbc_poolhouse.md (the `## Async API (experimental)` heading prose from 28-02)
  </read_first>
  <files>docs/src/guides/async.md, docs/src/index.md, docs/src/guides/configuration.md, docs/src/changelog.md, docs/src/reference/adbc_poolhouse.md</files>
  <action>
    Apply the humanizer checklist (docs-author skill Step 3 + the referenced humanizer SKILL.md) to every passage added or substantially rewritten in plans 28-01/02/03. Eliminate the technical-docs patterns the skill calls out: promotional language ("powerful", "seamlessly", "robust", "comprehensive", "effortlessly"), AI vocabulary ("delve", "leverage", "streamline", "it's worth noting", "ensure that"), vague attributions ("this allows you to", "this enables"), superficial -ing openers, rule-of-three padding, and em-dash overuse (max one per paragraph). Do NOT touch passages that predate phase 28 unless they were substantially rewritten this phase. Preserve all factual content — especially the audited concurrency numbers (28-01) and the experimental caveat wording (28-01). Keep the changes prose-only; do not alter mkdocstrings block syntax or code examples.
  </action>
  <verify>
    <automated>! grep -rniE 'seamlessly|leverage|delve|effortlessly|it'"'"'s worth noting' docs/src/guides/async.md docs/src/index.md docs/src/guides/configuration.md docs/src/reference/adbc_poolhouse.md && echo HUMANIZED_OK</automated>
  </verify>
  <done>No promotional/AI-vocabulary patterns remain in the phase-28 prose; factual content (numbers, caveat, entry points) is preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Strict mkdocs build gate (DOCS-04)</name>
  <read_first>
    docs/src/guides/async.md, docs/src/index.md, docs/src/guides/configuration.md, docs/src/changelog.md, docs/src/reference/adbc_poolhouse.md (all phase-28 outputs),
    mkdocs.yml (nav + mkdocstrings config)
  </read_first>
  <files>(verification only — no edits unless the build surfaces a warning to fix)</files>
  <action>
    Run `.venv/bin/mkdocs build --strict` (the sandbox-preferred form per memory uv-sandbox-workarounds; `uv run mkdocs build --strict` is equivalent and also satisfies DOCS-04). It MUST exit 0 with zero warnings. Strict mode fails on any broken autoref, unresolved mkdocstrings path (the D-28-02 risk — if a `_async.*` block does not resolve, this is where it surfaces), or dead internal link introduced by the new cross-links (configuration→async, index→async, reference heading). Fix any warning at its source: a broken `[text][ref]` autoref, an unresolved `:::` path, or a missing nav/link target. Re-run until clean. This task is the phase's final completion gate.
  </action>
  <verify>
    <automated>.venv/bin/mkdocs build --strict 2>&1 | tee /tmp/claude/mkdocs-28.log | tail -8; rc=${PIPESTATUS[0]}; test "$rc" = "0" && ! grep -qiE 'WARNING|ERROR' /tmp/claude/mkdocs-28.log && test -f site/index.html && echo STRICT_OK</automated>
  </verify>
  <done>`mkdocs build --strict` exits 0 with no WARNING/ERROR lines and produces `site/index.html`; all phase-28 cross-links and mkdocstrings blocks resolve.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Documentation-only gate: builds the static site, no runtime code paths or inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04-I | Information Disclosure | built site prose/examples | mitigate | strict build reviews the full rendered async prose; confirm no credential literals or secret values leaked into any rendered example |
| T-28-04-T | Tampering | doc accuracy post-humanize | mitigate | humanizer pass is constrained to preserve factual content (numbers, caveat, entry points); strict build catches any structural breakage |
</threat_model>

<verification>
- `.venv/bin/mkdocs build --strict` exits 0 with zero WARNING/ERROR lines.
- `site/index.html` and `site/reference/adbc_poolhouse/index.html` exist with the async symbols rendered.
- No promotional/AI-vocabulary patterns remain in the phase-28 prose.
</verification>

<success_criteria>
DOCS-04 satisfied: `mkdocs build --strict` passes and a humanizer pass has been applied to all new or substantially rewritten prose across the five phase-28 files. Phase 28 is complete and the v1.4.0 async documentation closeout is done.
</success_criteria>

<artifacts_this_phase_produces>
- Humanized revisions across `docs/src/guides/async.md`, `docs/src/index.md`, `docs/src/guides/configuration.md`, `docs/src/changelog.md`, `docs/src/reference/adbc_poolhouse.md`.
- A green `mkdocs build --strict` (site/ build artifact, gitignored).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/28-documentation/28-04-SUMMARY.md` when done
</output>
