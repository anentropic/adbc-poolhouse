---
phase: 28-documentation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/src/guides/configuration.md
  - docs/src/changelog.md
autonomous: true
requirements: [DOCS-03]
must_haves:
  truths:
    - "The configuration reference lists the [async] extra and the three async entry points (it has zero async mentions today)"
    - "The changelog has a v1.4.0 entry summarizing the async API and marking it experimental"
    - "index.md already covers the extra + entry points (confirmed in 28-01, not duplicated here)"
  artifacts:
    - path: "docs/src/guides/configuration.md"
      provides: "Async extra + entry-point documentation"
      contains: "[async]"
    - path: "docs/src/changelog.md"
      provides: "v1.4.0 changelog entry marking async experimental"
      contains: "1.4.0"
  key_links:
    - from: "docs/src/guides/configuration.md"
      to: "docs/src/guides/async.md"
      via: "cross-link from the new async section to the async guide"
      pattern: "async\\.md"
---

<objective>
Close the `configuration.md` async gap (DOCS-03 — it currently has ZERO async mentions) by documenting the `[async]` extra and the three async entry points, and add a v1.4.0 changelog entry marking the async API experimental (D-28-01 changelog leg). The index Async section already covers the extra + entry points (added/confirmed in 28-01) — confirm, do not duplicate.

Purpose: The configuration page is where users tune pools and discover extras; an async user who lands there must find the `[async]` extra and the async entry points without bouncing to the guide. The changelog records the milestone honestly as experimental.

Output: An updated `docs/src/guides/configuration.md` (new async section) and `docs/src/changelog.md` (v1.4.0 entry).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@.claude/skills/adbc-poolhouse-docs-author/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-documentation/28-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the [async] extra and async entry points to configuration.md</name>
  <read_first>
    docs/src/guides/configuration.md (the file being edited — note it has zero async mentions today; the "Pool tuning" table fields pool_size/max_overflow are reused by the async limiter),
    .planning/phases/28-documentation/28-CONTEXT.md (DOCS-03 scope: add extra + entry points to configuration; do not duplicate index.md),
    docs/src/index.md (confirm the extra + entry points are already listed there so this page cross-references rather than re-explains)
  </read_first>
  <files>docs/src/guides/configuration.md</files>
  <action>
    Add a short async subsection to `docs/src/guides/configuration.md` (a sensible location is after "Pool tuning", since the async pool reuses `pool_size`/`max_overflow` to size its per-pool `CapacityLimiter`). The subsection MUST: (1) name the `[async]` extra and its install command `pip install adbc-poolhouse[async]`; (2) name the three async entry points `create_async_pool`, `managed_async_pool`, `close_async_pool` and state they mirror the sync `create_pool`/`managed_pool`/`close_pool` signatures (same `pool_size`/`max_overflow`/`timeout`/`recycle`/`pre_ping` defaults documented in the Pool tuning table); (3) note that each async pool's `CapacityLimiter` is sized to `pool_size + max_overflow` (so the existing tuning fields govern async concurrency too); (4) cross-link to `async.md` for the full guide and the experimental caveat. Keep it tight — this is a config-reference pointer, not a second copy of the guide. Use project voice. Add the cross-link to the page's existing "See also" list as well.
  </action>
  <verify>
    <automated>grep -q '\[async\]' docs/src/guides/configuration.md && grep -q 'create_async_pool' docs/src/guides/configuration.md && grep -q 'managed_async_pool' docs/src/guides/configuration.md && grep -q 'close_async_pool' docs/src/guides/configuration.md && grep -q 'async.md' docs/src/guides/configuration.md && echo OK</automated>
  </verify>
  <done>configuration.md documents the `[async]` extra, all three entry points, the limiter-sizing note, and links the async guide; index.md is confirmed to already list the extra + entry points (no duplication added there).</done>
</task>

<task type="auto">
  <name>Task 2: Add the v1.4.0 changelog entry marking async experimental</name>
  <read_first>
    docs/src/changelog.md (note the existing convention: bare `## [Unreleased]` at top, then dated `## [1.3.1] - YYYY-MM-DD` sections with `### Features` / `### Bug Fixes` subsections),
    .planning/phases/28-documentation/28-CONTEXT.md (D-28-01 changelog leg: the v1.4.0 entry marks async experimental),
    .planning/ROADMAP.md (Phase 22-27 scope — what the async milestone shipped: offload, per-pool limiter, AsyncPool/Connection/Cursor, cancellation, [async] extra)
  </read_first>
  <files>docs/src/changelog.md</files>
  <action>
    Add a v1.4.0 entry to `docs/src/changelog.md` following the file's existing heading convention. Place it directly under `## [Unreleased]` (use a `## [1.4.0]` heading — match the dated-section style already used for 1.3.1/1.3.0; leave the date as the release date or `- Unreleased` consistent with the existing `## [Unreleased]` placeholder, whichever matches the file's pattern). Under a `### Features` subsection, summarize the async API: the optional `[async]` extra adding an anyio-based async surface (asyncio + trio) — `create_async_pool` / `managed_async_pool` / `close_async_pool`, `AsyncPool` / `AsyncConnection` / `AsyncCursor`, full awaited DBAPI surface incl. `fetch_arrow_table`, per-pool `CapacityLimiter`, shielded checkin, and `adbc_cancel`-based cancellation. CRITICAL: explicitly mark the async API as **experimental** in this entry (D-28-01) and note the sync path is unchanged and gains no async dependency. Do not list deferred features as shipped. Keep entries terse and factual in the changelog's existing bullet style.
  </action>
  <verify>
    <automated>grep -q '1.4.0' docs/src/changelog.md && grep -qi 'experimental' docs/src/changelog.md && grep -q 'async' docs/src/changelog.md && grep -q 'CapacityLimiter\|\[async\]' docs/src/changelog.md && echo OK</automated>
  </verify>
  <done>The changelog has a v1.4.0 entry summarizing the async API, marked experimental, in the file's existing heading + bullet convention, with no deferred feature listed as shipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Documentation-only change: no runtime code paths, inputs, or auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-03-I | Information Disclosure | configuration.md async snippet | mitigate | any install/usage snippet uses DuckDB or env-var placeholders, never a real secret literal |
| T-28-03-T | Tampering | changelog accuracy | accept | entry is sourced from the ROADMAP milestone scope; async marked experimental, no deferred feature listed as shipped |
</threat_model>

<verification>
- configuration.md lists the `[async]` extra, the three entry points, the limiter-sizing note, and links async.md.
- changelog.md has a v1.4.0 entry marked experimental.
- No duplication of the index.md async listing (index covered in 28-01).
</verification>

<success_criteria>
DOCS-03 satisfied: the configuration page lists the `[async]` extra and the async entry points (gap closed), the index page already covers them (confirmed), and the changelog records v1.4.0 as an experimental async release.
</success_criteria>

<artifacts_this_phase_produces>
- `docs/src/guides/configuration.md`: new async subsection (extra + entry points + limiter-sizing + cross-link) and a "See also" link to async.md.
- `docs/src/changelog.md`: new `## [1.4.0]` entry under Unreleased, async marked experimental.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/28-documentation/28-03-SUMMARY.md` when done
</output>
