---
phase: 28-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/src/reference/adbc_poolhouse.md
autonomous: true
requirements: [DOCS-02]
must_haves:
  truths:
    - "The API reference renders AsyncPool, AsyncConnection, and AsyncCursor with their Google-style docstrings"
    - "The three async entry-point functions are documented in the reference (already rendered via the package page; reference page cross-links or confirms them)"
    - "The classes are documented at their real _async module paths, NOT promoted to __all__ or the PEP-562 lazy exports"
  artifacts:
    - path: "docs/src/reference/adbc_poolhouse.md"
      provides: "Explicit mkdocstrings blocks for the three async classes with a per-block filters: [\"!^__\"] override"
      contains: "adbc_poolhouse._async"
  key_links:
    - from: "docs/src/reference/adbc_poolhouse.md"
      to: "src/adbc_poolhouse/_async/_pool.py"
      via: "explicit ::: adbc_poolhouse._async._pool.AsyncPool block"
      pattern: "_async\\._pool\\.AsyncPool"
---

<objective>
Satisfy DOCS-02 by making `AsyncPool`, `AsyncConnection`, and `AsyncCursor` render in the API reference via explicit mkdocstrings blocks at their real `_async` module paths (D-28-02, option a), WITHOUT adding them to `__all__` or the PEP-562 lazy exports. The Google-style docstrings already exist on the classes (written during the Phase 24/25 docs gates), so the blocks render real content immediately.

Purpose: Users never construct these classes — they are returned objects from the entry-point functions, so they do not belong in the constructible public API; and the surface is experimental (D-28-01), so promoting them to `__all__` would harden them prematurely.

Output: An updated `docs/src/reference/adbc_poolhouse.md` carrying explicit per-class mkdocstrings blocks that resolve and render under `mkdocs build --strict`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@.claude/skills/adbc-poolhouse-docs-author/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-documentation/28-CONTEXT.md
@docs/src/reference/adbc_poolhouse.md
@src/adbc_poolhouse/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit async-class mkdocstrings blocks at their real _async paths</name>
  <read_first>
    docs/src/reference/adbc_poolhouse.md (the single `::: adbc_poolhouse` block this extends),
    .planning/phases/28-documentation/28-CONTEXT.md (D-28-02: option a, real paths, do NOT promote to __all__, fallback rule),
    mkdocs.yml (the mkdocstrings handler config — note the global `filters: ["!^_"]` under handlers.python.options),
    docs/scripts/gen_ref_pages.py (confirms auto-gen SKIPS any `_`-prefixed module part, so these classes will never auto-render — explicit blocks are required),
    src/adbc_poolhouse/_async/_pool.py (AsyncPool, line ~26),
    src/adbc_poolhouse/_async/_connection.py (AsyncConnection, line ~56),
    src/adbc_poolhouse/_async/_cursor.py (AsyncCursor, line ~77)
  </read_first>
  <files>docs/src/reference/adbc_poolhouse.md</files>
  <action>
    Append three explicit mkdocstrings blocks to `docs/src/reference/adbc_poolhouse.md` (below the existing `::: adbc_poolhouse` block), one per class, at their real module paths:
      - `::: adbc_poolhouse._async._pool.AsyncPool`
      - `::: adbc_poolhouse._async._connection.AsyncConnection`
      - `::: adbc_poolhouse._async._cursor.AsyncCursor`

    CRITICAL — the global mkdocstrings option `filters: ["!^_"]` (mkdocs.yml, handlers.python.options) excludes every underscore-prefixed identifier, which includes these `_async._*` module paths. Each explicit block MUST override that global filter locally. Use this single canonical form on every block: a per-block `options:` carrying `filters: ["!^__"]`. The per-block `options.filters` key REPLACES (does not merge with) the global `mkdocs.yml` filter for that block — so `["!^__"]` admits the single-underscore `_async._*` path that the global `["!^_"]` would otherwise strip, while still hiding dunder members (`__init__`, `__repr__`, etc.). Do NOT use `filters: []` — that also admits dunder members and is not the chosen form. Also set `show_root_heading: true` and `members_order: source` on each block to match the package page. Add a short prose heading above the blocks (e.g. `## Async API (experimental)`) noting these are returned objects, not constructible entry points (consistent with D-28-01) — keep it factual, no per-symbol caveat noise.

    Do NOT edit `src/adbc_poolhouse/__init__.py` — the classes stay out of `__all__` and the PEP-562 lazy `__getattr__` (D-28-02). This task only edits the reference markdown.

    FALLBACK (only if griffe cannot resolve the `_async.*` paths cleanly under `mkdocs build --strict` even with the `filters: ["!^__"]` override): create a dedicated reference page (e.g. `docs/src/reference/async.md`) that documents the classes, and wire it into the nav — but try the in-place explicit-block approach on `adbc_poolhouse.md` first. Record which path was taken in the SUMMARY.
  </action>
  <verify>
    <automated>grep -q 'adbc_poolhouse._async._pool.AsyncPool' docs/src/reference/adbc_poolhouse.md && grep -q 'adbc_poolhouse._async._connection.AsyncConnection' docs/src/reference/adbc_poolhouse.md && grep -q 'adbc_poolhouse._async._cursor.AsyncCursor' docs/src/reference/adbc_poolhouse.md && grep -q '"!\^__"' docs/src/reference/adbc_poolhouse.md && .venv/bin/mkdocs build --strict 2>&1 | tail -5 && test -f site/reference/adbc_poolhouse/index.html && grep -qi 'AsyncPool' site/reference/adbc_poolhouse/index.html && echo RENDER_OK</automated>
  </verify>
  <done>`mkdocs build --strict` exits 0, each explicit block uses the per-block `options: filters: ["!^__"]` override (not `filters: []`), the built reference page contains rendered `AsyncPool`/`AsyncConnection`/`AsyncCursor` symbols (not just the raw `:::` text), and `src/adbc_poolhouse/__init__.py` is byte-for-byte unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm the three async entry-point functions are documented in the reference</name>
  <read_first>
    src/adbc_poolhouse/__init__.py (the TYPE_CHECKING block + __all__ already include create_async_pool/managed_async_pool/close_async_pool, so the package `::: adbc_poolhouse` block renders them),
    docs/src/reference/adbc_poolhouse.md (the package block)
  </read_first>
  <files>docs/src/reference/adbc_poolhouse.md</files>
  <action>
    DOCS-02 also requires the three entry-point functions (`create_async_pool`, `managed_async_pool`, `close_async_pool`) be documented with Google-style docstrings (Args/Returns/Raises + Example). They are already in `__all__` and the `TYPE_CHECKING` block, so the existing `::: adbc_poolhouse` package block should render them via griffe. Verify they actually appear in the built reference page. If griffe does NOT pick them up through the lazy package (PEP-562 `__getattr__` can hide them from static collection), add explicit blocks for the three functions at their real path `adbc_poolhouse._async._factory.<fn>` using the same per-block `options: filters: ["!^__"]` override pattern as Task 1. Do not duplicate rendering if they already appear — verify first, add only if missing.
  </action>
  <verify>
    <automated>.venv/bin/mkdocs build --strict >/dev/null 2>&1 && grep -qi 'create_async_pool' site/reference/adbc_poolhouse/index.html && grep -qi 'managed_async_pool' site/reference/adbc_poolhouse/index.html && grep -qi 'close_async_pool' site/reference/adbc_poolhouse/index.html && echo ENTRYPOINTS_OK</automated>
  </verify>
  <done>The built reference page renders all three async entry-point functions with their docstrings; if explicit blocks were needed, they use the real `_async._factory` path with the `filters: ["!^__"]` override and __init__.py is still unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Documentation-only change: mkdocstrings wiring, no runtime code paths or inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02-T | Tampering | public API surface | mitigate | classes are documented at `_async` paths only and NOT added to `__all__`/PEP-562 exports — prevents accidentally hardening an experimental surface into the public contract (D-28-02) |
| T-28-02-I | Information Disclosure | rendered docstrings | accept | docstrings are factual API docs with no secrets; Example blocks (if any) use DuckDB without credentials |
</threat_model>

<verification>
- `.venv/bin/mkdocs build --strict` exits 0 with the explicit blocks present.
- Each explicit block carries a per-block `options: filters: ["!^__"]` override (admits the `_async._*` path, hides dunders).
- The built `site/reference/adbc_poolhouse/index.html` contains rendered `AsyncPool`, `AsyncConnection`, `AsyncCursor`, and the three entry-point functions.
- `src/adbc_poolhouse/__init__.py` is unchanged (classes not promoted to `__all__`).
</verification>

<success_criteria>
DOCS-02 satisfied: the API reference documents `AsyncPool`, `AsyncConnection`, `AsyncCursor`, and the three entry-point functions with their Google-style docstrings, rendered at the real `_async` paths via explicit mkdocstrings blocks (each using the per-block `filters: ["!^__"]` override), without promoting the classes to the public exports.
</success_criteria>

<artifacts_this_phase_produces>
- `docs/src/reference/adbc_poolhouse.md`: three new explicit mkdocstrings blocks (`AsyncPool`, `AsyncConnection`, `AsyncCursor`), each with a per-block `options: filters: ["!^__"]` override, plus an `## Async API (experimental)` heading; optional explicit entry-point blocks only if griffe misses them.
- (fallback only) `docs/src/reference/async.md` + nav wiring, if the in-place approach cannot resolve under strict build.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/28-documentation/28-02-SUMMARY.md` when done
</output>
</content>
</invoke>
