---
phase: 22-feasibility-spike
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - .planning/phases/22-feasibility-spike/22-GO-NO-GO.md
autonomous: true
requirements: [SPIKE-03]
must_haves:
  truths:
    - "A written go/no-go document records the verdict (GO with a named materialization caveat) and gates Phase 24"
    - "The doc records BOTH measured speedup ratios (execute vs fetch_arrow_table) from a real local run, with N, dataset size, single/wall/ideal/serial, and median-of-trials methodology"
    - "The doc states the Claim (GIL released during execute → real parallelism, inferred I/O concurrency) and the Disclaim (large fetch_arrow_table materialization is GIL-bound, does not parallelize)"
    - "The doc names the inference gap explicitly: in-proc DuckDB has no network wait, so the spike proves GIL release / CPU parallelism but INFERS I/O concurrency — it does not directly measure it"
    - "The doc gives Phase 24 offload-granularity guidance (offload at whole-operation granularity; do not split execute from fetch hoping to overlap)"
    - "The benchmarks/ directory is verified to be excluded from the built wheel"
  artifacts:
    - path: ".planning/phases/22-feasibility-spike/22-GO-NO-GO.md"
      provides: "SPIKE-03 go/no-go deliverable covering the 8-point Go/No-Go Document Contract"
      min_lines: 60
      contains: "GO"
  key_links:
    - from: ".planning/phases/22-feasibility-spike/22-GO-NO-GO.md"
      to: "benchmarks/gil_release.py"
      via: "transcribes the measured medians produced by plan 01"
      pattern: "speedup|execute|fetch_arrow_table"
    - from: ".planning/phases/22-feasibility-spike/22-GO-NO-GO.md"
      to: "Phase 24"
      via: "offload-granularity guidance + explicit gate"
      pattern: "Phase 24|offload"
---

<objective>
Write the SPIKE-03 go/no-go document that interprets the two measured speedup ratios from plan 01, states what the async layer may honestly claim vs must disclaim, names the parallelism-vs-I/O-concurrency inference gap, and gives Phase 24 offload-granularity guidance — explicitly gating Phase 24. Also verify that the kept `benchmarks/` directory stays out of the built wheel.

Purpose: This is the milestone gate. Before any async production code is written (Phases 23–28), the go/no-go fixes exactly what concurrency claim the async layer is allowed to make, so the docs (Phase 28) and the offload design (Phase 24) are honest from the start. Terminology is load-bearing: PARALLELISM (threads doing C-side work because the GIL is released) is distinct from I/O CONCURRENCY (overlapping network waits — the inferred real-world payoff).

Output: `.planning/phases/22-feasibility-spike/22-GO-NO-GO.md` (planning prose artifact, NOT user-facing `docs/src/` — Phase 28 owns the user guide).

Scope guard (LOCKED, from 22-CONTEXT.md): the doc MUST use the parallelism / I/O-concurrency terminology precisely and MUST NOT dress up "GIL released → threads parallelize a heavy query" as "we proved I/O concurrency." It must name the inference gap, the measured `fetch_arrow_table` materialization caveat, and offload-granularity guidance for Phase 24. Do NOT treat the fetch serialization number as a NO-GO (22-RESEARCH Pitfall 5) — it is a documented limit, not a blocker.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@.claude/skills/adbc-poolhouse-docs-author/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-feasibility-spike/22-CONTEXT.md
@.planning/phases/22-feasibility-spike/22-RESEARCH.md
@.planning/phases/22-feasibility-spike/22-01-SUMMARY.md   # plan 01's recorded measured medians — the numbers to transcribe
@benchmarks/gil_release.py                                # re-run source for the medians if needed
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify benchmarks/ is excluded from the built wheel</name>
  <files>pyproject.toml (verify-only - edit only if a packaging glob leaks benchmarks/)</files>
  <read_first>
    - 22-RESEARCH.md Runtime State Inventory (lines 271-281) + Assumption A2 (line 394): `benchmarks/` lives outside `src/`, so `uv_build` excludes it from the wheel by default — confirm with a one-line build check.
    - 22-PATTERNS.md pyproject.toml section (lines 128-130): verify-only, no edit expected; flag if any packaging glob would pick up `benchmarks/`.
    - pyproject.toml `[build-system]` (lines 40-42): `uv_build` backend.
  </read_first>
  <action>
    Build the wheel and confirm `benchmarks/` is NOT inside it. Run `.venv/bin/python -m build --wheel --outdir "$TMPDIR/wheel22"` (or `uv build --wheel`), then inspect the wheel contents (e.g. `python -c "import zipfile,glob; z=zipfile.ZipFile(glob.glob('$TMPDIR/wheel22/*.whl')[0]); print([n for n in z.namelist() if 'benchmark' in n.lower()])"`) and assert the list is EMPTY. This is verify-only — do NOT edit pyproject.toml unless the wheel actually contains `benchmarks/` (it should not, being outside `src/`). If (and only if) a packaging glob picks it up, add the minimal exclusion and re-verify. Record the result (wheel name + empty benchmarks match) for the SUMMARY.
  </action>
  <verify>
    <automated>.venv/bin/python -m build --wheel --outdir "$TMPDIR/wheel22" >/dev/null 2>&1 && .venv/bin/python -c "import zipfile,glob; z=zipfile.ZipFile(sorted(glob.glob('$TMPDIR/wheel22/*.whl'))[-1]); hits=[n for n in z.namelist() if 'benchmark' in n.lower()]; assert hits==[], hits; print('wheel clean of benchmarks/')"</automated>
  </verify>
  <done>The built wheel contains no `benchmarks/` entry; no pyproject.toml edit was required (or, if it was, the minimal exclusion is in place and re-verified).</done>
  <acceptance_criteria>
    - Wheel built successfully; `benchmark`-matching namelist entries == `[]`.
    - pyproject.toml unchanged unless a glob actually leaked `benchmarks/` into the wheel.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Write 22-GO-NO-GO.md (SPIKE-03) per the 8-point Go/No-Go Document Contract</name>
  <files>.planning/phases/22-feasibility-spike/22-GO-NO-GO.md</files>
  <read_first>
    - 22-RESEARCH.md "Go/No-Go Document Contract" (lines 367-378): the 8-point checklist the doc MUST satisfy — (1) verdict line, (2) both measured ratios with methodology, (3) Claim, (4) Disclaim, (5) inference gap named, (6) Phase 24 offload-granularity guidance, (7) Phase 28 doc-honesty handoff (DOCS-01), (8) optional free-threaded note.
    - 22-CONTEXT.md `<decisions>` Terminology framing (lines 28-33): the LOCKED parallelism vs I/O-concurrency distinction the prose must use precisely.
    - 22-01-SUMMARY.md: the actual measured medians from plan 01's local run (N, rows, trials, single/wall/ideal/serial/speedup_x for execute and fetch). Transcribe THESE numbers — do not invent. If the SUMMARY lacks them, re-run `.venv/bin/python -m benchmarks.gil_release --measure both` and record what it prints.
    - 22-RESEARCH.md Pitfall 5 (lines 309-313): do NOT frame the fetch serialization as a NO-GO; it is a named caveat + offload guidance, not a milestone blocker.
    - .claude/skills/adbc-poolhouse-docs-author/SKILL.md: voice (direct, second person, practical), and apply a humanizer pass to the prose.
  </read_first>
  <action>
    Write `.planning/phases/22-feasibility-spike/22-GO-NO-GO.md` covering all 8 points of the Go/No-Go Document Contract (22-RESEARCH lines 367-378):
    1. **Verdict line:** GO / NO-GO in one sentence. Expected: GO with a named materialization caveat.
    2. **The two measured speedup ratios** (execute vs `fetch_arrow_table`), each with N, dataset size, single-call / wall / ideal-parallel / full-serial times, and the median-of-trials methodology stated. Transcribe the real numbers from 22-01-SUMMARY.md (or a fresh local run) — these are the empirical record; do not re-assert them in CI.
    3. **Claim:** the ADBC C path releases the GIL during `execute`, so thread-offload yields real PARALLELISM of C-side work and — by inference — real I/O CONCURRENCY on network backends (overlapping query waits).
    4. **Disclaim:** NOT blanket parallelism. Large `fetch_arrow_table` materialization is GIL-bound and does NOT parallelize across concurrent fetches; concurrent big-result fetches approach serialization.
    5. **The inference gap, named explicitly:** in-proc DuckDB has no network wait, so this spike proves GIL release / CPU parallelism but INFERS I/O concurrency; it does not directly measure it. Phase 27's dual-backend matrix exercises real-backend behaviour.
    6. **Offload-granularity guidance for Phase 24:** offload at WHOLE-OPERATION granularity (one `to_thread` per `execute`, one per `fetch_arrow_table`); do NOT split a single query's execute and fetch hoping for intra-operation overlap — fetch serializes regardless. The dedicated `CapacityLimiter(pool_size + max_overflow)` governs cross-query concurrency, where the I/O-bound win is real.
    7. **Doc-honesty handoff to Phase 28 (DOCS-01):** the async guide must state async wins are largest for latency/I/O-bound queries and smaller for CPU-bound large-result materialization.
    8. **(Optional) Free-threaded note:** measured on the standard GIL build only; no-GIL behaviour is out of scope and unmeasured.

    Add an explicit "Gates Phase 24" statement (the doc IS the milestone gate). Use the LOCKED terminology precisely throughout — PARALLELISM (real simultaneous CPU work via GIL-released C threads) vs I/O CONCURRENCY (overlapping network waits) — and do NOT conflate them or claim the spike proved I/O concurrency. Apply the docs-author voice and a humanizer pass to all prose; Markdown (no RST `:role:`).
  </action>
  <verify>
    <automated>test -f .planning/phases/22-feasibility-spike/22-GO-NO-GO.md && grep -qiE 'parallelism' .planning/phases/22-feasibility-spike/22-GO-NO-GO.md && grep -qiE 'i/o concurrency|i\/o-concurrency|io concurrency' .planning/phases/22-feasibility-spike/22-GO-NO-GO.md && grep -qiE 'infer' .planning/phases/22-feasibility-spike/22-GO-NO-GO.md && grep -qiE 'fetch_arrow_table' .planning/phases/22-feasibility-spike/22-GO-NO-GO.md && grep -qiE 'phase 24' .planning/phases/22-feasibility-spike/22-GO-NO-GO.md && echo "GO-NO-GO contract markers present"</automated>
  </verify>
  <done>`22-GO-NO-GO.md` exists, covers all 8 contract points, records the real measured ratios, uses the parallelism/I-O-concurrency terminology precisely, names the inference gap, gives Phase 24 offload guidance, and explicitly gates Phase 24.</done>
  <acceptance_criteria>
    - All 8 Go/No-Go Document Contract points present (verdict, both ratios + methodology, claim, disclaim, inference gap, Phase 24 offload guidance, Phase 28 handoff, free-threaded note).
    - Verdict is GO with a named materialization caveat (fetch serialization is a documented limit, NOT a NO-GO).
    - The two measured ratios are the real numbers from plan 01 (transcribed, not invented), with N/size/methodology.
    - Terminology: "parallelism" and "I/O concurrency" used distinctly; inference gap named; no claim that the spike directly measured I/O concurrency.
    - Explicit "gates Phase 24" statement; humanizer pass applied; Markdown, no RST.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none meaningful) | This plan writes one local markdown planning artifact and runs a local `uv build` to inspect wheel contents. No network, no untrusted input, no auth, no persistence, no credentials. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-02 | Tampering | `benchmarks/` in built wheel | mitigate | Task 1 builds the wheel and asserts `benchmarks/` is absent, preventing spike code from shipping to users. This is the active mitigation of the boundary flagged in plan 01. |
| T-22-03 | Information Disclosure | `22-GO-NO-GO.md` prose | accept | Records benchmark medians and architectural reasoning only — no secrets, credentials, or PII. Lives under `.planning/` (already in-repo). No mitigation warranted. |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | NO packages installed this phase. `python -m build` uses the already-present `uv_build` backend; no new dependency added. No legitimacy gate applies. |

No HIGH threats: no untrusted input, no network, no auth, no new dependencies. Threats enumerated honestly.
</threat_model>

<verification>
- `.venv/bin/python -m build --wheel ...` + namelist inspection — wheel contains no `benchmarks/` entry (Task 1).
- `test -f 22-GO-NO-GO.md && grep ...` — the doc exists and contains the contract markers (parallelism, I/O concurrency, infer, fetch_arrow_table, Phase 24) (Task 2).
- Manual review (per 22-VALIDATION.md Manual-Only Verifications): the go/no-go verdict honesty — parallelism vs inferred I/O concurrency, named materialization caveat, all 8 contract points — reviewed against 22-CONTEXT.md terminology constraints. Prose judgment, not machine-checkable.
</verification>

<success_criteria>
- `22-GO-NO-GO.md` is a GO-with-named-caveat verdict that records the two real measured ratios, states the claim/disclaim split, names the parallelism-vs-I/O-concurrency inference gap, gives Phase 24 whole-operation offload guidance, hands off doc-honesty to Phase 28, and explicitly gates Phase 24.
- The built wheel is confirmed free of `benchmarks/` (kept spike code never ships to users).
- No async/production code; the doc is a planning artifact, not `docs/src/` content.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (plan 02)

| Artifact | Symbols / Contents | Requirement |
|----------|--------------------|-------------|
| `.planning/phases/22-feasibility-spike/22-GO-NO-GO.md` | SPIKE-03 go/no-go: verdict, two measured ratios + methodology, claim, disclaim, inference gap, Phase 24 offload guidance, Phase 28 handoff, free-threaded note, explicit Phase 24 gate | SPIKE-03 |
| (verification) wheel-exclusion check | confirms `benchmarks/` absent from built wheel (no artifact, verify-only) | SPIKE-01/02 packaging hygiene |
</artifacts_produced>

<output>
Create `.planning/phases/22-feasibility-spike/22-02-SUMMARY.md` when done. Record the final verdict, the transcribed measured ratios, and the wheel-exclusion confirmation.
</output>
