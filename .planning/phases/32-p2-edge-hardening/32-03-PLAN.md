---
phase: 32-p2-edge-hardening
plan: 03
type: execute
wave: 2
depends_on: ["32-01", "32-02"]
files_modified:
  - tests/async/test_edge_checkpoint.py
  - tests/async/test_edge_contextvars.py
  - tests/async/test_edge_timeout_precision.py
  - tests/async/test_edge_shutdown.py
autonomous: false
requirements: [EDGE-08, EDGE-13, EDGE-14, EDGE-24, EDGE-31, EDGE-32]
must_haves:
  truths:
    - "All six EDGE requirements (08/13/14/24/31/32) are green under both asyncio and trio across the full async suite at ADBC_ASYNC_REPEAT=20 (0 hangs)"
    - "The full async suite (all prior tests + the four new modules) still passes — no regression"
    - "The AST import-lint guard still passes over _async/ (no import asyncio, no bare to_thread) and basedpyright strict is 0 errors"
    - "The docs gate is satisfied: .venv/bin/mkdocs build --strict passes; any contingency production symbol has Google-style docstrings"
    - "If a contingency fired, a minimal production fix at the shared offload chokepoint landed IN this phase (not deferred) and its threat register was re-verified"
  artifacts:
    - path: "tests/async/test_edge_checkpoint.py"
      provides: "EDGE-08 (confirmed green in the full-suite gate)"
    - path: "tests/async/test_edge_timeout_precision.py"
      provides: "EDGE-31/32 (confirmed green in the x20 full-suite gate)"
    - path: "tests/async/test_edge_shutdown.py"
      provides: "EDGE-24 (confirmed green in the x20 full-suite gate)"
  key_links:
    - from: "the four new test modules"
      to: "the full async suite"
      via: "ADBC_ASYNC_REPEAT=20 .venv/bin/pytest tests/async/ -q"
      pattern: "test_edge_(checkpoint|contextvars|timeout_precision|shutdown)"
---

<objective>
Close Phase 32: run the six new-path EDGE requirements through the full-suite x20 loop gate, resolve the DISCOVERED-NECESSITY CONTINGENCY (production fix IN-phase only if a RED cannot be made green test-only), confirm the import-lint + basedpyright-strict gates, apply the docs gate, and require the Linux-CI x20 confirmation before verify.

Purpose: The per-module gates in Waves 1 land each requirement RED-first then green in isolation. This gate proves they hold TOGETHER against the full async suite (no regression, no cross-test interaction) and enforces the two contingency-and-quality obligations from the locked decisions: (a) if any timing/shutdown leg is genuinely RED, the minimal production fix lands here — escalated via a blocking checkpoint, never invented; (b) the docs/type/import gates that apply to every phase ≥ 7 are green.

Output: A green full async suite under the x20 loop, a satisfied docs gate, and (only if triggered) a minimal in-phase production fix with full docstrings.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@.claude/skills/adbc-poolhouse-docs-author/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-p2-edge-hardening/32-RESEARCH.md
@.planning/phases/32-p2-edge-hardening/32-VALIDATION.md
@.planning/phases/32-p2-edge-hardening/32-01-SUMMARY.md
@.planning/phases/32-p2-edge-hardening/32-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite x20 loop gate + import-lint + basedpyright-strict</name>
  <files>tests/async/test_edge_checkpoint.py, tests/async/test_edge_contextvars.py, tests/async/test_edge_timeout_precision.py, tests/async/test_edge_shutdown.py</files>
  <read_first>
    - .planning/phases/32-p2-edge-hardening/32-01-SUMMARY.md (what Wave 1 landed; any contingency notes)
    - .planning/phases/32-p2-edge-hardening/32-02-SUMMARY.md (timing/shutdown results; any RED escalations)
    - .planning/phases/32-p2-edge-hardening/32-VALIDATION.md (the phase-gate commands + Manual-Only Linux-CI row)
    - MEMORY: feedback_zsh_bang_breaks_loop_verification (use `rc=$?` + grep for the pass line; do NOT rely on `if ! cmd` in a for-loop)
  </read_first>
  <action>
    Run the full async suite under the x20 loop and the static gates. Because the Bash tool runs zsh (MEMORY zsh-bang gotcha), capture the exit code explicitly with `rc=$?` and grep the pytest summary line for the pass count rather than relying on `if ! cmd` inside any loop.
    1. Full suite loop gate: `ADBC_ASYNC_REPEAT=20 .venv/bin/pytest tests/async/ -q` — assert exit 0 and a summary line showing the new modules' tests among the passes (no failures, no hangs — pytest-timeout would hard-fail a genuine hang).
    2. Import-lint guard: run the existing AST import-lint over `_async/` (per PKG-03; the guard command lives in the test suite / a lint task — reuse the same invocation prior phases used) and assert it passes (no `import asyncio`, no bare `to_thread`). If no production file changed this phase, this is a re-confirmation.
    3. basedpyright strict: `.venv/bin/basedpyright` over the four new test modules (and any contingency-touched production file) — assert 0 errors. Trust `.venv/bin/basedpyright`, NOT harness Pyright (MEMORY IDE-diagnostics false-positives).
    Do NOT modify test bodies here except to fix a genuine cross-test interaction surfaced only in the full-suite run (e.g. a leaked contextvar or an unreleased worker) — such a fix stays test-only unless it reveals a production gap, which routes to Task 2.
  </action>
  <verify>
    <automated>ADBC_ASYNC_REPEAT=20 .venv/bin/pytest tests/async/ -q; rc=$?; test $rc -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `ADBC_ASYNC_REPEAT=20 .venv/bin/pytest tests/async/ -q` exits 0 with the four new modules' tests passing across all 20 iterations under both asyncio and trio (0 hangs)
    - The AST import-lint guard passes over `_async/` (PKG-03 re-confirmed: no `import asyncio`, no bare `to_thread`)
    - `.venv/bin/basedpyright` reports 0 errors over the four new modules (+ any contingency file)
    - No prior async test regressed (the full-suite pass count is ≥ the pre-phase count plus the new tests)
  </acceptance_criteria>
  <done>All six EDGE requirements are green together in the full async suite under x20; static gates pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: DISCOVERED-NECESSITY CONTINGENCY resolution + Linux-CI x20 confirmation</name>
  <files>.planning/phases/32-p2-edge-hardening/32-01-SUMMARY.md, .planning/phases/32-p2-edge-hardening/32-02-SUMMARY.md</files>
  <action>
    Blocking human-verify checkpoint (autonomous:false). Pause for the user to confirm two things the automated gates cannot decide, then present the Waves-1 SUMMARYs and the Linux-CI result and wait for "approved". Do NOT auto-advance even under `workflow.auto_advance` — a platform-dependent Linux-only hang and a possible in-phase production fix both require human judgment. The two confirmations map to the `<what-built>` / `<how-to-verify>` detail below.
  </action>
  <what-built>
    Wave 1 landed the six EDGE tests RED-first; Task 1 proved them green together on this machine (macOS). Two things require human confirmation before the phase closes:

    (a) CONTINGENCY STATUS: per locked decision #3, if any EDGE-08/24/31/32 leg could not be made green by test-only means, the minimal production fix from RESEARCH §Discovered-Necessity (at the shared `cancellable_offload`/`offload` chokepoint — e.g. re-wire a mis-routed dispatch, or guard the on_dispatch/watcher bridge at shutdown) must land IN this phase, NOT be deferred. The SUMMARYs from Waves 1 record whether any RED required this. Default expectation per RESEARCH A1: NO production change (all four new methods share the proven chokepoint). If a fix DID land, its new/changed public symbol needs full Google-style docstrings (Args/Returns/Raises + Example) and any consumer-facing behaviour reflected in the relevant guide (CLAUDE.md docs gate), and the Plan 02 threat register must be re-verified against the changed dispatch.

    (b) LINUX-CI x20 GATE: macOS passing 20/20 is NOT authoritative — cancel/lost-wakeup races can pass on macOS and hang on Linux (MEMORY platform-dependent-lost-wakeup). The real phase gate for EDGE-24/31/32 is the async job's `ADBC_ASYNC_REPEAT=20` loop green on Linux CI (0 hangs).
  </what-built>
  <how-to-verify>
    1. Read `32-01-SUMMARY.md` and `32-02-SUMMARY.md`. Confirm whether any EDGE leg required a production fix (contingency fired). If YES: confirm the fix is minimal (a chokepoint re-wire/guard, not new machinery), matches a RESEARCH §Discovered-Necessity row, has full docstrings, and the guide is updated; approve only then. If NO: confirm the SUMMARYs explicitly state "no production change" — this is the expected path.
    2. Push the branch and confirm the CI async job's `ADBC_ASYNC_REPEAT=20` loop is GREEN on Linux (0 hangs) for the four new modules. This is the authoritative gate for EDGE-24/31/32.
    3. Confirm no `anyio.fail_after`-as-watchdog and no wedged-worker race slipped into any new module (spot-check the four files).
    Type "approved" once (a) the contingency is correctly resolved (or confirmed not needed) and (b) the Linux-CI x20 loop is green. Otherwise describe what must change.
  </how-to-verify>
  <verify>
    <human-check>Contingency resolved (or confirmed not needed) AND the Linux-CI ADBC_ASYNC_REPEAT=20 loop is green (0 hangs) for the four new modules.</human-check>
  </verify>
  <resume-signal>Type "approved" or describe the contingency/CI gap to fix.</resume-signal>
  <done>The contingency is resolved per locked decision #3 (default: no production change) and the authoritative Linux-CI x20 gate is green.</done>
</task>

<task type="auto">
  <name>Task 3: Docs gate — strict build + docstrings + humanizer (only where prose changed)</name>
  <files>tests/async/test_edge_checkpoint.py, tests/async/test_edge_contextvars.py, tests/async/test_edge_timeout_precision.py, tests/async/test_edge_shutdown.py</files>
  <read_first>
    - .claude/skills/adbc-poolhouse-docs-author/SKILL.md (docstring style, humanizer pass, strict-build discipline)
    - MEMORY: docstring style (Google-style, Markdown not RST; `Example:` singular in docstrings); uv-sandbox workaround (`.venv/bin/mkdocs` over `uv run mkdocs`)
    - CLAUDE.md Documentation Quality Gate
  </read_first>
  <action>
    Apply the reduced docs gate for this test-only phase (per planning context: no new public API symbols unless a contingency fired):
    1. Strict build must still pass: `DISABLE_MKDOCS_2_WARNING=true .venv/bin/mkdocs build --strict` — assert exit 0 (do NOT grep; use the exit code, MEMORY strict-build gate).
    2. Any NEW test helper/probe symbol added in Waves 1 (e.g. the contextvar probe, `_drain_one`) that carries a docstring must use Google-style (Args/Returns/Raises) + Markdown (NOT RST `:role:` syntax). Test-internal helpers do not need mkdocstrings rendering, but keep the house style.
    3. IF the contingency fired and a production symbol changed: ensure it has full Google-style docstrings with an `Example:` block and that the relevant guide reflects the changed consumer-facing behaviour; apply the humanizer pass to that new/rewritten prose. IF no production change (expected), no guide edit is required and the humanizer pass is limited to any substantially-rewritten docstring prose.
  </action>
  <verify>
    <automated>DISABLE_MKDOCS_2_WARNING=true .venv/bin/mkdocs build --strict; rc=$?; test $rc -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `DISABLE_MKDOCS_2_WARNING=true .venv/bin/mkdocs build --strict` exits 0
    - Any docstring added this phase is Google-style + Markdown (no RST `:func:`/`:class:`/`:meth:` roles)
    - IF a contingency production symbol landed: it has Args/Returns/Raises + `Example:`, the relevant guide reflects the behaviour change, and the humanizer pass was applied to new/rewritten prose
    - IF no production change: SUMMARY records "docs gate reduced to strict-build + docstring-style, no guide change (test-only phase)"
  </acceptance_criteria>
  <done>The strict docs build passes and the docstring/humanizer obligations are satisfied at the level this test-only phase requires.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan produces no NEW files (it gates and, only under the contingency, may touch one production file). It confirms the four test modules from Waves 1 hold together:
- `tests/async/test_edge_checkpoint.py`, `tests/async/test_edge_contextvars.py`, `tests/async/test_edge_timeout_precision.py`, `tests/async/test_edge_shutdown.py` — all green in the full-suite x20 gate.
CONTINGENCY-ONLY (default: does NOT fire): a minimal production edit at the shared `cancellable_offload`/`offload` chokepoint per a RESEARCH §Discovered-Necessity row, with full Google-style docstrings + guide reflection. No new public symbol is expected.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| full-suite integration | Cross-test interaction boundary: the six new EDGE assertions must hold together with the whole async suite (no leaked contextvar/worker across tests). No untrusted input; test-only. |
| Linux CI (platform boundary) | The authoritative gate for cancel/lost-wakeup races; macOS-green is not sufficient (MEMORY platform-dependent-lost-wakeup). |

Per RESEARCH §Security Domain this is a test-only phase with no new attack surface. This plan VERIFIES the three robustness properties (cancellation not swallowed → no DoS/leak; no dead-loop callback at shutdown; no contextvar bleed) hold together and on Linux.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-06 | Denial of Service | platform-dependent lost-wakeup passing on macOS, hanging on Linux (EDGE-24/31/32) | mitigate | Task 2 requires the Linux-CI `ADBC_ASYNC_REPEAT=20` loop green (0 hangs) as the authoritative gate; macOS-green is explicitly not accepted. |
| T-32-07 | Tampering | contingency production fix at the shared chokepoint regressing other paths | mitigate | Task 2 blocking checkpoint requires the fix be minimal + match a RESEARCH §Discovered-Necessity row; the full-suite x20 gate (Task 1) proves `execute`/`fetch_arrow_table` paths did not regress; Plan 02 threat register re-verified. |
| T-32-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH §Package Legitimacy Audit: not applicable — zero new packages). No `[ASSUMED]`/`[SUS]`/`[SLOP]` surface. |

No HIGH-severity unmitigated threats. Because a contingency production fix could in principle be introduced here, T-32-07 is `mitigate` (blocking checkpoint + full-suite regression gate) rather than `accept`.
</threat_model>

<verification>
- `ADBC_ASYNC_REPEAT=20 .venv/bin/pytest tests/async/ -q` — full suite green, x20, both backends, on macOS (Task 1) AND on Linux CI (Task 2, authoritative).
- `.venv/bin/basedpyright` — 0 errors; AST import-lint over `_async/` passes (PKG-03 re-confirmed).
- `DISABLE_MKDOCS_2_WARNING=true .venv/bin/mkdocs build --strict` — exit 0.
- Blocking checkpoint (Task 2) resolves the contingency and confirms the Linux-CI gate before the phase is marked complete.
</verification>

<success_criteria>
- All six EDGE requirements (08/13/14/24/31/32) green together in the full async suite under x20, on macOS and on Linux CI.
- No regression; import-lint + basedpyright-strict green.
- Contingency correctly resolved (default: not needed; SUMMARY states "no production change") or a minimal in-phase fix landed with full docstrings + guide reflection.
- `mkdocs build --strict` green; docstring/humanizer obligations met at the test-only level.
</success_criteria>

<output>
Create `.planning/phases/32-p2-edge-hardening/32-03-SUMMARY.md` when done.
</output>
