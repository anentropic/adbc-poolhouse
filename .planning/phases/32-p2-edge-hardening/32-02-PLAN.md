---
phase: 32-p2-edge-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/async/test_edge_timeout_precision.py
  - tests/async/test_edge_shutdown.py
autonomous: true
requirements: [EDGE-31, EDGE-32, EDGE-24]
must_haves:
  truths:
    - "move_on_after(0) still cancels a blocked streaming pull AND a blocked ingest cleanly: adbc_cancel fires once, connection invalidates (EDGE-31), both backends"
    - "An op completing at deadline−ε (released by a real side thread) is NOT over-cancelled: no adbc_cancel, no invalidate, clean return — streaming pull + ingest (EDGE-32), both backends"
    - "A pending offload at loop shutdown — mid-stream and mid-ingest — raises no library-attributable exception (no 'was never awaited', no 'Task was destroyed'); trio nursery strictness is the canary (EDGE-24)"
    - "A real duckdb_async_pool with a drained-but-open connection closes at teardown raising nothing (EDGE-24 real leg)"
  artifacts:
    - path: "tests/async/test_edge_timeout_precision.py"
      provides: "EDGE-31 move_on_after(0) + EDGE-32 deadline−ε on streaming pull + ingest"
      contains: "move_on_after"
      min_lines: 60
    - path: "tests/async/test_edge_shutdown.py"
      provides: "EDGE-24 loop-shutdown cleanliness, mid-stream + mid-ingest + real drained-close"
      contains: "catch_warnings"
      min_lines: 50
  key_links:
    - from: "tests/async/test_edge_timeout_precision.py"
      to: "tests/_async_harness/clock.py"
      via: "virtual_clock as the deadline trigger; real_clock_watchdog as the guard"
      pattern: "virtual_clock"
    - from: "tests/async/test_edge_timeout_precision.py"
      to: "tests/_async_harness/stubs.py"
      via: "record_batch_batches armed before fetch_record_batch (Pitfall 5); adbc_cancel_call_count / invalidate_call_count"
      pattern: "record_batch_batches"
    - from: "tests/async/test_edge_shutdown.py"
      to: "tests/_async_harness/stubs.py"
      via: "release-in-teardown so the abandon_on_cancel=False worker joins (Pitfall 3)"
      pattern: "\\.release\\(\\)"
---

<objective>
Land the RED-first deterministic tests for the timing-precision and loop-shutdown edge cases on the new paths: EDGE-31 (`move_on_after(0)` still cancels a blocked pull/ingest), EDGE-32 (deadline−ε op is not over-cancelled), and EDGE-24 (pending offload at loop shutdown raises nothing) — extended to mid-stream and mid-ingest, run under both asyncio and trio, and looped x20 on the concurrency legs.

Purpose: These are proven for the FIRST time on the new-method paths (RESEARCH: these are the only legs that could surface a real gap). All four new methods reuse the SAME `cancellable_offload` / `offload` machinery already proven for `execute`, so no production change is expected — but per the locked DISCOVERED-NECESSITY CONTINGENCY, if a RED test cannot be made green test-only the minimal production fix lands IN Phase 32 (handled at the Wave 2 gate, Plan 03). This plan's job is to write the tests so a genuine gap surfaces RED before any GREEN claim.

Output: Two new test modules under `tests/async/`, gated with `concurrency_marks`, using `virtual_clock` as the deadline trigger and `real_clock_watchdog` (never `anyio.fail_after`) as the hang guard.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@.claude/skills/adbc-poolhouse-docs-author/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-p2-edge-hardening/32-RESEARCH.md
@.planning/phases/32-p2-edge-hardening/32-PATTERNS.md
@.planning/phases/32-p2-edge-hardening/32-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: EDGE-31/32 — timeout precision on a blocked new-method op (streaming pull + ingest)</name>
  <files>tests/async/test_edge_timeout_precision.py</files>
  <read_first>
    - tests/async/test_reader_cancel.py (lines 41-47 import block; 93-127 `test_timeout_during_pull_aborts_and_invalidates` — the streaming-pull EDGE-31 analog; 167-171 `_pull_started`; 133-159 real-pool checkedout discipline)
    - tests/async/test_ingest_cancel.py (lines 44-51 import block; 96-127 `test_timeout_during_ingest_aborts_and_invalidates`; 163-165 `_drive_ingest`)
    - tests/async/test_edge_cancel_depth.py (lines 73-84 `_release_when_entered` — copy VERBATIM for EDGE-32; 319-351 `test_move_on_after_on_finished_op_is_noop` — the EDGE-32 analog)
    - tests/_async_harness/clock.py (virtual_clock — the deadline trigger)
    - tests/_async_harness/stubs.py (record_batch_batches default ~line 504; BlockingStubReader read_call_count ~755; adbc_cancel_call_count; invalidate_call_count)
    - 32-PATTERNS.md §test_edge_timeout_precision.py (the two skeletons)
  </read_first>
  <action>
    Create `tests/async/test_edge_timeout_precision.py` with the Shared Pattern A import block, `pytestmark = concurrency_marks`, and `virtual_clock` imported as the deadline trigger. Copy `_release_when_entered` VERBATIM from `test_edge_cancel_depth.py:73-84`.

    EDGE-31 (`move_on_after_zero` in the test name for `-k` selection) — two legs, streaming pull + ingest:
    - Streaming leg: arm `stub_conn.cursors[-1].record_batch_batches = [object()]` BEFORE `await cur.fetch_record_batch()` (Pitfall 5 — otherwise the pull never blocks). Enter `real_clock_watchdog(stub_conn.cursors)` and `virtual_clock(anyio_backend_name)`, then `with anyio.move_on_after(0) as scope: await reader.__anext__()`. Assert `tripped[0] is False`, `scope.cancelled_caught is True`, `sc.adbc_cancel_call_count == 1`, `stub_conn.invalidate_call_count == 1`.
    - Ingest leg: same guards, drive the blocked ingest inside an `anyio.create_task_group()` (clone `_drive_ingest` from `test_ingest_cancel.py:163-165`) with `move_on_after(0)`; assert the same four (`adbc_cancel_call_count == 1`, `invalidate_call_count == 1`).

    EDGE-32 (`deadline_epsilon` in the test name) — two legs, streaming pull + ingest:
    - The releaser MUST be a REAL side thread: `threading.Thread(target=_release_when_entered, args=(sc,), daemon=True)` started before the gated op, so the op COMPLETES before a large virtual deadline fires (Pitfall 2 — a loop-side releaser is starved under MockClock). Wrap the op in `anyio.move_on_after(5)` under `virtual_clock`; for the streaming leg arm one batch so the first pull enters the block then the releaser unblocks it. Assert `scope.cancelled_caught is False`, `sc.adbc_cancel_call_count == 0`, `stub_conn.invalidate_call_count == 0`, and `tripped[0] is False`. `join()` the releaser thread at the end.

    Use `await_inside` (not the stub's `threading.Event` on the loop — Pitfall 4) where a "worker is inside" gate is needed. Give each of the four tests a distinct `-k`-selectable name so `-k move_on_after_zero` and `-k deadline_epsilon` each select both their legs (VALIDATION commands).
  </action>
  <verify>
    <automated>ADBC_ASYNC_REPEAT=20 .venv/bin/pytest tests/async/test_edge_timeout_precision.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `ADBC_ASYNC_REPEAT=20 .venv/bin/pytest tests/async/test_edge_timeout_precision.py -k move_on_after_zero` exits 0 (EDGE-31, both backends × pull+ingest, 20 iterations, 0 hangs): `adbc_cancel_call_count == 1`, `invalidate_call_count == 1`, `cancelled_caught is True`
    - `ADBC_ASYNC_REPEAT=20 .venv/bin/pytest tests/async/test_edge_timeout_precision.py -k deadline_epsilon` exits 0 (EDGE-32, both backends × pull+ingest): `cancelled_caught is False`, `adbc_cancel_call_count == 0`, `invalidate_call_count == 0`
    - Every gated leg asserts `tripped[0] is False` (real_clock_watchdog did not fire)
    - `virtual_clock` is the deadline trigger; `real_clock_watchdog` is the guard; `anyio.fail_after` is NOT used as a watchdog; `_release_when_entered` runs on a real thread (EDGE-32)
    - `record_batch_batches = [object()]` armed before `fetch_record_batch()` on every streaming leg (Pitfall 5)
    - basedpyright clean: `.venv/bin/basedpyright tests/async/test_edge_timeout_precision.py` reports 0 errors
    - CONTINGENCY: if any EDGE-31/32 leg is RED and cannot be made green by test-only means (e.g. adbc_cancel does not fire on a blocked pull, or a deadline−ε op is spuriously cancelled), STOP and escalate to the Plan 03 contingency gate — do NOT invent machinery; the minimal fix per RESEARCH §Discovered-Necessity table lands at the chokepoint in this phase.
  </acceptance_criteria>
  <done>EDGE-31 and EDGE-32 are pinned on both the streaming-pull and ingest paths, green under both backends across the x20 loop.</done>
</task>

<task type="auto">
  <name>Task 2: EDGE-24 — loop-shutdown cleanliness on the new paths (mid-stream + mid-ingest + real drained-close)</name>
  <files>tests/async/test_edge_shutdown.py</files>
  <read_first>
    - tests/async/test_reader_resource.py (lines 55-113: `warnings.catch_warnings(record=True)` + `simplefilter("always")` + the offending-warning list-comprehension filter at 107-113)
    - tests/async/test_reader_cancel.py (lines 80-90 release-in-teardown gating; 167-171 `_pull_started`; 133-159 real duckdb_async_pool checkedout discipline)
    - tests/async/test_ingest_cancel.py (lines 84-91 release-in-teardown gating)
    - tests/async/_edge_helpers.py (await_inside, real_clock_watchdog, concurrency_marks)
    - tests/async/conftest.py (make_stub_async_connection; duckdb_async_pool)
    - 32-PATTERNS.md §test_edge_shutdown.py (the mid-ingest / mid-stream / real-leg skeletons)
  </read_first>
  <action>
    Create `tests/async/test_edge_shutdown.py` with the Shared Pattern A import block and `pytestmark = concurrency_marks`.

    Mid-ingest leg: inside `warnings.catch_warnings(record=True)` with `warnings.simplefilter("always")`, open `async with anyio.create_task_group() as tg:`, `tg.start_soon(functools.partial(cur.adbc_ingest, "t", object()))`, then `await await_inside(lambda: stub_conn.cursors[0].ingest_call_count == 1)`, then RELEASE every stub cursor (`for c in stub_conn.cursors: c.release()`) so the `abandon_on_cancel=False` worker joins cleanly at teardown (Pitfall 3 — an unreleased worker turns shutdown into a join-hang and trips the x20 gate). After the task group exits, build the offending list: warnings whose message contains `"was never awaited"` or `"Task was destroyed"`; assert `offending == []`.

    Mid-stream leg: same shape but arm `record_batch_batches = [object()]` before `fetch_record_batch()`, `tg.start_soon` a small `_drain_one(reader)` coroutine that pulls once, gate on `read_call_count >= 1` via `await_inside`, then release. Same offending-warning assertion.

    Real drained-close leg: use the `duckdb_async_pool` fixture; check out a connection, run a real op to completion, drain it, and assert `await pool.close()` (or fixture teardown) raises nothing and `pool.checkedout() == 0`. Do NOT race a genuinely-wedged worker (RESEARCH Pitfall 3 / A3) — the release-in-teardown / drained-close discipline is the deterministic proof.

    Keep both backends; note in a docstring that trio's nursery strictness is the discriminating canary (RESEARCH §Pattern-3). Guard every stub leg with `real_clock_watchdog` and assert `tripped[0] is False`.
  </action>
  <verify>
    <automated>ADBC_ASYNC_REPEAT=20 .venv/bin/pytest tests/async/test_edge_shutdown.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `ADBC_ASYNC_REPEAT=20 .venv/bin/pytest tests/async/test_edge_shutdown.py` exits 0 (EDGE-24, both backends, 20 iterations, 0 hangs)
    - Each stub leg asserts `offending == []` (no "was never awaited", no "Task was destroyed") and `tripped[0] is False`
    - The real leg uses `duckdb_async_pool`, drains before close, and asserts `pool.close()` raises nothing with `checkedout() == 0`
    - Every blocked worker is RELEASED in the teardown window before the task group joins (Pitfall 3); no wedged-worker race
    - basedpyright clean: `.venv/bin/basedpyright tests/async/test_edge_shutdown.py` reports 0 errors
    - CONTINGENCY: if a pending offload at shutdown raises a library-attributable exception that cannot be resolved test-only (e.g. a worker→loop callback into a dead loop), STOP and escalate to the Plan 03 contingency gate — the minimal fix (guard the on_dispatch/watcher bridge, RESEARCH §Discovered-Necessity) lands in this phase.
  </acceptance_criteria>
  <done>EDGE-24 is pinned mid-stream, mid-ingest, and on a real drained-close, green under both backends across the x20 loop.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan produces (test-only; no new public production symbols):
- `tests/async/test_edge_timeout_precision.py` — EDGE-31 (`move_on_after(0)` cancels a blocked pull/ingest) + EDGE-32 (deadline−ε not over-cancelled), on streaming pull + ingest, dual-backend, looped. Copies `_release_when_entered` verbatim as a module-local test helper.
- `tests/async/test_edge_shutdown.py` — EDGE-24 loop-shutdown cleanliness (mid-stream + mid-ingest stub legs + real duckdb drained-close), dual-backend, looped. Introduces a module-local `_drain_one` test coroutine.
No new harness/fixture symbols (all reused: `make_stub_async_connection`, `duckdb_async_pool`, `await_inside`, `real_clock_watchdog`, `virtual_clock`, `concurrency_marks`).

CONTINGENCY (per locked decision #3): if any EDGE-31/32/24 leg goes RED and cannot be made green test-only, a minimal production fix per the RESEARCH §Discovered-Necessity table (at the shared `cancellable_offload`/`offload` chokepoint) lands in this phase via the Plan 03 gate. Such a fix would add/change a public-symbol behavior → full Google-style docstrings + guide reflection required (CLAUDE.md docs gate). Default expectation: no production change.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| loop task → worker thread (cancellable_offload chokepoint) | The cancel/timeout path all four new methods share; EDGE-31/32 verify cancel-on-deadline fires exactly once and invalidates, EDGE-32 verifies no over-cancel. No untrusted input crosses. |
| event-loop teardown → pending offload (EDGE-24) | Shutdown boundary; a pending non-cancellable worker must join cleanly with no library-attributable exception. trio nursery strictness is the canary. |

Per RESEARCH §Security Domain this is a test-only phase over an in-process offload layer with no new attack surface. The tests VERIFY already-mitigated robustness properties.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-03 | Denial of Service | cancellation swallowed → resource leak / hang (EDGE-31) | accept (already mitigated) | `cancellable_offload` fires `adbc_cancel` once + invalidates on a blocked pull/ingest; test ASSERTS `adbc_cancel_call_count == 1` + `invalidate_call_count == 1` + `checkedout()==0`. Pre-existing D-25-06 discipline, extended to new paths, not added. |
| T-32-04 | Denial of Service | over-cancel of a finishing op (EDGE-32) | accept (already mitigated) | Deadline−ε op is not spuriously cancelled/invalidated; test ASSERTS `adbc_cancel_call_count == 0`, `invalidate_call_count == 0`, `cancelled_caught is False`. Pre-existing gating, verified not added. |
| T-32-05 | Denial of Service | pending-offload callback into a dead loop at shutdown (EDGE-24) | accept (already mitigated) | `offload` runs `on_dispatch` on the loop thread (no dead-loop `from_thread` bridge on the hot path); test ASSERTS no library-attributable exception mid-stream/mid-ingest, trio nursery as canary. Pre-existing design, verified not added. |
| T-32-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH §Package Legitimacy Audit: not applicable — zero new packages). No `[ASSUMED]`/`[SUS]`/`[SLOP]` surface. |

No HIGH-severity unmitigated threats. If the CONTINGENCY fires and a chokepoint fix lands, re-run this register against the changed dispatch at the Plan 03 gate.
</threat_model>

<verification>
- `ADBC_ASYNC_REPEAT=20 .venv/bin/pytest tests/async/test_edge_timeout_precision.py tests/async/test_edge_shutdown.py -q` — both modules green under asyncio and trio across 20 iterations (0 hangs).
- `.venv/bin/basedpyright tests/async/test_edge_timeout_precision.py tests/async/test_edge_shutdown.py` — 0 errors.
- Phase gate (Plan 03): the x20 loop must be confirmed on Linux CI — cancel/lost-wakeup races can pass 20/20 on macOS but hang on Linux (MEMORY platform-dependent-lost-wakeup). macOS-green is NOT authoritative for EDGE-24/31/32.
</verification>

<success_criteria>
- EDGE-31: `move_on_after(0)` cancels a blocked pull AND ingest — `adbc_cancel` once, invalidate, both backends, x20.
- EDGE-32: deadline−ε op not over-cancelled on pull AND ingest — no `adbc_cancel`, no invalidate, both backends, x20.
- EDGE-24: no library-attributable exception at shutdown mid-stream, mid-ingest, and on real drained-close — both backends, x20.
- All legs use `virtual_clock` trigger + `real_clock_watchdog` guard + real-thread releaser (EDGE-32); no `anyio.fail_after` watchdog; no wedged-worker race.
</success_criteria>

<output>
Create `.planning/phases/32-p2-edge-hardening/32-02-SUMMARY.md` when done.
</output>
