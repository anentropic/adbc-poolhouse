---
phase: 32-p2-edge-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/async/test_edge_checkpoint.py
  - tests/async/test_edge_contextvars.py
autonomous: true
requirements: [EDGE-08, EDGE-13, EDGE-14]
must_haves:
  truths:
    - "A cancel set before a new-method (adbc_ingest) offload with no intervening checkpoint is delivered at the offload boundary; the stub's ingest worker never runs (ingest_call_count == 0), on both asyncio and trio (trio = discriminator)"
    - "A contextvar set before a new-method (fetch_df) offload is visible to the worker thread (copied in)"
    - "A worker's contextvar mutation does not leak back to the calling task after the offload returns"
  artifacts:
    - path: "tests/async/test_edge_checkpoint.py"
      provides: "EDGE-08 cancel-at-offload assertion on adbc_ingest, dual-backend"
      contains: "ingest_call_count"
      min_lines: 30
    - path: "tests/async/test_edge_contextvars.py"
      provides: "EDGE-13 copy-in + EDGE-14 no-leak-back on a fetch_df offload"
      contains: "ContextVar"
      min_lines: 30
  key_links:
    - from: "tests/async/test_edge_checkpoint.py"
      to: "tests/_async_harness/stubs.py"
      via: "BlockingStubCursor.ingest_call_count / adbc_cancel_call_count counters"
      pattern: "ingest_call_count"
    - from: "tests/async/test_edge_contextvars.py"
      to: "tests/_async_harness/stubs.py"
      via: "register_on_enter worker-thread probe reading the module ContextVar"
      pattern: "register_on_enter"
---

<objective>
Land the RED-first deterministic tests for the three "assert-don't-add" anyio guarantees — EDGE-08 (checkpoint delivered at the offload boundary), EDGE-13 (contextvar copy-in), EDGE-14 (no leak-back) — each pinned on a representative NEW-method offload (`adbc_ingest` for EDGE-08, `fetch_df` for EDGE-13/14) and run under both asyncio and trio.

Purpose: These are guarantees of `anyio.to_thread.run_sync` enforced at the single `offload()` chokepoint every new method routes through. RESEARCH verified all three empirically against the installed anyio 4.14.1 / trio 0.33.0. No production code is expected — the tests assert an existing guarantee on the new paths. These are the lowest-risk legs and need NO gating for EDGE-13/14.

Output: Two new test modules under `tests/async/`, both green on first run against the shipped async layer (they assert a guarantee that already holds).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@.claude/skills/adbc-poolhouse-docs-author/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-p2-edge-hardening/32-RESEARCH.md
@.planning/phases/32-p2-edge-hardening/32-PATTERNS.md
@.planning/phases/32-p2-edge-hardening/32-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: EDGE-08 — cancel delivered at the offload boundary on a new-method offload (adbc_ingest)</name>
  <files>tests/async/test_edge_checkpoint.py</files>
  <read_first>
    - tests/async/test_edge_cancel_depth.py (lines 59-116: the sibling-helper import block + `test_cancel_before_offload_is_clean` — the exact analog to clone)
    - tests/async/_edge_helpers.py (real_clock_watchdog, concurrency_marks)
    - tests/_async_harness/stubs.py (BlockingStubCursor.adbc_ingest → ingest_call_count ~line 397; adbc_cancel_call_count; observed_cancel)
    - tests/async/conftest.py (make_stub_async_connection fixture)
  </read_first>
  <action>
    Create `tests/async/test_edge_checkpoint.py` cloning the choreography of `test_edge_cancel_depth.py::test_cancel_before_offload_is_clean` verbatim (Shared Pattern A import block + `pytestmark = concurrency_marks`). Swap the offloaded call `await cur.execute("SELECT 1")` for `await cur.adbc_ingest("t", object())`, and swap the counter assertion `sc.execute_call_count == 0` for `sc.ingest_call_count == 0`. Keep the pre-cancelled `anyio.CancelScope()` self-cancel form (bare, NOT wrapped in `pytest.raises` — a pre-cancelled scope swallows the cancel at scope exit, per PATTERNS EDGE-08 note; the load-bearing assertion is `ingest_call_count == 0`). Keep `del anyio_backend_name` but add a docstring line stating trio is the discriminating backend (it guarantees every `await` is a checkpoint) per RESEARCH §Pattern-1. Retain all four negative assertions from the analog: `tripped[0] is False`, `sc.adbc_cancel_call_count == 0`, `stub_conn.invalidate_call_count == 0`, `sc.observed_cancel is False`. Use `real_clock_watchdog(stub_conn.cursors)` as the hang guard — NEVER `anyio.fail_after`.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/async/test_edge_checkpoint.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/async/test_edge_checkpoint.py -x -q` exits 0 (both asyncio and trio params pass)
    - The test asserts `sc.ingest_call_count == 0` (the driver method never ran — cancel delivered at the checkpoint)
    - The test asserts `sc.adbc_cancel_call_count == 0` and `stub_conn.invalidate_call_count == 0` (nothing to abort — fn never started)
    - `pytestmark = concurrency_marks` present; `real_clock_watchdog` used as the guard, `anyio.fail_after` NOT used as a watchdog anywhere in the file
    - basedpyright clean: `.venv/bin/basedpyright tests/async/test_edge_checkpoint.py` reports 0 errors
  </acceptance_criteria>
  <done>EDGE-08 is pinned on the `adbc_ingest` offload path, green under both backends, with the trio discriminator noted.</done>
</task>

<task type="auto">
  <name>Task 2: EDGE-13/14 — contextvar copy-in + no-leak-back on a new-method offload (fetch_df)</name>
  <files>tests/async/test_edge_contextvars.py</files>
  <read_first>
    - tests/async/test_edge_cancel_depth.py (lines 59-70: the importlib sibling-helper import block shape only)
    - tests/_async_harness/stubs.py (lines 232-261: BlockingStubCursor.register_on_enter; the worker-thread `_block` hook dispatch ~line 747; fetch_df → df_call_count ~line 340)
    - tests/async/conftest.py (make_stub_async_connection)
    - 32-RESEARCH.md §"EDGE-13/14" Code Example (the verified `_worker` read/mutate body)
    - 32-PATTERNS.md §test_edge_contextvars.py (Pattern 1(a) register_on_enter mechanism; Pattern 1(b) fallback)
  </read_first>
  <action>
    Create `tests/async/test_edge_contextvars.py` with the importlib sibling-import block BUT omit `virtual_clock`, `real_clock_watchdog`, and `pytestmark = concurrency_marks` — these are deterministic single-offload assertions that need NO gating (RESEARCH §Pattern-1). Define a module-level `_cv = contextvars.ContextVar("trace")`. Use Pattern 1(a): register a worker-side probe via `stub_cursor.register_on_enter(probe)` where `probe` appends `_cv.get("MISSING")` to a captured list (this hook fires from inside `_block` ON THE WORKER THREAD). Write two test functions with `-k`-selectable names: one containing `copied_in` (EDGE-13) and one containing `no_leak` (EDGE-14). Both set `_cv.set("outer")` before `await cur.fetch_df()`, release the stub in a `finally` so the offloaded `fetch_df` worker returns, and call the `register_on_enter` cleanup callable in the same `finally`. `copied_in` asserts the probe recorded `"outer"` (worker saw the copied-in value); `no_leak` has the worker mutate (`_cv.set("inner")` inside the probe) and asserts the caller's `_cv.get()` is unchanged (`== "outer"`) after the offload returns. Both are `@pytest.mark.anyio` (dual-backend). If `register_on_enter` proves awkward for the mutation leg, fall back to Pattern 1(b) — a direct `offload(_worker, limiter=anyio.CapacityLimiter(1))` import from `adbc_poolhouse._async._offload` with a comment that it is the shared chokepoint — but prefer 1(a) on the real `fetch_df` path.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/async/test_edge_contextvars.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/async/test_edge_contextvars.py -k copied_in -x` exits 0 (EDGE-13, both backends): worker observed `"outer"`
    - `.venv/bin/pytest tests/async/test_edge_contextvars.py -k no_leak -x` exits 0 (EDGE-14, both backends): caller `_cv.get() == "outer"` after the offload (worker's `"inner"` discarded)
    - The module has NO `pytestmark = concurrency_marks`, NO `real_clock_watchdog`, NO `virtual_clock` (deterministic, no gating)
    - The assertion runs on a real new-method path (`fetch_df`) via `register_on_enter` (Pattern 1(a)), or, if that is rejected, on a directly-imported `offload` chokepoint (Pattern 1(b)) with an explaining comment
    - basedpyright clean: `.venv/bin/basedpyright tests/async/test_edge_contextvars.py` reports 0 errors
  </acceptance_criteria>
  <done>EDGE-13 and EDGE-14 are pinned on a fetch_df offload, green under both backends, with independently `-k`-selectable test names.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan produces (test-only; no new public production symbols):
- `tests/async/test_edge_checkpoint.py` — EDGE-08 cancel-at-offload test on `adbc_ingest` (dual-backend).
- `tests/async/test_edge_contextvars.py` — EDGE-13 copy-in + EDGE-14 no-leak-back tests on `fetch_df` (dual-backend). Introduces a module-level `_cv = contextvars.ContextVar("trace")` test-local symbol and a worker-side probe callback (test helpers, not public API).
No new test fixtures or harness symbols (all reused: `make_stub_async_connection`, `register_on_enter`, `real_clock_watchdog`, `concurrency_marks`).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| loop task → worker thread (offload chokepoint) | The `offload()` boundary every new method crosses; contextvar copy-in and checkpoint delivery are the properties verified here. No untrusted input crosses — tests use fixed literals and `object()` sentinels. |

Per RESEARCH §Security Domain this is a test-only phase over an in-process offload layer with no new attack surface. The tests VERIFY already-mitigated robustness properties; they add no new threats.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Denial of Service | cancel delivery at the offload boundary (EDGE-08) | accept (already mitigated) | anyio delivers the cancel at the `async with limiter` checkpoint before `fn` runs; test ASSERTS `ingest_call_count == 0` — no starvation, no missing cancellation point. Pre-existing behavior, verified not added. |
| T-32-02 | Information Disclosure | contextvar bleed across tasks (EDGE-14) | accept (already mitigated) | anyio `copy_context` discards worker mutations; test ASSERTS the caller's `_cv.get()` is unchanged after the offload — no context bleed. Pre-existing behavior, verified not added. |
| T-32-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH §Package Legitimacy Audit: not applicable — zero new packages). No `[ASSUMED]`/`[SUS]`/`[SLOP]` surface. |

No HIGH-severity unmitigated threats.
</threat_model>

<verification>
- `.venv/bin/pytest tests/async/test_edge_checkpoint.py tests/async/test_edge_contextvars.py -q` — both modules green under asyncio and trio.
- `.venv/bin/basedpyright tests/async/test_edge_checkpoint.py tests/async/test_edge_contextvars.py` — 0 errors (authoritative; ignore harness Pyright false import-resolution errors per MEMORY).
- Import-lint guard still passes over `_async/` (no change to production; re-verified at the Wave 2 gate).
</verification>

<success_criteria>
- EDGE-08 asserted on `adbc_ingest`: cancel delivered at the boundary, `ingest_call_count == 0`, both backends, trio discriminator noted.
- EDGE-13 asserted on `fetch_df`: worker sees the copied-in contextvar (`"outer"`).
- EDGE-14 asserted on `fetch_df`: worker mutation does not leak back (caller `_cv.get() == "outer"`).
- Both modules basedpyright-clean; no `anyio.fail_after` used as a watchdog.
</success_criteria>

<output>
Create `.planning/phases/32-p2-edge-hardening/32-01-SUMMARY.md` when done.
</output>
