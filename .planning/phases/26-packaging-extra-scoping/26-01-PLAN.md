---
phase: 26-packaging-extra-scoping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - tests/test_pkg_extra.py
autonomous: true
requirements: [PKG-01]
must_haves:
  truths:
    - "The package declares an `async` optional-dependency extra pinning `anyio>=4.13` and nothing else"
    - "The `all` extra includes `adbc-poolhouse[async]`"
    - "uv.lock is coherent with pyproject.toml so `uv sync --locked` succeeds"
  artifacts:
    - path: "pyproject.toml"
      provides: "[async] extra row + [all] aggregation entry"
      contains: "async = [\"anyio>=4.13\"]"
    - path: "uv.lock"
      provides: "relocked lockfile carrying the new extra"
    - path: "tests/test_pkg_extra.py"
      provides: "metadata assertion that the async extra is declared and aggregated"
  key_links:
    - from: "pyproject.toml [project.optional-dependencies].all"
      to: "pyproject.toml [project.optional-dependencies].async"
      via: "self-reference adbc-poolhouse[async]"
      pattern: "adbc-poolhouse\\[async\\]"
---

<objective>
Declare the `[async]` optional-dependency extra (PKG-01, D-02): add `async = ["anyio>=4.13"]`
to `[project.optional-dependencies]`, add `"adbc-poolhouse[async]"` to the `all`
aggregate, relock with `uv lock`, and commit the refreshed `uv.lock`. Add a small
metadata-assertion test proving the extra is declared and aggregated.

Purpose: ships the async surface behind an opt-in extra so sync installs pull no anyio
(zero-cost-sync goal). This plan is also a build-time prerequisite for the no-anyio CI
job (Plan 04), whose `--locked` install fails if `uv.lock` is stale.
Output: edited `pyproject.toml`, relocked `uv.lock`, new `tests/test_pkg_extra.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@.claude/skills/adbc-poolhouse-docs-author/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-packaging-extra-scoping/26-CONTEXT.md
@.planning/phases/26-packaging-extra-scoping/26-RESEARCH.md
@.planning/phases/26-packaging-extra-scoping/26-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the [async] extra and relock</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>
    - pyproject.toml lines 16-32 (the existing `[project.optional-dependencies]` table — `duckdb`/`sqlite` rows + the `all` aggregate to mirror) and line 56 (the `dev`-group `"anyio>=4.13"` floor to match)
    - 26-PATTERNS.md "pyproject.toml — `[async]` extra (PKG-01)" (exact insertion points + pin rationale)
    - 26-RESEARCH.md "Installation (PKG-01 — exact pyproject.toml shape)" and Pitfall 5 (relock-or-CI-fails)
  </read_first>
  <action>
    Per D-02 / PKG-01: add a new single-element row `async = ["anyio>=4.13"]` to the
    `[project.optional-dependencies]` table (place it after the `sqlite` row, before `all`).
    The floor is `>=4.13` (NOT the `>=4.0.0` the original requirement text read) to match the
    existing `dev`-group anyio floor. The extra adds anyio and nothing else. Then append
    `"adbc-poolhouse[async]",` as the final element of the `all` list, following the existing
    `adbc-poolhouse[<name>]` self-reference aggregation pattern. Do NOT touch any other extra
    row, the `[dependency-groups]`, or the runtime `dependencies`. After editing, run
    `uv lock` to refresh `uv.lock` (RESEARCH Pitfall 5: the `quality` job's `uv sync --locked`
    fails if pyproject and the lockfile disagree). Commit the regenerated `uv.lock`.
  </action>
  <verify>
    <automated>uv lock --locked && grep -A1 '^async = ' pyproject.toml | grep -q 'anyio>=4.13' && grep -q 'adbc-poolhouse\[async\]' pyproject.toml</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` contains a line `async = ["anyio>=4.13"]` in `[project.optional-dependencies]`
    - The `all` list contains `"adbc-poolhouse[async]"`
    - `uv lock --locked` exits 0 (lockfile coherent — no "out of date" error)
    - No runtime `dependencies` and no other extra rows changed (diff is additive only)
  </acceptance_criteria>
  <done>The `[async]` extra exists pinning `anyio>=4.13`, `[all]` includes it, and `uv.lock` is coherent.</done>
</task>

<task type="auto">
  <name>Task 2: Metadata-assertion test for the extra</name>
  <files>tests/test_pkg_extra.py</files>
  <read_first>
    - tests/test_driver_imports.py lines 1-19 (module-docstring + `importlib` convention for the house style)
    - 26-PATTERNS.md "tests/test_pkg_extra.py (NEW, optional — PKG-01 assertion)" (the `importlib.metadata.metadata(...).get_all("Provides-Extra")` pattern)
  </read_first>
  <action>
    Create `tests/test_pkg_extra.py` — an anyio-free, plain-sync test module
    (`from __future__ import annotations`, module docstring, no `@pytest.mark.anyio`) so it
    collects under the no-anyio CI job (Plan 04). Add a test asserting the installed package
    metadata declares the `async` extra: read
    `importlib.metadata.metadata("adbc-poolhouse")` and assert `"async"` is in
    `meta.get_all("Provides-Extra")`. This is a belt-and-suspenders metadata check beside the
    `uv lock --locked` CI coherence check. Keep the module import-time anyio-free (use only
    `importlib.metadata`).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_pkg_extra.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/test_pkg_extra.py -q` passes
    - The module imports no anyio at collection time (grep: no `import anyio` / `pytest.mark.anyio`)
  </acceptance_criteria>
  <done>A passing test asserts the `async` extra is present in package metadata.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package index → installer | The `[async]` extra names `anyio`, resolved from a package index at install time (supply-chain surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | `anyio` resolved by the `[async]` extra (dependency-confusion / supply-chain) | accept | `anyio` is a mature (>5yr), high-download, already-direct `dev`-group dependency (RESEARCH Package Legitimacy Audit — verdict OK); no NEW package is introduced; floor `>=4.13` matches the audited 4.14.1. ASVS L1: no further control warranted. |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | No new third-party package is added; the extra references an already-present, audited dependency. No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages → no legitimacy checkpoint required. |
</threat_model>

<verification>
- `uv lock --locked` clean (lockfile coherent with the new extra)
- `grep` confirms the `async` row and the `all` self-reference
- `.venv/bin/pytest tests/test_pkg_extra.py -q` green
</verification>

<success_criteria>
- PKG-01 satisfied: `[async]` extra pins `anyio>=4.13`, `[all]` includes it, lockfile coherent
- New metadata test green; module is anyio-free at import
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)
- New `[async]` optional-dependency extra in `pyproject.toml` (`async = ["anyio>=4.13"]`)
- `"adbc-poolhouse[async]"` aggregation entry in the `[all]` extra
- Refreshed `uv.lock`
- New test module `tests/test_pkg_extra.py` with `test_async_extra_is_declared` (or similar)
</artifacts_produced>

<output>
Create `.planning/phases/26-packaging-extra-scoping/26-01-SUMMARY.md` when done
</output>
