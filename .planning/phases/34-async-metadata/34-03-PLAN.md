---
phase: 34-async-metadata
plan: 03
type: execute
wave: 3
depends_on: ["34-02"]
files_modified:
  - docs/src/guides/async.md
autonomous: true
requirements: [META-04]
must_haves:
  truths:
    - "The async.md 'incomplete' caveat shrinks: the 'Async ADBC metadata' bullet is removed and metadata is listed in 'What you get today'; the prepared-statements bullet stays (Phase 35 removes it) (META-04)"
    - "A 'Connection metadata' how-to section documents adbc_get_info / adbc_get_table_schema / adbc_get_table_types and streaming adbc_get_objects, including the can't-abort-in-flight caveat and the reader-lifetime lock (META-04, locked decisions #1/#2)"
    - "The API reference auto-renders the six new public methods with Args/Returns/Raises + Example via the existing AsyncConnection mkdocstrings block — no gen_ref_pages.py change (META-04)"
    - "DISABLE_MKDOCS_2_WARNING=true .venv/bin/mkdocs build --strict exits 0 (META-04)"
    - "Humanizer pass applied to all new/rewritten prose (META-04, CLAUDE.md docs gate)"
  artifacts:
    - path: "docs/src/guides/async.md"
      provides: "shrunk caveat + Connection metadata how-to section"
      contains: "Connection metadata"
  key_links:
    - from: "src/adbc_poolhouse/_async/_connection.py docstrings (plan 34-02)"
      to: "docs/src/reference (AsyncConnection mkdocstrings render)"
      via: "existing '::: adbc_poolhouse._async._connection.AsyncConnection' block, members_order source, filters !^__"
      pattern: "AsyncConnection"
---

<objective>
Document the six async metadata methods (META-04): shrink the async guide's "incomplete" caveat,
add a "Connection metadata" how-to section, confirm the API reference auto-renders the new methods
from the plan 34-02 docstrings, run the humanizer pass, and gate on `mkdocs build --strict`.

Purpose: Close the docs half of the async/sync parity for metadata. The API reference needs NO
`gen_ref_pages.py` change — the existing `AsyncConnection` mkdocstrings block renders the six new
public methods automatically from their docstrings (written in 34-02).
Output: Modified `docs/src/guides/async.md`; strict build green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@.claude/skills/adbc-poolhouse-docs-author/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-async-metadata/34-RESEARCH.md

# The doc being edited + the source docstrings that drive the reference render:
@docs/src/guides/async.md
@src/adbc_poolhouse/_async/_connection.py
</context>

<artifacts_this_phase_produces>
This plan produces no new symbols — it documents the six methods created in plan 34-02
(`adbc_get_info`, `adbc_get_table_schema`, `adbc_get_table_types`, `adbc_get_objects`,
`adbc_get_statistics`, `adbc_get_statistic_names`) in the async guide, and verifies the API
reference render + strict build.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Shrink the caveat and add the Connection metadata how-to section</name>
  <files>docs/src/guides/async.md</files>
  <read_first>
    - docs/src/guides/async.md lines 18–31 (the `!!! warning "Experimental"` block) — the exact
      caveat to shrink; lines 44–68 (the "A first query" prose + code style) for voice/format.
    - 34-RESEARCH.md §Docs Surface (lines 415–424) — the precise edit map.
    - .claude/skills/adbc-poolhouse-docs-author/SKILL.md — voice (second person, "See also"),
      how-to-guide rules (illustrative snippets, MkDocs tabs if a driver variant helps), and the
      "no `!!! example` box around authored code" rule (MEMORY.md — use a plain fenced block).
  </read_first>
  <action>
    Edit the `!!! warning "Experimental"` block (lines 18–31). Do NOT drop the whole block. Keep the
    "surface may change" paragraph (lines 18–21). In the "It is also incomplete" list, REMOVE the
    "**Async ADBC metadata** — `adbc_get_table_schema`, `adbc_get_objects`, `adbc_get_info`" bullet
    (line 25). LEAVE the "**Async prepared statements**" bullet (line 26) — Phase 35 removes it. In
    the "What you get today is …" paragraph (lines 28–31), ADD connection metadata introspection to
    the enumerated list of available features.

    Add a new `## Connection metadata` how-to section (place it after the streaming / `adbc_ingest`
    material, before or near the "See also"). Follow docs-author voice (second person, direct). Show
    a plain fenced python code block (triple-backtick python) (NOT wrapped in `!!! example`) illustrating:
    - `info = await conn.adbc_get_info()` → a dict; `schema = await conn.adbc_get_table_schema("t")`
      → a `pyarrow.Schema`; `types = await conn.adbc_get_table_types()` → a list.
    - the streaming form: `async with await conn.adbc_get_objects(depth="tables") as reader: async
      for batch in reader:` (mirrors the RESEARCH §Code Examples consumer usage, lines 305–315).
    In prose, note the two caveats that match the docstrings: (a) these calls are not cooperatively
    cancellable — a surrounding timeout/cancel cannot abort an in-flight metadata call (same as
    `commit`/`rollback`); (b) the streaming readers hold a reader-lifetime lock on the connection,
    so use `async with` and drain/close before another op (a foreign call while a reader is live
    raises `ConnectionBusyError`). Add a "See also" cross-reference to the streaming section and the
    API reference.

    Do not touch `docs/src/changelog.md` — the version bump/changelog is the deferred release step
    after Phase 35 (STATE.md). Do not hand-edit anything under `docs/src/reference/` — it is
    auto-generated.
  </action>
  <verify>
    <automated>grep -q "Connection metadata" docs/src/guides/async.md && grep -q "prepared statements" docs/src/guides/async.md && [ "$(grep -c 'Async ADBC metadata' docs/src/guides/async.md)" -eq 0 ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The "Async ADBC metadata" bullet is gone from the caveat; the "Async prepared statements"
      bullet remains.
    - "What you get today" lists connection metadata introspection.
    - A `## Connection metadata` section exists with a plain fenced `python` example covering the
      three value methods and the streaming `adbc_get_objects` form, plus prose on the
      non-cancellable and reader-lifetime caveats and a "See also" cross-reference.
    - No code block in the new section is wrapped in `!!! example`; no edits under
      `docs/src/reference/`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Humanizer pass, API-reference render check, and strict-build gate</name>
  <files>docs/src/guides/async.md</files>
  <read_first>
    - @/Users/paul/.claude/skills/humanizer/SKILL.md — the full humanizer checklist (loaded per the
      docs-author skill Step 3).
    - .claude/skills/adbc-poolhouse-docs-author/SKILL.md §Step 3 — the technical-docs patterns to
      eliminate (promotional language, AI vocabulary, vague attributions, rule-of-three, em-dash
      overuse).
    - 34-RESEARCH.md §Docs Surface (API reference row): the existing
      `::: adbc_poolhouse._async._connection.AsyncConnection` block renders the six public methods
      automatically (`members_order: source`, `filters: ["!^__"]`).
  </read_first>
  <action>
    Apply the humanizer pass to all new/rewritten prose in `async.md` (the shrunk caveat + the new
    Connection metadata section): remove promotional language, AI vocabulary, vague "this
    enables/allows" attributions, superficial `-ing` openers, rule-of-three lists, and hold em
    dashes to at most one per paragraph. Keep the second-person, direct voice.

    Build the docs with the strict gate and confirm exit 0 (check the exit code, NOT grep —
    carried-forward gotcha): `DISABLE_MKDOCS_2_WARNING=true .venv/bin/mkdocs build --strict`.

    Confirm the API reference renders the six new public methods. After the build, inspect the
    generated `AsyncConnection` reference page under `site/` (or run `.venv/bin/mkdocs serve` briefly
    if needed) and verify each of `adbc_get_info`, `adbc_get_table_schema`, `adbc_get_table_types`,
    `adbc_get_objects`, `adbc_get_statistics`, `adbc_get_statistic_names` appears with its
    Parameters/Returns/Raises tables and an Example block — driven purely by the plan 34-02
    docstrings, no `gen_ref_pages.py` change. If a docstring renders with rogue colons (RST
    `:role:` syntax leaked into a docstring), that is a 34-02 regression — fix the docstring in
    `_connection.py` to Markdown syntax and rebuild.
  </action>
  <verify>
    <automated>DISABLE_MKDOCS_2_WARNING=true .venv/bin/mkdocs build --strict; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `DISABLE_MKDOCS_2_WARNING=true .venv/bin/mkdocs build --strict` exits 0.
    - The rendered `AsyncConnection` reference page shows all six `adbc_get_*` methods with
      Parameters/Returns/Raises + an Example block.
    - The new/rewritten prose passes the humanizer checklist (no promotional/AI vocabulary, no
      vague attributions, ≤ one em dash per paragraph).
    - No RST `:role:` syntax renders as rogue colons in the six method docstrings.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored docs → reader | Documentation prose; no executable trust boundary. The build gate is the only integrity control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-04 | Information Disclosure | docs example leaks credentials | accept | Examples use DuckDB / illustrative snippets with no secrets (docs-author rule) |
| T-34-05 | Tampering | stale/incorrect caveat misleads users about async capability | mitigate | The `mkdocs build --strict` gate (exit 0) plus the explicit caveat-shrink acceptance criteria keep the guide consistent with the shipped surface |
| T-34-SC | Tampering | package installs | accept (N/A) | No package installs this plan; docs toolchain (`mkdocs`, mkdocstrings) already present |
</threat_model>

<verification>
- `DISABLE_MKDOCS_2_WARNING=true .venv/bin/mkdocs build --strict` — exit 0 (the phase docs gate).
- `grep -q "Connection metadata" docs/src/guides/async.md` and `! grep -q "Async ADBC metadata"
  docs/src/guides/async.md` — caveat shrunk, section added.
- Manual render inspection: the six `adbc_get_*` methods appear in the `AsyncConnection` API
  reference with Parameters/Returns/Raises + Example.
</verification>

<success_criteria>
- The async guide documents the six metadata methods and its "incomplete" caveat shrinks to only
  the prepared-statements line (META-04).
- The API reference auto-renders the six methods from their docstrings; `mkdocs build --strict`
  passes; the humanizer pass is applied.
</success_criteria>

<output>
Create `.planning/phases/34-async-metadata/34-03-SUMMARY.md` when done.
</output>
