---
phase: 34-async-metadata
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/async/test_meta_signature.py
  - tests/async/test_meta_roundtrip.py
  - tests/async/test_meta_stream.py
  - tests/async/test_meta_unsupported.py
autonomous: true
requirements: [META-01, META-02, META-03]
must_haves:
  truths:
    - "RED scaffold: test_meta_signature.py pins all six adbc_get_* methods exist on AsyncConnection with the correct positional/kw-only shape (META-01)"
    - "RED scaffold: test_meta_roundtrip.py pins adbc_get_info->dict, adbc_get_table_types->list, adbc_get_table_schema->pyarrow.Schema on DuckDB (META-01/02, locked decision #4)"
    - "RED scaffold: test_meta_stream.py pins adbc_get_objects streams via AsyncRecordBatchReader without eager materialization (META-02, locked decision #1)"
    - "RED scaffold: test_meta_unsupported.py pins adbc_get_statistics / adbc_get_statistic_names surface the native NotSupportedError unchanged on DuckDB (META-03, locked decision #6)"
    - "All four new files observe tests/async discipline: @pytest.mark.anyio on every async test, no import asyncio, no positive-duration sleep (test_meta_guard.py still passes)"
  artifacts:
    - path: "tests/async/test_meta_signature.py"
      provides: "META-01 signature/existence contract (sync introspection, no marker)"
      contains: "inspect.signature"
    - path: "tests/async/test_meta_roundtrip.py"
      provides: "META-01/02 value-method round-trip on DuckDB"
      contains: "adbc_get_info"
    - path: "tests/async/test_meta_stream.py"
      provides: "META-02 streaming-reader drain on DuckDB"
      contains: "adbc_get_objects"
    - path: "tests/async/test_meta_unsupported.py"
      provides: "META-03 native-error surfacing on DuckDB"
      contains: "NotSupportedError"
  key_links:
    - from: "tests/async/test_meta_*.py"
      to: "adbc_poolhouse._async._connection.AsyncConnection"
      via: "import + duckdb_async_pool fixture"
      pattern: "from adbc_poolhouse._async._connection import AsyncConnection"
---

<objective>
Create the Wave-0 RED test scaffolding for Phase 34 async metadata: four new files in
`tests/async/` that encode the observable META-01/02/03 contracts before any production
symbol exists. Every test FAILS now (the six `adbc_get_*` methods are not yet on
`AsyncConnection`) — that RED signal is the acceptance criterion. Plan 34-02 turns them
GREEN.

Purpose: Nyquist Dimension 8 — each requirement maps to an automated verify that exists
before the implementation, so the GREEN plan has a fixed target it cannot fake.
Output: `test_meta_signature.py`, `test_meta_roundtrip.py`, `test_meta_stream.py`,
`test_meta_unsupported.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@.claude/skills/adbc-poolhouse-docs-author/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-async-metadata/34-RESEARCH.md
@.planning/phases/34-async-metadata/34-PATTERNS.md
@.planning/phases/34-async-metadata/34-VALIDATION.md

# Test analogs to clone (read these — they are the templates):
@tests/async/test_ingest_signature.py
@tests/async/test_ingest_roundtrip.py
@tests/async/test_reader_lifetime.py
@tests/async/conftest.py
</context>

<artifacts_this_phase_produces>
Phase 34 (across plans 01–03) produces these new symbols. This plan creates the RED tests
that pin the first six:
- `AsyncConnection.adbc_get_info` — `-> dict[str | int, Any]` (value)
- `AsyncConnection.adbc_get_table_schema` — `-> pyarrow.Schema` (value, kw filters)
- `AsyncConnection.adbc_get_table_types` — `-> list[str]` (value)
- `AsyncConnection.adbc_get_objects` — `-> AsyncRecordBatchReader` (stream)
- `AsyncConnection.adbc_get_statistics` — `-> AsyncRecordBatchReader` (stream)
- `AsyncConnection.adbc_get_statistic_names` — `-> AsyncRecordBatchReader` (stream)
- `_SyncConnection` Protocol (typing bridge) — plan 34-02
- `_noop_cancel` module function — plan 34-02
- Four new test files (`tests/async/test_meta_signature.py`, `test_meta_roundtrip.py`,
  `test_meta_stream.py`, `test_meta_unsupported.py`) — this plan
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Signature RED test (test_meta_signature.py)</name>
  <files>tests/async/test_meta_signature.py</files>
  <read_first>
    - tests/async/test_ingest_signature.py (whole file, 77 lines) — the exact clone target:
      `_KEYWORD_ONLY` / `_POSITIONAL` constants, the three `inspect.signature` loop tests, the
      `hasattr` existence test, and the `if __name__ == "__main__"` tail.
    - 34-RESEARCH.md §Standard Stack lines 84–96 (verified ADBC signatures) and §Concrete test
      designs line 398.
    - 34-PATTERNS.md "test_meta_signature.py" section (lines 184–194).
  </read_first>
  <action>
    Clone `test_ingest_signature.py`. Import target is
    `from adbc_poolhouse._async._connection import AsyncConnection`. This is a SYNC
    introspection test file — carry NO `@pytest.mark.anyio` (mirror `test_ingest_signature.py`,
    which has none) and use no fixtures.

    Assert `hasattr(AsyncConnection, name)` for all six methods: `adbc_get_info`,
    `adbc_get_objects`, `adbc_get_table_schema`, `adbc_get_table_types`, `adbc_get_statistics`,
    `adbc_get_statistic_names`.

    Assert keyword-only / positional shape with `inspect.signature`, per the verified signatures
    in 34-RESEARCH §Standard Stack:
    - `adbc_get_objects` params `depth`, `catalog_filter`, `db_schema_filter`,
      `table_name_filter`, `table_types_filter`, `column_name_filter` are all `KEYWORD_ONLY`.
    - `adbc_get_table_schema`: `table_name` is positional (NOT keyword-only); `catalog_filter`
      and `db_schema_filter` are `KEYWORD_ONLY`.
    - `adbc_get_statistics` params `catalog_filter`, `db_schema_filter`, `table_name_filter`,
      `approximate` are all `KEYWORD_ONLY`.
    - `adbc_get_info`, `adbc_get_table_types`, `adbc_get_statistic_names` take no params beyond
      `self`.
    Also assert `adbc_get_objects` `depth` default is `"all"`.

    This encodes META-01 at the signature level; it FAILS RED now because the methods do not
    exist yet (`hasattr` returns False → `inspect.signature` on a missing attribute raises).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/async/test_meta_signature.py -q; test $? -ne 0 && echo "RED-as-expected (methods not yet defined)"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/async/test_meta_signature.py` exists and collects without a collection error.
    - Running it FAILS (RED) with an `AssertionError`/`AttributeError` referencing a missing
      `adbc_get_*` method — the expected pre-implementation signal.
    - File carries no `@pytest.mark.anyio` and no `import asyncio`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Value round-trip + unsupported RED tests (test_meta_roundtrip.py, test_meta_unsupported.py)</name>
  <files>tests/async/test_meta_roundtrip.py, tests/async/test_meta_unsupported.py</files>
  <read_first>
    - tests/async/test_ingest_roundtrip.py (whole file, 98 lines) — the class +
      `@pytest.mark.anyio async def` + `duckdb_async_pool` fixture shape, and the
      `pyarrow` top import.
    - tests/async/test_reader_lifetime.py line 75 — the `duckdb_async_pool._pool.checkedout()
      == 0  # noqa: SLF001` accessor pattern.
    - 34-RESEARCH.md §Concrete test designs lines 395 and 397; §Validation Architecture (both
      unsupported methods VERIFIED to raise `NotSupportedError` on DuckDB).
    - 34-PATTERNS.md "test_meta_roundtrip.py" (lines 198–211) and "test_meta_unsupported.py"
      (lines 234–250).
  </read_first>
  <action>
    Create `test_meta_roundtrip.py` (META-01/02, locked decision #4). Clone the
    `test_ingest_roundtrip.py` class + `@pytest.mark.anyio` + `duckdb_async_pool` shape. Inside
    one `async with await duckdb_async_pool.connect() as conn:` scope:
    - `await conn.adbc_get_info()` → assert `isinstance(result, dict)`.
    - `await conn.adbc_get_table_types()` → assert `isinstance(result, list)`.
    - Create a table first (a cursor `await cur.execute("CREATE TABLE t (...)")` or an
      `adbc_ingest`, mirroring `test_ingest_roundtrip.py`), then
      `await conn.adbc_get_table_schema("t")` → assert `isinstance(result, pyarrow.Schema)` and
      the field names match the created columns.
    After the scope exits, assert `duckdb_async_pool._pool.checkedout() == 0` (with the
    `# noqa: SLF001` accessor) to prove clean check-in. Import `pyarrow` at top.

    Create `test_meta_unsupported.py` (META-03, locked decision #6). Import the native error name
    verified in RESEARCH: `from adbc_driver_manager import NotSupportedError` (use the exact
    attribute `adbc_driver_manager.NotSupportedError` if the direct import is unavailable). In a
    `duckdb_async_pool` connection scope, assert
    `with pytest.raises(NotSupportedError): await conn.adbc_get_statistics()` and the same for
    `await conn.adbc_get_statistic_names()`. After each, assert `checkedout() == 0` to prove the
    failed call still checks in cleanly. Every async test carries `@pytest.mark.anyio`.

    Both files FAIL RED now (`AttributeError` — methods not yet defined).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/async/test_meta_roundtrip.py tests/async/test_meta_unsupported.py -q; test $? -ne 0 && echo "RED-as-expected"</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist and collect cleanly (no `import asyncio`, no positive-duration `sleep`).
    - `test_meta_roundtrip.py` asserts `dict` / `list` / `pyarrow.Schema` return types and
      post-scope `checkedout() == 0`.
    - `test_meta_unsupported.py` uses `pytest.raises(NotSupportedError)` for both statistics
      methods and asserts `checkedout() == 0` after.
    - Running both FAILS RED with `AttributeError` for the missing `adbc_get_*` methods.
    - Every `async def test_*` carries `@pytest.mark.anyio`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Streaming RED test (test_meta_stream.py)</name>
  <files>tests/async/test_meta_stream.py</files>
  <read_first>
    - tests/async/test_reader_lifetime.py lines 54–127 (the DuckDB `TestStream04DrainThenCheckin`
      + read-after-close legs) — the `async with await ... as reader:` / `async for batch in
      reader:` drain pattern and the `checkedout() == 0` accessor. Do NOT copy the Snowflake
      `TestEdge33Snowflake` legs (Pitfall 2: the cassette cannot replay metadata).
    - 34-RESEARCH.md §Concrete test designs line 396 and Open Question 1 (the optional busy-guard
      leg recommendation).
    - 34-PATTERNS.md "test_meta_stream.py" (lines 215–230).
  </read_first>
  <action>
    Create `test_meta_stream.py` (META-02 streaming, locked decision #1). In a `duckdb_async_pool`
    connection scope, drive the streaming metadata reader directly off the connection (no
    `execute` needed — `adbc_get_objects` is connection metadata):
    - `async with await conn.adbc_get_objects(depth="tables") as reader:` — assert
      `isinstance(reader.schema, pyarrow.Schema)` (schema is a sync property passthrough), then
      `async for batch in reader:` and assert each `batch` is a `pyarrow.RecordBatch`. Drain fully.
    - After the scope exits, assert `duckdb_async_pool._pool.checkedout() == 0`.
    Add the optional busy-guard leg (locked decision #5, ~10 lines, Open Question 1): while a
    metadata reader is live inside the scope, a foreign `await conn.commit()` raises
    `ConnectionBusyError` (import from `adbc_poolhouse`). This locks the two-tier `_reader_open`
    guarantee cheaply.

    Every async test carries `@pytest.mark.anyio`. Import `pyarrow` at top. The test FAILS RED now
    (`AttributeError` — `adbc_get_objects` not yet defined).

    Note the carried-forward loop-stability gotcha: a streaming leg is exercised under
    `ADBC_ASYNC_REPEAT=20` at wave merge (plan 34-02), so keep the drain body loop-safe (no shared
    mutable state across iterations).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/async/test_meta_stream.py -q; test $? -ne 0 && echo "RED-as-expected"; .venv/bin/pytest tests/async/test_meta_guard.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/async/test_meta_stream.py` exists and collects cleanly.
    - It drains `adbc_get_objects(depth="tables")` via `async for`, asserts `reader.schema` is a
      `pyarrow.Schema` and each batch is a `pyarrow.RecordBatch`, and asserts post-scope
      `checkedout() == 0`.
    - Includes the busy-guard leg asserting a foreign `await conn.commit()` while the reader is
      live raises `ConnectionBusyError`.
    - Running it FAILS RED with `AttributeError` for `adbc_get_objects`.
    - `test_meta_guard.py` STILL PASSES over the whole `tests/async/` package with the four new
      files present (proves discipline: anyio marker, no `import asyncio`, no positive `sleep`).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| async task → AsyncConnection | Caller supplies `table_name` / `*_filter` strings (this plan only encodes tests; no production surface added) |
| offload worker → sync ADBC driver | The blocking C metadata call runs off-loop; the driver owns identifier handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Tampering | `table_name` / `*_filter` args to `adbc_get_*` | accept | Tests only forward literals to DuckDB; identifiers are the driver's responsibility (same contract as `adbc_ingest`). No new SQL construction, no sanitization added |
| T-34-02 | Denial of Service | connection left busy after a failed/unsupported metadata call | mitigate | `test_meta_unsupported.py` asserts `checkedout() == 0` after a native error; `test_meta_stream.py` asserts clean check-in — proving the `_offloading()` guard releases the connection |
| T-34-SC | Tampering | package installs | accept (N/A) | No package installs this plan (all symbols ship in `adbc_driver_manager` 1.11.0 + existing dev deps); no legitimacy checkpoint required |
</threat_model>

<verification>
- `.venv/bin/pytest tests/async/test_meta_signature.py tests/async/test_meta_roundtrip.py tests/async/test_meta_stream.py tests/async/test_meta_unsupported.py -q` — all four files collect and FAIL RED (missing `adbc_get_*` methods), the pre-implementation signal.
- `.venv/bin/pytest tests/async/test_meta_guard.py -x -q` — PASSES: the four new files observe the `tests/async/` discipline.
</verification>

<success_criteria>
- Four new test files exist under `tests/async/`, encoding META-01 (signature), META-01/02
  (value round-trip), META-02 (streaming), and META-03 (native error).
- Each fails RED for the right reason (methods not yet defined), so plan 34-02 has a fixed GREEN
  target.
- `test_meta_guard.py` still passes, certifying discipline.
</success_criteria>

<output>
Create `.planning/phases/34-async-metadata/34-01-SUMMARY.md` when done.
</output>
