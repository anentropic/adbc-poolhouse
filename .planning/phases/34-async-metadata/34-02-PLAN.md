---
phase: 34-async-metadata
plan: 02
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - src/adbc_poolhouse/_async/_connection.py
autonomous: true
requirements: [META-01, META-02, META-03]
must_haves:
  truths:
    - "All six adbc_get_* methods are awaitable on AsyncConnection, each a plain non-cancellable offload through the pool CapacityLimiter (META-01, locked decision #2 — mirror commit/rollback, NOT cancellable_offload)"
    - "adbc_get_info->dict, adbc_get_table_types->list[str], adbc_get_table_schema->pyarrow.Schema (META-02, locked decision #4)"
    - "adbc_get_objects / adbc_get_statistics / adbc_get_statistic_names return an AsyncRecordBatchReader wrapping the native sync reader without eager materialization (META-02, locked decision #1)"
    - "The connection is reached via the SQLAlchemy fairy cast to a _SyncConnection Protocol — self._fairy, no driver_connection unwrap (locked decision #3)"
    - "Each method brackets its offload with self._offloading(); the streaming trio set self._reader_open = True AFTER the span on the success path only (locked decision #5)"
    - "An unsupported backend surfaces the driver's native NotSupportedError unchanged — no new error types, no find_spec pre-check (META-03, locked decision #6)"
    - "basedpyright strict over _connection.py reports 0 errors and the async import-lint guard stays clean (plain offload only, no bare to_thread)"
  artifacts:
    - path: "src/adbc_poolhouse/_async/_connection.py"
      provides: "_SyncConnection Protocol, _noop_cancel, and the six adbc_get_* methods with Google-style docstrings"
      contains: "class _SyncConnection"
  key_links:
    - from: "AsyncConnection.adbc_get_*"
      to: "self._fairy (cast _SyncConnection)"
      via: "offload / functools.partial through self._limiter"
      pattern: "cast\\(\"_SyncConnection\", self._fairy\\)"
    - from: "AsyncConnection.adbc_get_objects"
      to: "AsyncRecordBatchReader"
      via: "wrap sync reader with _noop_cancel after _reader_open = True"
      pattern: "AsyncRecordBatchReader\\(.*_noop_cancel\\)"
---

<objective>
Implement the six async connection-metadata methods on `AsyncConnection` as offload wrappers over
the sync ADBC `dbapi.Connection` reached through the SQLAlchemy fairy. Add the `_SyncConnection`
typing-bridge Protocol and the module-level `_noop_cancel`. Each method carries a full Google-style
docstring (Args/Returns/Raises + `Example:`). This turns the plan 34-01 RED tests GREEN.

Purpose: Close the metadata half of the async/sync parity gap (META-01/02/03). The phase is
assembly, not invention — every method clones a verified in-repo template (`commit`/`rollback` for
the value trio, `fetch_record_batch` for the streaming trio).
Output: Modified `src/adbc_poolhouse/_async/_connection.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@.claude/skills/adbc-poolhouse-docs-author/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-async-metadata/34-RESEARCH.md
@.planning/phases/34-async-metadata/34-PATTERNS.md

# The file being modified + the two clone templates (read these):
@src/adbc_poolhouse/_async/_connection.py
@src/adbc_poolhouse/_async/_cursor.py
@src/adbc_poolhouse/_async/_reader.py
</context>

<artifacts_this_phase_produces>
This plan produces the production symbols:
- `_SyncConnection` Protocol (`_connection.py`) — structural typing bridge, mirrors `_SyncCursor`
- `_noop_cancel` module function (`_connection.py`) — no-op cancel hook for cursor-less readers
- `AsyncConnection.adbc_get_info` → `dict[str | int, Any]`
- `AsyncConnection.adbc_get_table_types` → `list[str]`
- `AsyncConnection.adbc_get_table_schema` → `pyarrow.Schema`
- `AsyncConnection.adbc_get_objects` → `AsyncRecordBatchReader`
- `AsyncConnection.adbc_get_statistics` → `AsyncRecordBatchReader`
- `AsyncConnection.adbc_get_statistic_names` → `AsyncRecordBatchReader`
</artifacts_this_phase_produces>

<interface_contracts>
Verified ADBC signatures (from 34-RESEARCH §Standard Stack lines 84–96, introspected from
`adbc_driver_manager` 1.11.0) — the async methods mirror these exactly:
- `adbc_get_info(self) -> dict[str | int, Any]`
- `adbc_get_objects(self, *, depth: Literal["all","catalogs","db_schemas","tables","columns"] = "all", catalog_filter: str | None = None, db_schema_filter: str | None = None, table_name_filter: str | None = None, table_types_filter: list[str] | None = None, column_name_filter: str | None = None) -> pyarrow.RecordBatchReader`
- `adbc_get_table_schema(self, table_name: str, *, catalog_filter: str | None = None, db_schema_filter: str | None = None) -> pyarrow.Schema`
- `adbc_get_table_types(self) -> list[str]`
- `adbc_get_statistics(self, *, catalog_filter: str | None = None, db_schema_filter: str | None = None, table_name_filter: str | None = None, approximate: bool = True) -> pyarrow.RecordBatchReader`
- `adbc_get_statistic_names(self) -> pyarrow.RecordBatchReader`
The public async streaming trio return `AsyncRecordBatchReader` (wrapping the sync reader), NOT the
raw `pyarrow.RecordBatchReader`.
</interface_contracts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Typing bridge, no-op cancel, and the value-returning trio</name>
  <files>src/adbc_poolhouse/_async/_connection.py</files>
  <behavior>
    - `await conn.adbc_get_info()` returns a `dict` (test_meta_roundtrip.py).
    - `await conn.adbc_get_table_types()` returns a `list`.
    - `await conn.adbc_get_table_schema("t")` returns a `pyarrow.Schema` with matching field names.
    - After the connection scope exits, `pool.checkedout() == 0`.
    - basedpyright strict over `_connection.py` reports 0 errors.
  </behavior>
  <read_first>
    - _connection.py lines 245–284 (`commit` / `rollback`) — the exact value-method template
      (`with self._offloading(): await offload(self._fairy.<m>, limiter=self._limiter)`) and the
      non-cancellable-caveat docstring wording at lines 253–259.
    - _cursor.py lines 58–95 (`_SyncCursor` Protocol) — the structural-Protocol idiom to mirror.
    - _cursor.py lines 550–564 (`adbc_ingest` `functools.partial`-through-offload) — the kw-arg
      forwarding boundary for `adbc_get_table_schema`.
    - _cursor.py lines 489–548 (`adbc_ingest` docstring) — the Google-style Markdown Args/Returns/
      Raises + singular `Example:` fenced python code block (triple-backtick python) to match.
    - 34-PATTERNS.md lines 28–124 (Protocol shape, cast idiom, imports to add, value-method clone).
  </read_first>
  <action>
    Add imports to `_connection.py` per 34-PATTERNS lines 73–79: `functools` (runtime); `Any` and
    `Literal` (add to the `typing` imports — `Literal` may sit under `TYPE_CHECKING` if only used in
    annotations, but the Protocol needs it importable for the signature); a runtime
    `from adbc_poolhouse._async._reader import AsyncRecordBatchReader`; and `pyarrow` under
    `TYPE_CHECKING`. `offload` and `cast` are ALREADY imported (lines 43, 38). Do NOT import
    `cancellable_offload` — locked decision #2 uses plain `offload` only.

    Add the `_SyncConnection` Protocol at module level (mirror `_SyncCursor` idiom, shape from
    34-PATTERNS lines 48–61 / 34-RESEARCH §Pattern 3): declare all six methods with the precise
    signatures in `<interface_contracts>`. It is driver-agnostic and declared (not imported).

    Add the module-level `_noop_cancel() -> None` function (analog: `_pull` in `_reader.py` lines
    96–121 — a module-level function keeps the `cancellable_offload` `TypeVarTuple` arity and the
    `scan_async_package` source-guard matcher clean; RESEARCH §Alternatives line 114). Give it a
    one-line docstring noting it is the no-op cancel hook for connection-level metadata readers
    (no `adbc_cancel` exists on a connection).

    Implement the three value-returning methods on `AsyncConnection`, cloning `commit` exactly
    (locked decision #3/#5). At the top of each: `sync_conn = cast("_SyncConnection", self._fairy)`.
    - `adbc_get_info(self) -> dict[str | int, Any]`: `with self._offloading(): return await
      offload(sync_conn.adbc_get_info, limiter=self._limiter)`.
    - `adbc_get_table_types(self) -> list[str]`: same shape, `sync_conn.adbc_get_table_types`.
    - `adbc_get_table_schema(self, table_name: str, *, catalog_filter=None, db_schema_filter=None)
      -> pyarrow.Schema`: forward through `functools.partial(sync_conn.adbc_get_table_schema,
      table_name, catalog_filter=catalog_filter, db_schema_filter=db_schema_filter)` inside plain
      `offload` — copy the partial boundary from `adbc_ingest` (lines 550–564) but with plain
      `offload`, NO `cancellable_offload`, NO `on_abort`.

    Give each method a full Google-style Markdown docstring (Args/Returns/Raises + singular
    `Example:` with a fenced python code block (triple-backtick python)), matching `adbc_ingest`. Document the
    non-cancellable caveat by copying the `commit` wording (lines 253–259): the call is not
    cooperatively cancellable; a surrounding timeout/cancel cannot abort an in-flight metadata
    call. Use Markdown role syntax only (`` `adbc_get_info` ``), never RST `:func:` (MEMORY.md).
    Add a `Raises: ConnectionBusyError` line (mirrors `commit`).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/async/test_meta_roundtrip.py -x -q && .venv/bin/basedpyright src/adbc_poolhouse/_async/_connection.py</automated>
  </verify>
  <acceptance_criteria>
    - `_connection.py` defines `class _SyncConnection(Protocol)` with all six method signatures and
      a module-level `_noop_cancel() -> None`.
    - `adbc_get_info`, `adbc_get_table_types`, `adbc_get_table_schema` exist on `AsyncConnection`,
      each using plain `offload` inside `with self._offloading():` (no `cancellable_offload`).
    - `test_meta_roundtrip.py` passes (GREEN) under both asyncio and trio.
    - `.venv/bin/basedpyright src/adbc_poolhouse/_async/_connection.py` reports 0 errors.
    - Each new method has a Google-style Markdown docstring with Args/Returns/Raises + singular
      `Example:` block; no RST `:role:` syntax.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The streaming trio (get_objects / get_statistics / get_statistic_names)</name>
  <files>src/adbc_poolhouse/_async/_connection.py</files>
  <behavior>
    - `async with await conn.adbc_get_objects(depth="tables") as reader:` yields `pyarrow.RecordBatch`
      chunks; `reader.schema` is a `pyarrow.Schema`; the pool drains to `checkedout() == 0`.
    - A foreign `await conn.commit()` while the reader is live raises `ConnectionBusyError`.
    - `await conn.adbc_get_statistics()` and `await conn.adbc_get_statistic_names()` raise the
      driver's native `NotSupportedError` unchanged on DuckDB, and `checkedout() == 0` after.
    - `inspect.signature` kw-only/positional shape matches (test_meta_signature.py).
  </behavior>
  <read_first>
    - _cursor.py lines 566–626 (`fetch_record_batch`) — the streaming template: the `_offloading()`
      span, the post-span `_reader_open = True` on the success path only (comment at 614–618), and
      the `AsyncRecordBatchReader(...)` construction. Also its docstring (lines 566–605) for the
      reader-lifetime-lock wording.
    - _reader.py lines 162–193 (`AsyncRecordBatchReader.__init__`) — the 4-arg constructor contract
      (`sync_reader, limiter, owner, adbc_cancel`); the 4th arg is invoked ONLY on the cancel path,
      so a no-op is harmless (Pitfall 1).
    - 34-PATTERNS.md lines 128–166 (streaming clone + the four Phase-34 adaptations) and 34-RESEARCH
      §Pattern 2 (lines 197–224) + Pitfalls 1 and 3.
  </read_first>
  <action>
    Implement the three streaming methods on `AsyncConnection`, cloning `fetch_record_batch` with
    the four Phase-34 adaptations (34-PATTERNS lines 148–157):
    1. The method lives on the connection, so use `self._offloading()` and `self._reader_open`
       directly (no `self._owner.` indirection — the connection IS the owner).
    2. Replace `cancellable_offload(self._adbc_cancel, fn, ..., on_abort=...)` with PLAIN
       `offload(fn, limiter=self._limiter)` — the connection has no `adbc_cancel` (locked decision
       #2). Do NOT pass `on_abort`.
    3. Wrap the driver call in `functools.partial` when it has kw-only filters:
       - `adbc_get_objects(self, *, depth="all", catalog_filter=None, db_schema_filter=None,
         table_name_filter=None, table_types_filter=None, column_name_filter=None) ->
         AsyncRecordBatchReader` — partial forwards all six kw args.
       - `adbc_get_statistics(self, *, catalog_filter=None, db_schema_filter=None,
         table_name_filter=None, approximate=True) -> AsyncRecordBatchReader` — partial forwards the
         four kw args.
       - `adbc_get_statistic_names(self) -> AsyncRecordBatchReader` — a bare bound method, NO partial.
       `depth` is a `Literal["all","catalogs","db_schemas","tables","columns"]`.
    4. Construct and return `AsyncRecordBatchReader(sync_reader, self._limiter, self, _noop_cancel)`
       — the 4th positional arg is the module-level `_noop_cancel` from task 1, NOT a cursor cancel.

    `_reader_open` timing is load-bearing (Pitfall 3): set `self._reader_open = True` AFTER the
    `with self._offloading():` block exits, on the success path ONLY — never inside the span (the
    span clears `_in_use`; `_reader_open` is owned by `reader.close()`/checkin). Copy the rationale
    comment from `_cursor.py` lines 614–618. At the top of each method:
    `sync_conn = cast("_SyncConnection", self._fairy)`.

    Give each method a full Google-style Markdown docstring (Args/Returns/Raises + singular
    `Example:` fenced python code block (triple-backtick python) showing the `async with await conn.adbc_get_objects(...) as
    reader: async for batch in reader:` usage). Document BOTH caveats: the non-cancellable creation
    (commit/rollback wording) AND the reader-lifetime lock (copy `fetch_record_batch` docstring
    wording, `_cursor.py` lines 574–588) plus the per-pull-invalidate asymmetry from Pitfall 1 / A2
    (a cancelled pull invalidates the connection, inherited from the shared reader). Add
    `Raises: ConnectionBusyError`.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/async/test_meta_signature.py tests/async/test_meta_stream.py tests/async/test_meta_unsupported.py -x -q && .venv/bin/basedpyright src/adbc_poolhouse/_async/_connection.py</automated>
  </verify>
  <acceptance_criteria>
    - `adbc_get_objects`, `adbc_get_statistics`, `adbc_get_statistic_names` exist on
      `AsyncConnection`, each using plain `offload` inside `with self._offloading():` and returning
      `AsyncRecordBatchReader(sync_reader, self._limiter, self, _noop_cancel)`.
    - `self._reader_open = True` is set AFTER the `_offloading()` span, success-path only.
    - `test_meta_signature.py`, `test_meta_stream.py`, `test_meta_unsupported.py` all pass (GREEN)
      under both asyncio and trio.
    - `.venv/bin/basedpyright src/adbc_poolhouse/_async/_connection.py` reports 0 errors.
    - Each streaming docstring documents the non-cancellable caveat AND the reader-lifetime lock,
      in Google-style Markdown with a singular `Example:` block.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| async task → AsyncConnection | Caller supplies `table_name` / `*_filter` strings forwarded to the driver untouched |
| offload worker → sync ADBC driver (via fairy `__getattr__`) | The blocking C metadata call runs off-loop under the pool limiter; the driver owns identifier handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Tampering | `table_name` / `*_filter` args forwarded to `adbc_get_*` | accept | Forwarded to the driver unchanged, exactly like `adbc_ingest`; poolhouse constructs no SQL and does not sanitize. Documented in the method docstrings (`table_name` passed straight to the driver). No new deserialization or input surface |
| T-34-02 | Denial of Service | connection left busy/poisoned after a cancelled or unsupported metadata call | mitigate | Every method brackets its offload in `self._offloading()`; the streaming trio's cancel path reuses the shared `AsyncRecordBatchReader` shielded `invalidate`, keeping `pool.checkedout()` correct. Proven by `test_meta_unsupported.py` (`checkedout() == 0` after a native error) and `test_meta_stream.py` clean drain |
| T-34-03 | Information Disclosure | metadata leaks catalog/schema names | accept | Metadata visibility is the driver's / warehouse's access-control concern, unchanged by the async wrapper (ASVS V4 N/A — see 34-RESEARCH §Security Domain) |
| T-34-SC | Tampering | package installs | accept (N/A) | No package installs this phase — all symbols ship in `adbc_driver_manager` 1.11.0; no new deps/extras. No legitimacy checkpoint required |
</threat_model>

<verification>
- `.venv/bin/pytest tests/async/test_meta_signature.py tests/async/test_meta_roundtrip.py tests/async/test_meta_stream.py tests/async/test_meta_unsupported.py -x -q` — all GREEN under asyncio and trio.
- `.venv/bin/basedpyright src/adbc_poolhouse/_async/_connection.py` — 0 errors (strict).
- Wave-merge loop gate (carried-forward gotcha — a streaming leg was added):
  `ADBC_ASYNC_REPEAT=20 .venv/bin/pytest tests/async -q` — 0 hangs.
- `.venv/bin/pytest tests/async/test_meta_guard.py -x -q` and the async import-lint guard — clean
  (`_connection.py` uses plain `offload` only, no bare `to_thread`, no `import asyncio`).
</verification>

<success_criteria>
- Six `adbc_get_*` methods are awaitable on `AsyncConnection`, each an offload wrapper over the sync
  fairy method routed through the pool `CapacityLimiter` (META-01).
- Return types mirror the sync methods: dict / list / `pyarrow.Schema` / wrapped
  `AsyncRecordBatchReader` (no eager materialization) (META-02).
- An unsupported backend surfaces the native `NotSupportedError` unchanged (META-03).
- basedpyright strict clean; guard-clean; full async suite green including the ×20 loop gate.
</success_criteria>

<output>
Create `.planning/phases/34-async-metadata/34-02-SUMMARY.md` when done.
</output>
