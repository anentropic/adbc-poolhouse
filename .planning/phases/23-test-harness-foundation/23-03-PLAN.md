---
phase: 23-test-harness-foundation
plan: 03
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - tests/_async_harness/guard.py
  - tests/test_async_guard.py
autonomous: true
requirements: [TEST-05]
must_haves:
  truths:
    - "scan_async_package(root) is an AST/source-scan callable returning a list[Finding] a later EDGE test can assert is empty — per D-05"
    - "The guard flags `import asyncio` / `from asyncio import …` and bare `to_thread.run_sync(...)` calls lacking a `limiter=` keyword, scoped to the configurable path — per D-05"
    - "scan_async_package no-ops gracefully (returns []) on an absent or empty directory, because src/adbc_poolhouse/_async/ does not exist until Phase 24 — per D-05"
    - "A `to_thread.run_sync(..., limiter=L)` call is NOT flagged; the documented alias-limitation (run_sync imported under an alias) is asserted as a known gap, not a bug"
  artifacts:
    - path: "tests/_async_harness/guard.py"
      provides: "Finding dataclass + _GuardVisitor + scan_async_package callable (HARD CONTRACT for Phases 24/25/27 meta-guards)"
      contains: "def scan_async_package"
    - path: "tests/test_async_guard.py"
      provides: "Sync self-tests against synthetic source strings (no real _async/ scan)"
      contains: "scan_async_package"
  key_links:
    - from: "tests/test_async_guard.py"
      to: "tests/_async_harness/guard.py"
      via: "writes synthetic .py into tmp_path then asserts findings"
      pattern: "scan_async_package\\("
---

<objective>
Build the source-scan / import-lint guard (D-05): a pure-stdlib `ast.NodeVisitor`-based `scan_async_package(root) -> list[Finding]` callable that bans `import asyncio` and bare `to_thread.run_sync(...)` without an explicit `limiter=`, scoped to a configurable path, no-opping gracefully when that path is absent. Ship it WITH its sync self-tests (synthetic source strings via `tmp_path`) since these tests need no anyio machinery and can run independently.

Purpose: This guard is the callable the EDGE suite (Phases 24/25/27 meta-guards: EDGE-25/27/28) asserts returns an empty list against the real `_async/` package. Phase 23 ships only the callable + its fixture self-tests — the real `src/adbc_poolhouse/_async/` does not exist until Phase 24, so the guard must no-op on its absence and be proven against synthetic sources instead.
Output: `tests/_async_harness/guard.py`, `tests/test_async_guard.py`. Fully independent of Plan 02 (no file overlap, no anyio) — runs in parallel.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@.claude/skills/adbc-poolhouse-docs-author/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-test-harness-foundation/23-CONTEXT.md
@.planning/phases/23-test-harness-foundation/23-RESEARCH.md
@.planning/phases/23-test-harness-foundation/23-PATTERNS.md

# Analogs to mirror
@benchmarks/_harness.py
@tests/integration/conftest.py
@tests/test_benchmarks_harness.py

# Foundation this plan builds on
@.planning/phases/23-test-harness-foundation/23-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: scan_async_package AST guard (D-05)</name>
  <files>tests/_async_harness/guard.py</files>
  <behavior>
    - `import asyncio` and `import asyncio.tasks` → one Finding each, rule "banned-asyncio-import".
    - `from asyncio import sleep` (and `from asyncio.x import …`) → Finding, rule "banned-asyncio-import".
    - `to_thread.run_sync(fn)` and `anyio.to_thread.run_sync(fn)` WITHOUT a `limiter=` kwarg → Finding, rule "to_thread-without-limiter".
    - `to_thread.run_sync(fn, limiter=L)` → NO finding.
    - `scan_async_package(nonexistent_path)` → returns `[]` (graceful no-op).
    - `scan_async_package(empty_dir)` → returns `[]`.
    - Each Finding carries path, lineno, rule, message.
  </behavior>
  <read_first>
    - 23-RESEARCH.md Pattern 4 lines 323-391 (the FULL guard: `Finding` frozen dataclass, `_GuardVisitor` with visit_Import/visit_ImportFrom/visit_Call, `_is_to_thread_run_sync`, `scan_async_package` with the absent-dir no-op — copy verbatim)
    - 23-RESEARCH.md Pattern 4 line 391 + Pitfall 3 (the accepted alias-limitation: `from anyio.to_thread import run_sync as rs; rs(...)` is NOT caught — document it)
    - 23-PATTERNS.md §guard.py lines 126-155 (style analog + the path-exists idiom from tests/integration/conftest.py lines 19-24)
  </read_first>
  <action>
    Create `tests/_async_harness/guard.py` with the header `from __future__ import annotations`, `import ast`, `from dataclasses import dataclass`, `from pathlib import Path`, and a module docstring stating it is the pure-stdlib (no third-party) source-scan guard (mirror `benchmarks/_harness.py` "deliberately X-free" framing).

    Implement verbatim from RESEARCH Pattern 4: the frozen `Finding` dataclass (`path: str`, `lineno: int`, `rule: str`, `message: str`); `_GuardVisitor(ast.NodeVisitor)` with `visit_Import` / `visit_ImportFrom` (ban `asyncio` and `asyncio.*`) and `visit_Call` (flag `to_thread.run_sync(...)` when no `limiter=` kwarg is present), plus the static `_is_to_thread_run_sync` matching the attribute-chain tail so both `anyio.to_thread.run_sync` and `to_thread.run_sync` are caught; and the public `scan_async_package(root: str | Path) -> list[Finding]` that returns `[]` when `root` does not exist (D-05 graceful no-op — mirror the `tests/integration/conftest.py` `.exists()`-then-act idiom), else `ast.parse`s each `*.py` under `root.rglob` and aggregates findings.

    `scan_async_package` is a key entry point → Google-style docstring with Args/Returns and an `Example:` (singular) block. Document the accepted alias-limitation (a fully-aliased `run_sync` re-import slips past — RESEARCH Pitfall 3) in the docstring. Markdown not RST, basedpyright-strict clean, fully typed.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import ast; src=open('tests/_async_harness/guard.py').read(); t=ast.parse(src); fns={n.name for n in ast.walk(t) if isinstance(n,(ast.FunctionDef,ast.ClassDef))}; assert {'scan_async_package','Finding','_GuardVisitor'} <= fns, fns; print('ok')" && .venv/bin/basedpyright tests/_async_harness/guard.py 2>&1 | tail -3</automated>
  </verify>
  <done>`scan_async_package`, `Finding`, `_GuardVisitor` defined; pure-stdlib; basedpyright-strict clean; absent-dir no-op present.</done>
  <acceptance_criteria>
    - `tests/_async_harness/guard.py` defines `Finding`, `_GuardVisitor`, `scan_async_package`
    - Bans `import asyncio` / `from asyncio import …` and bare `to_thread.run_sync` without `limiter=`
    - Returns `[]` for an absent path; `scan_async_package` has an `Example:` docstring + documented alias-limitation
    - Pure stdlib (no third-party import); basedpyright strict passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Sync self-tests against synthetic source strings (D-05)</name>
  <files>tests/test_async_guard.py</files>
  <read_first>
    - 23-RESEARCH.md §Validation Architecture rows: test_guard_bans_asyncio, test_guard_to_thread_limiter, test_guard_noop_absent_dir (lines 553-555)
    - 23-RESEARCH.md Pattern 4 line 391 ("feed synthetic source strings via a tmp_path fixture writing .py files … do NOT scan the real (empty) _async/")
    - 23-PATTERNS.md §test file analog — tests/test_benchmarks_harness.py (class TestX grouping, one-line behaviour docstrings, synthetic inputs only)
  </read_first>
  <action>
    Create `tests/test_async_guard.py` — PLAIN sync tests (NOT `@pytest.mark.anyio`; the guard is stdlib-only and needs no event loop), grouped under a `class TestAsyncGuard:` with one-line behaviour docstrings per method (mirror `tests/test_benchmarks_harness.py`). Use the `tmp_path` fixture to write synthetic `.py` files, then call `scan_async_package(tmp_path)` and assert on the returned findings:

    - `test_bans_asyncio_import`: writing `import asyncio` yields a finding with rule `banned-asyncio-import`; `from asyncio import sleep` likewise.
    - `test_to_thread_without_limiter_flagged`: `anyio.to_thread.run_sync(fn)` yields a `to_thread-without-limiter` finding; `to_thread.run_sync(fn, limiter=L)` yields NO finding (assert empty for the compliant form).
    - `test_noop_absent_dir`: `scan_async_package(tmp_path / "does_not_exist")` returns `[]`.
    - `test_noop_empty_dir`: scanning an empty dir returns `[]`.
    - `test_alias_limitation_documented`: assert the known gap — a fully-aliased `from anyio.to_thread import run_sync as rs; rs(fn)` is NOT flagged — so the limitation is locked by a test as expected behaviour (RESEARCH Pitfall 3), not silently regressed.

    Module docstring: state these exercise the guard on synthetic source strings only, no real `_async/` scan. basedpyright-strict clean, fully typed.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_async_guard.py -q 2>&1 | tail -10</automated>
  </verify>
  <done>All guard self-tests pass; the compliant `limiter=` form produces zero findings; absent/empty dir returns `[]`; the alias-limitation is asserted as a known gap.</done>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/test_async_guard.py -q` is green
    - Tests cover: asyncio-import ban, to_thread-without-limiter flag, compliant-limiter-clean, absent-dir no-op, empty-dir no-op, alias-limitation gap
    - Tests are plain sync (no `@pytest.mark.anyio`), synthetic sources via `tmp_path`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem path → ast.parse | `scan_async_package(root)` reads `.py` files from a CONFIGURABLE path and parses them. This is the only file-read surface in the phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-06 | Information Disclosure / Tampering | `scan_async_package(root)` reading arbitrary files from a caller-supplied path (path-traversal / arbitrary-file-read) | mitigate | This is the sole (low-severity) item flagged in the security gate. The guard reads source ONLY; it never executes parsed code (`ast.parse`, not `exec`/`import`). In practice callers scope `root` to the repo's `src/adbc_poolhouse/_async/` dir. Document in the docstring that `root` should point at the in-repo async package; no untrusted/remote path is ever passed. Severity: low (test-only tool, repo-scoped, read-only, no code execution). |
| T-23-07 | Denial of Service | a malformed `.py` raising `SyntaxError` in `ast.parse` aborting the scan | accept | Scanned files are the repo's own `_async/` sources (Phase 24+), which must already parse to be importable; a syntax error surfacing as a test failure is the desired signal, not a vulnerability. |
</threat_model>

<verification>
- `tests/_async_harness/guard.py` defines `scan_async_package`/`Finding`/`_GuardVisitor`, pure stdlib, basedpyright-strict clean
- `.venv/bin/pytest tests/test_async_guard.py -q` green: asyncio ban, to_thread-without-limiter, compliant-clean, absent/empty no-op, alias-limitation gap
- The guard reads source only (no `exec`/`import` of scanned files)
</verification>

<success_criteria>
- `scan_async_package` is a callable returning a `list[Finding]` an EDGE test can assert empty, banning `import asyncio` + bare `to_thread.run_sync` (TEST-05 / D-05, ROADMAP criterion 3)
- Graceful no-op on absent/empty dir (the real `_async/` does not exist until Phase 24) (D-05)
- Self-tested against synthetic source strings, including the documented alias-limitation (Pitfall 3)
</success_criteria>

<artifacts_this_phase_produces>
This plan contributes these symbols to the phase's HARD CONTRACT (consumed by Phases 24/25/27 meta-guards EDGE-25/27/28):
- `tests/_async_harness/guard.py::Finding` — frozen dataclass (`path`, `lineno`, `rule`, `message`).
- `tests/_async_harness/guard.py::scan_async_package(root) -> list[Finding]` — the import-lint callable; no-ops on absent/empty `root`. Rules: `banned-asyncio-import`, `to_thread-without-limiter`.
- Accepted limitation (documented): a fully-aliased `run_sync` re-import is not caught; the canonical `anyio.to_thread.run_sync(...)` form IS caught.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/23-test-harness-foundation/23-03-SUMMARY.md` when done.
</output>
