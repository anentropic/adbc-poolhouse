---
phase: 23-test-harness-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - tests/_async_harness/__init__.py
  - tests/_async_harness/conftest.py
autonomous: true
requirements: [TEST-05]
must_haves:
  truths:
    - "anyio, trio, and aiotools are installed in the dev dependency group only (not runtime / not the wheel) — per D-07"
    - "A nested anyio_backend fixture parametrizes async tests over asyncio AND trio, with the trio leg injecting a fresh MockClock(autojump_threshold=0) per test — per D-01/D-06"
    - "The existing sync test suite still collects and passes with no anyio collection errors (the anyio_backend fixture is scoped to tests/_async_harness/, never the root conftest)"
  artifacts:
    - path: "pyproject.toml"
      provides: "anyio>=4.13, trio>=0.31, aiotools>=2.2 in [dependency-groups] dev"
      contains: "aiotools"
    - path: "tests/_async_harness/__init__.py"
      provides: "Package marker for the async harness (test-only, never shipped)"
    - path: "tests/_async_harness/conftest.py"
      provides: "Function-scoped anyio_backend fixture, params asyncio + (trio, {clock: MockClock})"
      contains: "anyio_backend"
  key_links:
    - from: "tests/_async_harness/conftest.py"
      to: "trio.testing.MockClock"
      via: "anyio_backend tuple option for the trio leg"
      pattern: "MockClock\\(autojump_threshold=0\\)"
---

<objective>
Stand up the Wave-0 foundation every other plan in this phase depends on: install the three dev-only async libraries (anyio, trio, aiotools per D-07), create the `tests/_async_harness/` package, and wire the function-scoped `anyio_backend` parametrization (D-06) in a NESTED conftest so the existing sync suite is never pulled under the anyio plugin.

Purpose: The harness modules (stubs/gating/clock/guard) and their dual-backend self-tests cannot exist until anyio/trio/aiotools are installed and the `["asyncio", "trio"]` parametrization is available. This plan is the unblocking foundation — no harness behavior ships here.
Output: Updated `pyproject.toml`, `tests/_async_harness/__init__.py`, `tests/_async_harness/conftest.py`. A passing existing sync suite proves the parametrization is correctly scoped.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@.claude/skills/adbc-poolhouse-docs-author/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-test-harness-foundation/23-CONTEXT.md
@.planning/phases/23-test-harness-foundation/23-RESEARCH.md
@.planning/phases/23-test-harness-foundation/23-PATTERNS.md

# Analogs to mirror
@tests/integration/conftest.py
@tests/conftest.py
@benchmarks/__init__.py
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install anyio, trio, aiotools into the dev group (D-07)</name>
  <files>pyproject.toml</files>
  <read_first>
    - 23-RESEARCH.md §Standard Stack (floors: anyio>=4.13, trio>=0.31, aiotools>=2.2) + §"Installation (dev group only — D-07)"
    - 23-PATTERNS.md §pyproject.toml (modify) — append to the existing `[dependency-groups] dev` list ONLY
    - pyproject.toml lines 44-56 (the existing `dev` list)
  </read_first>
  <action>
    Run `uv add --dev anyio trio aiotools` to add the three libraries to `[dependency-groups] dev` in pyproject.toml (per D-07 — dev/test-only; the runtime `[async]` extra is deferred to Phase 26). Pin floors to match RESEARCH: anyio>=4.13, trio>=0.31, aiotools>=2.2 (edit pyproject.toml after `uv add` if the resolved constraints are looser). Do NOT touch `[project.dependencies]` or `[project.optional-dependencies]` — adding anyio to runtime would contradict the zero-cost-sync-path goal. Do NOT add `anyio_mode = "auto"` to `[tool.pytest.ini_options]` (keep the plugin opt-in via `@pytest.mark.anyio` — per RESEARCH Pitfall 4). If `uv add` triggers a sandbox prompt, prefer editing pyproject.toml directly then `uv sync` per MEMORY uv-sandbox-workarounds.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import anyio, trio, aiotools; print(anyio.__version__, trio.__version__); print(hasattr(aiotools, 'VirtualClock'))"</automated>
  </verify>
  <done>All three import cleanly from `.venv`; `aiotools.VirtualClock` exists (resolves Assumption A2); the three appear in `[dependency-groups] dev` and NOT in `[project.dependencies]`.</done>
  <acceptance_criteria>
    - `import anyio, trio, aiotools` succeeds from `.venv/bin/python`
    - `aiotools.VirtualClock` is present and exposes `patch_loop`
    - pyproject `[dependency-groups] dev` lists anyio, trio, aiotools; runtime deps untouched
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create the async harness package + nested anyio_backend conftest (D-06)</name>
  <files>tests/_async_harness/__init__.py, tests/_async_harness/conftest.py</files>
  <read_first>
    - 23-RESEARCH.md Pattern 2 lines 258-267 (the anyio_backend fixture — function-scoped, trio leg returns `("trio", {"clock": trio.testing.MockClock(autojump_threshold=0)})`)
    - 23-RESEARCH.md Open Question 2 (function-scoped rationale: fresh MockClock per test avoids clock-state bleed)
    - 23-PATTERNS.md §conftest.py (nested, not top-level — Pitfall 4) and §__init__.py (one-line "test-only, never shipped" docstring, mirror benchmarks/__init__.py)
    - tests/integration/conftest.py (the project's nested-conftest precedent)
    - tests/conftest.py lines 36-46 (fixture-docstring style)
  </read_first>
  <action>
    Create `tests/_async_harness/__init__.py` with a single-line module docstring stating the package is the test-only async harness, never shipped in `src/` (mirror the `benchmarks/__init__.py` shape).

    Create `tests/_async_harness/conftest.py` defining a FUNCTION-scoped `anyio_backend` fixture with `params=["asyncio", "trio"]`: when `request.param == "trio"` return the tuple `("trio", {"clock": trio.testing.MockClock(autojump_threshold=0)})`, else return `"asyncio"` (verbatim from RESEARCH Pattern 2). Function scope is deliberate — it constructs a fresh MockClock per test to avoid clock-state bleed (document this WHY in the fixture docstring per RESEARCH Open Question 2). Add a module docstring explaining the fixture is nested HERE (not the root `tests/conftest.py`) so the existing sync suite is never forced under the anyio plugin (RESEARCH Pitfall 4 — protects future PKG-04). Use Google-style docstrings, Markdown not RST, fully typed (`request: pytest.FixtureRequest`), basedpyright-strict clean.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/_async_harness/ --collect-only -q 2>&1 | tail -5; .venv/bin/python -c "import ast,sys; t=ast.parse(open('tests/_async_harness/conftest.py').read()); sys.exit(0 if any(isinstance(n,ast.FunctionDef) and n.name=='anyio_backend' for n in ast.walk(t)) else 1)"</automated>
  </verify>
  <done>`tests/_async_harness/__init__.py` and `conftest.py` exist; the `anyio_backend` fixture is defined function-scoped with both params; the trio leg constructs `MockClock(autojump_threshold=0)`.</done>
  <acceptance_criteria>
    - `tests/_async_harness/conftest.py` defines `anyio_backend` with `params=["asyncio", "trio"]`
    - The trio branch returns `("trio", {"clock": trio.testing.MockClock(autojump_threshold=0)})`
    - The fixture is function-scoped (no `scope=` other than default); docstring explains the fresh-clock rationale
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Prove the sync suite is unaffected by the new parametrization</name>
  <files>tests/_async_harness/conftest.py</files>
  <read_first>
    - 23-RESEARCH.md Pitfall 4 (anyio_backend leaking into the sync suite) + §Validation Architecture (full-suite-must-stay-green sampling rule)
  </read_first>
  <action>
    Run the full existing sync suite and confirm zero collection errors and zero new failures introduced by the anyio plugin registration. This is a guard task, not new code: if the suite errors with "fixture 'anyio_backend' not found" or async-collection complaints on existing tests (`test_configs.py`, `test_pool_factory.py`, etc.), the fixture is mis-scoped — move it strictly under `tests/_async_harness/` and re-run. No `anyio_mode = "auto"` anywhere. (Protects future PKG-04: the sync suite must pass without the async plugin interfering.)
  </action>
  <verify>
    <automated>.venv/bin/pytest -q 2>&1 | tail -15</automated>
  </verify>
  <done>Full existing suite passes (or matches the pre-change baseline) with no anyio-related collection errors; the async harness package collects nothing yet (no test files in it).</done>
  <acceptance_criteria>
    - `.venv/bin/pytest -q` reports no errors mentioning `anyio_backend` or async collection on existing sync tests
    - The pass/fail count for the pre-existing suite is unchanged from baseline
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan only edits a dependency manifest and adds a test-only pytest fixture. No untrusted input, no network at test time, no auth, no parsing of external data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Tampering | new dev deps anyio/trio/aiotools | accept | All three verified legitimate in 23-RESEARCH.md §Package Legitimacy Audit (canonical PyPI/GitHub-backed, MIT). No `[SLOP]`/`[SUS]` packages — no human checkpoint required. |
| T-23-SC | Tampering | `uv add --dev` install | accept | Packages pre-audited OK in RESEARCH; supply-chain risk is the standard ecosystem baseline, not phase-specific. |
| T-23-02 | Denial of Service | leaked anyio into runtime deps | mitigate | Install is dev-group ONLY (D-07); verify task asserts runtime deps untouched, so the shipped wheel gains no async dependency. |
</threat_model>

<verification>
- `.venv/bin/python -c "import anyio, trio, aiotools"` succeeds
- `.venv/bin/pytest -q` (full existing suite) stays green — no anyio collection regression
- `tests/_async_harness/` package importable; `anyio_backend` fixture defined function-scoped with both backends
</verification>

<success_criteria>
- anyio>=4.13, trio>=0.31, aiotools>=2.2 in `[dependency-groups] dev` only (TEST-05 / D-07)
- `tests/_async_harness/` package exists with a nested function-scoped `anyio_backend` fixture parametrized over `["asyncio", "trio"]`, trio leg injecting `MockClock(autojump_threshold=0)` (TEST-05 / D-01 / D-06)
- Existing sync suite unaffected (no anyio plugin leakage — RESEARCH Pitfall 4)
</success_criteria>

<artifacts_this_phase_produces>
This plan contributes these symbols/artifacts to the phase's hard contract:
- `tests/_async_harness/` — the test-only async harness package (never shipped in `src/`)
- `tests/_async_harness/conftest.py::anyio_backend` — function-scoped fixture, `params=["asyncio", "trio"]`; trio leg = `("trio", {"clock": trio.testing.MockClock(autojump_threshold=0)})`. Consumed by every async self-test in this phase and every async/EDGE test in Phases 24/25/27.
- Dev-group dependencies: `anyio>=4.13`, `trio>=0.31`, `aiotools>=2.2`.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/23-test-harness-foundation/23-01-SUMMARY.md` when done.
</output>
