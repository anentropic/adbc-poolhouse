---
phase: 33-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/src/index.md
  - docs/src/guides/async.md
autonomous: true
requirements: [DOCS-01, DOCS-02, DOCS-03, DOCS-04]

must_haves:
  truths:
    - "docs/src/index.md no longer claims DataFrame fetches are unavailable (DOCS-03)"
    - "The async guide documents Arrow streaming: fetch_record_batch -> async for -> close, the reader-lifetime contract, and honest concurrency framing (per-batch offload; GIL re-acquired during materialization) (DOCS-01)"
    - "The async guide documents adbc_ingest with the mode Literal table and an explicit 'replace drops the table' warning (DOCS-02)"
    - "The async guide documents fetch_df/fetch_polars as user-supplied deps that surface a native ModuleNotFoundError with no poolhouse pre-check (DOCS-03)"
    - "A humanizer pass is applied to every new or substantially rewritten section of async.md and index.md (DOCS-04 humanizer clause)"
    - ".venv/bin/mkdocs build --strict exits 0 after every prose edit"
  artifacts:
    - path: "docs/src/index.md"
      provides: "Corrected async availability statement (DataFrame fetches no longer listed as unavailable)"
      contains: "guides/async.md"
    - path: "docs/src/guides/async.md"
      provides: "Streaming + ingest-mode-table + DataFrame prose, honest concurrency framing, humanized"
      contains: "replace"
  key_links:
    - from: "docs/src/index.md"
      to: "docs/src/guides/async.md"
      via: "async pool guide link"
      pattern: "guides/async.md"
    - from: "docs/src/guides/async.md"
      to: "adbc_poolhouse.ConnectionBusyError / AsyncRecordBatchReader cross-refs"
      via: "mkdocstrings cross-reference resolution under --strict"
      pattern: "\\[.*\\]\\[adbc_poolhouse"
---

<objective>
Reconcile and humanize the consumer-facing async prose so the v1.5.0 async surface (streaming,
bulk ingest, DataFrame convenience) is documented accurately and reads in one consistent voice.

The DOCS-01/02/03 content already exists in `docs/src/guides/async.md` — it was authored piecemeal
across four commits in three phases (29-04, 30-02, 31-02, plus a follow-up fix). This plan is an
AUDIT + RECONCILE + HUMANIZE pass, NOT a rewrite. The one required correctness change is the
`docs/src/index.md` line-71 contradiction that still says DataFrame fetches "are not available yet"
(they shipped in Phase 31).

Purpose: A milestone consolidation must not ship a front page that contradicts the guide, and the
guide prose must not read as four different authors stitched together.
Output: Edited `docs/src/index.md` and `docs/src/guides/async.md`. No new files.
</objective>

<artifacts_this_phase_produces>
NONE new. This plan edits two existing files only: `docs/src/index.md` and `docs/src/guides/async.md`.
No new public symbols, no new pages, no new reference files (reference pages are auto-generated — see
plan 33-02). If the audit finds the existing prose already correct, the edit set for a section may be
limited to humanizer touch-ups; that is an expected and valid outcome.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@.claude/skills/adbc-poolhouse-docs-author/SKILL.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-documentation/33-RESEARCH.md
@.planning/phases/33-documentation/33-VALIDATION.md
@docs/src/index.md
@docs/src/guides/async.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the index.md DataFrame-availability contradiction (DOCS-03)</name>
  <read_first>docs/src/index.md (esp. lines 67-91), docs/src/guides/async.md lines 18-32 (the authoritative experimental block), 33-RESEARCH.md Pitfall 2</read_first>
  <files>docs/src/index.md</files>
  <action>
    Rewrite the async availability sentence at docs/src/index.md line 71 so it no longer claims
    DataFrame fetches are unavailable (per DOCS-03 — fetch_df/fetch_polars shipped in Phase 31).
    The only genuinely-unavailable async features are async ADBC metadata (adbc_get_objects,
    adbc_get_table_schema, adbc_get_info) and async prepared statements — list only those two as
    not-yet-shipped, matching the authoritative experimental block in async.md lines 18-32. Keep the
    "experimental / surface may change" caveat and the existing link to guides/async.md. Do NOT list
    metadata or prepared statements as available (they are not implemented — Deferred/out of scope).
    Keep the edit minimal: the quickstart stays short and its fetch_arrow_table example is correct and
    unchanged. The literal string "not available" MUST no longer appear anywhere in index.md.
  </action>
  <verify>
    <automated>test -z "$(grep -n 'not available' docs/src/index.md)" && DISABLE_MKDOCS_2_WARNING=true .venv/bin/mkdocs build --strict; echo "exit=$?"</automated>
  </verify>
  <done>`grep -n "not available" docs/src/index.md` returns nothing; index.md lists only async metadata + async prepared statements as unavailable; DataFrame fetches are not called unavailable; `.venv/bin/mkdocs build --strict` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Audit + reconcile the async.md streaming / ingest / DataFrame prose (DOCS-01/02/03)</name>
  <read_first>docs/src/guides/async.md (full), 33-RESEARCH.md "Honest Concurrency Framing" + "Content-Specific Facts the Guide Must State", the AsyncRecordBatchReader reader-lifetime contract notes in RESEARCH Symbol Inventory</read_first>
  <files>docs/src/guides/async.md</files>
  <action>
    Audit the three DOCS sections against the research's technically-correct source of truth and edit
    ONLY what is stale, inaccurate, or tonally inconsistent — do not re-author correct prose (Pitfall 1).
    Verify and preserve, editing only where wrong:
    (a) DOCS-01 — the "Streaming a result set batch by batch" section documents fetch_record_batch ->
        async with -> async for -> close, states the reader-lifetime contract (a live reader locks its
        connection for its WHOLE lifetime; the lock clears on close(), not on drain; read-after-close or
        read-after-checkin surfaces the driver's native pyarrow.lib.ArrowInvalid, never a segfault), and
        keeps the honest concurrency framing (each read_next_batch pull offloaded individually; the GIL
        is re-acquired during Arrow materialization; one reader is not a parallel pipeline — batches
        arrive one at a time, in order; real overlap = separate readers on separate connections).
    (b) DOCS-02 — the "Bulk-loading Arrow data" section keeps the four-row mode table (create / append /
        create_append / replace) and the explicit warning that `replace` **drops** the existing table and
        recreates it (not a row-level upsert). Do not soften the destructive-replace warning.
    (c) DOCS-03 — the "Fetching a DataFrame" section states pandas/polars are user-supplied (install it
        yourself), poolhouse never imports them, and a missing install surfaces the native
        ModuleNotFoundError unchanged with no find_spec pre-check and no wrapping.
    If a heading is renamed, update every intra-page `(#anchor)` link and `[text][adbc_poolhouse...]`
    cross-ref in the SAME edit (Pitfall 3) so --strict stays green. Do NOT document async metadata or
    prepared statements as available (Deferred / out of scope).
  </action>
  <verify>
    <automated>grep -q 'replace' docs/src/guides/async.md && grep -qi 'drop' docs/src/guides/async.md && grep -q 'ModuleNotFoundError' docs/src/guides/async.md && grep -q 'ArrowInvalid' docs/src/guides/async.md && DISABLE_MKDOCS_2_WARNING=true .venv/bin/mkdocs build --strict; echo "exit=$?"</automated>
  </verify>
  <done>The three sections are verified accurate against RESEARCH; the mode table + "`replace` **drops** the table" warning, the native-ModuleNotFoundError note, and the reader-lifetime/ArrowInvalid contract are all present and unsoftened; any renamed heading has its anchors/cross-refs updated; `.venv/bin/mkdocs build --strict` exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Humanizer pass over new/rewritten prose in async.md + index.md (DOCS-04 humanizer clause)</name>
  <read_first>@/Users/paul/.claude/skills/humanizer/SKILL.md, @.claude/skills/adbc-poolhouse-docs-author/SKILL.md (Step 3 patterns), the sections edited in Tasks 1-2, 33-RESEARCH.md Pitfall 4 (em-dash convention)</read_first>
  <files>docs/src/index.md, docs/src/guides/async.md</files>
  <action>
    Apply the humanizer skill checklist to every section that is new or substantially rewritten in this
    phase (the index.md availability paragraph from Task 1 and any async.md prose touched in Task 2).
    Eliminate the docs-common tells called out in the docs-author SKILL: promotional language
    ("powerful", "seamlessly", "robust", "comprehensive", "effortlessly"), AI vocabulary ("delve",
    "leverage", "streamline", "it's worth noting", "ensure that"), vague attributions ("this allows you
    to", "this enables"), superficial -ing openers, rule-of-three padding, and em-dash overuse (cap one
    per paragraph). Preserve the direct, second-person HTTPX/SQLAlchemy voice and every technical fact —
    humanizing must not soften the honest concurrency framing or the destructive-replace warning. Do NOT
    open a docstring-wide em-dash sweep (Pitfall 4) — docstrings were signed off in prior phases and are
    handled as render-audit in plan 33-02; only touch guide/index prose you actually edited this phase.
  </action>
  <verify>
    <automated>DISABLE_MKDOCS_2_WARNING=true .venv/bin/mkdocs build --strict; echo "exit=$?"</automated>
    <human-check>Read each edited section; confirm no AI-writing tells remain, em-dashes are within one per paragraph, and all technical claims (reader-lifetime, GIL re-acquisition, replace-drops) are intact.</human-check>
  </verify>
  <done>Every section edited this phase reads in the project voice with no promotional/AI-vocabulary tells and em-dashes within the one-per-paragraph cap; all technical facts preserved; `.venv/bin/mkdocs build --strict` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> reader (human) | Documentation is read by developers who then call the async API; inaccurate docs can cause data loss |

No executable code, endpoints, or parsers are added in this phase, so there is no runtime trust boundary and no new network/input surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Information Disclosure / Integrity | `adbc_ingest` `mode="replace"` docs (async.md) | mitigate | Keep the explicit "`replace` **drops** the existing table" warning in the mode table and prose (DOCS-02); Task 3 humanizer must not soften it |
| T-33-02 | Repudiation (factual) | index.md async availability statement | mitigate | Task 1 removes the false "DataFrame fetches not available" claim so the front page cannot mislead users about shipped behavior (DOCS-03) |

No package-manager installs occur in this phase (docs-only; RESEARCH confirms no dependency changes), so no supply-chain (T-33-SC) legitimacy gate is required. No high-severity threats.
</threat_model>

<verification>
- `grep -n "not available" docs/src/index.md` returns nothing.
- `.venv/bin/mkdocs build --strict` exits 0 (run after each prose edit; baseline was green this session).
- async.md retains: the four-mode table, the "`replace` **drops**" warning, the native-`ModuleNotFoundError` DataFrame note, and the reader-lifetime / `ArrowInvalid` streaming contract.
- Humanizer checklist applied to every section edited this phase.
</verification>

<success_criteria>
DOCS-01/02/03 prose verified accurate and consolidated into one voice; the index.md contradiction is
removed (`grep -n "not available" docs/src/index.md` empty); the destructive-`replace` warning and
honest concurrency framing are intact and humanized; `.venv/bin/mkdocs build --strict` exits 0.
</success_criteria>

<output>
Create `.planning/phases/33-documentation/33-01-SUMMARY.md` when done.
</output>
