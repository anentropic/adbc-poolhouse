---
phase: 33-documentation
plan: 02
type: execute
wave: 2
depends_on: [33-01]
files_modified:
  - src/adbc_poolhouse/_async/_cursor.py
  - src/adbc_poolhouse/_async/_reader.py
autonomous: true
requirements: [DOCS-02, DOCS-04]

must_haves:
  truths:
    - "The generated API reference renders AsyncRecordBatchReader and all four new AsyncCursor methods (fetch_record_batch, adbc_ingest, fetch_df, fetch_polars) with Google-style Args/Returns/Raises + Example blocks (DOCS-04)"
    - "The API reference renders the adbc_ingest mode Literal values and the 'replace drops the table' warning (DOCS-02 reference half)"
    - ".venv/bin/mkdocs build --strict exits 0 as the single automated completion gate (DOCS-04)"
  artifacts:
    - path: "site/reference/adbc_poolhouse/index.html"
      provides: "Rendered reference for the async surface (build output, audited not committed)"
      contains: "AsyncRecordBatchReader"
    - path: "docs/scripts/gen_ref_pages.py"
      provides: "The _ASYNC_REFERENCE_BLOCK that injects the four async classes into the reference page (verified unchanged / functioning)"
      contains: "AsyncRecordBatchReader"
  key_links:
    - from: "docs/scripts/gen_ref_pages.py"
      to: "adbc_poolhouse._async._reader.AsyncRecordBatchReader + _cursor.AsyncCursor"
      via: "mkdocstrings ::: blocks with filters: ['!^__'] under gen-files"
      pattern: "::: adbc_poolhouse._async"
    - from: "src/adbc_poolhouse/_async/_cursor.py adbc_ingest docstring"
      to: "rendered reference mode table + replace-drops warning"
      via: "Google-style Args section rendered by mkdocstrings (docstring_section_style: table)"
      pattern: "replace"
---

<objective>
Verify the auto-generated API reference renders the v1.5.0 async surface correctly, then run the
strict-build completion gate for the phase.

The docstrings for `AsyncRecordBatchReader` and the four new `AsyncCursor` methods are already complete
(Args/Returns/Raises + `Example:`), and `gen_ref_pages.py` already injects them via `_ASYNC_REFERENCE_BLOCK`.
The strict build passes today. So this plan is a RENDER-FIDELITY AUDIT + FINAL GATE, not a docstring
authoring task. Repair a docstring ONLY if the audit finds a real render gap (research predicts none).

Purpose: DOCS-04 requires the reference to actually render the four methods + reader with Google-style
sections and an Example, and `mkdocs build --strict` to pass as the phase's single automated gate. This
plan proves both against the built site (after plan 33-01's prose edits have landed).
Output: Confirmation that the rendered reference is complete and the strict build is green. No new files;
docstring edits only if a gap is found in `_cursor.py` / `_reader.py`.
</objective>

<artifacts_this_phase_produces>
NONE new expected. Reference pages under `docs/src/reference/` are auto-generated at build time by
`docs/scripts/gen_ref_pages.py` (SKILL forbids hand-writing them) and are NOT committed source. The
only files this plan MIGHT edit are the existing docstrings in `src/adbc_poolhouse/_async/_cursor.py`
or `_reader.py`, and only if the render audit finds a missing Args/Returns/Raises/Example section —
research (33-RESEARCH Symbol Inventory) verified these are already complete, so the expected edit set
is empty. "No edits needed — render verified" is a valid and expected completion state.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@.claude/skills/adbc-poolhouse-docs-author/SKILL.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-documentation/33-RESEARCH.md
@.planning/phases/33-documentation/33-VALIDATION.md
@docs/scripts/gen_ref_pages.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render-fidelity audit of the async reference (DOCS-04, DOCS-02 reference half)</name>
  <read_first>docs/scripts/gen_ref_pages.py (the _ASYNC_REFERENCE_BLOCK), mkdocs.yml mkdocstrings options, 33-RESEARCH.md Symbol Inventory (docstring line numbers) + "Docs Structure & Mechanics", src/adbc_poolhouse/_async/_cursor.py and _reader.py docstrings (read only if the audit flags a gap)</read_first>
  <files>src/adbc_poolhouse/_async/_cursor.py, src/adbc_poolhouse/_async/_reader.py</files>
  <action>
    Build the site with `DISABLE_MKDOCS_2_WARNING=true .venv/bin/mkdocs build --strict` and inspect the
    generated `site/reference/adbc_poolhouse/index.html`. Confirm the reference renders, with Google-style
    Args/Returns/Raises tables plus an Example block, each of: AsyncRecordBatchReader and the four new
    AsyncCursor methods — fetch_record_batch, adbc_ingest, fetch_df, fetch_polars. Also confirm the
    adbc_ingest reference carries the mode Literal values and the destructive "`replace` drops the table"
    warning (DOCS-02's reference half), rendered from the _cursor.py docstring Args section.
    This is a VERIFY task: the docstrings and the gen_ref_pages.py injection block are already complete
    (33-RESEARCH). Edit a docstring in _cursor.py or _reader.py ONLY if the rendered output is actually
    missing a required section — do not rewrite complete docstrings (regression risk, Don't Hand-Roll).
    Do NOT hand-write anything under docs/src/reference/ (auto-generated; SKILL forbids it).
  </action>
  <verify>
    <automated>DISABLE_MKDOCS_2_WARNING=true .venv/bin/mkdocs build --strict && f=site/reference/adbc_poolhouse/index.html && for s in AsyncRecordBatchReader fetch_record_batch adbc_ingest fetch_df fetch_polars; do grep -q "$s" "$f" || { echo "MISSING $s"; exit 1; }; done && grep -qi 'drop' "$f" && grep -q 'Example' "$f"; echo "exit=$?"</automated>
  </verify>
  <done>`.venv/bin/mkdocs build --strict` exits 0; `site/reference/adbc_poolhouse/index.html` contains AsyncRecordBatchReader + all four method names, at least one `Example` block, and the `replace`/drop warning; no docstring edit was needed (or a targeted repair closed a real render gap).</done>
</task>

<task type="auto">
  <name>Task 2: Final strict-build completion gate + phase-consistency check (DOCS-04)</name>
  <read_first>33-VALIDATION.md (Sampling Rate + Validation Sign-Off), 33-RESEARCH.md "The mkdocs Strict Build Gate"</read_first>
  <files>src/adbc_poolhouse/_async/_cursor.py</files>
  <action>
    Run the single automated completion gate for the phase after plan 33-01's prose edits are on disk:
    `DISABLE_MKDOCS_2_WARNING=true .venv/bin/mkdocs build --strict` MUST exit 0. Confirm the phase-level
    consistency invariants hold together: the index.md contradiction is gone
    (`grep -n "not available" docs/src/index.md` returns nothing) and the async.md destructive-replace
    warning + native-ModuleNotFoundError note survived any humanizer edits. The benign INFO relative-link
    notes and the injected third-party "ProperDocs" ad warning are NOT failures (33-RESEARCH A2) — gate on
    the exit code, never on stderr grep. Make no source edits in this task unless the gate reveals a broken
    cross-ref introduced by an earlier edit, in which case fix that specific reference.
  </action>
  <verify>
    <automated>test -z "$(grep -n 'not available' docs/src/index.md)" && grep -qi 'drop' docs/src/guides/async.md && grep -q 'ModuleNotFoundError' docs/src/guides/async.md && DISABLE_MKDOCS_2_WARNING=true .venv/bin/mkdocs build --strict; echo "exit=$?"</automated>
  </verify>
  <done>`.venv/bin/mkdocs build --strict` exits 0; `grep -n "not available" docs/src/index.md` is empty; the replace-drops warning and ModuleNotFoundError note are still present in async.md; DOCS-01..04 are all satisfied and the phase is documentation-complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docstring source -> rendered reference | mkdocstrings renders Args/Returns/Raises from source docstrings; a malformed section silently drops from the reference |

No executable code or runtime input surface is added; the boundary here is render fidelity, not a security perimeter.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-03 | Information Disclosure / Integrity | adbc_ingest reference render (mode table + replace warning) | mitigate | Task 1 asserts the rendered `site/reference/...index.html` contains the drop/replace warning so the destructive semantics reach the API-reference audience (DOCS-02) |
| T-33-04 | Tampering (build correctness) | mkdocs --strict cross-references | mitigate | Gate on `mkdocs build --strict` exit code so a broken/renamed cross-ref or missing `:::` identifier fails the phase rather than shipping a broken reference (DOCS-04) |

No package-manager installs occur (docs-only; RESEARCH confirms no dependency changes), so no supply-chain (T-33-SC) legitimacy gate applies. No high-severity threats.
</threat_model>

<verification>
- `.venv/bin/mkdocs build --strict` exits 0 (the single automated completion gate).
- `site/reference/adbc_poolhouse/index.html` renders AsyncRecordBatchReader + fetch_record_batch + adbc_ingest + fetch_df + fetch_polars with Args/Returns/Raises + Example, plus the `replace`-drops warning.
- `grep -n "not available" docs/src/index.md` returns nothing (cross-plan invariant from 33-01).
- Gate on exit code only; the injected third-party ad warning and relative-link INFO notes are non-failures.
</verification>

<success_criteria>
DOCS-04 satisfied: the reference renders all four new methods + AsyncRecordBatchReader with Google-style
docstrings and Example blocks, the adbc_ingest replace-drops warning appears in the reference (DOCS-02),
and `mkdocs build --strict` passes as the phase's single automated gate — with the humanizer pass from
plan 33-01 already applied to the rewritten prose.
</success_criteria>

<output>
Create `.planning/phases/33-documentation/33-02-SUMMARY.md` when done.
</output>
