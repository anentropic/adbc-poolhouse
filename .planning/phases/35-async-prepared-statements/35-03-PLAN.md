---
phase: 35-async-prepared-statements
plan: 03
type: execute
wave: 3
depends_on: ["35-02"]
files_modified:
  - docs/src/guides/async.md
  - docs/src/index.md
autonomous: true
requirements: [PREP-03]
must_haves:
  truths:
    - "The async.md 'It is also incomplete' / 'not available yet' block is REMOVED entirely — prepared statements was its last bullet after Phase 34, so the whole block goes; the 'surface may change' experimental warning stays (PREP-03, D-35-08)"
    - "The 'What you get today' paragraph is updated to include the prepared-statement methods (adbc_prepare / adbc_execute_schema) (PREP-03, D-35-08)"
    - "A short 'prepare then execute' snippet is added to the async guide as a PLAIN fenced python block (never wrapped in !!! example) (PREP-03, Claude's Discretion — snippet recommended)"
    - "docs/src/index.md:71 drops the stale 'async ADBC metadata and prepared statements have not shipped yet' clause entirely (metadata shipped in Phase 34; prepared statements ship here) (PREP-03, D-35-08)"
    - "The API reference auto-renders adbc_prepare + adbc_execute_schema with Args/Returns/Raises + Example via the existing AsyncCursor mkdocstrings block — no gen_ref_pages.py / nav / ::: change (PREP-03)"
    - "DISABLE_MKDOCS_2_WARNING=true .venv/bin/mkdocs build --strict exits 0 (PREP-03)"
    - "Humanizer pass applied to all new/rewritten prose (PREP-03, CLAUDE.md docs gate)"
  artifacts:
    - path: "docs/src/guides/async.md"
      provides: "removed 'not available yet' block + updated 'What you get today' + prepare/execute snippet"
      contains: "adbc_prepare"
    - path: "docs/src/index.md"
      provides: "stale not-shipped clause dropped from line 71"
      contains: "experimental"
  key_links:
    - from: "src/adbc_poolhouse/_async/_cursor.py docstrings (plan 35-02)"
      to: "docs/src/reference (AsyncCursor mkdocstrings render)"
      via: "existing AsyncCursor mkdocstrings block, filters !^__, no nav edit"
      pattern: "AsyncCursor"
---

<objective>
Document the two async prepared-statement methods (PREP-03) and retire the async caveat that
named them. Remove the async guide's "not available yet" block entirely (prepared statements
was its last remaining bullet after Phase 34), update "What you get today" to include
`adbc_prepare` / `adbc_execute_schema`, add a short "prepare then execute" snippet, and drop
the stale clause at `docs/src/index.md:71`. Confirm the API reference auto-renders both methods
from the plan 35-02 docstrings, run the humanizer pass, and gate on `mkdocs build --strict`.

Purpose: Close the docs half of the LAST async/sync parity gap. After this, the async guide
carries no "incomplete" caveat — the experimental "surface may change" warning is all that
remains. The API reference needs NO `gen_ref_pages.py` change: the existing `AsyncCursor`
mkdocstrings block renders the two new public methods automatically from their docstrings.
Output: Modified `docs/src/guides/async.md` and `docs/src/index.md`; strict build green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@.claude/skills/adbc-poolhouse-docs-author/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-async-prepared-statements/35-CONTEXT.md
@.planning/phases/35-async-prepared-statements/35-RESEARCH.md
@.planning/phases/35-async-prepared-statements/35-PATTERNS.md

# The docs being edited + the source docstrings that drive the reference render:
@docs/src/guides/async.md
@docs/src/index.md
@src/adbc_poolhouse/_async/_cursor.py
</context>

<artifacts_this_phase_produces>
This plan produces no new symbols — it documents the two methods created in plan 35-02
(`adbc_prepare`, `adbc_execute_schema`) in the async guide + index, and verifies the API
reference render + strict build. No files under `docs/src/reference/` are hand-edited (they are
auto-generated).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Remove the caveat block, update "What you get today", add the prepare snippet, fix index.md</name>
  <files>docs/src/guides/async.md, docs/src/index.md</files>
  <read_first>
    - docs/src/guides/async.md lines 18–32 (the `!!! warning "Experimental"` block) — after Phase 34,
      the "It is also incomplete" list is a SINGLE bullet ("Async prepared statements"); lines 27–31
      are the "What you get today" paragraph. Also read the `adbc_ingest` / Connection metadata sections
      further down for the how-to voice + plain-fenced-block style.
    - docs/src/index.md lines 67–72 (the "## Async" section) — line 71 carries the stale
      "async ADBC metadata and prepared statements have not shipped yet" clause.
    - src/adbc_poolhouse/_async/_cursor.py (the plan 35-02 `adbc_prepare` / `adbc_execute_schema`
      docstrings) — so the guide prose matches the shipped semantics.
    - 35-CONTEXT.md D-35-08 + 35-PATTERNS.md lines 226–238 (the caveat-removal + snippet edit map);
      .claude/skills/adbc-poolhouse-docs-author/SKILL.md — voice (second person, "See also") and the
      "no `!!! example` box around authored code" rule (MEMORY.md — plain fenced block).
  </read_first>
  <action>
    In `docs/src/guides/async.md`, edit the `!!! warning "Experimental"` block (lines 18–32):
    - KEEP the "surface may change" paragraph (lines 20–21) — the API is still experimental.
    - REMOVE the entire "It is also incomplete. The following are not available yet…" block INCLUDING
      its single remaining bullet ("**Async prepared statements** — `adbc_prepare`,
      `adbc_execute_schema`") — that was the last item, so the whole block goes (D-35-08).
    - UPDATE the "What you get today is …" paragraph (lines 27–31) to ADD the prepared-statement methods
      (`adbc_prepare` / `adbc_execute_schema`) to the enumerated available-features list. Since nothing
      async-surface remains unshipped except the deferred partitioned result sets, either drop the
      trailing "The rest is on the roadmap." sentence or soften it to note only partitioned result sets
      remain deferred — your call, but do not re-introduce a "not available yet" list.

    Add a short `## Prepared statements` how-to section (place it near the `adbc_ingest` / Connection
    metadata material, before the "See also"). Follow docs-author voice (second person, direct). Show a
    PLAIN fenced ```python block (NEVER wrapped in `!!! example`, MEMORY.md) illustrating the value of
    prepare — reuse of a prepared operation across `execute` — e.g.
    `schema = await cursor.adbc_prepare("SELECT * FROM t WHERE id = ?")` (a `pyarrow.Schema` or `None`),
    then `await cursor.execute("SELECT * FROM t WHERE id = ?", [1])`; and
    `result_schema = await cursor.adbc_execute_schema("SELECT id, name FROM t")` returning the result
    schema WITHOUT running the query. In prose, note the two behaviors that match the docstrings: (a)
    `adbc_execute_schema` returns the result schema without executing (no rows, no side effects); (b)
    both are cooperatively cancellable but non-poisoning — a cancelled prepare aborts the in-flight call
    and returns the connection clean (no invalidate). Note that some backends (DuckDB) do not implement
    `adbc_execute_schema` and raise the driver's native `NotSupportedError` unchanged. Add a "See also"
    cross-reference to the API reference.

    In `docs/src/index.md` line 71, DROP the entire stale clause "and async ADBC metadata and prepared
    statements have not shipped yet" — metadata shipped in Phase 34 and prepared statements ship now, so
    the sentence becomes just the experimental / surface-may-change note plus the guide cross-reference.

    Do not touch `docs/src/changelog.md` (the version bump/changelog is the deferred release step after
    Phase 35, per STATE.md). Do not hand-edit anything under `docs/src/reference/` (auto-generated).
  </action>
  <verify>
    <automated>grep -q "Prepared statements" docs/src/guides/async.md && grep -q "adbc_execute_schema" docs/src/guides/async.md && ! grep -q "not available yet" docs/src/guides/async.md && ! grep -q "have not shipped yet" docs/src/index.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The "It is also incomplete" / "not available yet" block (and its prepared-statements bullet) is
      gone from `async.md`; the "surface may change" experimental warning remains.
    - "What you get today" lists the prepared-statement methods; no "not available yet" list remains.
    - A `## Prepared statements` section exists with a plain fenced `python` example covering
      `adbc_prepare` and `adbc_execute_schema`, plus prose on the no-execute contract, the non-poisoning
      cancel, and the `NotSupportedError` passthrough, and a "See also" cross-reference.
    - No code block in the new section is wrapped in `!!! example`; no edits under `docs/src/reference/`.
    - `docs/src/index.md:71` no longer claims metadata or prepared statements have not shipped.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Humanizer pass, API-reference render check, and strict-build gate</name>
  <files>docs/src/guides/async.md, docs/src/index.md</files>
  <read_first>
    - @/Users/paul/.claude/skills/humanizer/SKILL.md — the full humanizer checklist (loaded per the
      docs-author skill Step 3).
    - .claude/skills/adbc-poolhouse-docs-author/SKILL.md §Step 3 — technical-docs patterns to eliminate
      (promotional language, AI vocabulary, vague "this enables/allows" attributions, rule-of-three,
      em-dash overuse).
    - 35-RESEARCH.md §Project Constraints (the existing `AsyncCursor` mkdocstrings block renders the two
      new public methods automatically — `filters: ["!^__"]`, no nav/`:::` edit).
  </read_first>
  <action>
    Apply the humanizer pass to all new/rewritten prose in `async.md` (the trimmed caveat + the new
    Prepared statements section) and the edited sentence in `index.md`: remove promotional language, AI
    vocabulary, vague "this enables/allows" attributions, superficial `-ing` openers, rule-of-three
    lists, and hold em dashes to at most one per paragraph. Keep the second-person, direct voice.

    Build the docs with the strict gate and confirm exit 0 (check the exit code, NOT grep — carried-forward
    gotcha): `DISABLE_MKDOCS_2_WARNING=true .venv/bin/mkdocs build --strict`.

    Confirm the API reference renders both new public methods. After the build, inspect the generated
    `AsyncCursor` reference page under `site/` (or run `.venv/bin/mkdocs serve` briefly) and verify
    `adbc_prepare` and `adbc_execute_schema` each appear with their Parameters/Returns/Raises tables and
    an Example block — driven purely by the plan 35-02 docstrings, no `gen_ref_pages.py` change. If a
    docstring renders with rogue colons (RST `:role:` syntax leaked into a docstring), that is a 35-02
    regression — fix the docstring in `_cursor.py` to Markdown syntax and rebuild.
  </action>
  <verify>
    <automated>DISABLE_MKDOCS_2_WARNING=true .venv/bin/mkdocs build --strict; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `DISABLE_MKDOCS_2_WARNING=true .venv/bin/mkdocs build --strict` exits 0.
    - The rendered `AsyncCursor` reference page shows `adbc_prepare` and `adbc_execute_schema` with
      Parameters/Returns/Raises + an Example block.
    - The new/rewritten prose passes the humanizer checklist (no promotional/AI vocabulary, no vague
      attributions, ≤ one em dash per paragraph).
    - No RST `:role:` syntax renders as rogue colons in the two method docstrings.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored docs → reader | Documentation prose; no executable trust boundary. The `mkdocs build --strict` gate is the only integrity control |

## STRIDE Threat Register

Honest minimal model: this plan edits prose and runs a docs build. No inputs, no I/O sinks, no
auth surface, no new dependencies. D-35-06 governs the documented behavior (native errors
surface unchanged).

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-04 | Information Disclosure | docs example leaks credentials | accept | Examples use DuckDB / illustrative snippets with no secrets (docs-author rule) |
| T-35-05 | Tampering | stale caveat misleads users about async capability | mitigate | The `mkdocs build --strict` gate (exit 0) plus the explicit caveat-removal + `index.md` acceptance criteria keep the guide consistent with the shipped surface |
| T-35-SC | Tampering | package installs | accept (N/A) | No package installs this plan; docs toolchain (`mkdocs`, mkdocstrings) already present |
</threat_model>

<verification>
- `DISABLE_MKDOCS_2_WARNING=true .venv/bin/mkdocs build --strict` — exit 0 (the phase docs gate).
- `grep -q "Prepared statements" docs/src/guides/async.md` and `! grep -q "not available yet"
  docs/src/guides/async.md` — section added, caveat removed.
- `! grep -q "have not shipped yet" docs/src/index.md` — stale clause dropped.
- Manual render inspection: `adbc_prepare` + `adbc_execute_schema` appear in the `AsyncCursor` API
  reference with Parameters/Returns/Raises + Example.
</verification>

<success_criteria>
- The async guide documents `adbc_prepare` / `adbc_execute_schema` and no longer carries an "incomplete"
  caveat — only the experimental surface-may-change warning remains (PREP-03).
- `docs/src/index.md` no longer claims metadata or prepared statements are unshipped.
- The API reference auto-renders both methods from their docstrings; `mkdocs build --strict` passes; the
  humanizer pass is applied.
</success_criteria>

<output>
Create `.planning/phases/35-async-prepared-statements/35-03-SUMMARY.md` when done.
</output>
