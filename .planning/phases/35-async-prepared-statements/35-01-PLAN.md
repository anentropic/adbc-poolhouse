---
phase: 35-async-prepared-statements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/_async_harness/stubs.py
  - tests/async/test_prep_signature.py
  - tests/async/test_prep_roundtrip.py
  - tests/async/test_prep_no_execute.py
  - tests/async/test_prep_unsupported.py
  - tests/async/test_prep_cancel.py
autonomous: true
requirements: [PREP-01, PREP-02]
must_haves:
  truths:
    - "BlockingStubCursor gains blocking adbc_prepare / adbc_execute_schema methods (injectable result) + prepare_call_count / execute_schema_call_count counters that NEVER touch execute_call_count (the PREP-02 no-execute proof reads execute_call_count == 0)"
    - "RED scaffold: test_prep_signature.py pins both adbc_prepare + adbc_execute_schema exist on AsyncCursor (PREP-01)"
    - "RED scaffold: test_prep_roundtrip.py pins adbc_prepare returns pyarrow.Schema OR None on real DuckDB and checks the connection back in (checkedout()==0), tolerating both return shapes (PREP-01, D-35-02)"
    - "RED scaffold: test_prep_no_execute.py pins adbc_execute_schema returns the injected result Schema with execute_call_count == 0 on the stub — the no-execute proof (PREP-02, D-35-06)"
    - "RED scaffold: test_prep_unsupported.py pins adbc_execute_schema surfaces DuckDB's native NotSupportedError unchanged (PREP-02, EDGE-17, D-35-06 native errors)"
    - "RED scaffold: test_prep_cancel.py pins an in-flight cancel fires adbc_cancel ONCE (cancellable) while invalidate is NOT called (non-poisoning, D-35-04) — dual-backend, looped, real-clock watchdog"
    - "All new test files observe tests/async discipline: @pytest.mark.anyio on every async test, no import asyncio, no positive-duration sleep (test_prep_* pass the guard)"
  artifacts:
    - path: "tests/_async_harness/stubs.py"
      provides: "BlockingStubCursor blocking prepare / execute_schema + counters + injectable results"
      contains: "prepare_call_count"
    - path: "tests/async/test_prep_signature.py"
      provides: "PREP-01 existence contract (sync introspection, no marker)"
      contains: "hasattr"
    - path: "tests/async/test_prep_roundtrip.py"
      provides: "PREP-01 adbc_prepare DuckDB round-trip (Schema | None)"
      contains: "adbc_prepare"
    - path: "tests/async/test_prep_no_execute.py"
      provides: "PREP-02 stub value + execute_call_count == 0"
      contains: "execute_call_count"
    - path: "tests/async/test_prep_unsupported.py"
      provides: "PREP-02 DuckDB NotSupportedError passthrough"
      contains: "NotSupportedError"
    - path: "tests/async/test_prep_cancel.py"
      provides: "PREP-01/02 non-poisoning cancel (D-35-04)"
      contains: "invalidate_call_count == 0"
  key_links:
    - from: "tests/async/test_prep_*.py"
      to: "adbc_poolhouse._async._cursor.AsyncCursor"
      via: "import + duckdb_async_pool / make_stub_async_connection fixtures"
      pattern: "from adbc_poolhouse._async._cursor import AsyncCursor"
    - from: "tests/async/test_prep_no_execute.py + test_prep_cancel.py"
      to: "tests/_async_harness/stubs.py BlockingStubCursor"
      via: "make_stub_async_connection → stub_conn.cursors[-1] (prepare/execute_schema counters)"
      pattern: "prepare_call_count|execute_schema_call_count"
---

<objective>
Create the Wave-0 RED test scaffolding for Phase 35 async prepared statements plus the
`BlockingStubCursor` harness extension the stub-driven legs need. Five new files in
`tests/async/` encode the observable PREP-01/02 contracts before any production symbol
exists, and `tests/_async_harness/stubs.py` gains two blocking methods + counters that the
no-execute and cancel legs consume. Every new test FAILS now (`AsyncCursor.adbc_prepare` /
`adbc_execute_schema` are not yet defined) — that RED signal is the acceptance criterion.
Plan 35-02 turns them GREEN.

Purpose: Nyquist Dimension 8 — each requirement maps to an automated verify that exists
before the implementation, so the GREEN plan has a fixed target it cannot fake. The stub
extension is the interface-first prerequisite: the no-execute proof (`execute_call_count ==
0`) and the deterministic in-flight cancel both read counters that must exist first.
Output: extended `stubs.py` + `test_prep_signature.py`, `test_prep_roundtrip.py`,
`test_prep_no_execute.py`, `test_prep_unsupported.py`, `test_prep_cancel.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@.claude/skills/adbc-poolhouse-docs-author/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-async-prepared-statements/35-CONTEXT.md
@.planning/phases/35-async-prepared-statements/35-RESEARCH.md
@.planning/phases/35-async-prepared-statements/35-PATTERNS.md
@.planning/phases/35-async-prepared-statements/35-VALIDATION.md

# Test analogs to clone (read these — they are the templates):
@tests/async/test_ingest_signature.py
@tests/async/test_ingest_roundtrip.py
@tests/async/test_ingest_cancel.py
@tests/async/test_meta_cancel.py
@tests/async/test_meta_unsupported.py
@tests/async/conftest.py
@tests/_async_harness/stubs.py
</context>

<artifacts_this_phase_produces>
Phase 35 (across plans 01–03) produces these new symbols. This plan creates the harness
extension + the RED tests that pin the two public methods:
- `AsyncCursor.adbc_prepare(operation: bytes | str) -> pyarrow.Schema | None` — plan 35-02
- `AsyncCursor.adbc_execute_schema(operation: str, parameters: object = None) -> pyarrow.Schema` — plan 35-02
- `_SyncCursor.adbc_prepare` / `_SyncCursor.adbc_execute_schema` Protocol signatures — plan 35-02
- `BlockingStubCursor.adbc_prepare` / `.adbc_execute_schema` blocking methods + `prepare_call_count`
  / `execute_schema_call_count` counters + `_prepare_result` / `_execute_schema_result` injectables — this plan
- Five new test files (`tests/async/test_prep_signature.py`, `test_prep_roundtrip.py`,
  `test_prep_no_execute.py`, `test_prep_unsupported.py`, `test_prep_cancel.py`) — this plan
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extend BlockingStubCursor + signature RED test</name>
  <files>tests/_async_harness/stubs.py, tests/async/test_prep_signature.py</files>
  <read_first>
    - tests/_async_harness/stubs.py lines 170–211 (`__init__` counters + injectable `_fetch_df_raises`
      pattern), lines 105–124 (the `Attributes:` docstring block listing every locked counter), and
      lines 397–441 (`adbc_ingest`: record-under-lock + `self._block()` + return an injectable value —
      the EXACT method body to mirror; note it does NOT touch `execute_call_count`).
    - tests/async/test_ingest_signature.py (whole file, 77 lines) — the `hasattr` existence test and
      the `if __name__ == "__main__"` tail. NOTE: unlike ingest, the two Phase-35 methods forward
      PLAIN POSITIONALS (D-35-02/03), so there is NO keyword-only assertion to make — existence only.
    - 35-RESEARCH.md §Harness extension (lines 311–328) and §Protocol extension; 35-PATTERNS.md
      "BlockingStubCursor extension" (lines 108–148) and "test_prep_signature.py" (lines 152–161).
  </read_first>
  <action>
    Extend `BlockingStubCursor` in `tests/_async_harness/stubs.py`, mirroring `adbc_ingest`
    (lines 397–441):
    - In `__init__` (near line 180, alongside `ingest_call_count`), add
      `self.prepare_call_count: int = 0` and `self.execute_schema_call_count: int = 0`, plus injectable
      results `self._prepare_result: object = None` and `self._execute_schema_result: object = None`
      (settable on `cursors[-1]`, mirroring `_fetch_df_raises` at line 184).
    - Add two blocking methods (mirror the `adbc_ingest` body at lines 437–441):
      `def adbc_prepare(self, operation: object = None) -> object:` — `del operation`; bump
      `prepare_call_count` under `self._lock`; `self._block()`; `return self._prepare_result`.
      `def adbc_execute_schema(self, operation: object = None, parameters: object = None) -> object:` —
      `del operation, parameters`; bump `execute_schema_call_count` under `self._lock`; `self._block()`;
      `return self._execute_schema_result`.
    - CRITICAL invariant (PREP-02 no-execute proof): NEITHER method may touch `execute_call_count`.
      No cancel wiring lives in these methods — the sticky `_cancelled`/`_closed` flags in `_block`
      (lines 283–297) and `adbc_cancel` (lines 443–467) already unblock a parked call, exactly as for
      `adbc_ingest`.
    - Update the `Attributes:` docstring block (lines ~109–119) to list `prepare_call_count` and
      `execute_schema_call_count` (with a one-line note that the cancel test polls
      `prepare_call_count >= 1` before cancelling), keeping the D-04 hard-contract docstring complete.
    - Give each new method a short Google-style docstring (Args/Returns) in the `adbc_ingest` voice,
      Markdown not RST (MEMORY.md). Stay basedpyright-strict-clean.

    Create `tests/async/test_prep_signature.py` by cloning the `test_ingest_signature.py` shape.
    Import `from adbc_poolhouse._async._cursor import AsyncCursor`. This is a SYNC introspection file —
    carry NO `@pytest.mark.anyio` and no fixtures. Assert `hasattr(AsyncCursor, "adbc_prepare")` and
    `hasattr(AsyncCursor, "adbc_execute_schema")` (existence only — no keyword-only shape to pin;
    basedpyright-strict in plan 35-02 is the authoritative Protocol-coverage gate). It FAILS RED now
    because neither method exists yet.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/async/test_prep_signature.py -q; test $? -ne 0 && echo "RED-as-expected (methods not yet defined)"; .venv/bin/basedpyright tests/_async_harness/stubs.py</automated>
  </verify>
  <acceptance_criteria>
    - `BlockingStubCursor` defines blocking `adbc_prepare` / `adbc_execute_schema` returning the
      injectable `_prepare_result` / `_execute_schema_result`, plus `prepare_call_count` /
      `execute_schema_call_count` counters initialized to 0.
    - Neither new stub method references or mutates `execute_call_count`.
    - The `Attributes:` docstring lists both new counters.
    - `.venv/bin/basedpyright tests/_async_harness/stubs.py` reports 0 errors.
    - `tests/async/test_prep_signature.py` exists, collects cleanly (no `import asyncio`,
      no `@pytest.mark.anyio`), and FAILS RED with an `AssertionError` for the missing methods.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Round-trip + no-execute + unsupported RED tests</name>
  <files>tests/async/test_prep_roundtrip.py, tests/async/test_prep_no_execute.py, tests/async/test_prep_unsupported.py</files>
  <read_first>
    - tests/async/test_ingest_roundtrip.py (whole file, 98 lines) — the class + `@pytest.mark.anyio
      async def` + `duckdb_async_pool` + `async with await ... connect()` shape and the top `pyarrow`
      import; also how it creates a table via `adbc_ingest`/`execute` first.
    - tests/async/test_ingest_cancel.py lines 80–83 (the `make_stub_async_connection` factory usage:
      checkout a cursor, then grab `stub_conn.cursors[-1]`) and line 156 (`_pool.checkedout()` accessor
      with `# noqa: SLF001`).
    - tests/async/test_meta_unsupported.py (whole file) — the `from adbc_driver_manager import
      NotSupportedError` + `pytest.raises(NotSupportedError)` + post-scope `checkedout() == 0` shape.
    - 35-RESEARCH.md §Test design notes (lines 394–414), Pitfall 1 (execute_schema unsupported on
      DuckDB), Pitfall 2 (adbc_prepare returns Schema OR None); 35-PATTERNS.md "test_prep_roundtrip.py"
      (165–179), "test_prep_no_execute.py" (183–190), "test_prep_unsupported.py" (194–206).
  </read_first>
  <action>
    Create `test_prep_roundtrip.py` (PREP-01, D-35-02). Clone the `test_ingest_roundtrip.py` class +
    `@pytest.mark.anyio` + `duckdb_async_pool` shape, `import pyarrow` at top. Inside one
    `async with await duckdb_async_pool.connect() as conn:` scope: create a table first (via
    `await cursor.execute("CREATE TABLE t (id INTEGER)")` or `adbc_ingest`), then
    `result = await cursor.adbc_prepare("SELECT * FROM t WHERE id = ?")` and assert
    `isinstance(result, (pyarrow.Schema, type(None)))` — Pitfall 2: do NOT assert non-null. After the
    scope exits assert `duckdb_async_pool._pool.checkedout() == 0  # noqa: SLF001` (no reader lifetime
    lock — the connection checks in the moment the offload returns).

    Create `test_prep_no_execute.py` (PREP-02 no-execute proof, D-35-06). Use the
    `make_stub_async_connection` factory (checkout a cursor, grab `stub_cursor = stub_conn.cursors[-1]`).
    Inject a sentinel `pyarrow.Schema` as `stub_cursor._execute_schema_result` (build a trivial schema,
    e.g. `pyarrow.schema([("id", pyarrow.int64())])`). Release the stub gate so the blocked call returns
    (drive it in a task group and `c.release()` in `finally`, or set the result and release before the
    await — mirror the stub-driven pattern the cancel suite uses). Assert the awaited `result is` the
    sentinel object AND `stub_cursor.execute_call_count == 0` — the no-execute proof. DuckDB CANNOT host
    this leg (it raises `NotSupportedError`), which is exactly why the stub carries the positive value.

    Create `test_prep_unsupported.py` (PREP-02 native-error passthrough, EDGE-17 / D-35-06). Import
    `from adbc_driver_manager import NotSupportedError`. In a `duckdb_async_pool` connection scope assert
    `with pytest.raises(NotSupportedError): await cursor.adbc_execute_schema("SELECT 1")`, then after the
    scope assert `checkedout() == 0` (the failed call still checks in cleanly). This doubles as the
    "no invented async error type" assertion — the driver's native `NOT_IMPLEMENTED:
    AdbcStatementExecuteSchema not implemented` surfaces unwrapped through the single offload chokepoint.

    Every async test carries `@pytest.mark.anyio`; no `import asyncio`; no positive-duration `sleep`.
    All three files FAIL RED now (`AttributeError` — the methods are not yet defined).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/async/test_prep_roundtrip.py tests/async/test_prep_no_execute.py tests/async/test_prep_unsupported.py -q; test $? -ne 0 && echo "RED-as-expected"</automated>
  </verify>
  <acceptance_criteria>
    - `test_prep_roundtrip.py` asserts `isinstance(result, (pyarrow.Schema, type(None)))` for
      `adbc_prepare` on DuckDB and post-scope `checkedout() == 0`.
    - `test_prep_no_execute.py` injects a sentinel `pyarrow.Schema`, asserts the awaited value IS the
      sentinel AND `stub_cursor.execute_call_count == 0`.
    - `test_prep_unsupported.py` uses `pytest.raises(NotSupportedError)` for `adbc_execute_schema` on
      DuckDB and asserts `checkedout() == 0` after.
    - All three FAIL RED with `AttributeError` for the missing methods; every `async def test_*` carries
      `@pytest.mark.anyio`; no `import asyncio`, no positive `sleep`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Deterministic non-poisoning cancel RED test</name>
  <files>tests/async/test_prep_cancel.py</files>
  <read_first>
    - tests/async/test_ingest_cancel.py (whole file, 166 lines) — the CURSOR-cancel structure:
      `real_clock_watchdog`, `await_inside`, `concurrency_marks` (the `pytestmark` x-loop gate),
      `make_stub_async_connection`, the task-group cancel + `finally: c.release()` scaffold, and the
      second `virtual_clock` + `anyio.fail_after` timeout leg (lines 96–127).
    - tests/async/test_meta_cancel.py lines 103–107 — the NON-poisoning assertion
      (`invalidate_call_count == 0`, NOT `== 1`); note the contrast (ingest asserts `== 1`).
    - 35-RESEARCH.md §Test design notes "Cancel" (lines 407–414); 35-PATTERNS.md "test_prep_cancel.py"
      (lines 210–222); MEMORY carried-forward gotchas: real-clock watchdog (NEVER `anyio.fail_after` as
      the watchdog — it autojumps under the trio MockClock), 0-hang loop gate, dual-backend.
  </read_first>
  <action>
    Create `test_prep_cancel.py` as the HYBRID of `test_ingest_cancel.py` (cursor-cancel scaffold) and
    `test_meta_cancel.py` (non-poisoning assertion). Copy the `importlib` helper shim + `pytestmark =
    _helpers.concurrency_marks` header from `test_ingest_cancel.py`. Drive a single
    `await cursor.adbc_prepare("SELECT 1")` (or `adbc_execute_schema`) as the cancellable unit.

    In the explicit-cancel leg: `make_stub_async_connection()`, checkout a cursor, grab
    `stub_cursor = stub_conn.cursors[-1]`, wrap in `real_clock_watchdog(stub_conn.cursors)`, start the
    prepare in a task group, `await await_inside(lambda: stub_conn.cursors[-1].prepare_call_count >= 1)`,
    then `tg.cancel_scope.cancel()`, and `for c in stub_conn.cursors: c.release()` in `finally`. Assert:
    - `tripped[0] is False` (no hang),
    - `stub_cursor.adbc_cancel_call_count == 1` — CANCELLABLE: `cancellable_offload` fired the cursor's
      `adbc_cancel` once (this is the axis that DIFFERS from `test_meta_cancel.py`, whose no-op hook
      leaves it at 0),
    - `stub_conn.invalidate_call_count == 0` — NON-POISONING (D-35-04): `on_abort` is omitted, so a
      cancelled prepare NEVER invalidates the connection (this is the axis that DIFFERS from
      `test_ingest_cancel.py`, which asserts `== 1`).

    Add the timeout twin (mirror `test_ingest_cancel.py` lines 96–127): `virtual_clock` +
    `anyio.fail_after(5)` ONLY as the cancellation TRIGGER (watchdog stays `real_clock_watchdog`), catch
    `TimeoutError`, and assert the same `adbc_cancel_call_count == 1` / `invalidate_call_count == 0`.

    Both legs `@pytest.mark.anyio`, dual-backend via `anyio_backend_name`, run under the
    `concurrency_marks` x-loop gate. FAILS RED now (`AttributeError` — `adbc_prepare` not yet defined).
  </action>
  <verify>
    <automated>ADBC_ASYNC_REPEAT=20 .venv/bin/pytest tests/async/test_prep_cancel.py -q; test $? -ne 0 && echo "RED-as-expected"</automated>
  </verify>
  <acceptance_criteria>
    - `test_prep_cancel.py` gates on `prepare_call_count >= 1`, cancels from the task group, and asserts
      `tripped[0] is False`, `stub_cursor.adbc_cancel_call_count == 1`, and
      `stub_conn.invalidate_call_count == 0` (the two-axis: cancellable AND non-poisoning, D-35-04).
    - It includes a `virtual_clock` + `anyio.fail_after` timeout twin asserting the same two counters.
    - It uses `real_clock_watchdog` (never `anyio.fail_after`) as the watchdog and the
      `concurrency_marks` x-loop gate; both legs carry `@pytest.mark.anyio` and run dual-backend.
    - It FAILS RED with `AttributeError` for the missing `adbc_prepare` method.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test task → AsyncCursor | This plan only encodes tests + a stub; no production surface is added. The methods it pins forward a SQL string / bind params to the same sync ADBC cursor the sync path already trusts |
| offload worker → sync ADBC driver | The blocking C call runs off-loop; the driver owns SQL binding and identifier handling (unchanged from `execute`) |

## STRIDE Threat Register

Honest minimal model (per the phase security note): this plan adds test scaffolding + two
blocking stub methods. No new inputs, no new I/O sinks, no auth surface, no new dependencies,
no new trust boundary crossed. D-35-06: native driver errors surface unchanged with no new
error handling.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Tampering | `operation` / `parameters` forwarded to `adbc_prepare` / `adbc_execute_schema` | accept | Not poolhouse's boundary — the caller passes SQL to the driver exactly as with sync `adbc_prepare` (mirrors `execute`, D-35-06). Tests forward only literals to DuckDB / the stub; poolhouse constructs no SQL and adds no sanitization |
| T-35-02 | Denial of Service | connection left busy after a failed/unsupported/cancelled prepare | mitigate | `test_prep_unsupported.py` asserts `checkedout() == 0` after a native error; `test_prep_cancel.py` asserts `invalidate_call_count == 0` with no hang — proving the `_offloading()` guard releases a clean connection (non-poisoning, D-35-04) |
| T-35-SC | Tampering | package installs | accept (N/A) | No package installs this plan — all symbols ship in `adbc_driver_manager` 1.11.0 + existing dev deps; no legitimacy checkpoint required |
</threat_model>

<verification>
- `.venv/bin/pytest tests/async/test_prep_signature.py tests/async/test_prep_roundtrip.py tests/async/test_prep_no_execute.py tests/async/test_prep_unsupported.py tests/async/test_prep_cancel.py -q` — all five files collect and FAIL RED (missing `adbc_prepare` / `adbc_execute_schema`), the pre-implementation signal.
- `.venv/bin/basedpyright tests/_async_harness/stubs.py` — 0 errors (the harness extension stays strict-clean).
- `.venv/bin/pytest tests/async/test_meta_guard.py -x -q` — PASSES: the five new files observe the `tests/async/` discipline (anyio marker, no `import asyncio`, no positive sleep).
</verification>

<success_criteria>
- `BlockingStubCursor` carries blocking `adbc_prepare` / `adbc_execute_schema` + counters that leave
  `execute_call_count` untouched (the no-execute proof's foundation).
- Five new test files exist under `tests/async/`, encoding PREP-01 (signature + DuckDB round-trip) and
  PREP-02 (stub no-execute, DuckDB native-error passthrough, non-poisoning cancel).
- Each fails RED for the right reason (methods not yet defined), so plan 35-02 has a fixed GREEN target.
- `test_meta_guard.py` still passes, certifying discipline.
</success_criteria>

<output>
Create `.planning/phases/35-async-prepared-statements/35-01-SUMMARY.md` when done.
</output>
