---
phase: 21.1-adbc-dispatch-uri-positional-fix
plan: 02
type: execute
wave: 2
depends_on:
  - 21.1-01
files_modified:
  - tests/test_driver_imports.py
  - tests/test_drivers.py
autonomous: true
requirements:
  - DISP-03
  - DISP-04
  - DISP-05
  - DISP-06
  - DISP-08
  - DISP-09

must_haves:
  truths:
    - "TestQuackImports patches adbc_driver_quack.dbapi.connect with a signature-preserving stub (a real def connect literal, not a bare MagicMock)"
    - "TestPostgreSQLImports patches adbc_driver_postgresql.dbapi.connect with a signature-preserving stub"
    - "TestFlightSQLImports patches adbc_driver_flightsql.dbapi.connect with a signature-preserving stub"
    - "Each of the three upgraded tests asserts the uri arrived positionally and the rest of the kwargs arrived inside db_kwargs"
    - "The duplicate test_quack_returns_short_name function at tests/test_drivers.py line 184 is deleted"
    - "Full pytest suite continues to pass with >= 265 tests"
  artifacts:
    - path: "tests/test_driver_imports.py"
      provides: "Three signature-preserving stub upgrades on TestQuackImports / TestPostgreSQLImports / TestFlightSQLImports"
      contains: "def stub_connect"
    - path: "tests/test_drivers.py"
      provides: "Duplicate-free PyPI driver-path test module"
      contains: "TestPyPIDriverPath"
  key_links:
    - from: "tests/test_driver_imports.py::TestQuackImports"
      to: "_driver_api.create_adbc_connection Family A' branch"
      via: "signature-preserving stub triggers inspect.Parameter.empty detection"
      pattern: "def stub_connect\\(uri"
    - from: "tests/test_driver_imports.py::TestPostgreSQLImports"
      to: "_driver_api.create_adbc_connection Family A' branch"
      via: "signature-preserving stub matching live adbc_driver_postgresql signature"
      pattern: "def stub_connect\\(uri"
    - from: "tests/test_driver_imports.py::TestFlightSQLImports"
      to: "_driver_api.create_adbc_connection Family A' branch"
      via: "signature-preserving stub matching live adbc_driver_flightsql signature"
      pattern: "def stub_connect\\(uri"
---

<objective>
Replace the bare-`MagicMock` patches in `TestQuackImports`, `TestPostgreSQLImports`,
and `TestFlightSQLImports` with signature-preserving stubs so the
driver-installed branch actually exercises the fixed Family A' dispatch from
plan 21.1-01. Delete the duplicate `test_quack_returns_short_name` at
`tests/test_drivers.py` line 184. Confirm the full pytest suite still passes.

Purpose: closes the test gap that hid the dispatch bug for three releases
(RESEARCH.md Pitfall 1) — `MagicMock` accepts any args/kwargs and its
wildcard signature lets the old dispatch slip through; a real function literal
with the affected shape forces the dispatcher to take the correct branch.

Output: three upgraded `Test*Imports` classes in
`tests/test_driver_imports.py`, one deletion in `tests/test_drivers.py`,
and a green full-suite run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@.claude/skills/adbc-poolhouse-docs-author/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21.1-adbc-dispatch-uri-positional-fix/21.1-RESEARCH.md
@.planning/phases/21.1-adbc-dispatch-uri-positional-fix/21.1-01-dispatch-fix-and-unit-test-PLAN.md

<interfaces>
<!-- Live signature shapes the stubs must reproduce (RESEARCH.md Signature Evidence). -->

adbc_driver_quack.dbapi.connect:
  - `uri` POSITIONAL_OR_KEYWORD (no default — required)
  - `db_kwargs` KEYWORD_ONLY = None
  - `conn_kwargs` KEYWORD_ONLY = None
  - `autocommit` KEYWORD_ONLY = False

adbc_driver_postgresql.dbapi.connect:
  - `uri` POSITIONAL_OR_KEYWORD (no default — required)
  - `db_kwargs` POSITIONAL_OR_KEYWORD = None
  - `conn_kwargs` POSITIONAL_OR_KEYWORD = None
  - `autocommit` KEYWORD_ONLY = False
  - `**kwargs`

adbc_driver_flightsql.dbapi.connect:
  - `uri` POSITIONAL_OR_KEYWORD (no default — required)
  - `db_kwargs` POSITIONAL_OR_KEYWORD = None
  - `conn_kwargs` POSITIONAL_OR_KEYWORD = None
  - `**kwargs`

<!-- Existing precedent: signature-preserving stub via inline def connect literal -->
<!-- already lives in tests/test_driver_api.py:19 and :36 (the TestDbApiModuleSignatureDispatch class). -->

<!-- Existing conditional-mock pattern to preserve: -->
<!-- if _driver_installed(...): patch the driver's own dbapi.connect -->
<!-- else: patch adbc_driver_manager.dbapi.connect (Foundry fallback) -->

<!-- Duplicate to delete: tests/test_drivers.py line 184 `def test_quack_returns_short_name()` -->
<!-- The canonical equivalent lives at line 122 inside TestPyPIDriverPath as -->
<!-- `test_quack_missing_returns_package_name`; assertion byte-for-byte identical -->
<!-- (RESEARCH.md Code Examples Example 4 + Assumption A3). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Upgrade TestQuackImports / TestPostgreSQLImports / TestFlightSQLImports to signature-preserving stubs</name>
  <files>tests/test_driver_imports.py</files>
  <read_first>
    - tests/test_driver_imports.py (entire file — to preserve the conditional-mock pattern and to keep Snowflake/BigQuery/SQLite/Foundry classes untouched)
    - tests/test_driver_api.py (lines 14-47 — the existing signature-preserving stub precedent to mirror)
    - .planning/phases/21.1-adbc-dispatch-uri-positional-fix/21.1-RESEARCH.md (Code Examples Example 3; Signature Evidence section)
  </read_first>
  <behavior>
    - With `adbc_driver_quack` installed, `create_pool(QuackConfig(uri="quack://h:1234"))`
      MUST cause the upgraded stub to receive `uri` as the FIRST POSITIONAL argument
      (not as a kwarg) and a `db_kwargs` dict that does NOT contain `"uri"`.
    - With `adbc_driver_postgresql` installed,
      `create_pool(PostgreSQLConfig(uri="postgresql://h:5432/db"))` MUST behave the
      same way against the postgres stub.
    - With `adbc_driver_flightsql` installed,
      `create_pool(FlightSQLConfig(uri="grpc://localhost:8815"))` MUST behave the
      same way against the flightsql stub.
    - In all three cases the stub returns the prepared `mock_conn` so pool creation
      and dispose complete without touching a real server.
    - The driver-not-installed branch of each class keeps the existing
      `adbc_driver_manager.dbapi.connect` mock and assertion (Foundry fallback).
  </behavior>
  <action>
    Edit the three classes `TestQuackImports`, `TestPostgreSQLImports`, and
    `TestFlightSQLImports` in `tests/test_driver_imports.py` (per DISP-06,
    DISP-03, DISP-04, DISP-05). For each:

    - Replace the driver-installed `patch("<driver>.dbapi.connect", return_value=mock_conn)`
      bare-MagicMock idiom with:
      * A `captured: dict[str, Any]` dict (typed via local annotation; reuse the
        existing `from typing import Any` import — add to imports if not present).
      * An inline `def stub_connect(uri, *, db_kwargs=None, conn_kwargs=None, ...) -> MagicMock`
        whose parameter list matches the live signature for that driver (see the
        `<interfaces>` block above for the exact shapes). The stub records `uri`
        and `db_kwargs` into `captured`, then returns `mock_conn`.
        - For Quack: parameters are `(uri, *, db_kwargs=None, conn_kwargs=None, autocommit=False)`.
        - For Postgres: parameters are `(uri, db_kwargs=None, conn_kwargs=None, *, autocommit=False, **kwargs)`.
        - For FlightSQL: parameters are `(uri, db_kwargs=None, conn_kwargs=None, **kwargs)`.
        It is acceptable to use simpler `(uri, *, db_kwargs=None, **kwargs)` shape for
        Postgres and FlightSQL — what matters for the dispatch detection rule is
        (a) `"db_kwargs" in params` and (b) `params["uri"].default is
        inspect.Parameter.empty`. Match the live shape as closely as practical without
        over-engineering.
      * Use `with patch("<driver>.dbapi.connect", new=stub_connect):` — NOT
        `return_value=` and NOT `side_effect=` (RESEARCH.md Pitfall 4: `new=` with a
        real function literal is the unambiguous form).
      * After the `with` block: `assert captured["uri"] == "<the uri you passed in>"`
        and `assert "uri" not in (captured.get("db_kwargs") or {})`. For Quack
        specifically, also assert that any non-uri config keys appear inside
        `captured["db_kwargs"]` (e.g. `host`-derived URI → no extra keys; if the
        config sets `token`, the corresponding `adbc.quack.token` key should be in
        `db_kwargs`). Keep the existing input config (`QuackConfig(host="h")` →
        check `captured["uri"]` is the synthesised `quack://h` URI, or upgrade
        the config to a `uri=`-form construction if cleaner).

    Constraints:
    - DO NOT touch any other class in this file (Snowflake, BigQuery, SQLite,
      DuckDB, Foundry, ClickHouse, etc.) — they are explicitly out of scope per
      RESEARCH.md Locked Decisions.
    - DO NOT introduce a `MagicMock(spec=...)` stub (RESEARCH.md Pitfall 4 explains
      why a real function literal is preferred over `spec=`).
    - DO NOT extract a shared helper for the stub factory unless the duplication
      is intolerable (research's recommended scope: per-class inline stub mirrors
      `test_driver_api.py` precedent; no new test-only helper module).
    - DO preserve the conditional `if _driver_installed(...) / else:` structure.
      The `else` branch (driver not installed) keeps the existing
      `adbc_driver_manager.dbapi.connect` mock unchanged.
    - DO ensure any new imports (`from typing import Any`) are added to the file
      header, not inside the method body.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_driver_imports.py::TestQuackImports tests/test_driver_imports.py::TestPostgreSQLImports tests/test_driver_imports.py::TestFlightSQLImports -x -q</automated>
    grep -c "def stub_connect" tests/test_driver_imports.py  # must be >= 3
    grep -c "MagicMock(return_value=" tests/test_driver_imports.py  # baseline count from before edit; must not increase
  </verify>
  <acceptance_criteria>
    - `tests/test_driver_imports.py` contains at least three `def stub_connect(`
      occurrences (one each inside `TestQuackImports`, `TestPostgreSQLImports`,
      `TestFlightSQLImports`).
    - Each `stub_connect` literal's first parameter is `uri` with no default value
      (verifiable by reading the signatures).
    - Each upgraded test passes when its driver is installed (the three drivers
      are confirmed installed in `.venv/` per RESEARCH.md Environment Availability).
      The signature-preserving stub upgrade is the proof for DISP-03 / DISP-04 /
      DISP-05: the stub reproduces the real driver's `connect()` signature exactly,
      so a passing test means dispatch routes through the fixed Family A' branch
      end-to-end.
    - The `else` branch (driver not installed; `adbc_driver_manager.dbapi.connect`
      Foundry-fallback mock) is unchanged by this task and inherits its prior green
      status from the pre-phase suite. DISP-06 explicitly targets the installed-driver
      stubs only; the not-installed path is out of scope for this phase.
    - Snowflake, BigQuery, SQLite, DuckDB, ClickHouse, Databricks, Redshift, Trino,
      MSSQL, MySQL test classes in this file are unchanged (git diff shows zero
      lines changed inside those class bodies).
  </acceptance_criteria>
  <done>
    Three driver-installed test paths now exercise the real Family A' dispatch via
    signature-preserving stubs; the three were previously hiding the bug by feeding
    `MagicMock`'s wildcard signature into `inspect.signature`. Future signature-shape
    regressions of this class are caught at the test layer.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete duplicate test_quack_returns_short_name and run full suite</name>
  <files>tests/test_drivers.py</files>
  <read_first>
    - tests/test_drivers.py (lines 80-200 — to identify and remove the duplicate; the canonical version at line 122 stays)
    - .planning/phases/21.1-adbc-dispatch-uri-positional-fix/21.1-RESEARCH.md (Code Examples Example 4; Assumption A3)
  </read_first>
  <action>
    Delete the standalone function `test_quack_returns_short_name` (currently
    at `tests/test_drivers.py:184` per RESEARCH.md) along with its docstring
    (per DISP-08). The canonical equivalent
    `TestPyPIDriverPath::test_quack_missing_returns_package_name` at line 122
    of the same file MUST remain untouched — the deletion targets only the
    standalone duplicate at module scope.

    Constraints:
    - Delete the function, its docstring, and any leading/trailing blank lines
      that exist only to separate it from neighbours. Do NOT delete blank lines
      that delimit `TestPyPIDbApiModule` (which precedes the duplicate) from
      `TestFoundryDriverPath` (which follows).
    - Do NOT touch any other test in this file.
    - Do NOT rename or move the canonical
      `TestPyPIDriverPath::test_quack_missing_returns_package_name`.

    After the deletion lands, run the full pytest suite to confirm DISP-09
    (`>= 265` tests passing). The deletion drops the count by exactly one;
    plan 21.1-01 added exactly one new test; the net should be 0 vs the
    pre-Phase-21.1 baseline.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -x -q</automated>
    test "$(grep -c '^def test_quack_returns_short_name' tests/test_drivers.py)" -eq 0
    test "$(grep -c 'test_quack_missing_returns_package_name' tests/test_drivers.py)" -ge 1
  </verify>
  <acceptance_criteria>
    - `tests/test_drivers.py` contains zero module-scope functions named
      `test_quack_returns_short_name` (verified by `grep -c '^def test_quack_returns_short_name'`).
    - `tests/test_drivers.py` still contains the canonical method
      `test_quack_missing_returns_package_name` inside `TestPyPIDriverPath`.
    - `.venv/bin/python -m pytest -x -q` exits 0 with at least 265 tests reported
      (DISP-09).
    - `git diff tests/test_drivers.py` shows only the duplicate-deletion change,
      no incidental edits to neighbouring tests.
  </acceptance_criteria>
  <done>
    Duplicate is gone; full test suite remains green; the assertion is still
    covered by the canonical in-class test.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
.venv/bin/python -m pytest tests/test_driver_imports.py tests/test_drivers.py tests/test_driver_api.py -x -q
.venv/bin/python -m pytest -x -q   # full suite must remain green; >= 265 tests
grep -c "def stub_connect" tests/test_driver_imports.py    # must be >= 3
grep -c "^def test_quack_returns_short_name" tests/test_drivers.py   # must be 0
```

This plan does NOT modify `_driver_api.py` (plan 21.1-01 owns that) and does
NOT modify any documentation (plan 21.1-03 owns that).
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test stub → dispatcher | The signature-preserving stub function literal substitutes for a real driver `connect`; `inspect.signature` reads its parameter list to decide the dispatch branch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21.1-04 | Tampering (test integrity) | bare `MagicMock` patches hiding signature regressions | mitigate | Replace with `def stub_connect(uri, *, db_kwargs=None, ...)` literal so `inspect.signature` reflects the real driver's shape (DISP-06) |
| T-21.1-05 | Repudiation (silent test pass) | duplicate test gives a false signal of double coverage | mitigate | Delete the duplicate so only one assertion records the behaviour; future failures attribute correctly (DISP-08) |
| T-21.1-SC | Tampering | npm/pip/cargo installs | n/a | Phase adds zero packages; RESEARCH.md "Package Legitimacy Audit: not applicable" |
</threat_model>

<success_criteria>
- All three `Test*Imports` classes in `tests/test_driver_imports.py` use a
  signature-preserving stub on the driver-installed branch.
- Duplicate `test_quack_returns_short_name` removed from `tests/test_drivers.py`.
- Full pytest suite passes with at least 265 tests.
- DISP-03, DISP-04, DISP-05, DISP-06, DISP-08, DISP-09 evidence is captured in
  the source artifacts and full-suite run.
</success_criteria>

<output>
Create `.planning/phases/21.1-adbc-dispatch-uri-positional-fix/21.1-02-SUMMARY.md` when done.
</output>
