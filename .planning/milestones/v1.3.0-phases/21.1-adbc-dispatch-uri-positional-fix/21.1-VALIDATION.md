---
phase: 21.1
slug: adbc-dispatch-uri-positional-fix
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-20
---

# Phase 21.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Extracted from `21.1-RESEARCH.md` § Validation Architecture (lines 467–504).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 9.0.2 (in `.venv/`); `>=8.0.0` declared in pyproject |
| **Config file** | `pyproject.toml` → `[tool.pytest.ini_options]` |
| **Quick run command** | `.venv/bin/python -m pytest tests/test_driver_api.py tests/test_driver_imports.py -x -q` |
| **Full suite command** | `.venv/bin/python -m pytest -x -q` |
| **Estimated runtime** | ~1 s full suite; <5 s quick subset |

Note: 265 tests pass post-Phase 21. DISP-07 adds at least one test; DISP-08 deletes one duplicate. Post-21.1 count must be ≥ 265.

---

## Sampling Rate

- **After every task commit:** Run `.venv/bin/python -m pytest tests/test_driver_api.py tests/test_driver_imports.py -x -q`
- **After every plan wave:** Run `.venv/bin/python -m pytest -x -q` + `uv run mkdocs build --strict`
- **Before `/gsd:verify-work`:** Full suite green + mkdocs strict pass + humanizer grep clean
- **Max feedback latency:** ~5 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 21.1-01-01 | 01 | 1 | DISP-01, DISP-02 | — | Dispatcher pops `uri` and passes positionally when required-positional `uri` + `db_kwargs` sig detected; other shapes unchanged | unit | `pytest tests/test_driver_api.py -x` | ✅ | ⬜ pending |
| 21.1-01-02 | 01 | 1 | DISP-07 | — | New dispatch unit test pins the Family A' branch against a fake module | unit | `pytest tests/test_driver_api.py::TestDbApiModuleSignatureDispatch::test_family_a_prime_pops_uri_when_required_positional -x` | ❌ W0 (new test method) | ⬜ pending |
| 21.1-02-01 | 02 | 2 | DISP-03, DISP-04, DISP-05, DISP-06 | — | Three `Test*Imports` use signature-preserving `def connect(uri, *, db_kwargs=None)` stubs; happy-path `create_pool` returns working `QueuePool` for Quack/Postgres/FlightSQL when driver installed | semi-integration | `pytest tests/test_driver_imports.py::TestQuackImports tests/test_driver_imports.py::TestPostgreSQLImports tests/test_driver_imports.py::TestFlightSQLImports -x -q` | ✅ (upgrade existing) | ⬜ pending |
| 21.1-02-02 | 02 | 2 | DISP-08, DISP-09 | — | Duplicate `test_quack_returns_short_name` deleted from `tests/test_drivers.py`; full suite green | code-inspection + unit | `! grep -n '^def test_quack_returns_short_name' tests/test_drivers.py` && `pytest -x -q` | ✅ (deletion) | ⬜ pending |
| 21.1-03-01 | 03 | 2 | DISP-10, DISP-11 | — | "Dispatch contract" subsection appended to `_dbapi_module()` section of `custom-backends.md`; strict build passes; humanizer-grep clean | build + manual | `grep -q 'Dispatch contract' docs/src/guides/custom-backends.md && uv run mkdocs build --strict` | ✅ (extend) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] `tests/test_driver_api.py` — existing; new `TestDbApiModuleSignatureDispatch::test_family_a_prime_pops_uri_when_required_positional` method added in plan 21.1-01
- [x] `tests/test_driver_imports.py` — existing; stubs upgraded in plan 21.1-02
- [x] `tests/test_drivers.py` — existing; duplicate test removed in plan 21.1-02
- [x] `docs/src/guides/custom-backends.md` — existing; extended in plan 21.1-03
- [x] `pytest` — already installed (`.venv/`)
- [x] `mkdocs` + `mkdocs-material` + `mkdocstrings` — already installed

No new test fixtures, test files, or doc files required. Wave 0 is complete because the test/doc surface this phase modifies already exists.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Humanizer pass on new prose in `custom-backends.md` | DISP-11 | Style review is judgement-based; grep checks for the obvious AI tells but a human pass is still required | After writing the new subsection, apply the humanizer skill (or grep `grep -iE 'powerful\|seamlessly\|robust\|comprehensive\|delve\|leverage' docs/src/guides/custom-backends.md` returns no matches) and confirm the prose reads like the surrounding sections |

---

## Validation Sign-Off

- [x] All tasks have automated verify commands or Wave 0 dependencies satisfied
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (every task has a `<verify>` block per the PLAN.md files)
- [x] Wave 0 covers all MISSING references (none missing)
- [x] No watch-mode flags
- [x] Feedback latency < 10s (quick subset under 5s; full suite ~1s)
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-05-20
