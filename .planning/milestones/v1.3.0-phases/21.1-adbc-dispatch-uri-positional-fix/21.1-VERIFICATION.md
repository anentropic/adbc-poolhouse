---
phase: 21.1-adbc-dispatch-uri-positional-fix
verified: 2026-05-20T10:36:39Z
status: passed
score: 11/11 must-haves verified
overrides_applied: 0
---

# Phase 21.1: ADBC Dispatch URI-Positional Fix — Verification Report

**Phase Goal:** `create_pool()` returns a working `QueuePool` for every PyPI-driver backend (Quack, Postgres, FlightSQL) when the matching driver is installed — fixing the `TypeError: connect() missing 1 required positional argument: 'uri'` that breaks the documented quickstart.

**Verified:** 2026-05-20T10:36:39Z
**Status:** passed
**Re-verification:** No — initial verification
**Branch:** gsd/v1.3.0-quack-backend (HEAD = 07f5a55)

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Success Criterion | Status | Evidence |
| --- | ----------------- | ------ | -------- |
| SC-1 | `_driver_api.create_adbc_connection` correctly dispatches to PyPI driver `connect()` functions with required-positional `uri` AND `db_kwargs`, by popping `"uri"` and passing positionally | VERIFIED | `src/adbc_poolhouse/_driver_api.py:98-105` — `if uri_param is not None and uri_param.default is inspect.Parameter.empty: uri_val = kwargs.pop("uri"); mod.connect(uri_val, db_kwargs=kwargs)` |
| SC-2 | `create_pool(QuackConfig(uri="quack://..."))` returns a working `QueuePool` when `adbc-driver-quack` is installed | VERIFIED | `tests/test_driver_imports.py::TestQuackImports::test_create_pool_wiring` PASSED; asserts `captured["uri"] == "quack://h"` and `pool.dispose()` succeeds |
| SC-3 | `create_pool(PostgreSQLConfig(...))` returns a working `QueuePool` when driver installed | VERIFIED | `tests/test_driver_imports.py::TestPostgreSQLImports::test_create_pool_wiring` PASSED; asserts `captured["uri"] == "postgresql://h:5432/db"` after dispatch |
| SC-4 | `create_pool(FlightSQLConfig(...))` returns a working `QueuePool` when driver installed | VERIFIED | `tests/test_driver_imports.py::TestFlightSQLImports::test_create_pool_wiring` PASSED; FlightSQL stub with required-positional `uri` consumed via Family A' branch |
| SC-5 | Test mocks for Quack/Postgres/FlightSQL imports use signature-preserving stubs so this class of regression is caught by CI | VERIFIED | `grep -c 'def stub_connect' tests/test_driver_imports.py` → 3 (lines 130, 221, 276) — each defines a `def stub_connect(uri: str, ...)` literal, not bare `MagicMock` |
| SC-6 | Dedicated `tests/test_driver_api.py` unit test exercises the new uri-positional dispatch branch against a fake module | VERIFIED | `tests/test_driver_api.py:63-87` — `test_family_a_prime_pops_uri_when_required_positional` asserts `call_record["uri_positional"] == "quack://h:1"` and `"uri" not in db_kwargs` |
| SC-7 | Duplicate `test_quack_returns_short_name` removed from `tests/test_drivers.py` | VERIFIED | `grep -c '^def test_quack_returns_short_name' tests/test_drivers.py` → 0; in-class equivalent at `TestPyPIDriverPath::test_quack_missing_returns_package_name` (line 122) preserved |
| SC-8 | All existing tests pass; `mkdocs build --strict` passes; humanizer pass applied | VERIFIED | `.venv/bin/python -m pytest -x -q` → 265 passed in 0.56s; `.venv/bin/mkdocs build --strict` exits clean; humanizer grep on `docs/src/guides/custom-backends.md` returns no matches |

**Score:** 8/8 ROADMAP success criteria verified.

### Requirements Coverage (DISP-01 .. DISP-11)

| Req | Source Plan | Description | Status | Evidence |
| --- | ----------- | ----------- | ------ | -------- |
| DISP-01 | 21.1-01 | Detect required-positional `uri` + `db_kwargs` and pop `uri` to pass positionally | SATISFIED | `_driver_api.py:96-105` — `if "db_kwargs" in params:` then `if uri_param.default is inspect.Parameter.empty:` → `kwargs.pop("uri"); mod.connect(uri_val, db_kwargs=kwargs)` |
| DISP-02 | 21.1-01 | Other signature shapes (Family A optional-uri, no-uri; Family B no-db_kwargs) unchanged | SATISFIED | `_driver_api.py:106-111` — `else: conn = mod.connect(db_kwargs=kwargs)` for Family A; `else: conn = mod.connect(**kwargs)` for Family B. Verified by `test_family_a_uses_db_kwargs` and `test_family_b_unpacks_kwargs` PASS, plus `test_family_b_duckdb_integration` real-driver round-trip |
| DISP-03 | 21.1-02 | `create_pool(QuackConfig(uri="quack://h:p"))` returns working `QueuePool` when driver installed | SATISFIED | `TestQuackImports::test_create_pool_wiring` PASSED with signature-preserving stub; `pool.dispose()` succeeds and `captured["uri"] == "quack://h"` |
| DISP-04 | 21.1-02 | `create_pool(PostgreSQLConfig(...))` returns working `QueuePool` when driver installed | SATISFIED | `TestPostgreSQLImports::test_create_pool_wiring` PASSED; stub mirrors `adbc_driver_postgresql.dbapi.connect` shape (required-positional `uri`, `db_kwargs`, `**kwargs`) |
| DISP-05 | 21.1-02 | `create_pool(FlightSQLConfig(...))` returns working `QueuePool` when driver installed | SATISFIED | `TestFlightSQLImports::test_create_pool_wiring` PASSED; stub at `test_driver_imports.py:276-284` |
| DISP-06 | 21.1-02 | Three import tests patch `dbapi.connect` with signature-preserving stubs (not bare `MagicMock`) | SATISFIED | Three `def stub_connect(uri: str, ...)` literals at lines 130, 221, 276; each invoked via `patch("adbc_driver_<name>.dbapi.connect", new=stub_connect)` (lines 141, 233, 286) |
| DISP-07 | 21.1-01 | Dispatch-level unit test in `tests/test_driver_api.py` exercising the new uri-positional branch | SATISFIED | `test_family_a_prime_pops_uri_when_required_positional` at `test_driver_api.py:63-87` — uses real function (not MagicMock) so `inspect.signature` sees required-positional `uri` |
| DISP-08 | 21.1-02 | Delete duplicate `test_quack_returns_short_name` from `tests/test_drivers.py` | SATISFIED | `grep -c '^def test_quack_returns_short_name' tests/test_drivers.py` → 0; in-class `test_quack_missing_returns_package_name` retained as the canonical test |
| DISP-09 | 21.1-02 | All existing tests continue to pass (≥265 post-21) | SATISFIED | `pytest -x -q` → `265 passed in 0.56s` |
| DISP-10 | 21.1-03 | `docs/src/guides/custom-backends.md` updated with `_dbapi_module()` dispatch contract | SATISFIED | `Dispatch contract` H4 at line 118; three-row signature-shape table at lines 124-128 covering Family B (no `db_kwargs`), Family A (optional `uri`), and Family A' (required-positional `uri`) with concrete driver examples |
| DISP-11 | 21.1-03 | `mkdocs build --strict` passes; humanizer pass on new prose | SATISFIED | `.venv/bin/mkdocs build --strict` → clean exit (1.12s); `grep -iE 'powerful\|seamlessly\|robust\|comprehensive\|delve\|leverage\|elevate\|harness\|underpin' docs/src/guides/custom-backends.md` → no matches |

**Score:** 11/11 requirements satisfied.

### Required Artifacts (Three-Level Verification)

| Artifact | Expected | Exists | Substantive | Wired | Status |
| -------- | -------- | ------ | ----------- | ----- | ------ |
| `src/adbc_poolhouse/_driver_api.py` | Family A' branch with `inspect.Parameter.empty` check | YES | YES (line 98) | YES (called from `_driver.py`/`pool.py` via `create_adbc_connection`) | VERIFIED |
| `tests/test_driver_api.py` | New `test_family_a_prime_pops_uri_when_required_positional` | YES | YES (lines 63-87, 4 tests pass) | YES (collected and PASSED) | VERIFIED |
| `tests/test_driver_imports.py` | Three signature-preserving `stub_connect` for Quack/Postgres/FlightSQL | YES | YES (3 stubs, lines 130/221/276) | YES (13 tests PASSED) | VERIFIED |
| `tests/test_drivers.py` | Duplicate `test_quack_returns_short_name` removed | YES (file present) | YES (in-class equivalent retained line 122) | YES | VERIFIED |
| `docs/src/guides/custom-backends.md` | "Dispatch contract" subsection with three signature shapes | YES | YES (line 118 + table 124-128) | YES (rendered in mkdocs build) | VERIFIED |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| `create_pool()` (pool.py) | `_driver_api.create_adbc_connection` | function call with `dbapi_module=` | WIRED | TestQuackImports etc. exercise the full path end-to-end through `create_pool(config)` |
| `create_adbc_connection` | `mod.connect(uri_val, db_kwargs=kwargs)` | Family A' branch (line 105) | WIRED | `test_family_a_prime_pops_uri_when_required_positional` asserts both the positional `uri` and the kwargs-stripped `db_kwargs` |
| `QuackConfig._dbapi_module()` | `"adbc_driver_quack.dbapi"` | string return triggering dispatch | WIRED | TestQuackImports stub patches `adbc_driver_quack.dbapi.connect` and is hit by the dispatcher |
| `docs/src/guides/custom-backends.md` | mkdocs site | nav include | WIRED | `mkdocs build --strict` succeeds; file present in source tree |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Full test suite passes | `.venv/bin/python -m pytest -x -q` | `265 passed in 0.56s` | PASS |
| Dispatch unit tests pass | `.venv/bin/python -m pytest tests/test_driver_api.py -v` | `4 passed in 0.17s` (incl. `test_family_a_prime_pops_uri_when_required_positional`) | PASS |
| Driver import wiring passes | `.venv/bin/python -m pytest tests/test_driver_imports.py -v` | `13 passed in 0.15s` (all Quack/Postgres/FlightSQL stubs exercised) | PASS |
| mkdocs strict build | `.venv/bin/mkdocs build --strict` | clean exit, `Documentation built in 1.12 seconds` | PASS |
| Family A' branch present | `grep -n 'inspect.Parameter.empty' src/adbc_poolhouse/_driver_api.py` | `98:            if uri_param is not None and uri_param.default is inspect.Parameter.empty:` | PASS |
| Three signature-preserving stubs | `grep -c 'def stub_connect' tests/test_driver_imports.py` | `3` | PASS |
| Duplicate removed | `grep -c '^def test_quack_returns_short_name' tests/test_drivers.py` | `0` | PASS |
| Dispatch contract documented | `grep -q 'Dispatch contract' docs/src/guides/custom-backends.md` | match at line 118 | PASS |
| Humanizer clean | `grep -iE 'powerful\|seamlessly\|robust\|comprehensive\|delve\|leverage\|elevate\|harness\|underpin' docs/src/guides/custom-backends.md` | no matches | PASS |

### Anti-Patterns Found

None. Scanned files modified by this phase:
- `src/adbc_poolhouse/_driver_api.py`: no TBD/FIXME/XXX; no empty implementations; pop-with-KeyError is intentional fail-loud (documented at lines 102-103)
- `tests/test_driver_api.py`: real functions used so `inspect.signature` works correctly (not `MagicMock`)
- `tests/test_driver_imports.py`: signature-preserving stubs replace prior bare `MagicMock` patches
- `tests/test_drivers.py`: top-level duplicate cleanly removed
- `docs/src/guides/custom-backends.md`: prose passes humanizer grep; mkdocs strict build clean

### Gaps Summary

No gaps. Every ROADMAP success criterion has direct codebase evidence; every DISP-* requirement is satisfied by the artifacts and tests described above. The full test suite (265 tests) passes on the verifier's own re-run, the strict mkdocs build is clean, and the bug-fixing Family A' branch is exercised by both a focused dispatch-level unit test (`test_family_a_prime_pops_uri_when_required_positional`) and the three driver-specific semi-integration tests that mirror the affected real-driver signatures.

The fix closes Phase 21's QUACK-08 gap and addresses the latent v1.0.0 Postgres/FlightSQL bugs of the same shape. The dispatch contract is now documented for third-party backend authors at `docs/src/guides/custom-backends.md` line 118.

---

_Verified: 2026-05-20T10:36:39Z_
_Verifier: Claude (gsd-verifier)_
