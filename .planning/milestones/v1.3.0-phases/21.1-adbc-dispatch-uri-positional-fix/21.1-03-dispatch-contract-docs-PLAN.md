---
phase: 21.1-adbc-dispatch-uri-positional-fix
plan: 03
type: execute
wave: 2
depends_on:
  - 21.1-01
files_modified:
  - docs/src/guides/custom-backends.md
autonomous: true
requirements:
  - DISP-10
  - DISP-11

must_haves:
  truths:
    - "docs/src/guides/custom-backends.md _dbapi_module() section explains the three signature shapes the dispatcher handles"
    - "Third-party backend authors can read the guide and pick a connect() signature that the dispatcher routes correctly"
    - "uv run mkdocs build --strict exits 0"
    - "The new prose passes the humanizer checklist: no promotional adjectives, no AI vocabulary, ≤ one em dash per paragraph, second person"
  artifacts:
    - path: "docs/src/guides/custom-backends.md"
      provides: "Dispatch contract subsection inside the _dbapi_module() section"
      contains: "Dispatch contract"
  key_links:
    - from: "docs/src/guides/custom-backends.md `_dbapi_module()` section"
      to: "the three signature shapes Family A / Family A' / Family B"
      via: "table or prose listing each shape and the resulting call form"
      pattern: "Dispatch contract"
---

<objective>
Extend the `_dbapi_module()` section in `docs/src/guides/custom-backends.md`
with a short "Dispatch contract" subsection that documents the three
signature shapes the ADBC dispatcher handles. Third-party backend authors
should read this and know which `connect()` shape to choose so their driver
routes correctly, without having to read `_driver_api.py`.

Purpose: closes the documentation gap that contributed to three latent
dispatch bugs (Quack newly added; Postgres and FlightSQL latent since
v1.0.0). The Protocol contract itself is unchanged — only the dispatch
behaviour expansion is documented.

Output: an edited `custom-backends.md` whose `_dbapi_module()` section
appends a "Dispatch contract" subsection. `mkdocs build --strict` passes
and the humanizer pass leaves the new prose clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@.claude/skills/adbc-poolhouse-docs-author/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21.1-adbc-dispatch-uri-positional-fix/21.1-RESEARCH.md

<interfaces>
<!-- Existing _dbapi_module() section in docs/src/guides/custom-backends.md (lines 98-116): -->
<!-- - Heading: ### `_dbapi_module()` -->
<!-- - Explains: dotted Python module path vs None -->
<!-- - Code example: importlib.util.find_spec pattern -->
<!-- The Dispatch contract subsection appends AFTER this code block, BEFORE the next ## section ("Pool tuning"). -->

<!-- The three signature shapes (per RESEARCH.md Code Examples Example 5): -->

| Signature shape                                          | Call form                                | Examples                   |
|----------------------------------------------------------|------------------------------------------|----------------------------|
| No `db_kwargs` parameter                                  | `connect(**to_adbc_kwargs())`            | DuckDB, SQLite             |
| Has `db_kwargs`; `uri` absent or has a default            | `connect(db_kwargs=to_adbc_kwargs())`    | Snowflake, BigQuery        |
| Has `db_kwargs`; `uri` required-positional (no default)   | `connect(uri, db_kwargs={...})` (uri popped from kwargs) | PostgreSQL, FlightSQL, Quack |

<!-- Humanizer rules (per .claude/skills/adbc-poolhouse-docs-author/SKILL.md): -->
<!-- - Second person ("you") -->
<!-- - No promotional adjectives ("powerful", "seamlessly", "robust", "comprehensive") -->
<!-- - No AI vocabulary ("delve", "leverage", "elevate", "navigate the complexities") -->
<!-- - Max one em dash per paragraph -->
<!-- - Markdown syntax in any inline-symbol references (`QuackConfig`, NOT :class:`QuackConfig`) -->
<!-- - No new public symbols added so no Google-style docstring updates needed -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append "Dispatch contract" subsection to _dbapi_module() and verify strict build</name>
  <files>docs/src/guides/custom-backends.md</files>
  <read_first>
    - docs/src/guides/custom-backends.md (lines 80-120 — context around the `_dbapi_module()` section; verify the exact append point)
    - .planning/phases/21.1-adbc-dispatch-uri-positional-fix/21.1-RESEARCH.md (Code Examples Example 5 for the source markdown; Project Constraints section for humanizer rules)
    - .claude/skills/adbc-poolhouse-docs-author/SKILL.md (voice, humanizer checklist, em-dash policy)
  </read_first>
  <action>
    Append a `#### Dispatch contract` subsection immediately after the
    code block at the end of the existing `### \`_dbapi_module()\`` section
    in `docs/src/guides/custom-backends.md` (per DISP-10). The subsection
    should:

    - Open with one or two sentences explaining that when `_dbapi_module()`
      returns a string, `create_pool()` imports the module and inspects
      `connect()`'s signature to choose the call shape. Phrasing in
      second person ("you ship a driver", "your `connect()` signature").
    - Present the three signature shapes as a Markdown table with columns
      "Signature shape", "Call form", "Examples". Use the rows shown in the
      `<interfaces>` block above. Use Markdown inline code (backticks) for
      identifiers; do NOT use RST roles like `:func:` or `:class:`.
    - Close with one or two sentences of guidance: the simplest safe choice
      for new drivers is to keep `uri` optional (default `None`) so the
      dispatcher does not need to pop it from `db_kwargs`. Mention that
      `_dbapi_module()` should return `None` when the driver should route
      through `adbc_driver_manager.dbapi.connect()` (the Foundry path) —
      this is the right default for native shared-library drivers without
      a Python `dbapi` submodule.
    - Use Example 5 in RESEARCH.md as the source draft; you may rephrase
      for voice and humanizer compliance, but the table content must be
      preserved.

    Humanizer pass (per the adbc-poolhouse-docs-author skill):
    - Second person voice throughout.
    - No promotional adjectives ("powerful", "seamless", "seamlessly", "robust",
      "comprehensive", "elegant", "blazing", "blazingly").
    - No AI vocabulary ("delve", "leverage", "elevate", "underpin", "harness",
      "navigate", "ensure" used in the AI sense).
    - Maximum one em dash per paragraph (count both ASCII `--` and unicode `—`).
    - No vague attributions ("studies show", "many users").
    - Inline code uses Markdown backticks; cross-references use Markdown form
      `[QuackConfig][adbc_poolhouse.QuackConfig]` if used at all (none required
      for this subsection — keep it self-contained).

    Constraints:
    - Do NOT change any other content in `custom-backends.md` outside this
      append point.
    - Do NOT add cross-references to symbols whose anchors are not already
      resolved in the rendered docs (strict mode will fail on broken refs).
    - Do NOT introduce code that the strict build would treat as a broken
      cross-reference (use plain backtick inline code for all identifiers
      in the new subsection).
    - The heading level is `####` (four hashes) — it nests one level inside
      the existing `### \`_dbapi_module()\`` heading.
  </action>
  <verify>
    <automated>uv run mkdocs build --strict</automated>
    grep -q "Dispatch contract" docs/src/guides/custom-backends.md
    grep -iE "powerful|seamlessly|seamless|robust|comprehensive|delve|leverage|elevate|harness|underpin" docs/src/guides/custom-backends.md && exit 1 || exit 0
  </verify>
  <acceptance_criteria>
    - `docs/src/guides/custom-backends.md` contains a literal heading `#### Dispatch contract`
      inside the existing `### \`_dbapi_module()\`` section (verifiable by source assertion).
    - The new subsection contains a Markdown table with at least three rows describing
      the three signature shapes (`No db_kwargs parameter`, `Has db_kwargs ... uri ... default`,
      `Has db_kwargs ... uri required-positional`).
    - `uv run mkdocs build --strict` exits 0 (DISP-11 build gate).
    - The humanizer grep (`grep -iE 'powerful|seamlessly|seamless|robust|comprehensive|delve|leverage|elevate|harness|underpin'` against the file) returns zero matches inside the new subsection.
    - No existing content in `custom-backends.md` outside the new subsection is modified
      (git diff shows insertion-only change apart from possibly one trailing newline).
    - The new subsection uses Markdown inline code (backticks), not RST `:role:` syntax.
  </acceptance_criteria>
  <done>
    Third-party backend authors have a clear, self-contained note on the three
    signature shapes the dispatcher handles. Strict-mode build passes. Humanizer
    grep is clean.
  </done>
</task>

</tasks>

<verification>
After the task completes:

```bash
uv run mkdocs build --strict                              # DISP-11 build gate
grep -n "Dispatch contract" docs/src/guides/custom-backends.md   # DISP-10 source assertion
grep -iE 'powerful|seamlessly|seamless|robust|comprehensive|delve|leverage|elevate|harness|underpin' \
  docs/src/guides/custom-backends.md                       # must be empty
.venv/bin/python -m pytest -x -q                          # full suite remains green (cross-check that we did not break anything)
```
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs site → reader | Public documentation; misinformation could lead third-party authors to build dispatch-incompatible drivers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21.1-06 | Information Disclosure (incorrect specification) | new dispatch-contract subsection | mitigate | Source the three signature shapes from live `inspect.signature(...)` evidence captured in 21.1-RESEARCH.md; do not paraphrase the dispatch rule, copy it from the verified table |
| T-21.1-07 | Repudiation (broken strict build) | strict-mode mkdocs gate | mitigate | Verify `uv run mkdocs build --strict` exits 0 before marking the task complete; only Markdown syntax inside the new subsection, no RST roles or unresolved cross-refs |
| T-21.1-SC | Tampering | npm/pip/cargo installs | n/a | Phase adds zero packages |
</threat_model>

<success_criteria>
- `docs/src/guides/custom-backends.md` has a `#### Dispatch contract` subsection
  inside `### \`_dbapi_module()\``.
- The subsection clearly maps the three signature shapes to their call forms with
  concrete driver examples.
- `uv run mkdocs build --strict` exits 0.
- Humanizer grep is clean on the new prose.
- DISP-10 and DISP-11 evidence is captured in the source artifact and the build run.
</success_criteria>

<output>
Create `.planning/phases/21.1-adbc-dispatch-uri-positional-fix/21.1-03-SUMMARY.md` when done.
</output>
