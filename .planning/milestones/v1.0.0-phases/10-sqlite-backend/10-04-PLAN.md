---
phase: 10-sqlite-backend
plan: 04
type: execute
wave: 3
depends_on:
  - 10-02
  - 10-03
files_modified:
  - docs/src/guides/sqlite.md
  - mkdocs.yml
autonomous: false
requirements:
  - SQLT-05

must_haves:
  truths:
    - "uv run mkdocs build --strict passes with no errors"
    - "docs/src/guides/sqlite.md exists and is linked in mkdocs.yml nav under Warehouse Guides"
    - "SQLite guide covers install, file-backed connection, in-memory connection (pool_size=1 required), and env var loading"
    - "SQLite guide correctly states in-memory is SHARED across connections (not isolated per connection like DuckDB)"
    - "All new public symbols (SQLiteConfig) have Google-style docstrings in source code"
    - "Humanizer pass applied to sqlite.md prose"
  artifacts:
    - path: "docs/src/guides/sqlite.md"
      provides: "SQLite warehouse guide page"
      contains: "pool_size"
    - path: "mkdocs.yml"
      provides: "SQLite nav entry under Warehouse Guides"
      contains: "guides/sqlite.md"
  key_links:
    - from: "mkdocs.yml"
      to: "docs/src/guides/sqlite.md"
      via: "- SQLite: guides/sqlite.md in nav"
      pattern: "sqlite.md"
    - from: "docs/src/guides/sqlite.md"
      to: "src/adbc_poolhouse/_sqlite_config.py"
      via: "SQLiteConfig class referenced in code examples"
      pattern: "SQLiteConfig"
---

<objective>
Write the SQLite warehouse guide, add it to mkdocs.yml, and verify mkdocs build --strict passes. Then checkpoint for human verification of the rendered docs.

Purpose: Satisfies SQLT-05 and the CLAUDE.md quality gate — every phase from Phase 7 onward requires docs as a completion requirement.

Output: docs/src/guides/sqlite.md committed; mkdocs.yml updated; human approves rendered guide.
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
@.claude/skills/adbc-poolhouse-docs-author/SKILL.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-sqlite-backend/10-RESEARCH.md
@.planning/phases/10-sqlite-backend/10-01-SUMMARY.md
@.planning/phases/10-sqlite-backend/10-02-SUMMARY.md
@docs/src/guides/duckdb.md

<interfaces>
<!-- Current mkdocs.yml Warehouse Guides nav section: -->
```yaml
- Warehouse Guides:
  - Snowflake: guides/snowflake.md
  - DuckDB: guides/duckdb.md
  - BigQuery: guides/bigquery.md
  - PostgreSQL: guides/postgresql.md
  - FlightSQL: guides/flightsql.md
  - Databricks: guides/databricks.md
  - Redshift: guides/redshift.md
  - Trino: guides/trino.md
  - MSSQL: guides/mssql.md
  - Teradata: guides/teradata.md
  # ADD: - SQLite: guides/sqlite.md
```

<!-- duckdb.md structure to mirror (with SQLite-specific differences noted): -->
The duckdb.md guide has sections: install, Connection (file-backed, in-memory, read-only), env var loading, See also.
SQLite guide should have: install, Connection (file-backed, in-memory — no read-only section), env var loading, See also.
CRITICAL DIFFERENCE: SQLite in-memory text must say "shared across all connections" NOT "isolated per connection".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write SQLite guide and update mkdocs.yml</name>
  <files>docs/src/guides/sqlite.md, mkdocs.yml</files>
  <action>
Create docs/src/guides/sqlite.md mirroring docs/src/guides/duckdb.md structure, with these SQLite-specific differences:

1. Title: "# SQLite guide"

2. Install section: pip and uv install commands for adbc-poolhouse[sqlite]

3. Connection section:
   - File-backed subsection (same as DuckDB but with SQLiteConfig)
   - In-memory subsection — CRITICAL: the text must say:
     "pool_size=1 is required for in-memory databases. All connections in the pool share the same in-memory database — unlike DuckDB, where each connection gets its own isolated database."
     Do NOT say "each connection gets its own" for SQLite — that describes DuckDB behaviour.
   - NO read-only subsection (SQLite ADBC driver does not expose this option)

4. Env vars section: SQLITE_DATABASE, SQLITE_POOL_SIZE examples

5. See also section: links to configuration.md and pool-lifecycle.md (same as duckdb.md)

Voice guidelines (from docs-author skill):
- Direct and practical — second person ("you")
- No promotional language ("powerful", "seamlessly", "robust")
- No AI vocabulary ("leverage", "streamline", "ensure that")
- Self-contained with See also at bottom

After writing the draft, apply the humanizer pass:
- Remove any "this allows you to", "this enables", "this ensures" phrases
- No em-dash overuse (max one per paragraph)
- No superficial -ing openers ("By defining X, you can Y" → rewrite directly)

Then update mkdocs.yml:
- Find the Warehouse Guides section
- Add `- SQLite: guides/sqlite.md` as the last entry under Warehouse Guides (after Teradata)
  </action>
  <verify>
    <automated>cd /Users/paul/Documents/Dev/Personal/adbc-poolhouse && uv run mkdocs build --strict 2>&1 | tail -10</automated>
  </verify>
  <done>docs/src/guides/sqlite.md exists with correct content (shared in-memory note, no read-only section); mkdocs.yml has SQLite nav entry; uv run mkdocs build --strict exits 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human review of rendered SQLite guide</name>
  <action>Human verifies the rendered SQLite guide in the browser. No code changes — this is a verification-only checkpoint after Task 1 automation completes.</action>
  <verify>Human opens http://127.0.0.1:8000/guides/sqlite/ and confirms content is correct.</verify>
  <done>Human types "approved".</done>
  <what-built>
    SQLite warehouse guide at docs/src/guides/sqlite.md, linked in mkdocs.yml nav. The guide covers install, file-backed pool, in-memory pool (pool_size=1 required, shared-state note), env var loading, and See also links.
  </what-built>
  <how-to-verify>
    Run: `just serve` (or `uv run mkdocs serve --dirty`) then open http://127.0.0.1:8000/guides/sqlite/

    Check:
    1. SQLite appears under Warehouse Guides in the nav sidebar
    2. Install section shows pip and uv commands for adbc-poolhouse[sqlite]
    3. In-memory section says pool_size=1 is required and mentions "shared across all connections" (not "isolated")
    4. No read-only section is present
    5. Env vars section shows SQLITE_DATABASE and SQLITE_POOL_SIZE examples
    6. See also links resolve correctly
    7. Page reads naturally — no AI-sounding phrases
  </how-to-verify>
  <resume-signal>Type "approved" if the guide looks correct, or describe any issues to fix.</resume-signal>
</task>

</tasks>

<verification>
```bash
cd /Users/paul/Documents/Dev/Personal/adbc-poolhouse && uv run mkdocs build --strict 2>&1 | grep -E "ERROR|WARNING|Done" | tail -5
```
</verification>

<success_criteria>
- docs/src/guides/sqlite.md created with correct SQLite-specific content (shared in-memory behaviour, no read-only section)
- mkdocs.yml nav includes SQLite entry under Warehouse Guides
- uv run mkdocs build --strict exits 0
- Human approves guide at http://127.0.0.1:8000/guides/sqlite/
- Humanizer pass applied — no promotional or AI-vocabulary phrases
</success_criteria>

<output>
After completion, create `.planning/phases/10-sqlite-backend/10-04-SUMMARY.md`
</output>
