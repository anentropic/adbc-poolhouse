---
phase: 12-clickhouse-backend
plan: "04"
type: execute
wave: 4
depends_on:
  - "12-03"
files_modified:
  - docs/src/guides/clickhouse.md
  - docs/src/guides/configuration.md
  - mkdocs.yml
autonomous: false
requirements:
  - CH-05

must_haves:
  truths:
    - "docs/src/guides/clickhouse.md exists with Foundry install note, URI mode, individual fields mode, env var table, See also"
    - "configuration.md env_prefix table includes ClickHouseConfig row with CLICKHOUSE_ prefix"
    - "mkdocs.yml Warehouse Guides nav includes clickhouse.md"
    - "uv run mkdocs build --strict passes with zero warnings"
    - "ClickHouseConfig listed alongside Foundry drivers in the Foundry note in configuration.md"
  artifacts:
    - path: "docs/src/guides/clickhouse.md"
      provides: "ClickHouse warehouse guide page"
      min_lines: 60
    - path: "docs/src/guides/configuration.md"
      provides: "CLICKHOUSE_ row in env_prefix table"
      contains: "CLICKHOUSE_"
    - path: "mkdocs.yml"
      provides: "ClickHouse nav entry"
      contains: "clickhouse.md"
  key_links:
    - from: "docs/src/guides/clickhouse.md"
      to: "adbc_poolhouse.ClickHouseConfig"
      via: "mkdocstrings autorefs [adbc_poolhouse.ClickHouseConfig]"
      pattern: "\\[adbc_poolhouse\\.ClickHouseConfig\\]"
    - from: "mkdocs.yml"
      to: "docs/src/guides/clickhouse.md"
      via: "nav entry in Warehouse Guides section"
      pattern: "clickhouse\\.md"
---

<objective>
Create the ClickHouse warehouse guide page and update the three documentation surfaces (guide page, configuration.md env_prefix table, mkdocs.yml nav) so that `uv run mkdocs build --strict` passes with ClickHouse fully integrated.

Purpose: Documentation is a quality gate for all phases >= 7 (CLAUDE.md). Phase 12 is not complete until docs build strict.
Output: clickhouse.md guide, configuration.md row, mkdocs.yml nav entry — then human checkpoint to verify the docs are correct.
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
@.claude/skills/adbc-poolhouse-docs-author/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-clickhouse-backend/12-03-SUMMARY.md
@docs/src/guides/mysql.md
@docs/src/guides/configuration.md
@mkdocs.yml

<interfaces>
<!-- Key doc patterns extracted from mysql.md (reference guide for ClickHouse). -->

mysql.md structure (copy with ClickHouse-specific content):
1. Heading: # MySQL guide
2. Foundry distribution note + install command
3. pip install (no extra needed)
4. ## Connection (two modes: URI mode, Individual fields)
5. ## Loading from environment variables
6. ## See also

configuration.md env_prefix table location:
- Table has columns: Config class | env_prefix
- MySQLConfig row: | [`MySQLConfig`][adbc_poolhouse.MySQLConfig] | `MYSQL_` |
- Add ClickHouseConfig row immediately after MySQLConfig row (or in alphabetical position)

configuration.md Foundry note (line ~72):
- Currently lists: DatabricksConfig, MySQLConfig, RedshiftConfig, TrinoConfig, MSSQLConfig
- ADD ClickHouseConfig to this list

mkdocs.yml nav — Warehouse Guides section (line ~107):
- Current: MySQL: guides/mysql.md
- ADD: ClickHouse: guides/clickhouse.md (alphabetical — C before M)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create clickhouse.md warehouse guide and update 3 doc surfaces</name>
  <files>docs/src/guides/clickhouse.md, docs/src/guides/configuration.md, mkdocs.yml</files>
  <action>
**1. Create docs/src/guides/clickhouse.md** — model on mysql.md with ClickHouse-specific differences:

Key differences from MySQL guide:
- Install command: `dbc install --pre clickhouse` (note: `--pre` flag required — alpha only)
- Individual fields section: uses `username` not `user`; shows `password`, `database`, `port` as optional
- No password-required note (ClickHouse `default` user often has no password in dev setups)
- Default port note: 8123 (HTTP interface)
- Env prefix: `CLICKHOUSE_` (CLICKHOUSE_HOST, CLICKHOUSE_USERNAME, CLICKHOUSE_PASSWORD, CLICKHOUSE_DATABASE)

Write the guide following the docs-author skill voice (direct, second person, no promotional language):

```markdown
# ClickHouse guide

The ClickHouse ADBC driver is distributed via the ADBC Driver Foundry, not PyPI.
Follow the [Foundry installation guide](https://arrow.apache.org/adbc/current/driver/installation.html) to install it before using [`ClickHouseConfig`][adbc_poolhouse.ClickHouseConfig].

The driver is currently in alpha. Use the `--pre` flag when installing:

    dbc install --pre clickhouse

`adbc-poolhouse` does not need a separate extra for ClickHouse:

    pip install adbc-poolhouse

## Connection

[`ClickHouseConfig`][adbc_poolhouse.ClickHouseConfig] connects to a ClickHouse server. Specify the connection in one of two
ways: a full URI or individual fields (`host` and `username` together, with optional
`password`, `database`, and `port`).

Construction raises [`ConfigurationError`][adbc_poolhouse.ConfigurationError] if neither mode is fully specified.

### URI mode

    from pydantic import SecretStr
    from adbc_poolhouse import ClickHouseConfig, create_pool

    config = ClickHouseConfig(
        uri=SecretStr("http://default:password@localhost:8123/mydb"),  # pragma: allowlist secret
    )
    pool = create_pool(config)

### Individual fields

    from adbc_poolhouse import ClickHouseConfig, create_pool

    config = ClickHouseConfig(
        host="localhost",
        username="default",
        password="password",  # pragma: allowlist secret
        database="mydb",
    )
    pool = create_pool(config)

`password` and `database` are optional. `port` defaults to `8123` (HTTP interface).

The field name is `username`, not `user`. The Columnar ClickHouse driver uses `username`
as the connection kwarg. Passing the wrong key causes a silent authentication failure.

## Loading from environment variables

[`ClickHouseConfig`][adbc_poolhouse.ClickHouseConfig] reads all fields from environment variables with the `CLICKHOUSE_` prefix:

    export CLICKHOUSE_HOST=localhost
    export CLICKHOUSE_USERNAME=default
    export CLICKHOUSE_PASSWORD=password  # pragma: allowlist secret
    export CLICKHOUSE_DATABASE=mydb

    config = ClickHouseConfig()  # reads from env
    pool = create_pool(config)

## See also

- [Configuration reference](configuration.md) — env_prefix, pool tuning
- [Pool lifecycle](pool-lifecycle.md) — close_pool, pytest fixtures
```

Apply humanizer pass (docs-author skill Step 3) — eliminate "seamlessly", "powerful", "ensure that", "delve", "leverage". Check no em-dash overuse.

**2. Update docs/src/guides/configuration.md**:
- Add `| [\`ClickHouseConfig\`][adbc_poolhouse.ClickHouseConfig] | \`CLICKHOUSE_\` |` to the env_prefix table (after MySQLConfig row)
- Add `ClickHouseConfig` to the Foundry drivers note (the sentence listing DatabricksConfig, MySQLConfig, RedshiftConfig, TrinoConfig, MSSQLConfig — add ClickHouseConfig to this list)
- Read the full file first; only modify these two targeted locations

**3. Update mkdocs.yml**:
- Add `ClickHouse: guides/clickhouse.md` to the Warehouse Guides nav section (alphabetical — before MySQL, since C < M)
- Read the full file first; only modify the nav section

Run build to check: `uv run mkdocs build --strict 2>&1 | tail -5`
  </action>
  <verify>
    <automated>uv run mkdocs build --strict 2>&1 | tail -10</automated>
  </verify>
  <done>clickhouse.md exists with correct content; CLICKHOUSE_ row in configuration.md; clickhouse.md in mkdocs.yml nav; uv run mkdocs build --strict exits 0</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify complete ClickHouse backend</name>
  <action>Human verifies the complete ClickHouse backend implementation is correct and all automated checks pass.</action>
  <what-built>
    Complete ClickHouse backend (Plans 01-04):
    - ClickHouseConfig class with model_validator, SecretStr fields, env_prefix CLICKHOUSE_
    - translate_clickhouse() returning direct kwargs (username, host, port as str, optional password+database)
    - _FOUNDRY_DRIVERS registration, _translators.py dispatch, __init__.py public export
    - Full test suite: config, translator, driver resolution, mock pool wiring — all passing
    - Docs: clickhouse.md guide, configuration.md row, mkdocs.yml nav — mkdocs build --strict passes
  </what-built>
  <how-to-verify>
    1. Run full test suite: `uv run pytest -x -q` — verify all tests pass, look for any ClickHouse failures
    2. Run targeted ClickHouse tests: `uv run pytest -k "clickhouse or ClickHouse" -v` — verify at least 20 tests collected and all pass
    3. Run mkdocs build: `uv run mkdocs build --strict` — verify exit 0, no warnings
    4. Check public import: `uv run python -c "from adbc_poolhouse import ClickHouseConfig; print(list(ClickHouseConfig.model_fields.keys()))"` — verify username (not user) appears in field list
    5. Check username kwarg: `uv run python -c "from adbc_poolhouse._clickhouse_translator import translate_clickhouse; from adbc_poolhouse._clickhouse_config import ClickHouseConfig; r = translate_clickhouse(ClickHouseConfig(host='h', username='u')); assert 'username' in r and 'user' not in r; print('username kwarg OK:', r)"`
    6. Verify docs: Check docs/src/guides/clickhouse.md rendered — look for the username/user note, the --pre install flag, and both connection modes
    7. Check configuration.md has CLICKHOUSE_ row: `grep "CLICKHOUSE_" docs/src/guides/configuration.md`
  </how-to-verify>
  <verify>
    <automated>uv run pytest -x -q && uv run mkdocs build --strict 2>&1 | tail -3</automated>
  </verify>
  <done>Human approves: all tests green, mkdocs builds strict, username kwarg confirmed, docs content correct</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Pre-checkpoint automated verification:

```bash
# Full suite
uv run pytest -x -q

# ClickHouse-specific
uv run pytest -k "clickhouse or ClickHouse" -v

# Docs build
uv run mkdocs build --strict

# Public API check
uv run python -c "from adbc_poolhouse import ClickHouseConfig; assert 'ClickHouseConfig' in __import__('adbc_poolhouse').__all__; print('public API ok')"

# Kwarg naming check
uv run python -c "
from adbc_poolhouse._clickhouse_translator import translate_clickhouse
from adbc_poolhouse._clickhouse_config import ClickHouseConfig
r = translate_clickhouse(ClickHouseConfig(host='h', username='u'))
assert 'username' in r, f'username key missing: {r}'
assert 'user' not in r, f'user key should not be present: {r}'
print('username kwarg correct')
"
```
</verification>

<success_criteria>
- docs/src/guides/clickhouse.md exists with Foundry install note (dbc install --pre clickhouse), URI mode, individual fields mode (using username not user), env var table, See also section
- configuration.md CLICKHOUSE_ row present; ClickHouseConfig listed in Foundry backends note
- mkdocs.yml nav has ClickHouse: guides/clickhouse.md in Warehouse Guides section
- `uv run mkdocs build --strict` exits 0
- `uv run pytest -x -q` exits 0
- Human checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/12-clickhouse-backend/12-04-SUMMARY.md`
</output>
