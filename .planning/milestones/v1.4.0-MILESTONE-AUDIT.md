---
milestone: v1.4.0
milestone_name: Async API
audited: 2026-07-01
status: passed
scores:
  requirements: 63/63
  phases: 7/7
  integration: 4/4 flows wired (0 blockers)
  flows: 4/4
gaps: {}
tech_debt:
  - phase: 25-cancellation
    items:
      - "Docstring drift in src/adbc_poolhouse/_async/_cancel.py (lines ~88-91, ~170): the prose describes worker_started being bridged loop-ward via anyio.from_thread.run_sync, but the shipped implementation sets the flag synchronously on the loop thread in _offload.py (no from_thread bridge exists). Non-functional — the race-freedom claim (CANCEL-01) still holds; only the narrative is stale. Recommend aligning the _cancel.py docstring with the synchronous-on-loop-thread model."
  - phase: milestone-wide
    items:
      - "9 stale quick-task tracking artifacts flagged by audit-open (mostly pre-v1.4.0: readme/justfile/docs/mkdocs items). Tracking cruft, not v1.4.0 scope — defer to backlog cleanup (/gsd-cleanup)."
nyquist:
  compliant_phases: [24, 26]
  partial_phases: [22, 23, 25, 27]
  missing_phases: [28]
  overall: partial
  note: "Discovery only — VALIDATION.md frontmatter for 22/23/25/27 reads nyquist_compliant:false (mostly draft status), 28 has no VALIDATION.md (docs phase). Does NOT force gaps_found: every requirement is independently satisfied via VERIFICATION + SUMMARY + traceability + a clean integration check, and the full suite is green on Linux CI. Nyquist formalization is optional retroactive polish, not a coverage gap."
human_checkpoints_discharged:
  evidence: "GitHub Actions run 28481988002 (branch gsd/v1.4.0-async-api, push 2026-06-30)"
  jobs: ["Quality gates (3.11) = success", "Quality gates (3.14) = success", "Sync suite without anyio = success"]
  cleared:
    - "Phase 26 VERIFICATION human_needed → passed (PKG-04 live sync-no-anyio run observed green on Linux)"
    - "Phase 27 VERIFICATION human_needed → passed (dual-backend quality job green on Linux — the macOS-vs-Linux hang landmine cleared)"
    - "Phase 27 UAT pending scenario #1 → passed (same evidence)"
---

# Milestone Audit — v1.4.0 Async API

**Audited:** 2026-07-01
**Status:** ✅ passed
**Scope:** Phases 22–28 (optional async API behind an `[async]` extra, wrapping the sync core unchanged)

## Summary

All 63 v1.4.0 requirements are satisfied, all seven phases are verified, cross-phase
integration is clean (0 blockers, 4/4 E2E flows wired), and the full suite — including
the dual-backend async matrix under asyncio and trio — is green on Linux CI. The two
`human_needed` verification checkpoints and the one pending UAT scenario were all the
same thing (observe the green Linux CI run) and have been discharged with evidence.

## Requirements coverage (3-source cross-reference)

63/63 satisfied. Every REQ-ID is `[x]` in REQUIREMENTS.md, listed in its phase SUMMARY
`requirements-completed`, and marked verified in the phase VERIFICATION.md, with the
integration check confirming the wiring.

| Requirement group | Count | Phase | Verification | Final status |
|---|---|---|---|---|
| SPIKE-01..03 | 3 | 22 | passed | satisfied |
| CORE-01..04 | 4 | 24 | passed | satisfied |
| APOOL-01..03 | 3 | 24 | passed | satisfied |
| ACONN-01..06 | 6 | 24 | passed | satisfied |
| ACUR-01..07 | 7 | 24 | passed | satisfied |
| CANCEL-01..04 | 4 | 25 | passed | satisfied |
| PKG-01..05 | 5 | 26 | passed (checkpoint discharged) | satisfied |
| TEST-01..05 | 5 | 23, 27 | passed | satisfied |
| EDGE (22 ids) | 22 | 24, 25, 27 | passed | satisfied |
| DOCS-01..04 | 4 | 28 | passed | satisfied |

No unsatisfied requirements, no orphans (every REQ-ID appears in a phase VERIFICATION).

## Phase verification

| Phase | VERIFICATION | Notes |
|---|---|---|
| 22 Feasibility Spike | passed | GIL go/no-go recorded; gated Phase 24 |
| 23 Test Harness Foundation | passed | BlockingStub harness + AST guards |
| 24 Core Async Wrapper | passed | full DBAPI surface, per-pool limiter |
| 25 Cancellation | passed | adbc_cancel + shielded invalidate; 1 non-functional docstring drift (tech debt) |
| 26 Packaging & Extra Scoping | passed | `[async]` extra, PEP 562 lazy import; live sync-no-anyio CI green |
| 27 Dual-Backend Test Matrix | passed | asyncio+trio × DuckDB+Snowflake; Linux CI green |
| 28 Documentation | passed | async guide + API reference + mkdocs --strict |

## Cross-phase integration

Independent integration check (gsd-integration-checker): **0 blockers, 0 orphaned
exports, 4/4 E2E flows wired.**

1. Async happy path — `create_async_pool` → `_create_pool_impl` (sync core unchanged) → `AsyncPool.connect` → `AsyncCursor` → offloaded `execute`/`fetch_arrow_table` → shielded checkin → `close_async_pool`. WIRED.
2. Cancellation — cursor `on_abort=invalidate` → `cancellable_offload` watcher → single `adbc_cancel` + invalidate → `checkedout() == 0`. WIRED.
3. Zero-cost sync — PEP 562 `__getattr__` lazy export; `import adbc_poolhouse` + sync suite succeed with anyio absent; async access raises ImportError naming `[async]`. WIRED.
4. Arrow reset symmetry — async checkin/invalidate route through the shared `_release_arrow_allocators` reset event registered in `_create_pool_impl`. WIRED.

## Tech debt (non-blocking)

- **Docstring drift, `_async/_cancel.py`** — narrative describes a `from_thread` bridge the correct synchronous implementation doesn't use. Non-functional; align prose when convenient.
- **9 stale quick-task artifacts** — mostly pre-v1.4.0 (readme/justfile/docs). Defer to `/gsd-cleanup`.
- **Nyquist VALIDATION** — 22/23/25/27 read `nyquist_compliant:false` (draft), 28 has none. Optional retroactive formalization; coverage is independently satisfied.

## Verdict

Milestone v1.4.0 achieved its definition of done. Cleared to complete.
