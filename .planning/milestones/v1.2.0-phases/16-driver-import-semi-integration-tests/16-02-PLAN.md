---
phase: 16-driver-import-semi-integration-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - justfile
autonomous: true
requirements:
  - TEST-01

must_haves:
  truths:
    - "Developer can run `just install-all-drivers` to install all 12 driver packages"
    - "Command installs PyPI drivers via uv pip"
    - "Command installs Foundry drivers via dbc CLI"
  artifacts:
    - path: "justfile"
      provides: "install-all-drivers recipe"
      contains: "install-all-drivers"
  key_links:
    - from: "justfile"
      to: "PyPI + Foundry drivers"
      via: "uv pip + dbc install"
      pattern: "install-all-drivers"
---

<objective>
Add a justfile recipe for installing all 12 ADBC driver packages in one command, combining PyPI extras and Foundry dbc installations.

Purpose: Enable local developers to set up their environment with a single command for running the semi-integration tests.

Output: Updated `justfile` with `install-all-drivers` recipe.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.2.0-phases/01-driver-import-semi-integration-tests/01-CONTEXT.md
@.planning/milestones/v1.2.0-phases/01-driver-import-semi-integration-tests/01-RESEARCH.md

<interfaces>
From justfile (existing patterns):
```just
# Install the dbc CLI for Foundry driver management
install-dbc:
    command -v dbc || curl -LsSf https://dbc.columnar.tech/install.sh | sh

# Install Foundry ADBC drivers
install-foundry-drivers:
    dbc install mysql
    dbc install databricks
    dbc install --pre clickhouse
```

From RESEARCH.md driver installation reference:
- PyPI drivers (via extras): duckdb, snowflake, bigquery, postgresql, flightsql, sqlite
- Foundry drivers (via dbc): databricks, redshift, trino, mssql, mysql, clickhouse
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add install-all-drivers recipe to justfile</name>
  <files>justfile</files>
  <action>
    Add a new `install-all-drivers` recipe to the justfile that:

    1. First ensures dbc CLI is installed (calls existing `install-dbc` recipe)
    2. Installs PyPI drivers via uv pip with extras:
       - duckdb, snowflake, bigquery, postgresql, flightsql, sqlite
    3. Installs Foundry drivers via dbc:
       - databricks, redshift, trino, mssql, mysql, clickhouse

    Add this recipe after the existing `install-foundry-drivers` recipe:

    ```just
    # Install all 12 ADBC drivers (PyPI + Foundry) for semi-integration tests.
    # PyPI drivers: duckdb, snowflake, bigquery, postgresql, flightsql, sqlite
    # Foundry drivers: databricks, redshift, trino, mssql, mysql, clickhouse
    install-all-drivers: install-dbc
        uv pip install -e ".[duckdb,snowflake,bigquery,postgresql,flightsql,sqlite]"
        dbc install databricks
        dbc install redshift
        dbc install trino
        dbc install mssql
        dbc install mysql
        dbc install --pre clickhouse
    ```

    Notes:
    - Uses `install-dbc` as a dependency to ensure CLI is available
    - PyPI extras installed in single uv pip command for efficiency
    - Foundry drivers installed one at a time (dbc doesn't support multiple args per RESEARCH.md)
    - ClickHouse requires `--pre` flag (only alpha version published)
  </action>
  <verify>
    <automated>grep -q "install-all-drivers" justfile && just --list | grep -q "install-all-drivers"</automated>
  </verify>
  <done>
    `just --list` shows `install-all-drivers` recipe.
    Recipe installs all 12 driver packages when executed.
  </done>
</task>

</tasks>

<verification>
Phase complete when:
- [ ] `justfile` contains `install-all-drivers` recipe
- [ ] Recipe depends on `install-dbc` for CLI setup
- [ ] Recipe installs all 6 PyPI driver extras
- [ ] Recipe installs all 6 Foundry drivers via dbc
- [ ] `just --list` shows the new recipe
</verification>

<success_criteria>
- Single command (`just install-all-drivers`) installs all 12 drivers
- Recipe follows existing justfile patterns
- Recipe documented with comment explaining what it installs
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.2.0-phases/01-driver-import-semi-integration-tests/01-02-SUMMARY.md`
</output>
