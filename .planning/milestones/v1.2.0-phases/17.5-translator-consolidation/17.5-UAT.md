---
status: complete
phase: 17.5-translator-consolidation
source: 17.5-01-SUMMARY.md, 17.5-02-SUMMARY.md, 17.5-03-SUMMARY.md, 17.5-04-SUMMARY.md, 17.5-05-SUMMARY.md
started: 2026-03-14T19:00:00Z
updated: 2026-03-14T19:15:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Full Test Suite Passes
expected: Run `uv run pytest` — all 315 tests pass with zero failures, zero errors. No regressions from the translator consolidation.
result: [pending]

### 2. to_adbc_kwargs() Parity with Old Translators
expected: Run `uv run pytest tests/test_translators.py -k "matches_translate"` — all parity tests pass, confirming every config's to_adbc_kwargs() produces identical output to the old standalone translator functions.
result: issue
reported: "12 standalone _*_translator.py files are dead code — no runtime imports remain. Parity tests (matches_translate_*) are also unnecessary since to_adbc_kwargs() tests should assert correct output directly, not just match old functions. Both the translator files and parity tests should be deleted."
severity: minor

### 3. translate_config() Delegates to Config Method
expected: Run `uv run python -c "from adbc_poolhouse._translators import translate_config; from adbc_poolhouse._snowflake_config import SnowflakeConfig; c = SnowflakeConfig.model_validate({'account':'test','user':'u','password':'p','database':'db','schema':'PUBLIC','warehouse':'wh'}); print(translate_config(c))"` — prints a dict with ADBC Snowflake keys (adbc.snowflake.sql.account, username, password, etc). No errors.
result: issue
reported: "translate_config() is superfluous — it's a one-line wrapper around config.to_adbc_kwargs(). Only caller is _pool_factory.py:75. Should inline config.to_adbc_kwargs() at call site and delete _translators.py entirely."
severity: minor

### 4. Registry Stores 2-Tuple (No Translator)
expected: Run `uv run python -c "from adbc_poolhouse._registry import _registry, ensure_registered; ensure_registered('snowflake'); entry = _registry['snowflake']; print(f'Tuple length: {len(entry)}'); print(f'Type[0]: {entry[0].__name__}'); print(f'Type[1]: {type(entry[1]).__name__}')"` — shows "Tuple length: 2", config class name, and a string (driver path). Not a 3-tuple.
result: [pending]

### 5. No Translator Imports in _drivers.py
expected: Run `grep -c "translator" src/adbc_poolhouse/_drivers.py` — returns 0 (no translator references remain in driver registrations).
result: issue
reported: "Registration system is overcomplicated. _setup_lazy_registrations adds indirection for no clear benefit. If configs have a standard method for driver path (like to_adbc_kwargs()), registration is unnecessary — just pass the config. Also want a secondary create_pool that takes raw driver path + dict kwargs."
severity: major

## Summary

total: 5
passed: 2
issues: 3
pending: 0
skipped: 0

## Gaps

- truth: "Standalone translator files removed after consolidation to config.to_adbc_kwargs()"
  status: failed
  reason: "User reported: 12 standalone _*_translator.py files are dead code — no runtime imports remain. Parity tests (matches_translate_*) are also unnecessary since to_adbc_kwargs() tests should assert correct output directly, not just match old functions. Both the translator files and parity tests should be deleted."
  severity: minor
  test: 2
  artifacts: []
  missing: []
- truth: "translate_config() wrapper removed — callers use config.to_adbc_kwargs() directly"
  status: failed
  reason: "User reported: translate_config() is superfluous — one-line wrapper around config.to_adbc_kwargs(). Only caller is _pool_factory.py:75. Should inline and delete _translators.py."
  severity: minor
  test: 3
  artifacts: []
  missing: []
- truth: "Registration system simplified or removed — configs self-describe driver path"
  status: failed
  reason: "User reported: Registration system is overcomplicated. _setup_lazy_registrations adds indirection for no clear benefit. If configs have a standard method for driver path (like to_adbc_kwargs()), registration is unnecessary — just pass the config. Also want a secondary create_pool that takes raw driver path + dict kwargs."
  severity: major
  test: 5
  artifacts: []
  missing: []
