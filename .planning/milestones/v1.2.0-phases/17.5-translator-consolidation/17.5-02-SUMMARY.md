---
phase: 17.5-translator-consolidation
plan: 02
subsystem: api
tags: [pydantic, adbc, translator, config, duckdb, bigquery, sqlite, clickhouse]

# Dependency graph
requires:
  - phase: 17.5-translator-consolidation
    provides: "WarehouseConfig protocol with to_adbc_kwargs(), Snowflake reference implementation"
provides:
  - "DuckDBConfig.to_adbc_kwargs() -- database->path mapping with conditional access_mode"
  - "BigQueryConfig.to_adbc_kwargs() -- adbc.bigquery.sql.* prefixed keys with SecretStr extraction"
  - "SQLiteConfig.to_adbc_kwargs() -- database->uri single-key mapping"
  - "ClickHouseConfig.to_adbc_kwargs() -- URI mode and decomposed mode with port-as-string"
affects: [17.5-translator-consolidation, 18-entry-point-discovery]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Direct method implementation for simple backends (no Field aliases)"
    - "SecretStr.get_secret_value() for credential extraction in to_adbc_kwargs()"
    - "Parity test pattern: assert config.to_adbc_kwargs() == translate_backend(config)"

key-files:
  created: []
  modified:
    - "src/adbc_poolhouse/_duckdb_config.py"
    - "src/adbc_poolhouse/_bigquery_config.py"
    - "src/adbc_poolhouse/_sqlite_config.py"
    - "src/adbc_poolhouse/_clickhouse_config.py"
    - "tests/test_translators.py"

key-decisions:
  - "Direct method implementation for all 4 backends (no model_dump aliases) -- field-to-key mappings are too divergent for alias-based approach"

patterns-established:
  - "Parity tests: each TestXxxToAdbcKwargs class includes a test_matches_translate_xxx() method asserting identical output"
  - "Pool field exclusion test: every to_adbc_kwargs test class verifies pool_size/max_overflow/timeout/recycle are absent"

requirements-completed: []

# Metrics
duration: 5min
completed: 2026-03-14
---

# Plan 02: Simple Backend Implementations Summary

**to_adbc_kwargs() for DuckDB, BigQuery, SQLite, and ClickHouse with 28 parity-verified tests**

## Performance

- **Duration:** 5 min
- **Started:** 2026-03-14T18:20:53Z
- **Completed:** 2026-03-14T18:25:55Z
- **Tasks:** 4
- **Files modified:** 5

## Accomplishments
- Implemented to_adbc_kwargs() on 4 config classes (DuckDB, BigQuery, SQLite, ClickHouse)
- Added 28 new tests across 4 TestXxxToAdbcKwargs classes with parity checks against standalone translators
- translate_config() dispatch now uses to_adbc_kwargs() directly for these 4 backends (no fallback needed)

## Task Commits

Each task was committed atomically:

1. **Task 1: DuckDBConfig.to_adbc_kwargs** - `1140317` (feat)
2. **Task 2: BigQueryConfig.to_adbc_kwargs** - `0022130` (feat)
3. **Task 3: SQLiteConfig.to_adbc_kwargs** - `cc16e26` (feat)
4. **Task 4: ClickHouseConfig.to_adbc_kwargs** - `4ceed15` (feat)

_Note: TDD tasks each had RED (failing tests) followed by GREEN (implementation) in a single commit._

## Files Created/Modified
- `src/adbc_poolhouse/_duckdb_config.py` - Added to_adbc_kwargs() mapping database->path, read_only->access_mode
- `src/adbc_poolhouse/_bigquery_config.py` - Added to_adbc_kwargs() with adbc.bigquery.sql.* prefixed keys and SecretStr extraction
- `src/adbc_poolhouse/_sqlite_config.py` - Added to_adbc_kwargs() mapping database->uri (single key)
- `src/adbc_poolhouse/_clickhouse_config.py` - Added to_adbc_kwargs() with URI and decomposed mode support
- `tests/test_translators.py` - Added 4 test classes (TestDuckDBToAdbcKwargs, TestBigQueryToAdbcKwargs, TestSQLiteToAdbcKwargs, TestClickHouseToAdbcKwargs)

## Decisions Made
- Used direct method implementation for all 4 backends rather than Field aliases with model_dump(). The field-to-key mappings are too divergent (database->path for DuckDB, database->uri for SQLite, adbc.bigquery.sql.* prefix for BigQuery) for the alias approach to be cleaner than direct construction.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- 6 of 12 backends now implement to_adbc_kwargs() (Snowflake from Plan 01 + these 4 = 5, plus translate_config() fallback handles remaining 7)
- Plan 03 will implement the remaining complex backends (PostgreSQL, Redshift, MySQL, Databricks, FlightSQL, Trino, MSSQL)
- Parity test pattern established and ready for reuse

## Self-Check: PASSED

- All 5 modified files exist on disk
- All 4 task commits verified in git log
- All 4 config classes contain `def to_adbc_kwargs`
- 62 tests pass (20 original + 28 new to_adbc_kwargs + 14 dispatch)

---
*Phase: 17.5-translator-consolidation*
*Completed: 2026-03-14*
