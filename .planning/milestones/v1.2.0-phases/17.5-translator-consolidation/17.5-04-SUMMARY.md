---
phase: 17.5-translator-consolidation
plan: 04
subsystem: api
tags: [pydantic, uri-builder, postgresql, mysql, databricks, redshift, translator]

# Dependency graph
requires:
  - phase: 17.5-translator-consolidation-03
    provides: "Boolean-default backend to_adbc_kwargs() pattern established"
provides:
  - "PostgreSQLConfig.to_adbc_kwargs() with libpq URI builder"
  - "MySQLConfig.to_adbc_kwargs() with Go DSN builder"
  - "DatabricksConfig.to_adbc_kwargs() with databricks:// URI builder"
  - "RedshiftConfig.to_adbc_kwargs() with redshift:// URI builder and IAM kwargs"
affects: [17.5-translator-consolidation-05]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "URI construction in to_adbc_kwargs() using urllib.parse.quote(safe='')"
    - "SecretStr extraction for password/token fields"
    - "IAM kwargs as separate dict entries alongside URI"

key-files:
  created: []
  modified:
    - src/adbc_poolhouse/_postgresql_config.py
    - src/adbc_poolhouse/_mysql_config.py
    - src/adbc_poolhouse/_databricks_config.py
    - src/adbc_poolhouse/_redshift_config.py
    - tests/test_translators.py

key-decisions:
  - "Direct method implementation for all 4 URI-construction backends -- follows established pattern from plans 01-03"
  - "Redshift _build_uri() as private method to keep to_adbc_kwargs() readable -- mirrors translator module's helper pattern"

patterns-established:
  - "URI construction to_adbc_kwargs: check uri passthrough first, then decomposed field builder, with password/token URL-encoded"
  - "IAM/cluster kwargs pattern: emitted as separate dict entries alongside URI, not embedded in connection string"

requirements-completed: []

# Metrics
duration: 6min
completed: 2026-03-14
---

# Phase 17.5 Plan 04: URI Construction Backend Implementations Summary

**to_adbc_kwargs() for PostgreSQL (libpq URI), MySQL (Go DSN), Databricks (databricks:// URI), and Redshift (redshift:// URI with IAM kwargs) -- all 35 new tests pass with parity to existing translators**

## Performance

- **Duration:** 6 min
- **Started:** 2026-03-14T18:36:03Z
- **Completed:** 2026-03-14T18:42:44Z
- **Tasks:** 4
- **Files modified:** 5

## Accomplishments
- Implemented PostgreSQLConfig.to_adbc_kwargs() with three-mode support (URI, decomposed, empty/env-var fallback)
- Implemented MySQLConfig.to_adbc_kwargs() with Go DSN construction from decomposed fields
- Implemented DatabricksConfig.to_adbc_kwargs() with databricks:// URI and URL-encoded token
- Implemented RedshiftConfig.to_adbc_kwargs() with redshift:// URI and separate IAM kwargs
- All 35 new tests pass, including parity tests against existing translator functions

## Task Commits

Each task was committed atomically (TDD: tests + implementation together):

1. **Task 1: Implement PostgreSQLConfig.to_adbc_kwargs** - `a628607` (feat)
2. **Task 2: Implement MySQLConfig.to_adbc_kwargs** - `d04c231` (feat)
3. **Task 3: Implement DatabricksConfig.to_adbc_kwargs** - `25ca3fd` (feat)
4. **Task 4: Implement RedshiftConfig.to_adbc_kwargs** - `4fea453` (feat)

## Files Created/Modified
- `src/adbc_poolhouse/_postgresql_config.py` - Added to_adbc_kwargs() with URI passthrough, decomposed URI builder (libpq format), and empty-mode fallback
- `src/adbc_poolhouse/_mysql_config.py` - Added to_adbc_kwargs() with URI SecretStr extraction and Go DSN builder
- `src/adbc_poolhouse/_databricks_config.py` - Added to_adbc_kwargs() with URI SecretStr extraction and databricks:// URI builder
- `src/adbc_poolhouse/_redshift_config.py` - Added to_adbc_kwargs() with URI passthrough, redshift:// URI builder, and IAM/cluster kwargs
- `tests/test_translators.py` - Added 4 new test classes (35 total tests) mirroring existing translator tests

## Decisions Made
- Direct method implementation for all 4 URI-construction backends -- follows established pattern from plans 01-03, no model_dump aliases
- Redshift _build_uri() extracted as private method to keep to_adbc_kwargs() readable -- mirrors the translator module's _build_uri() helper pattern

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- 9 of 10 backends now have to_adbc_kwargs() implemented (all except BigQuery)
- Plan 05 (cleanup/removal of standalone translator functions) is unblocked
- All 164 translator tests pass with no regressions

## Self-Check: PASSED

- All 5 modified files exist on disk
- All 4 task commits verified in git history
- All 4 config classes contain to_adbc_kwargs() method
- 164/164 translator tests pass

---
*Phase: 17.5-translator-consolidation*
*Completed: 2026-03-14*
