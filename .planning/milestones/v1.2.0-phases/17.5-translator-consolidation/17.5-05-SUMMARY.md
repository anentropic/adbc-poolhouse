---
phase: 17.5-translator-consolidation
plan: 05
subsystem: registry
tags: [registry, translator, cleanup, refactor]

# Dependency graph
requires:
  - phase: 17.5-translator-consolidation (plans 01-04)
    provides: to_adbc_kwargs() on all 12 backend config classes
provides:
  - Registry stores 2-tuple (config_class, driver_path) without translator
  - register_backend() 3-param signature (name, config_class, driver_path)
  - translate_config() delegates directly to config.to_adbc_kwargs()
  - No translator imports in _drivers.py lazy registrations
affects: [18-entry-point-discovery, plugin-extensibility]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Config-owns-translation: to_adbc_kwargs() on config replaces standalone translator functions"
    - "Registry 2-tuple: (config_class, driver_path) -- translator no longer stored"

key-files:
  created: []
  modified:
    - src/adbc_poolhouse/_registry.py
    - src/adbc_poolhouse/_drivers.py
    - src/adbc_poolhouse/_translators.py
    - tests/test_registry.py

key-decisions:
  - "Removed TranslatorFunc type alias and get_translator() entirely rather than deprecating"
  - "Removed transitional fallback in translate_config() since all 12 backends have to_adbc_kwargs()"

patterns-established:
  - "Registry 2-tuple: _Registration = tuple[type[WarehouseConfig], str]"
  - "Direct delegation: translate_config() calls config.to_adbc_kwargs() with no fallback"

requirements-completed: []

# Metrics
duration: 5min
completed: 2026-03-14
---

# Phase 17.5 Plan 05: Registry Cleanup and Driver Updates Summary

**Registry trimmed to 2-tuple (config_class, driver_path), translator removed from registration and lazy imports**

## Performance

- **Duration:** 5 min
- **Started:** 2026-03-14T18:46:07Z
- **Completed:** 2026-03-14T18:50:54Z
- **Tasks:** 3
- **Files modified:** 4

## Accomplishments
- Registry stores (config_class, driver_path) 2-tuple instead of (config_class, translator, driver_path) 3-tuple
- register_backend() takes 3 params (name, config_class, driver_path) -- translator param removed
- get_translator() and TranslatorFunc type alias removed from _registry.py
- All 12 lazy registrations in _drivers.py no longer import translator modules
- translate_config() delegates directly to config.to_adbc_kwargs() with no fallback
- Full test suite (315 tests) passes

## Task Commits

Each task was committed atomically:

1. **Task 1: Update registry to remove translator from tuple** - `89bc36a` (test: RED), `314d73a` (feat: GREEN)
2. **Task 2: Update lazy registrations in Drivers** - `bd2af47` (feat)
3. **Task 3: Final verification and cleanup** - no commit needed (verification-only task)

_Note: TDD tasks have multiple commits (test -> feat)_

## Files Created/Modified
- `src/adbc_poolhouse/_registry.py` - Removed TranslatorFunc, get_translator(), updated _Registration to 2-tuple, register_backend to 3 params
- `src/adbc_poolhouse/_drivers.py` - Removed all 12 translator imports, updated register_backend calls to 3 params
- `src/adbc_poolhouse/_translators.py` - Removed transitional fallback to get_translator(), direct delegation to config.to_adbc_kwargs()
- `tests/test_registry.py` - Updated tests for 3-param register_backend, removed get_translator tests, added 2-tuple assertion

## Decisions Made
- Removed TranslatorFunc type alias and get_translator() entirely rather than deprecating -- both are internal symbols not exported from __init__.py, so no backward compatibility concern
- Removed transitional fallback in translate_config() since all 12 backends have to_adbc_kwargs() implemented (verified in plans 01-04)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Moved Callable import into TYPE_CHECKING block**
- **Found during:** Task 1 (GREEN phase)
- **Issue:** Ruff TC003 linting rule required Callable import inside TYPE_CHECKING since it was only used in type annotations (with `from __future__ import annotations`)
- **Fix:** Moved `from collections.abc import Callable` into the `if TYPE_CHECKING:` block
- **Files modified:** src/adbc_poolhouse/_registry.py
- **Verification:** Ruff and basedpyright pass
- **Committed in:** 314d73a (part of Task 1 GREEN commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Necessary for linting compliance. No scope creep.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Phase 17.5 (Translator Consolidation) is now complete -- all 5 plans executed
- The translator interface is consolidated: config.to_adbc_kwargs() is the single translation path
- Registry stores minimal 2-tuple, ready for plugin extensibility in Phase 18 (Entry Point Discovery)

## Self-Check: PASSED

All files verified present, all commits verified in git log.

---
*Phase: 17.5-translator-consolidation*
*Completed: 2026-03-14*
